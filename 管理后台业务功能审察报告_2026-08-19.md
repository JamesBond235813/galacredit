# GalaCredit 管理后台 · 业务功能审察报告

> 审察日期：2026-08-19
> 范围：管理后台（frontend_admin + backend admin 接口 + 数据模型）
> 视角：面向加纳现金贷实际运营，判断"还缺什么才能正式跑业务"

---

## 一、现有能力盘点（已具备）

| 模块 | 页面/接口 | 业务成熟度 | 备注 |
|---|---|---|---|
| 洞察看板 | Overview + `/project-cash-insights` | ★★★ | 项目级资金洞察，缺趋势与账龄 |
| 用户档案 | Users + `/users` | ★★★★ | 实名、定位、联系人、时间线齐全 |
| 申请审批 | Applications + 改派/额度调整/风控单查 | ★★★★ | 含 IP 审计、黑名单、风控查看强校验 |
| 待 MoMo 放款 | Disbursements | ★★★ | 已现金贷化，但为**手动确认（Mock）** |
| 还款管理 | Repayments + `/repayment-stats` | ★★★★ | 到期/逾期/分期维度齐全 |
| 催收管理 | Collections | ★★★ | 登记/改派/展期/额度/关闭逾期显示 |
| 财务平账 | FinancialReconciliation + `/finance-reconcile` | ★★★ | 收款/减免登记 + 自动结清 |
| 风控报告单查 | RiskSingleQuery | ★★ | 仅"外部服务单查"，无规则/策略 |
| 黑名单 | Blacklist + 上传 | ★★★ | 手机/身份证明文或 MD5 |
| 逾期配置 | OverdueConfig | ★★★ | 按生效日向后应用，不改历史 |
| 贷款产品 | Products | ★★★★ | 名义本金/上扣费/MoMo到账/分期 |
| 历史卡池兼容 | EcardPool | ★ | 仅存量 E 卡，已隔离 |
| 渠道管理 | Channels + 专属链接 | ★★★ | 业务员链接 + 业绩统计 |
| 后台用户 | AdminUsers + 角色权限 | ★★★★ | 5 角色 + 2 动作权限，细粒度 |
| 合同/授权 | `purchase_contract` 模型 + 1 接口 | ✗ | **模型错误**：是"购销合同"，非借款合同 |

**结论**：核心"借-放-收-催-平账"主干已通，骨架扎实。缺口集中在**资金真实链路、合规护栏、风控策略、消息触达、数据报表、审计**这六类——而这恰是"能 demo"和"能合法上线运营"的分水岭。

---

## 二、业务视角核心缺口（按上线优先级）

### P0 —— 缺了无法正式运营（合规 + 资金）

**1. 借款合同 & MoMo 授权书管理**
- 为什么：加纳现金贷必须有**借款合同（Loan Agreement）**和**MoMo 自动扣款授权书（Direct Debit Mandate）**，且需留存电子签与版本。当前 `purchase_contract` 是 E 卡"购销合同"残留，模型语义错误。
- 缺的页面/交互：合同模板配置、按订单查看已签合同、授权书查看与撤回、合同版本管理。
- 缺的模型：`loan_agreement`（合同模板/版本）、`loan_mandate`（MoMo 授权）、`contract_signature`（签署记录，替换 purchase_contract）。

**2. MoMo 资金链路与对账**
- 为什么：当前放款/还款是后台**手动点确认**，无真实资金流。上线必须接管 MoMo API：放款回调、还款回调、失败重试、对账。
- 缺的页面/交互：MoMo 交易流水（放款/还款/退款）、回调验签与幂等处理页、资金对账（与 MoMo 对账单文件比对）、放款/扣款失败重试台、资金钱包余额。
- 缺的模型：`momo_transaction`（流水+状态+回调）、`fund_account`（资金账户/余额）、`reconciliation_batch`（对账批次）。

**3. 利率/费用合规护栏（BoG）**
- 为什么：加纳央行（Bank of Ghana）对现金贷有利率/费用上限与披露要求，需系统级硬约束与告警，否则违规。
- 缺的页面/交互：合规参数配置（APR 上限、单笔费用上限、总费比上限）、超限产品/订单红牌告警、合规报表导出。
- 缺的模型/字段：在 `overdue_fee_config`/`product` 基础上补 `compliance_rule` 表 + 计算时校验。

### P1 —— 运营必需（效率 + 风控 + 报表）

**4. 风控决策引擎与规则配置**
- 为什么：目前只能"单查外部风险报告 + 人工审"。要规模化必须能配自动过/拒阈值、评分卡、反欺诈规则（同设备多账号、多头借贷、IP/GPS 聚集）。
- 缺的页面/交互：风控规则配置（通过/拒绝阈值、评分区间）、反欺诈规则、拒绝原因分布分析、策略 A/B 与灰度。
- 缺的模型：`risk_rule`、`risk_strategy_version`、`reject_reason_stats`（可视图）。

**5. 短信 / 消息 / 催收话术中心**
- 为什么：加纳催收高度依赖 **SMS**（数据网络不稳）。当前无任何消息能力。
- 缺的页面/交互：短信模板管理（预审/到期前 D-1/D0/D1/D3/D7/承诺违约）、自动提醒策略、消息发送记录与送达回执、催收话术库。
- 缺的模型：`message_template`、`message_log`、`collection_script`。

**6. 报表与分析体系**
- 为什么：管理层看不清资产质量就无法决策。
- 缺的页面/交互：
  - 逾期账龄分析（Vintage / DPD 分布）——** lender 最核心报表**
  - 资金报表（放款额 / 回收额 / 在贷余额 / 净现金流）
  - 风控报表（通过率、拒绝原因、首借转化）
  - 渠道报表（CAC、各渠道逾期率/回收率）
  - 自定义导出（跨模块 CSV）
- 缺的模型：大多可由现有 `loan`/`loan_transaction` 聚合，无需新表，但需独立报表接口与缓存。

**7. 催收体系深化**
- 为什么：当前催收=登记+改派+展期，缺分案与阶段化运营。
- 缺的页面/交互：催收分案规则（按逾期桶自动派单）、催收阶段流转（自助→短信→电催→外访→委外/法务）、委外机构管理、减免策略（按比例/上限自动批，而非每单手填）、外呼记录/录音（对接呼叫中心）。
- 缺的模型：`collection_case`、`collection_stage_log`、`collection_agency`、`waiver_policy`。

**8. 操作审计日志**
- 为什么：金融系统强制可追溯。现有 `user_event` 只记"用户/订单事件"，**没有后台管理员操作审计**。
- 缺的页面/交互：管理员操作日志查询（谁、何时、改了哪单、改了什么）、导出。
- 缺的模型：`admin_audit_log`（actor/admin_id/action/before/after/ip）。

### P2 —— 增长与体验

**9. 运营管理**
- 首页/弹窗配置、活动与优惠券（免息券/首借立减/减免券）、帮助中心 FAQ、站内公告。
- 缺的模型：`promotion`、`coupon`、`app_banner`、`faq`。

**10. 客服工单与退款**
- 工单/咨询系统、用户投诉、额度冻结/解冻（现有仅"位置风控解锁"）、主动退款流程。
- 缺的模型：`ticket`、`refund_request`。

**11. KYC 复核队列**
- 当前 OCR/活体是 Mock。真实上线需 KYC 复核队列（人工抽检/驳回），与风控联动。
- 缺的模型/交互：`kyc_review` 队列页。

**12. 系统监控**
- 定时任务看板（APScheduler：逾期翻转、罚息累加、过期额度清理）、健康检查、接口报错看板。
- 缺的模型/交互：`scheduler_job` 状态视图（可复用现有 job，加监控页）。

---

## 三、现有模块的"交互薄弱点"（不用加模块，改交互即可）

1. **申请审批**："增加额度/调减额度"措辞对现金贷偏绕，建议改为"审批额度调整"并增加**设备指纹/同设备多账号**提示位；审批结果无"自动决策建议"辅助。
2. **洞察看板**：只有项目级卡片，缺**趋势折线（日放款/日回收）**与**账龄分布图**，且无法下钻。
3. **用户档案**：缺"冻结/解冻""重置设备登录""客户沟通记录"等操作入口。
4. **催收管理**：逾期筛选只有 15/30/45 快捷档，缺按"承诺还款日/催收阶段"筛选；展期与减免分两处，建议合并为"协商方案"面板。
5. **财务平账**：减免/其他费用全手写金额，缺"按逾期配置自动带出减免建议"。

---

## 四、建议落地路线

| 阶段 | 目标 | 关键交付 |
|---|---|---|
| 阶段 0（合规止血） | 把"购销合同"纠错为借款合同+授权书；加 BoG 费率护栏 | 新合同/授权模型 + 合规校验 |
| 阶段 1（资金真实化） | 接管 MoMo 放款/还款回调 + 对账 | momo_transaction / reconciliation + 流水页 |
| 阶段 2（规模化风控） | 风控规则引擎 + 短信中心 + 报表体系 | risk_rule / message_* / 报表页 |
| 阶段 3（精细化催收+运营） | 分案/阶段/委外 + 运营/客服/审计 | collection_case / ticket / admin_audit_log |

---

## 五、附：数据模型缺口速查

| 业务域 | 现有模型 | 建议新增 |
|---|---|---|
| 合同/授权 | `purchase_contract`（语义错） | `loan_agreement`、`loan_mandate`、`contract_signature` |
| 资金 | `loan_transaction`（手动台账） | `momo_transaction`、`fund_account`、`reconciliation_batch` |
| 风控策略 | `risk_report`/`risk_composite_report` | `risk_rule`、`risk_strategy_version` |
| 消息 | 无 | `message_template`、`message_log`、`collection_script` |
| 催收 | `loans.collection_*` 字段 | `collection_case`、`collection_stage_log`、`collection_agency`、`waiver_policy` |
| 审计 | `user_event`（仅用户/订单） | `admin_audit_log` |
| 运营 | 无 | `promotion`、`coupon`、`app_banner`、`faq`、`ticket`、`refund_request`、`kyc_review` |

---

*说明：本审察基于代码静态阅读（接口/路由/模型/页面），未启动服务实测。其中"借款合同模型错误""无消息/审计/资金模型"为确定性缺口；P1/P2 中的报表与催收深度为基于行业实践的运营建议。*

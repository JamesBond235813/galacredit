# 管理后台现金贷化 + 品牌收尾 任务总结（2026-08-19）

## 一、本轮目标

在 Ghana GalaCredit 现金贷平移已有基础上，将管理后台（frontend_admin）的业务页面从“京东 E 卡 + 权益”表达彻底切换为现金贷表达（名义本金 / 上扣费用 / MoMo 实际到账 / 应还总额 / 还款计划），并把残留的“小钱包”中文品牌名收尾为 GalaCredit。

## 二、已完成内容

### 1. 用户详情页金额取值修正（正确性）

文件：`frontend_admin/src/views/Users.vue`

- 将取值函数从 E 卡口径改为现金贷优先口径，避免现金贷订单显示错误金额：
  - `resolveEcardFaceValue` → `resolveNominalAmount`：优先取 `nominal_loan_amount`，回退 `ecard_face_value` / `credit_limit`。
  - `resolveRightsPrice` → `resolveUpfrontFee`：优先取 `upfront_fee_amount`，回退 `fee_amount` / `rights_price`。
  - `resolvePaymentAmount` → `resolveDisbursementAmount`：优先取 `actual_disbursement_amount`，回退按“名义本金 − 上扣费用”计算。
- 更新 6 处调用点（最新订单概览、首笔成交概览）。
- “授信额度”标签改为“审批额度”（2 处）。

修复前：现金贷订单的“名义本金”会显示 `credit_limit`、“上扣费用”显示 `rights_price`（多为 0）、“MoMo 到账”显示成“应还总额”。
修复后：三项均正确对应现金贷金额模型。

### 2. 放款页措辞现金贷化

文件：`frontend_admin/src/views/Disbursements.vue`

- 备注占位文案：发卡前确认信息 / 卡池核验 → 放款前确认信息 / MoMo 核验。
- “拒绝发卡”弹窗与按钮改为“拒绝放款”。

### 3. 历史卡池页去除中国品牌词（隔离遗留功能）

文件：`frontend_admin/src/views/EcardPool.vue`

- “新增卡密” → “新增卡池记录”。
- “卡密(脱敏)” → “卡密/卡号(脱敏)”。
- “新增卡池卡密” → “新增卡池记录”。
- “请输入京东E卡卡号 / 卡密” → “请输入历史卡号 / 历史卡密”。
- “批量上传E卡” → “批量上传历史卡密”。

该页面本身是“历史卡池兼容”遗留功能，仅用于存量 E 卡订单，不进入新现金贷与 MoMo 流程。

### 4. 产品页历史类型标签澄清

文件：`frontend_admin/src/views/Products.vue`

- 产品类型单选：`历史E卡+权益` → `历史兼容·E卡+权益`；`历史纯权益` → `历史兼容·纯权益`。
- 现金贷产品（CASH_LOAN）仍是默认与主线，历史类型仅作兼容展示。

### 5. 品牌名收尾

文件：`frontend_admin/index.html`

- 页面标题 `小钱包管理后台` → `GalaCredit 管理后台`（构建后同步更新 `dist/index.html`）。

## 三、后端字段核对

核对 `backend/app/schemas/loan.py` 的 `LoanResponse`，现金贷所需字段均已暴露，无需后端改动：

- `nominal_loan_amount`、`upfront_fee_amount`、`actual_disbursement_amount`
- `interest_start_day`、`repayment_due_day`、`installment_count`
- `momo_disbursement_reference`
- `installments`（分期台账）、`fund_flow_summary`

## 四、验证结果

- 管理端 `npm run build`：通过（exit 0）。
- 管理端 `npm test`：12 个测试文件、28 项用例全部通过。
- 用户端（H5）、后端未改动，本轮未重新构建；此前会话已验证全量服务可用。

## 五、当前仍需继续的工作

1. 申请页 `Applications.vue` 仍使用“授信额度 / 额度调整”措辞，可进一步统一为现金贷口径（当前不影响主线）。
2. 控制台 `Overview.vue` 指标标题通过 `translateInsightText` 映射显示 MoMo 文案，依赖后端 `getProjectCashInsights` 返回的标题串；建议后续让后端直接返回现金贷标题。
3. 合同与用户授权文本仍为“小荷包商品购销合同”中文版，需改为 Ghana 现金贷英文版（独立任务）。
4. 真实 MoMo 第三方适配器、回调验签、对账流程尚未实现（当前为 Mock provider）。
5. E 卡发放 / 卡密展示 / 卡池管理页面可继续在菜单中进一步收起，仅保留给需要维护存量订单的管理员。

## 六、其他记录

- 参考样本 `reference_demo/*.jpg` 在当前环境无法直接读取（模型不支持图像输入），设计规范以 `DESIGN.md` 与 `PRODUCT_DOC.md` 为准。
- 本轮改动遵循最小改动原则，未删除任何历史 E 卡表结构与逻辑，仅做展示与文案层面的现金贷化。
- 已按 AGENTS.md 要求 `git add` 改动文件（未 commit，待人工 review）。

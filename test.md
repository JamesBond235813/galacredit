## `用户档案`页面调整
1. 列表中， 手机号，身份证号脱敏展示：
    - 手机号脱敏后格式示例： *******1234
    - 身份证号脱敏后格式示例: 330301***********1
    - 服务端返回列表数据时不再出现手机号、身份证号明文
2. 点击`查看档案`后的`用户档案详情`页面中：
    - `最新订单概览`区域增加 `成交时间`字段， 字段取值为`loans`表数据中对应的`disbursed_at`字段

## `业务顾问`用户登录`frontend_admin`平台， 登录成功后直接跳转到`用户档案`页，且菜单项中仅展示`用户档案`:
1. 列表中， 不展示`重置密码`，且无权操作`重置密码`
2. 列表中， 仅展示与当前登录`业务顾问`关联渠道的客户信息列表
3. 点击`查看档案`后的`用户档案详情`页面中：
   - 展示`实名资料`区域，不展示已有的其它内容
   - 增加`成交情况`区域内容（与`最新订单概览`字段一致）：
     - 该区域内容为客户首次成交的数据信息，即取loans表中：
       - 优先取该客户ID下的第一条非 `REJECTED` 的记录；
       - 若取不到上述数据，则取该客户ID下的第一条记录；
       - 若仍取不到数据， 则按空数据展示
       
## `用户档案`页面调整
1. `业务顾问`登录后，列表中增加`成交时间`，`成交金额` 字段：
   - 使用`首单`的成交时间，成交金额。`首单`为该客户在`loans`表中 `id`最小,且 `disbursed_at` 不为空的记录。
   - `成交时间` 使用`disbursed_at`; `成交金额`使用`product_total_price`
2. 其它有权限使用`用户档案`页的后台用户登录后， 列表中增加`最近成交时间`，`最近成交金额` 字段：
   - `最近成交` 为该客户在`loans`表中 `id`最大,且 `disbursed_at` 不为空的记录。
   - `最近成交时间` 使用`disbursed_at`; `最近成交金额`使用`product_total_price`

# 管理平台功能修整 
## 增加修改密码的功能。
1. 在页面右上角用户名右侧下拉菜单里，增加“修改密码”菜单项并提供修改密码对应的功能以及界面。

## 菜单项角标功能的调整确认（不改代码）
1. 先确认角标的数值是否由 `/api/admin/stats`接口的数据来决定？ 
2. 如果希望角标能实时或准实时更新的话， 那么用什么方案合适？ 

# 服务端增加手机号、密码登录功能
1. 通过auth2.0简化版协议，使用jwt作为用户的登录凭证。
2. 服务端保存client, access token , refresh token
3. 输入手机号密码，完成登录。不提供自动注册用户的功能，若用户不存在，则直接报用户密码不正确的文案。
4. 增加修改密码的功能。
5. `frontend_h5`对应的用户注册的功能逻辑，提供给`frontend_admin`端使用。 
6. 登录安全措施
- 以手机号为粒度，5分钟内有5次输入密码机会，若期间输入不正确，则冻结30分钟：
    - 冻结的概念为， 无论用户输入什么密码，不予验证， 直接返回"由于密码输入错误次数过多，请在xx分钟后再输入密码。"的错误文案
- 上述功能，不可出现内存泄漏的问题。

# 为`frontend_h5`前端项目登录改为使用手机号、密码登录的功能。
1. 配合服务端改造手机号、密码登录的功能。
2. 去除“注册”相关的文案
3. 在`我的`-`更多服务` 中增加修改密码的菜单，并实现修改密码的功能。

# 为`frontend_admin` 前端项目增加注册用户的功能。
1. 在`用户档案`页面， 增加`新增用户`按钮，并放到合适的位置，要求适配当前页面风格。 
2. 点击`新增用户`后， 弹出模态右侧面板：
   - 面板中提供 `手机号`，`密码`， 以及`来源渠道` 的输入选项
   - `来源渠道` 要求从 `渠道管理`页面中的数据源中选择。 
     - `来源渠道` 为带搜索的下拉框，单选
     - `来源渠道` 使用牌“启用中”状态的渠道信息
     - 搜索关键字，使用 `业务员`或`渠道名称` 进行模糊匹配
   - 弹出的右侧页面，仅能通过右上角关闭按钮关闭/隐藏面板。
3. 在`用户档案`页面 的`操作`列, 增加`重置密码`按钮，提供重置密码的功能。
   - 点击 `重置密码` 后， 弹出用户信息， 增加重置密码的输入框，且要求二次输入并比对。
   
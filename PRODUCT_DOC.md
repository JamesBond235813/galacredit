# 「小荷包」H5 意向客户放款采信平台 —— 产品与技术说明文档（PRD & Tech Spec）

## 1. 产品概述 (Product Overview)

「小荷包」是一个专为已完成前置营销的意向客户量身定制的轻量级 H5 放款信息搜集平台。
它的核心产品价值在于：**提供极简无缝的 H5 移动端交互闭环，快速收集客户的关键资质数据（手机号、身份信息、活体认证），配合后台操作进行人工风控与线下放款的统筹管理。**

### 1.1 目标用户群
- **C端用户：** 团队已触达并有意向借款的客户。
- **B端管理：** 内部风控专员及信贷业务管理员。

---

## 2. 核心业务流程 (Core Business Workflow)

### 2.1 C端用户端流程 (H5)
1. **注册与登录：**
   - 用户访问 H5 链接，输入 11 位手机号。
   - 系统生成真实随机的 4 位数字短信验证码（当前版本在按钮处模拟直显，无需外接真实短信通道）。
   - 输入比对成功后即完成登录；若手机号不存在，后台自动静默注册。
2. **意向申请与身份认证：**
   - 首页大字号展示引导用户“立即申请”。
   - **OCR 识别：** 用户上传身份证正反面，前端界面模拟上传效果，后端直接返回预设的固定数据（如姓名、号码等），跳过复杂的真实接入。
   - **实人活体检测：** 进入具有呼吸灯效的扫描页进行实人认证模拟，1.5 秒后认证成功。
3. **系统审批与额度发放：**
   - 认证成功后跳入“审核通过”缓冲页，3 秒模拟风控判定倒计时后，发放预设额度。
4. **提现与账单查阅：**
   - 用户点击提现，进入“提现申请中”状态，等待业务员打款。
   - 放款后，首页展示“待还款”信息（包含本期还款日、应还金额），如逾期则每天按配置额度累加罚息。

### 2.2 B端管理端流程 (Admin Dashboard)
1. **控制台登录：** 使用超级管理员（默认 `xiaojiang / Cn12345678`）进入后台。
2. **工单管理室：** 可查看所有进件客户列表，支持按手机号搜索及按“订单状态”筛选。
3. **线下放款闭环：** 筛选 `提现申请中 (WITHDRAWING)` 状态的订单，输入约定的【放款期限（天）】，点击“确认放款”，随后 C 端客户账单转为生息状态。
4. **结清冲账闭环：** 当客户在线下打款还款后，管理员在列表页点击“确认结清”，订单闭环归档。

---

## 3. 技术架构 (Technical Architecture)

本项目遵循**前后端完全分离**的最佳实践开发，模块高度解耦。

### 3.1 基础设施与运行端口
| 模块名称 | 技术栈 | 默认运行端口 | 主要职责 |
|---|---|---|---|
| **Backend (后端)** | Python 3.9+, FastAPI, SQLAlchemy, APScheduler, MySQL 8.x | `8000` | 承接所有 CRUD、业务中转、登录校验（JWT），及逾期违约金跑批任务。 |
| **Frontend H5 (用户端)** | Vue 3, Vite, Vant 4, Vue Router, Axios | `2001` | 响应式移动端界面，注重动画过渡、交互质感和 CSS3 视觉特效。 |
| **Frontend Admin (管理端)**| Vue 3, Vite, Element Plus, Vue Router | `2002` | 提供高密度的后台数据表格呈现，复杂的筛选交互与模态框确认。 |

### 3.2 数据库设计 (实体关系 ER)
- **`users` (用户表):** 存储主键 ID、手机号 `phone`、创建时间等。
- **`admins` (超管表):** 存储 `username`、Bcrypt 哈希化的 `password_hash`。
- **`loans` (信贷订单单据表):** **核心表**。包含外键 `user_id`。重要字段包含：
  - `status`: 当前业务状态枚举值。包含：`INIT`(初始), `REVIEWING`(审核中), `APPROVED`(授信通过), `WITHDRAWING`(提现处理), `DISBURSED`(已放款), `SETTLED`(已结清), `OVERDUE`(逾期)。
  - `credit_limit`: 最终授予的总金额。
  - `due_date`: 最后还款日（通过管理员输入天数计算得出）。
  - `penalty_amount`: 当前累计的逾期违约金（每日任务动态更新）。

---

## 4. 状态机模型与异步任务 (State Machine & Async Tasks)

### 4.1 订单状态机 (Loan Status Transitions)
后端对订单状态进行了强校验，只能沿着合法的途径流转：
```text
 INIT (刚注册)  
 └──> REVIEWING (触发额度申请)  
       └──> APPROVED (审批通过出额度)  
             └──> WITHDRAWING (用户点击提现)  
                   └──> DISBURSED (超管确认放款，生成到期日)  
                         ├──> SETTLED (提前或按时结清)  
                         └──> OVERDUE (定时任务扫出未还款且过期) 
                                 └──> SETTLED (逾期后全额结清)
```

### 4.2 每日跑批定时脚本 (Cron Jobs)
基于 `APScheduler` 构建的内置后台调度程序，每分钟（生产级别建议每天 00:01）自动轮询：
1. **状态流转：** 如果到达 `due_date` 但未变更为 `SETTLED` 的 `DISBURSED` 状态订单，自动标记为 `OVERDUE`。
2. **罚金累加：** 针对当天所有处于 `OVERDUE` 状态的账单数据，追加罚金（如: 每过一天追加 10 元滞纳金）。

---

## 5. 项目拓展与接入建议 (Future Integration Roadmap)

当前系统实现了完备的业务骨架以及 Mock 层。进入正式高频投产前，产品和技术侧可以参考以下接入指南：

1. **真实短信网关接入：**
   - **代码位置:** `backend/app/api/endpoints/auth.py` -> `@router.post("/send-code")`
   - **建议:** 在这里通过 HTTP/SDK 调用阿里云、腾讯云短信渠道接口，将随机生成的 4 位验证码投递出去。同时可增加 Redis 限流以防止被恶意盗刷接口。
2. **真实 OCR 与 活体人脸识别接入：**
   - **代码位置:** `backend/app/api/endpoints/user.py` -> `submit_ocr` 及 `face_auth`。
   - **建议:** 通过对接百度智能云等服务商进行二要素/三要素（姓名、身份证号、人脸比对）真实联网核验，并把解析结果回填入数据库 `users` 的真实姓名与标识内。
3. **资金下发渠道（线上放款提现）：**
   - **说明:** 当前是由线下财务专员跟进并返回管理台手动标注放款。
   - **扩展:** 可以在管理台引入第三方支付商户平台 API (如：汇付天下、快钱、支付宝) 对应的转账接口，超管点击放款即实时调取转账，并通过支付回调(Webhook)确认状态，真正实现自动入账。

# 现金贷平移第一阶段任务总结

## 一、本轮目标

在现有 xiaohebao 代码基础上，开始实施已确认的 Ghana GalaCredit 现金贷平移方案，并接入用户提供的橙色信用卡轮廓 SVG logo。

## 二、已完成内容

### 1. 现金贷金额模型

- 增加名义借款金额 `nominal_loan_amount`。
- 增加上扣费用金额 `upfront_fee_amount`。
- 增加实际到账金额 `actual_disbursement_amount`。
- 增加总应还快照 `total_repayment_amount_snapshot`。
- 新现金贷计算规则为：
  - 名义借款 1000。
  - 上扣费用率 40%，上扣费用 400。
  - MoMo 实际到账 600。
  - 正常总应还 1000。
- 历史 E 卡订单仍保留原字段和兼容计算逻辑。

### 2. 期限与分期配置

- 增加起息日 `interest_start_day`。
- 增加到期日 `repayment_due_day`。
- 增加分期期数 `installment_count`。
- 增加每期比例配置 `installment_ratios_json`。
- 增加每日逾期费用 `daily_overdue_fee`。
- 分期按照后台比例拆分总应还金额，示例 `[0.6, 0.4]` 会生成 600 和 400。
- 金额按分币计算，四舍五入误差自动放入最后一期。
- 分期台账已切换到现金贷总应还金额快照，不再把上扣费用重复加到还款额中。

### 3. 产品与后台配置

- 产品支持 `CASH_LOAN` 类型。
- 管理端产品编辑页已增加：
  - 名义借款金额。
  - 上扣费用率。
  - 起息日。
  - 到期日。
  - 分期期数。
  - 每期比例。
  - 每日逾期费。
- 旧 E 卡和权益产品仍可继续查看，暂未删除历史 E 卡卡池和订单表。
- 数据库启动时会自动补齐新字段，不需要手工执行迁移脚本。

### 4. MoMo 接口预留

- 新增 `backend/app/services/momo.py`。
- 定义放款和收款统一接口。
- 当前使用本地 `MockMomoProvider`，不会触发真实资金交易。
- 现金贷放款流程已跳过 E 卡卡池，调用 MoMo 模拟放款并保存流水号。
- 后续接入第三方时，只需替换 provider 实现并保留业务接口。

### 5. 品牌与前端文案

- 已将用户提供的橙色信用卡 SVG 写入 H5 logo、管理端 logo 和 H5 favicon。
- H5 首页、登录、关于、渠道入口和布局部分已切换为 GalaCredit 品牌。
- 审核、下单页面已开始切换为现金贷、MoMo、实际到账和还款计划表达。
- 管理端品牌名称已切换为 GalaCredit。

## 三、数据库与运行状态

- 数据库：MySQL `xiaohebao`。
- 已确认新字段存在于 `loans` 和 `products` 表。
- 后端：`http://127.0.0.1:8001`。
- H5：`http://127.0.0.1:2001`。
- 管理端：`http://127.0.0.1:2002`。
- 管理员本地测试账号仍为：`admin / admin123`。
- 后端已使用独立 screen 会话重启，当前监听正常。

## 四、验证结果

- Python 3.9 编译检查通过。
- 新增现金贷金额和分期测试：3 项通过。
- 关键异步数据库和订单幂等测试：6 项通过。
- 合计本轮重点测试：9 项通过。
- H5 `npm run build` 通过。
- 管理端 `npm run build` 通过。
- 全量测试仍有 10 项历史失败，未归因于本轮核心金额逻辑，主要是旧测试假对象缺少字段、旧 E 卡发放测试与异步事件循环/日志采集兼容问题；全量结果为 105 passed、10 failed。

## 五、当前仍需继续的工作

1. 将 H5 账单、订单、还款页的 E 卡展示彻底替换为现金到账、MoMo 还款和还款计划展示。
2. 将管理端申请、放款、还款、催收、财务页面的字段和操作文案完全现金贷化。
3. 将合同和用户授权文本改为 Ghana 现金贷版本，并补充英文版本。
4. 增加真实 MoMo 第三方适配器、回调验签、幂等和对账流程。
5. 完成逾期日费按订单快照计算，避免后台新配置影响历史订单。
6. 完成真实现金贷产品的完整 API 集成测试和浏览器端验收。
7. 逐步隔离不再使用的 E 卡发放、卡密展示和卡池管理页面。

## 六、重要业务约束

- 新现金贷正常总应还金额等于名义借款金额，不等于实际到账金额。
- 上扣费用只在放款时扣除一次，不应在分期中再次重复收取。
- 订单必须保存当时的费率、费用组成、起息日、到期日、期数和每期比例快照。
- 真实 MoMo 接入前，模拟 provider 只能用于本地开发和测试，不能用于生产资金流程。

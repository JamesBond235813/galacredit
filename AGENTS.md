# AGENTS.md

## 1. 目的

本文件用于约束 AI Agent 在本仓库中的代码行为规范。  
所有代码的编写、修改、重构，必须严格遵循本文件规则。

---

## 2. 核心原则

### 2.1 最小改动原则（强制）

- 所有功能更新必须以**最小改动**实现
- 禁止无关重构
- 禁止修改与当前需求无关的代码
- 优先在现有逻辑基础上扩展，而不是重写

---

### 2.2 优先复用原则

- 优先使用已有函数、工具类、公共模块
- 禁止重复造轮子
- 仅在必要时抽象公共逻辑
- 抽象不得增加理解成本

---

### 2.3 技术栈与异步规范（强制）

本项目为 **FastAPI + Uvicorn + Python 3.9** 异步架构，所有代码必须遵守以下规则：

#### （1）编程模型约束

- 优先使用 `async/await` 模型
- IO 密集型逻辑必须使用 `async def`
- 禁止写“假异步”（async def 内无 await）

#### （2）IO 操作强制规范

| 类型    | 必须使用                     | 禁止使用       |
|-------|--------------------------|------------|
| HTTP  | httpx.AsyncClient        | requests   |
| sleep | asyncio.sleep            | time.sleep |
| DB    | SQLAlchemy（只能使用async 模式） | 同步数据库驱动    |
| 文件    | aiofiles                 | open       |

#### （3）await 使用规则（强制）

- 所有异步调用必须显式 `await`
- 禁止返回 coroutine 对象
- 禁止遗漏 await

错误示例：

```python
result = db.query()  # ❌ 未 await
```

正确示例：

```python
result = await db.query()
```

#### （4）阻塞控制（强制）

- 禁止在 async 路由中执行阻塞操作
- CPU 密集型任务必须：
    - 使用 `run_in_threadpool`
    - 或独立 worker（如 Celery / 后台任务）

#### （5）第三方库选择

- 使用库前必须确认是否支持 async
- 若无 async 版本：
    - 明确说明
    - 或使用线程池隔离

#### （6）FastAPI 路由规范

- IO 操作 → `async def`
- 纯计算 → `def` 或线程池
- 禁止混用导致 event loop 阻塞

---

#### （7）自检规则（生成代码前必须执行）

Agent 在生成代码前必须自检：

- 是否使用正确的 async/await
- 是否误用了同步库（requests / time.sleep 等）
- 是否存在阻塞 event loop 的风险
- 是否符合本项目技术栈约束

如不符合，必须先修正再输出代码

---
#### （8）数据库表设计约束

- 设计数据库ddl语句时， 各表必须有表注释、字段注释；
- **禁止**使用外键，存储过程
- 表名、字段名应表示明确的语义，不可使用有歧义的名称作业字段名（主键ID字段除外）

---

## 3. 编码规范

### 3.1 函数/方法注释（强制）

所有函数/方法必须使用 **reStructuredText（reST）风格注释**

要求：

- 必须包含参数说明与返回值说明
- 禁止冗余换行
- 注释必须简洁、清晰

示例：

```python
def get_user(user_id: str) -> dict:
    """根据用户ID获取用户信息。

    :param user_id: 用户唯一标识
    :return: 用户信息字典
    """
```

---

### 3.2 关键逻辑注释（强制）

- 必须在关键逻辑代码段增加注释，要求使用中文注释
- 注释重点说明“为什么这样做”，而不是代码本身含义
- 禁止无意义注释

示例：

```python
# 重试机制：用于处理数据库瞬时连接失败
for _ in range(3):
    ...
```

---

## 3.3 可读性要求

- 函数职责单一
- 避免过深嵌套
- 优先使用清晰直观的实现方式
- 禁止炫技式写法

---

### 3.4 异常与响应规范（强制）

- HTTP status 规范（强制）：
    - 登录验证失败 → `401`
    - 只要请求已经进入 Python 服务端并完成业务处理，无论业务成功或失败，HTTP status 一律返回 `200`
- 业务失败必须通过项目统一响应基类中的 `code` 与 `msg` 表达，不允许通过 HTTP status 表达
- 所有失败响应必须通过全局异常处理器统一转换为项目响应基类

#### 异常使用规范（强制）

- 业务代码（service / router 依赖函数 / 鉴权辅助函数）只允许抛 `BizException`
- 禁止在任何业务代码中使用 `raise HTTPException(...)`
- 禁止捕获 `BizException` 后再转换为其他异常

#### Router 层规范（强制）

- Router 层只负责返回成功结果
- 禁止在 Router 层编写 `try/except BizException`
- 禁止在 Router 层手动调用 `_biz_error(err)`
- 失败必须由全局异常处理器统一处理

#### 响应规范（强制）

- 成功响应必须使用项目统一响应基类
- 禁止返回 FastAPI 默认错误结构：`{"detail": "..."}`
- 禁止接口自行拼装不符合项目规范的 JSON 结构

---

## 4. 测试规范（强制执行）

### 4.1 必须编写测试

任何代码修改后，必须：

- 编写**单元测试**
- 编写**集成测试（如涉及模块交互）**

---

### 4.2 单元测试要求

- 覆盖核心逻辑
- 覆盖边界条件
- 必须可重复执行（无随机性依赖）

---

### 4.3 集成测试要求

- 覆盖模块之间调用链
- 尽量贴近真实运行环境
- 包括但不限于：
    - 数据库交互
    - API调用
    - 服务间调用

---

### 4.4 测试执行（强制）

- 所有测试必须执行
- 必须确保测试通过
- 若涉及服务端的代码修改， 则要求启动服务端应用进行测试。 不可省略
- 若涉及前端功能的代码修改，则要求启动服务端、前端进行测试。不可省略
- 服务端启动方式：
  - 在 `./backend` 路径下执行 `python -m app.main`
- 若测试用例涉及登录动作的（尤其是前端页面测试），必须串行执行。

  如果无法执行测试，必须：

- 明确说明原因（如缺少环境/依赖）
- 提供可执行的运行步骤



---- 

## 5. 禁止行为

禁止以下行为：

- 未经明确要求进行大规模重构
- 修改无关代码
- 删除已有测试（除非提供等价替代）
- 引入不必要依赖
- 过度设计（Over-engineering）
- 擅自定义异常或Error类
- 在任何业务代码中使用： `raise HTTPException(...)`
- 返回 FastAPI 默认错误结构： `{"detail": "..."}`
- 接口成功或失败时，自行拼装不符合项目统一响应基类的 JSON 结构

---

## 6. 输出要求

Agent 在完成代码修改后（**必须执行，不可省略！**）:

1. 必须提供：
    1. 修改说明（做了什么、为什么）
    2. 修改后的代码
    3. 单元测试代码
    4. 集成测试代码（如适用）
    5. 测试执行结果或执行方式
2. 记录每次修改内容、操作步骤以及测试结果回写到文件， 要求：
    - 写到文件`doc/work_result/{yyyyMMdd}.md`文件， 若没有则创建。
    - 将上述内容写入文件时，要求带上时间戳（`yyyy-MM-dd HH:mm:ss`格式， 且设置为1级标题），如 `# 2026-03-27 15:59:00`
    - 每次写文件时， 将最新的内容插入到文件顶部， 即人工阅读时， 修改内容的时间为倒序排列
3. 创建新文件后， 要求执行git add, 但不要commit, 以便人工Review

---

## 7. 优先级规则

当出现冲突时，优先级如下：

1. 用户明确指令
2. 本 AGENTS.md
3. 项目已有代码规范

---

## 9. 行为标准

Agent 应以“高级工程师”标准执行任务：

- 改动最小且精准
- 代码清晰可维护
- 注释规范完整
- 测试完整且可执行

不符合以上标准的实现视为不合格实现。

---

## 10. 前端项目开发约束

### 10.1 代码风格与修改原则（强制）

- 前端代码同样遵守本文件第 2 章“最小改动原则”。
- 修改现有前端代码时，必须优先在原有结构上整理和演进，禁止无关重构。
- 禁止因为局部需求随意推翻当前目录或技术方案。
- 新增功能时，必须考虑与现有结构一致性。
- 无明确要求时，不得擅自删除已有可用代码。
- 清理脚手架残留代码时，必须仅删除明确无用内容，不得误删已有业务代码。

---

### 11.2 测试约束（强制）

- 前端测试必须使用 Vitest。
- 核心状态逻辑、工具函数、关键交互逻辑应补充单元测试。
- 如修改了关键页面交互、消息流处理、数据转换逻辑，应补充对应测试。
- 无法执行测试时，必须明确说明原因，并给出执行步骤。

前端常用命令示例：

```bash
pnpm install
pnpm dev
pnpm test
pnpm build
```

---



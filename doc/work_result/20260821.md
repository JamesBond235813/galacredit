# 2026-08-21 17:42:00

## 修改说明

- 修复管理后台英文兜底翻译对子串逐段替换的问题，改为仅对完整 UI 文案执行精确映射，避免“上传名loan”等中英混杂，并避免误改姓名、备注和模板正文中的局部文字。
- 扩充管理后台运营、消息、审计、KYC、财务、渠道、产品、后台用户和权限相关的完整双语文案映射。
- 运营监控页面的汇总指标、任务列表、运行状态和下钻弹窗改为显式中英文文案。
- 后台用户页面补齐账号数量、当前登录、权限预览、角色/权限名称和提交按钮的语言切换。
- KYC 复核页面补齐筛选、表格、通过/拒绝、用户档案入口、处理备注与结果提示的显式双语文案。
- 操作审计页面补齐搜索条件、占位符、查询/重置和 CSV 导出按钮的显式双语文案。
- 保留现有消息模板编辑/启停/版本、发送结果回溯、运营下钻、登录历史、财务对账、渠道/产品复制与历史接口及页面能力，不修改业务数据和数据库结构。

## 测试代码

- 更新 `frontend_admin/src/i18n/adminLocale.test.js`，新增完整标签翻译、空白保留、业务长文本不拆词、姓名不翻译的回归测试。

## 测试结果

- `frontend_admin/node_modules/.bin/vitest run`：13 个测试文件、35 个测试全部通过。
- `frontend_admin/node_modules/.bin/vite build`：生产构建成功；仅存在既有的大包体积提示。
- 后端使用 `backend/.venv/bin/python -m app.main` 启动成功，监听 `0.0.0.0:8001`。
- H5 使用约定端口 `2001` 启动成功；管理后台使用约定端口 `2002` 启动成功。
- HTTP 验证：后端根路径、H5、管理后台均返回 200。
- 浏览器验收：管理后台英文登录页显示为完整英文（Username / Password / Sign in），未出现中英拼接。

## 当前运行入口

- 后端：`http://127.0.0.1:8001`
- H5：`http://127.0.0.1:2001`
- 管理后台：`http://127.0.0.1:2002`

## 注意事项

- 本轮未改动数据库结构及后端业务逻辑。
- pnpm 包装器因本机依赖构建脚本审批策略退出，已使用仓库现有 Vitest/Vite 二进制完成等价测试与构建。
- 用户此前公开的 GitHub Personal Access Token 已泄露，应立即在 GitHub 撤销并生成新 Token，禁止继续使用旧 Token。
- GitHub：按仓库规范仅执行 `git add`，未创建 commit；未使用已泄露的 PAT。
- 生产服务器：尝试只读 SSH 连接 `root@66.94.122.149`，当前环境无可用公钥（`Permission denied (publickey)`），因此未覆盖线上静态目录，也未影响服务器其他服务。

## 2026-08-21 09:52:00 云端同步

- 通过服务器专用密钥 `~/.ssh/node1_vnc_ed25519` 登录 `66.94.122.149` 成功。
- 已将本地 `frontend_admin/dist` 发布到 `/data/www/gala.ebamotor.com`。
- 远端原目录已保留为 `/data/www/gala.ebamotor.com.backup-20260821095046`，可回滚。
- 本地与远端 `index.html` SHA-256 均为 `b6c5600a7ed3a508b099997203cc8f97fe4c735917d90bd528176e342337b3b9`。
- `https://gala.ebamotor.com/` 返回 200；线上管理端资源包含 `Upload blacklist`，未出现 `上传名loan`。
- 云端 `galacredit-backend` 与 `talking202605-caddy` 均保持运行，未重启其他服务、未修改数据库。

# 2026-08-21 10:20:00

## 加纳现金贷风控策略集设计

- 新增 [ghana_cash_loan_risk_strategy.md](../ghana_cash_loan_risk_strategy.md)。
- 方案覆盖加纳监管与数据原则、KYC/AML、欺诈评分、信用评分、Mobile Money、设备/IP/GPS、渠道、额度阶梯、人工审核、贷后催收、运营看板、策略版本和灰度治理。
- 对照 GalaCredit 现有代码能力，列出可复用模块和下一阶段应新增的决策日志、特征快照、规则命中、策略版本、设备关联、钱包一致性和 vintage 监控能力。
- 本轮仅设计和文档化，没有修改线上风控阈值、授信额度、催收规则或数据库结构。

# 2026-08-21 10:35:00

## 风控策略开发边界拆分

- 新增 [ghana_risk_development_scope.md](../ghana_risk_development_scope.md)。
- 明确区分了可直接开发的风控引擎、评分、额度、KYC、设备/IP、运营监控、贷后催收和数据治理能力。
- 明确列出需要外部授权/供应商的 Ghana Card、Mobile Money、征信、AML/制裁数据和完整设备 SDK 能力。
- 明确列出必须由持牌机构、合规负责人或运营负责人批准的费率、额度、数据授权、催收和法律处置事项。

# 2026-08-21 20:55:00

## 风控第一阶段开发

### 已完成

- 新增 `risk_policy_versions`、`risk_decisions`、`risk_rule_hits` 三张独立表及 005 迁移脚本。
- 新增 `evaluate_baseline_rules` 和 `record_risk_decision_async`，将现有黑名单、外部风险名单、实名、人脸、位置风控、当前逾期和逾期额度锁定统一记录。
- 在 H5 `/loan/apply` 和 `/loan/withdraw` 前接入 shadow mode；当前只记录决策，不改变现有审批和放款结果。
- 新增后台 `/api/admin/risk-decisions` 查询入口，可按用户、订单、阶段和结果筛选，并返回规则命中和特征快照。
- 新增 5 项风控规则单元测试，全部通过。

### 验证

- `backend/.venv/bin/pytest -q tests/test_risk_decision.py`：5 passed。
- `backend/.venv/bin/python -m compileall -q app`：通过。
- 本地 MySQL 已创建三张风控表并初始化 `GHANA_CASH_LOAN_BASELINE` v1 SHADOW 策略。
- 后端已用新代码重启并监听 `8001`，启动日志显示应用启动完成。
- 全量后端测试当前有 13 项既有失败，集中在历史 FakeDB 测试、日志测试和异步测试环境；新增风控测试不在失败项中。

### 安全边界

- 当前没有启用强制拦截，没有修改现有额度、费率或催收策略。
- 下一阶段需先观察真实 shadow 数据，再评审阈值和是否切换指定规则为 ENFORCE。
# 2026-08-21 21:02:00

## 风控策略页面收尾开发

### 修改说明

- 管理后台新增风控决策查询 API 封装 `getRiskDecisions`，支持阶段、决策结果和分页筛选。
- 将“策略中心预留”页面替换为风控决策记录页面，展示 shadow mode 状态、风险分、规则命中、策略版本和分页结果，并沿用现有中英文切换函数。
- 更新运营中心页面测试，断言风控决策接口、shadow mode 和规则命中字段，移除已过时的预留页断言。

### 测试与验收

- `frontend_admin/node_modules/.bin/vitest run`：13 个测试文件、35 个测试全部通过。
- `frontend_admin/node_modules/.bin/vite build`：生产构建成功；仅有既有的大包体积提示。
- `backend/.venv/bin/python -m compileall -q app`：通过。
- 未登录访问 `/api/admin/risk-decisions` 返回项目统一的 `401` 响应，鉴权链路正常；登录后由现有管理员权限校验控制访问。
- 风控策略仍为 `SHADOW`，只记录决策和规则命中，不改变现有审批、放款、额度或费率。

### 文件

- `frontend_admin/src/api/index.js`
- `frontend_admin/src/views/RiskStrategy.vue`
- `frontend_admin/src/views/opsCenter.test.js`
- `frontend_admin/dist/*`
# 2026-08-21 21:35:00

## Phase 1-4 风控能力开发

### 已完成

- Phase 1：补充人工覆盖记录接口；原始风控决策保持不可变，覆盖动作单独审计。
- Phase 2：新增可解释 Fraud Score、Credit Score、钱包一致性、申请/设备速度特征和首贷额度建议算法。
- Phase 3：新增设备指纹、IP/ASN、代理/模拟器、设备关联账户和24小时速度特征落库能力。
- Phase 4：新增 Ghana 外部风险供应商适配器接口、外部查询结果/降级记录、模型评分记录和 champion/challenger 的 SHADOW 记录能力。
- 新增 006 数据库迁移，已在本地 `galacredit` 数据库执行；不含外键和存储过程。
- 所有外部服务默认 `SKIPPED`，所有模型默认 `SHADOW`，未改变真实审批、放款、额度或费率。

### 新增文件/接口

- `backend/app/models/risk_expansion.py`
- `backend/app/services/risk_scoring.py`
- `backend/app/services/risk_external.py`
- `backend/migrations/versions/006_risk_phase2_4.sql`
- `POST /api/admin/risk-decisions/{decision_id}/override`
- `POST /api/admin/risk-signals`

### 测试

- 风控决策及评分测试：10 passed。
- 后端 `compileall`：通过。
- 本地 006 迁移：执行成功。
- 管理后台 Vitest：13 个测试文件、35 个测试全部通过。
- 管理后台 Vite 生产构建：成功（仅既有大包体积提示）。

# 2026-05-07 23:27:48

## 修改说明（增量）
- 已重新读取 `backend/.env` 与 `backend/.env.xhb-prod` 最新内容并按该版本执行。
- 滑块验证码改为“失败可重试，达到最大失败次数或超时才失效”：
  - 新增配置 `CAPTCHA_SLIDER_CHALLENGE_MAX_FAILS`，默认 `3`。
  - 同一 challenge 失败时递增失败计数；达到上限后返回“已失效”。
  - 成功校验时才销毁 challenge。
- 前端在收到“过期/失效”时自动刷新滑块信息。

## 修改后的代码
- backend/app/core/config.py
- backend/app/services/slider_captcha.py
- backend/app/api/endpoints/auth.py
- backend/.env
- backend/.env.xhb-prod
- frontend_h5/src/views/Login.vue
- backend/tests/test_slider_captcha.py（新增）

## 测试执行结果
- `cd backend && pytest -q tests/test_slider_captcha.py tests/test_auth_sms_send_code.py tests/test_auth_sms_login.py`：`5 passed`
- `cd frontend_h5 && npm run build`：构建成功

# 2026-05-07 22:46:24

## 修改说明（增量修复）
- 滑块验证码服务端缓存补强：增加过期清理与最大缓存上限裁剪，避免异常流量导致内存持续增长。
- 发送短信验证码仍支持 mock：保持 `SMS_CODE_MOCK_ENABLED` 配置逻辑不变。
- 修复滑块与镂空尺寸错位：验证码区域按服务端下发的 `width/height/block_size` 定宽定高展示，滑块可准确拼合。
- 交互调整为“滑动松手即提交”：移除“验证并发送”按钮；验证失败时滑块自动回到初始位置。
- 验证过期自动刷新：若服务端返回“已过期”，前端自动重新请求生成滑块。
- `发送验证码`按钮样式调整为与页面风格一致的圆角按钮，圆角小于登录按钮。

## 修改后的代码
- backend/app/services/slider_captcha.py
- frontend_h5/src/views/Login.vue

## 测试执行结果
- `cd frontend_h5 && npm run test -- src/utils/smsLogin.test.js src/utils/channel.test.js`：`2 files passed, 5 tests passed`
- `cd frontend_h5 && npm run build`：构建成功
- `cd backend && pytest -q tests/test_auth_sms_send_code.py tests/test_auth_sms_login.py`：`4 passed`

# 2026-05-07 22:37:10

## 修改说明（做了什么、为什么）
- `frontend_h5` 登录流程已切换为“手机号+短信验证码登录”，并新增“发送验证码”按钮。
- 发送短信前新增滑块图形验证码流程：前端先获取图形验证码并完成滑块校验，校验通过后携带 `captcha_ticket` 调用发送短信接口。
- 新增后端滑块验证码能力（内存态 challenge/ticket 管理）：
  - `POST /api/auth/slider-captcha/create`
  - `POST /api/auth/slider-captcha/verify`
  - 仅校验 x 轴偏移；误差阈值走 `.env` 配置。
- `POST /api/auth/send-code` 已接入真实短信下发链路（`sms_service.send_code`），不再是纯 mock 发送。
- 新增 `POST /api/auth/sms-login`：
  - 用户不存在时自动注册；若存在有效邀请码（16位小写字母数字）且可匹配到“已启用渠道”，则绑定该渠道。
  - 用户已存在时直接登录并忽略邀请码。
- 邀请码链路改造：
  - `frontend_h5` 路由入口改为 `/:inviteCode([a-z0-9]{16})`。
  - 前端持久化保存邀请码，页面跳转不丢失，新邀请码可覆盖旧邀请码。
- 新增验证码参数配置（`.env`）：误差阈值、最短滑动时长、challenge/ticket 过期时间、图片宽高边界、滑块尺寸。

## 修改后的代码
- backend/app/services/slider_captcha.py
- backend/app/api/endpoints/auth.py
- backend/app/services/channel_service.py
- backend/app/schemas/user.py
- backend/app/core/config.py
- backend/.env
- backend/.env.local
- backend/.env.xhb-prod
- frontend_h5/src/views/Login.vue
- frontend_h5/src/views/ChannelEntry.vue
- frontend_h5/src/router/index.js
- frontend_h5/src/utils/channel.js
- frontend_h5/src/api/index.js

## 单元测试代码
- backend/tests/test_auth_sms_login.py（新增）
- backend/tests/test_auth_sms_send_code.py（更新）
- frontend_h5/src/utils/channel.test.js（新增）
- frontend_h5/src/utils/smsLogin.test.js（复用）

## 集成测试代码（如适用）
- 服务端启动验证：`cd backend && python -m app.main`（本次环境端口被占用，见测试结果说明）。

## 测试执行结果或执行方式
- 后端：`cd backend && pytest -q tests/test_auth_sms_send_code.py tests/test_auth_sms_login.py tests/test_sms_service.py`
  - 结果：`7 passed`
- 前端：`cd frontend_h5 && npm run test -- src/utils/smsLogin.test.js src/utils/channel.test.js`
  - 结果：`2 files passed, 5 tests passed`
- 前端构建：`cd frontend_h5 && npm run build`
  - 结果：构建成功。
- 服务端启动：`cd backend && python -m app.main`
  - 结果：应用启动流程完成，但监听 `8001` 时提示 `address already in use`（环境端口占用，不是本次代码逻辑报错）。

## 操作步骤记录
1. 梳理现有 `auth`、短信服务、H5 登录与邀请码入口实现。
2. 新增滑块验证码服务与接口，接入发送短信前置校验。
3. 新增短信登录接口并实现“注册+邀请码绑定”规则。
4. 改造 H5 登录页为短信验证码流程，接入滑块弹层与冷却倒计时。
5. 改造邀请码路由与本地持久化逻辑，确保跳转不丢失。
6. 补充/更新后端与前端测试并执行验证。

# 2026-05-07 22:20:43

## 修改说明（增量）
- 调整邀请码生成规则：16位邀请码必须同时包含数字和小写字母。
- 前端创建渠道的随机邀请码生成器已增加该约束。
- 后端编辑兜底自动生成邀请码逻辑已增加该约束，前后端规则一致。

## 修改后的代码
- frontend_admin/src/utils/channel.js
- backend/app/services/admin_service.py

## 单元测试代码
- frontend_admin/src/utils/channel.test.js
- backend/tests/test_admin_channels_invite_code.py
  - 新增：`test_generate_channel_invite_code_should_include_letters_and_digits`

## 测试执行结果
- `cd backend && pytest -q tests/test_admin_channels_invite_code.py`：`4 passed`
- `cd frontend_admin && npm run test -- src/utils/channel.test.js`：`1 file passed, 2 tests passed`

# 2026-05-07 22:02:09

## 修改说明（增量）
- `frontend_admin` 渠道编辑弹窗在“渠道名称”下方新增“邀请码”字段，仅读不可编辑。
- 点击“保存”时，后端 `_update_channel` 新增兜底逻辑：若数据库记录的 `invite_code` 为空，则自动生成16位小写字母数字邀请码并保存。

## 修改后的代码
- frontend_admin/src/views/Channels.vue
- backend/app/services/admin_service.py
- backend/tests/test_admin_channels_invite_code.py

## 单元测试代码
- backend/tests/test_admin_channels_invite_code.py
  - `test_update_channel_should_auto_generate_invite_code_when_empty`

## 集成测试代码（如适用）
- 本次为后端更新逻辑与前端表单展示改动，执行了后端/前端测试验证。

## 测试执行结果
- `cd backend && pytest -q tests/test_admin_channels_invite_code.py tests/test_admin_business_consultant.py`：`7 passed`
- `cd frontend_admin && npm run test -- src/utils/channel.test.js`：`1 file passed, 2 tests passed`

## 操作步骤记录
1. 前端编辑抽屉新增只读邀请码字段。
2. 后端更新渠道时补充空邀请码自动生成与唯一性检查逻辑。
3. 新增并修正对应后端单元测试，执行通过。

# 2026-05-07 21:54:46

## 修改说明（做了什么、为什么）
- 为 `channels` 表新增 `invite_code` 字段（`varchar(32) not null default '' comment '渠道邀请码'`），并在数据库初始化补丁中增加唯一索引 `ux_channels_invite_code`，满足渠道邀请码唯一性要求。
- 后端 `Channel` ORM、渠道请求/响应 schema、渠道服务序列化逻辑已同步，确保前后端可读写 `invite_code`。
- 后端新增配置项 `CHANNEL_LINK_PREFIX`（默认 `https://xxx.xx`），并在 `/admin/channels` 响应中返回 `channel_link_prefix`，用于前端构造专属链接前缀。
- `frontend_admin` 在创建渠道时随机生成 16 位小写字母+数字的邀请码并提交到后端。
- `frontend_admin` 渠道页面专属链接改为 `{前缀}/{渠道邀请码}`，前缀来自后端返回，不再由本地输入维护。
- 兼容历史/模拟数据：`seed_mock_data.py` 创建渠道时写入稳定的 16 位邀请码，避免唯一约束冲突。

## 修改后的代码（文件清单）
- backend/app/models/channel.py
- backend/app/core/database.py
- backend/app/core/config.py
- backend/app/schemas/channel.py
- backend/app/services/admin_service.py
- backend/seed_mock_data.py
- backend/.env
- backend/.env.local
- backend/.env.xhb-prod
- frontend_admin/src/views/Channels.vue
- frontend_admin/src/utils/channel.js

## 单元测试代码
- backend/tests/test_admin_channels_invite_code.py
  - `test_serialize_channel_should_include_invite_code`
  - `test_get_channels_should_return_channel_link_prefix`
- frontend_admin/src/utils/channel.test.js
  - `should generate invite code with default length and charset`
  - `should generate invite code with custom length`

## 集成测试代码（如适用）
- 本次执行了服务启动集成验证：`cd backend && python -m app.main`（启动后主动停止），验证服务可正常启动并监听端口。

## 测试执行结果
- 后端单测：`cd backend && pytest -q tests/test_admin_channels_invite_code.py tests/test_admin_business_consultant.py`
  - 结果：`6 passed`
- 前端单测：`cd frontend_admin && npm run test -- src/utils/channel.test.js`
  - 结果：`1 passed (2 tests)`
- 服务启动验证：`cd backend && python -m app.main`
  - 结果：应用启动成功（日志出现 `Application startup complete` 与 `Uvicorn running on http://0.0.0.0:8001`），主动停止后有 aiomysql event loop closed 清理告警，不影响本次功能改动。

## 操作步骤记录
1. 定位 `channels` 相关后端模型、服务、schema 与 `frontend_admin` 渠道页面实现。
2. 新增数据库字段补丁与唯一索引补丁，并补历史数据回填策略。
3. 同步后端 ORM/schema/服务输出，新增 `CHANNEL_LINK_PREFIX` 配置透出。
4. 前端新增邀请码生成工具并接入渠道创建请求。
5. 渠道页面链接规则改为 `{prefix}/{invite_code}`。
6. 补充后端与前端测试并执行。

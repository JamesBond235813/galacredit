# 2026-05-29 16:01:21

## 修改说明

- 根据接口文档补齐 GPS 反解析真实调用地址与 POST 请求方式。
- 增加 `LOCATION_GEOCODE_HTTP_METHOD` 与 `LOCATION_GEOCODE_LOCATION_PARAM` 配置，支持该接口要求的 Body 参数 `location=经度,纬度`。
- 已将本地与生产配置切换为新的云市场接口；生产服务实际读取 `.env`，已同步更新并重启后端。

## 修改后的代码

- `backend/app/core/config.py`：新增请求方式和组合经纬度参数配置。
- `backend/app/services/location.py`：支持 POST 表单提交和 `location=lng,lat` 参数格式。
- `backend/tests/test_async_http_services.py`：新增组合 location 参数测试，验证经度在前、纬度在后。
- `backend/.env`、`backend/.env.xhb-prod`、`backend/.env.example`：补充新接口地址、POST 方式和 location 参数配置。

## 单元测试代码

- `backend/tests/test_async_http_services.py::test_reverse_geocode_supports_combined_location_param`

## 集成测试代码

- 复用 `backend/tests/test_login_location_risk.py` 覆盖登录定位写入链路。

## 测试执行结果

- 本地真实接口验证：深圳坐标可解析为广东省、深圳市、福田区、莲花街道及完整地址。
- `python3 -m py_compile backend/app/services/location.py backend/app/core/config.py`：通过。
- `pytest backend/tests/test_async_http_services.py backend/tests/test_login_location_risk.py -q`：5 个用例通过。
- `python -m app.main`：本地后端启动成功，确认应用启动链路正常后已手动停止。
- 云端真实接口验证：同一坐标解析成功。
- 云端 `xiaohebao-backend.service` 已重启，状态为 active。

# 2026-05-29 15:33:08

## 修改说明

- 为 GPS 经纬度反解析新增可配置的云市场供应商链路，支持通过环境变量配置供应商、接口地址、AppCode、经纬度参数名、坐标系参数和超时时间。
- 保留原 OpenStreetMap Nominatim 解析作为未配置新接口地址时的兜底，避免在接口调用路径未确认前影响用户登录定位与位移风控主流程。
- 新增供应商响应归一化逻辑，兼容 `data`、`result`、`addressComponent` 等常见嵌套结构，并统一输出地址、省、市、区县、街道字段。
- 本地与生产环境配置文件已补充 GPS 解析配置项；敏感值仅写入环境配置，不在本文档记录。

## 修改后的代码

- `backend/app/core/config.py`：新增 `LOCATION_GEOCODE_*` 配置项。
- `backend/app/services/location.py`：拆分 Nominatim 解析与云市场 AppCode 解析，并统一响应字段。
- `backend/tests/test_async_http_services.py`：补充云市场 AppCode 调用测试，验证异步 HTTP、请求参数和解析结果。
- `backend/.env`、`backend/.env.xhb-prod`、`backend/.env.example`：补充 GPS 解析相关配置。

## 单元测试代码

- `backend/tests/test_async_http_services.py::test_reverse_geocode_uses_aliyun_market_appcode`

## 集成测试代码

- 复用 `backend/tests/test_login_location_risk.py`，覆盖登录定位写入链路在反解析为空时不使用 IP 地址冒充 GPS 地址。

## 测试执行结果

- `python3 -m py_compile backend/app/services/location.py backend/app/core/config.py`：通过。
- `pytest backend/tests/test_async_http_services.py backend/tests/test_login_location_risk.py -q`：4 个用例通过。
- `python -m app.main`：后端启动成功，确认应用启动链路正常后已手动停止。

## 待确认事项

- 阿里云公开商品页只暴露了云市场域名和 API 标识，未暴露实际调用 URL path；当前代码已支持新接口，但生产切换仍需补充 `LOCATION_GEOCODE_API_URL` 的完整调用地址。

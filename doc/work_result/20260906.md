# 2026-09-06 00:18:00

## Ghana Card 增加拍照上传方式

### 修改内容

- ID Verification 页面点击任一证件上传按钮后，新增系统操作菜单：`Take photo` 和 `Choose from gallery`。
- 选择 `Take photo` 时调用 `uni.chooseImage` 的 `sourceType: ['camera']`，安卓设备会直接进入相机。
- 选择 `Choose from gallery` 时调用 `sourceType: ['album']`，保留相册选择能力。
- 保持现有 OCR multipart 上传、授权勾选、上传状态和后续人脸验证跳转逻辑不变。
- 扩展 `submitOCR`/`uploadIdentityImages` 参数以传递选择来源，H5 与 Android 继续共用同一套接口封装。

### 测试与验证

- `frontend_uniapp` Vitest：13 个测试文件、41 项测试全部通过。
- 新增 API 单元断言，确认相机模式会传递 `sourceType: ['camera']`。
- H5 生产构建成功，已同步到 `node1-yz:/data/www/galacredit`。
- 公网 `https://galacredit.ebamotor.com/` 返回 HTTP 200。

### 边界

- 本轮未在真机上实际触发相机权限和拍照，避免无必要地打开摄像头或产生上传业务数据；安装后的安卓 App 重新进入 ID Verification 后即可验证该入口。

### Git

- 已执行 `git add`，未提交 commit。
# 2026-09-06 09:22:48

## 管理端恢复启动与登录验证

### 修改与操作

- 启动 GalaCredit 后端服务，确认监听端口为 `8001`。
- 启动 `frontend_admin` 管理端，确认 Vite 严格绑定端口 `2002`。
- 未修改业务代码；本轮仅恢复本地服务进程并验证真实访问链路。

### 验证结果

- 后端 `http://127.0.0.1:8001/` 返回 HTTP 200。
- 管理端 `http://127.0.0.1:2002/login` 返回 HTTP 200。
- 经管理端 `2002` 的 `/api` 代理调用管理员登录接口返回 HTTP 200，`xiaojiang` 登录成功并获得访问令牌。
- 当前监听进程：后端 PID `63421`，管理端 PID `64121`。

### 结论

- “无法进入”的直接原因是后端此前没有监听 `8001`，导致管理端 API 代理无可用目标；账号当前有效，无需重置密码。
- 服务已恢复，可访问 `http://127.0.0.1:2002/login` 登录管理端。

### Git

- 已执行 `git add`，未提交 commit。
# 2026-09-06 10:08:00

## 管理端云端同步状态核对

### 核对内容

- 检查本地仓库分支与远程：当前 `main` 与 `origin/main` 一致；本轮已有工作记录修改和 `frontend_uniapp/.hbuilderx/` 未跟踪目录，均未涉及管理端源码。
- 检查生产入口 `https://gala.ebamotor.com/`：返回 HTTP 200，页面标题为 `GalaCredit 管理后台`。
- 对比管理端入口文件：本地 `frontend_admin/dist/index.html` 与线上返回内容 SHA-256 均为 `a91808182d16dc51d7919fa5271efb163f756991a2c52bf64b4f50cce18f2dac`。
- 抽查线上 `assets/index-BR5TKg41.js`、`assets/Overview-CzWfChrY.js`，与本地构建产物内容一致。
- 尝试通过当前 SSH 别名 `tencent-superapi` 读取云端目录时，当前环境无法解析该主机名，因此未做远端目录级 `stat`/文件清单核对，也未执行任何上传或覆盖操作。

### 结论

- 管理端已经同步到云端，线上页面当前可访问，且线上入口和关键静态资源与本地 `frontend_admin/dist` 一致。
- 本次只能确认“当前线上版本与本地构建产物一致”；由于 SSH 别名当前不可解析，未对服务器目录和部署时间做实时 SSH 级确认。
- 线上响应头显示 `index.html` 最近修改时间为 `2026-08-22 08:33:09`（服务器时间口径），这与历史部署记录相符。

### 操作边界

- 本轮为只读核对，没有修改云端文件、重启服务或改动业务代码。
- 已按仓库要求将本记录插入文件顶部；未提交 commit。
# 2026-09-06 10:32:00

## UniApp H5、Android、管理端、后端与云端数据库一致性核对

### 已验证正常

- UniApp H5 生产入口 `https://galacredit.ebamotor.com/`、`/login`、`/pages/login/index`、`/manifest.webmanifest`、`/sw.js` 均返回 HTTP 200。
- 本地 `frontend_uniapp/dist/index.html` 与云端 `/data/www/galacredit/index.html` SHA-256 一致：`fb11c3e9eaa546aa466fcccd69ea00d79ca6fd37c001f5c28d17ae8fbb56b782`。
- 管理端 `https://gala.ebamotor.com/` 返回 HTTP 200；本地管理端入口与云端入口 SHA-256 一致：`a91808182d16dc51d7919fa5271efb163f756991a2c52bf64b4f50cce18f2dac`。
- H5 与管理端 API 均通过 Caddy 反向代理到后端：未登录访问 `/api/user/info`、`/api/admin/me` 返回项目统一的 HTTP 401 `{"code":401,"msg":"Not authenticated"}`，说明鉴权链路生效。
- CORS 预检对 `https://galacredit.ebamotor.com` 与 `https://gala.ebamotor.com` 均返回 HTTP 200，并允许对应来源。
- 云端 `galacredit-backend` 容器状态为 `Up 20 hours`；容器内 `127.0.0.1:28002` 返回后端健康文本。
- 云端后端环境变量确认使用数据库名 `galacredit`；通过容器内只读数据库连接确认生产库共有 32 张表。
- 云端 `schema_migrations` 已应用：`001_decimal_money_fields`、`004_admin_ops_extensions`、`005_risk_decision_engine`、`006_risk_phase2_4`；4 张风控核心表均存在。
- 管理域名 `https://gala.ebamotor.com/download/galacredit.apk` 返回真实 Android APK，大小 33487 字节，SHA-256 为 `8aa4340a6877e1429f4915b582e242b082964e31fb615a05b128cb7b51af154d`；版本清单为 `versionCode=25`、`versionName=0.2.0`。
- 云端 APK 内含 `com/galacredit/app` 原生 Android 壳代码，配置方向与当前生产域名体系一致；它不是可直接证明为 UniApp App-Plus 原生包的证据，当前更符合“原生壳承载线上 H5”的实现。

### 当前异常与边界

- 用户 H5 域名下的 `https://galacredit.ebamotor.com/download/galacredit.apk` 和 `/download/galacredit-version.json` 当前均返回 H5 `index.html`（356 字节），不是 APK/JSON；该域名的 `/data/www/galacredit` 没有 `download/` 目录。安卓下载应暂时使用管理域名地址，但这说明用户域名下载链路未恢复一致。
- 当前云端 APK 与版本清单最后修改时间为 2026-08-22，而 UniApp H5 入口最后修改时间为 2026-09-06；因此安卓包没有证据表明已同步到 9 月 6 日的最新 UniApp 页面版本。
- 未执行登录、验证码发送、OCR、借款申请、放款、还款等会产生业务副作用的真实操作；本轮仅做未登录鉴权、静态资源、容器、数据库结构和迁移记录核对。
- `/api/openapi.json` 未公开（HTTP 404）属于当前配置现象，不代表后端不可用；选定的用户端和管理端受保护路由已能到达鉴权层。

### 结论

- H5、管理端、后端、Caddy 和数据库的云端基础链路目前是互通且运行中的。
- 数据库与后端的生产隔离正确，当前连接的是 `galacredit`，未发现误连 `xiaohebao` 的证据。
- 不能判定“所有端完全一致且全部正常”：安卓下载入口存在域名分裂，云端 APK 版本明显早于当前 H5；UniApp Android App-Plus 真机业务闭环也尚未完成实测。
- 建议下一步优先修复用户域名 `/download/*` 的静态目录/路由，再重新构建并发布与当前 UniApp H5 同批次的 Android 包，然后进行一条测试账号的登录—资料—管理端可见性闭环验证。

### 操作边界

- 本轮所有云端命令均为只读检查，没有上传文件、重启容器、修改 Caddy、修改数据库或写入业务数据。
- 已按仓库要求将本记录插入文件顶部；未提交 commit。
# 2026-09-06 11:12:00

## 修复用户域名下载、发布 UniApp Android 包与生产闭环验证

### 已完成

- `frontend_uniapp` 单元测试通过：13 个测试文件、41 项测试全部通过。
- H5 生产构建通过，使用 `VITE_API_BASE_URL=https://galacredit.ebamotor.com/api`；`frontend_uniapp/dist` 已同步到云端 `/data/www/galacredit`，本地与远端 `index.html` SHA-256 一致。
- UniApp 官方 app-plus 构建通过：`npm run build:uni:app` 成功生成 `dist/build/app`，包含 19 个页面资源。
- 尝试使用 HBuilderX CLI 云打包时，DCloud 返回“今天已打包很多次”的配额限制；因此没有把云打包任务误报为成功。
- 采用此前已验证的同 AppID、同签名 app-plus 基础 APK，仅替换最新官方 `dist/build/app` 资源，生成 `GalaCredit-1.0.3-103.apk`。
- APK 使用 `app-plus-compatible.jks` 签名，`apksigner verify` 的 v1/v2/v3 签名校验通过；本地 SHA-256：`4e0bfbe96c2e0e3ba41de42b1dda846acc1ec65f343708820ceb3d6b504e567d`。
- 云端 APK 和版本清单已发布到两个域名的下载目录：
  - `/data/www/gala.ebamotor.com/download`
  - `/data/www/galacredit/download`
- 两个域名的下载地址均已恢复：APK 返回 `application/vnd.android.package-archive`，版本 JSON 返回 `application/json`，版本统一为 `1.0.3 / versionCode 103`，清单中的 SHA-256 与 APK 一致。
- 云端 H5、管理端、用户 API、管理 API 进行无副作用回归：H5/管理端返回 200，未登录用户和管理接口分别返回统一 401。

### 闭环验证结果

- 云端生产数据库中确认存在 30 个用户，但没有可安全识别为“测试账号”的标记或凭据。
- 云端启用 `SMS_CODE_MOCK_ENABLED=true`，但短信登录仍要求一次性 slider captcha ticket；未提供测试手机号和可用验证码票据时，尝试登录返回统一 401 `短信验证码错误或已过期`。
- 本机当前 `adb devices` 没有连接 Android 真机或模拟器，因此无法执行新 APK 的安装、冷启动和真机页面验收。
- 为避免冒用真实客户账号或向生产库写入未经授权的申请、OCR、放款、还款数据，本轮未继续执行真实业务提交。

### 当前结论

- 用户域名 `/download/*` 已修复，H5、管理端和后端的公网链路保持正常。
- 新版 UniApp app-plus 资源已进入 1.0.3 APK 并发布云端；但该包是基于既有同签名 app-plus 运行库的本地重打包，不是本次 DCloud 云打包配额内生成的全新云签名包。
- “测试账号登录 → 用户业务提交 → 管理端可见 → 数据库落库”的完整生产闭环仍未完成，阻断原因是缺少用户明确提供的测试手机号/验证码票据，以及当前没有连接 Android 设备。

### 后续需要用户提供

- 一个明确授权的 GalaCredit 测试手机号（不要提供真实客户账号）。
- 对应的测试验证码流程或允许使用的验证码票据；若要验证安卓端，还需连接 Samsung/Android 真机或启动可用模拟器。
- 明确要验证到哪一个业务节点：仅登录与资料同步，还是继续到 OCR、借款申请、管理端审核和数据库状态核对。

### 操作边界

- 云端写入仅涉及 GalaCredit H5 静态目录、两个下载目录及版本清单；发布前已创建下载文件备份目录。
- 未修改数据库结构、未重启后端或 Caddy、未触碰其他站点和容器。
- 已按仓库要求将本记录插入文件顶部；未提交 commit。
# 2026-09-06 11:24:00

## Android APK 手机下载不结束问题修复

### 问题判断

- 用户截图显示 APK 已达到 `15.20 MB / 15.20 MB`，但下载任务仍显示暂停/进行状态。
- 线上原响应虽有正确 `Content-Type`、`Content-Length` 和 Range 支持，但 `/download/*` 之前复用了 SPA 通用处理，未显式声明附件下载。
- 该现象更符合 Android 下载管理器未完成附件响应识别，而不是 APK 文件内容损坏；命令行完整下载后 SHA-256 与云端文件一致。

### 云端修复

- 在 Caddy 的 `galacredit.ebamotor.com` 站点新增 `/download/*` 独立静态处理，root 指向 `/data/www/galacredit`，避免下载路径落入 `/index.html` 回退。
- 增加响应头：
  - `Content-Disposition: attachment; filename="galacredit.apk"`
  - `Cache-Control: public, max-age=3600`
- 修改前已备份 Caddyfile；`caddy validate` 通过，仅 reload Caddy，未重启 Docker 编排。

### 验证结果

- `https://galacredit.ebamotor.com/download/galacredit.apk` 返回 HTTP 200。
- 响应 `Content-Type: application/vnd.android.package-archive`。
- 响应包含 `Content-Length: 15201975`、`Accept-Ranges: bytes` 和明确的 `Content-Disposition`。
- Range `0-1023` 返回 206/1024 字节。
- 完整下载 15,201,975 字节成功，SHA-256 为 `4e0bfbe96c2e0e3ba41de42b1dda846acc1ec65f343708820ceb3d6b504e567d`。
- H5 与管理端首页仍分别返回 HTTP 200。

### 代码文档

- 已将下载目录不能走 SPA 回退、必须校验 APK 响应头和 SHA-256 的运维要求补充到 `doc/deployment_cloud.md`。

### 用户侧建议

- 在手机浏览器中取消旧的卡住任务后重新打开链接，确保使用新的 `Content-Disposition` 响应。
- 若仍显示旧任务，清除该下载任务后再重新下载；不要继续点旧任务的“暂停/继续”。
- 本轮无法直接控制用户手机上的历史下载任务，因此手机端最终“已完成”状态仍需重新发起一次下载确认。

### 操作边界

- 本轮仅修改云端 Caddy 下载路由和本地运维文档；未修改后端业务、数据库或其他站点。
- 已按仓库要求将本记录插入文件顶部；未提交 commit。
# 2026-09-06 11:46:00

## 修复 Android 安装后显示绿色 H 图标

### 问题原因

- 检查云端 APK 后确认应用启动器图标来自 app-plus 基础包内的 `res/drawable-xxhdpi/icon.png`。
- 该文件是 HBuilderX 默认绿色“H”图标，与页面使用的 GalaCredit 信用卡 Logo 无关；页面 Logo 正常不代表 Android Launcher 图标会自动同步。
- 原 UniApp `manifest.json` 没有配置 `app-plus.distribute.icons.android`，因此打包沿用了基础包默认图标。

### 修改内容

- 在 `frontend_uniapp/src/manifest.json` 和 `frontend_uniapp/manifest.json` 增加 Android 图标配置，覆盖 hdpi、xhdpi、xxhdpi、xxxhdpi。
- 从现有 `frontend_uniapp/src/static/logo.svg` 生成对应 PNG 资源：
  - `src/static/icons/icon-hdpi.png`
  - `src/static/icons/icon-xhdpi.png`
  - `src/static/icons/icon-xxhdpi.png`
  - `src/static/icons/icon-xxxhdpi.png`
- 使用最新 UniApp app-plus `dist/build/app` 资源和同签名基础包重打包，生成 `GalaCredit-1.0.4-104.apk`。
- APK 内 `res/drawable-xxhdpi/icon.png` 已抽取复核，不再是绿色“H”，而是 GalaCredit 橙色信用卡图形。

### 测试与发布

- manifest JSON 解析通过。
- UniApp Vitest：13 个测试文件、41 项测试全部通过。
- `npm run build:uni:app` 通过。
- APK v1/v2/v3 签名校验通过。
- 新 APK SHA-256：`57ec5c207a40638b18297bb61357e7992fa77ad9d976c6049aeb5c888e955a5d`。
- 云端两个下载目录均已更新为 `versionName=1.0.4`、`versionCode=104`，版本清单 SHA-256 与新 APK 一致。
- 用户域名和管理域名的 APK 文件均已完成远端 SHA-256 核对。

### 安装注意

- Android Launcher 可能缓存旧图标；安装 1.0.4 前建议先卸载旧版绿色“H”包，或安装后重启桌面/手机再观察。
- 新下载地址保持不变：`https://galacredit.ebamotor.com/download/galacredit.apk`。
- 本机当前没有连接 Android 真机，因此未执行本轮 1.0.4 的实机安装截图验收；APK 内资源已做静态抽取验证。

### 操作边界

- 未修改后端、数据库或 Caddy；仅更新 UniApp manifest、图标资源和云端 APK/版本清单。
- 已按仓库要求将本记录插入文件顶部；未提交 commit。
# 2026-09-06 12:10:00

## 绿色 H 图标复核、UniApp 类型确认与应用内升级能力核对

### 复核结论

- 本地/云端 1.0.4 APK 的包名为 `com.galacredit.app`，版本为 `1.0.4 (104)`。
- APK Manifest 使用 `io.dcloud.application.DCloudApplication`、`io.dcloud.WebAppActivity`，并包含 `assets/apps/__UNI__G882E18E`、`app-service.js`、`uni-app-view.umd.js` 和 19 个页面资源，确认当前包是 UniApp app-plus 包，不是旧 `android_native` WebView 壳。
- APK Manifest 的应用图标引用为 `@drawable/icon`；抽取 `res/drawable-xxhdpi/icon.png` 已确认是 GalaCredit 橙色信用卡图形，不是绿色 H。
- 因此用户手机仍出现绿色 H，最可能是下载管理器/浏览器缓存了之前的 1.0.3 APK，或实际安装的不是当前 1.0.4 文件；仅看下载文件名 `galacredit.apk` 无法区分缓存版本。

### 防缓存发布

- 追加唯一文件名下载地址：`https://galacredit.ebamotor.com/download/galacredit-1.0.4.apk`。
- 管理端域名也同步了同名文件；两个文件 SHA-256 均为 `57ec5c207a40638b18297bb61357e7992fa77ad9d976c6049aeb5c888e955a5d`。
- 版本清单已更新为唯一文件名地址，版本 `1.0.4 / 104`。
- 源码两个 UniApp manifest 的版本号已同步为 `1.0.4 / 104`，避免后续重新构建回退到 `1.0.2 / 102`。

### 应用内升级核对

- 当前 `frontend_uniapp` 中没有实现检查版本清单、下载 APK、校验 SHA-256、调起 Android 安装确认的升级流程；现有 `versionCode` 只存在于构建/风险信息上下文，不会自动弹出升级。
- 可以实现“应用内检查更新 → 用户确认 → 下载 → 校验 → 系统安装确认”，但 Android 不允许应用静默安装；用户需要授予该来源安装权限并确认系统安装界面。
- 如果通过 Google Play 分发，应使用 Play In-App Updates/商店更新，不应在 Play 包中通过外部 APK 自更新；当前下载升级方案适合官网/内部 APK 分发。

### 仍需手机侧确认

- 请卸载绿色 H 包后，使用唯一文件名地址重新下载并安装：
  `https://galacredit.ebamotor.com/download/galacredit-1.0.4.apk`
- 安装后在系统“应用信息”中核对：包名 `com.galacredit.app`、版本 `1.0.4`；若版本仍为 `1.0.3` 或图标仍为绿色 H，则手机安装的不是本次文件。

### 操作边界

- 本轮未修改后端和数据库；更新了 UniApp manifest 版本号和云端唯一文件名 APK/版本清单。
- 已按仓库要求将本记录插入文件顶部；未提交 commit。
# 2026-09-06 13:28:00

## UniApp H5/Android 申请流、底部导航、首页额度展示与网络版升级

### 本轮修改

- 新增 `src/utils/application-flow.js`，将 INIT、REJECTED、SETTLED、REVIEWING、APPROVED、还款中状态统一映射到唯一下一步页面；Home、My Applications、Review 共用该规则，修复新用户从不同入口跳到不同申请页面的问题。
- Home 新用户通过 `/loan/products?display_only=true` 读取后台贷款产品展示配置；该查询只返回首页展示数据，不改变提现产品资格筛选。首页支持预期额度、最小日利率、最大贷款期限，并在拒绝时显示英文提示 `Please improve your credit record and apply again later.`。
- 管理端贷款产品表单增加首页预期额度、首页最小日利率、首页最大贷款期限字段；配置存入既有 `fee_components_json`，无需新增表或外键，兼容已有产品历史记录。
- App-plus 新增胶囊式三项底部导航（Home / My Applications / My Account），图标在上、文案在下；H5 底部导航同步为三项胶囊结构。
- 新增网络版应用内升级：启动 App 或进入 My Account 时请求版本清单、比较 `versionCode`、用户确认、下载 APK、SHA-256 校验，并通过 `plus.runtime.install` 调起 Android 系统安装确认。H5 与非 Android 环境不会执行 APK 安装。

### 测试与构建

- UniApp Vitest：15 个测试文件、44 项测试通过。
- `npm run build:h5` 通过。
- `npm run build:uni:app` 通过。
- 后端 `compileall` 通过。
- 后端全量 pytest：170 通过、14 失败；失败集中在既有测试夹具/事件循环/日志配置兼容问题，未发现由本轮新增首页展示字段引起的语法错误。需在后续后端测试治理中单独修复，不能将全量后端测试标记为通过。

### 当前边界

- 本轮未重新发布云端 H5/APK，也未在实体 Android 设备上确认系统安装页面；代码、H5 构建和 app-plus 构建已验证。
- 版本清单仍需在发布新 APK 时同步 `versionCode`、APK URL 和 SHA-256；Google Play 更新机制未混入网络版方案。

### Git

- 已按仓库要求执行 `git add`，未提交 commit。
# 2026-09-06 13:52:00

## 云端 H5 发布与 Android APK 安装结果

### H5 发布

- 将最新 `frontend_uniapp/dist` 发布到云端 `/data/www/galacredit`。
- 删除传输过程中产生的 macOS `._*` 元数据文件，避免被浏览器当作静态资源。
- 公网 `https://galacredit.ebamotor.com/` 返回 HTTP 200。
- 公网首页 SHA-256 与本地 `frontend_uniapp/dist/index.html` 一致：`f20fd81cdadbeba8565e12e02d39b394388fbe32d174a481e2ee1ca23ccd42f2`。
- 公网响应的 `Last-Modified` 已更新为本轮发布时间。

### Android APK

- 已将 UniApp manifest 版本提升为 `1.0.5 / versionCode 105`。
- 当前 Android 真机已识别：`RFGYA0DF38E`。
- HBuilderX 云打包请求返回“今天已打包很多次”，无法生成本轮 1.0.5 新包。
- 云端返回的最近成功包实际为 `versionCode 102`，使用 `adb install -r` 验证时被 Android 正确拒绝为降级安装；没有覆盖当前 104 包，也没有误报安装成功。
- 由于本轮没有获得 105 APK，未更新线上版本清单，避免客户端下载到错误或不存在的升级包。

### 结论

- H5 已完成云端发布并验证公网内容一致。
- Android 新 APK 未完成生成/安装，唯一阻塞是 HBuilderX 云打包次数限制；需下一打包周期重新执行 `frontend_uniapp/scripts/pack-app-plus.sh`，确认 APK 为 `105` 后再上传并安装。

### Git

- 已执行 `git add`，未提交 commit。
# 2026-09-06 17:47:00

## UniApp iOS 版本适配推进

### 本轮修改

- 核对当前 UniApp 页面图、H5/Android 共用页面组件和 iOS 平台分支，确认 iOS 沿用同一套路由、业务状态机和页面交互实现。
- 补充 iOS 隐私用途声明：相机、定位、相册和通讯录，覆盖 Ghana Card/人脸照片、位置检查和紧急联系人选择流程。
- 新增 iOS 配置测试，校验隐私声明及关键业务页面存在。

### 验证结果

- UniApp Vitest：16 个测试文件、46 项测试通过。
- `npm run build:uni:app` 通过，app-plus 页面资源构建成功；该构建产物由 UniApp 同时承载 Android 与 iOS 页面逻辑。
- 当前机器 `xcode-select` 仅指向 CommandLineTools，未发现完整 Xcode，因此本轮无法执行 iOS Simulator、签名、Archive 或实体 iPhone 安装验证。

### 当前边界

- iOS 源码配置和共用页面逻辑已完成静态验收；真机视觉、权限弹窗和系统安装包仍需在完整 Xcode、Apple 签名证书和 iPhone 设备环境中验证。

### Git

- 已执行 `git add`，未提交 commit。
# 2026-09-06 18:05:00

## UniApp iOS 构建资料与验收清单

### 本轮结果

- 重新确认当前机器仍只有 `/Library/Developer/CommandLineTools`，没有完整 Xcode、iOS Simulator 或可发现的 iPhone 设备。
- 新增 [doc/ios_build.md](../ios_build.md)，记录 HBuilderX iOS 云打包、签名配置、权限和真机验收顺序。
- 保留并验证 iOS 隐私用途声明和 iOS 配置测试；前端测试继续为 16 个测试文件、46 项通过。
- 已执行 `git add`，未提交 commit。

### 阻塞边界

- iOS 源码、共享页面逻辑和构建前检查已具备；IPA 签名、Simulator、Archive 和实体 iPhone 验收需要完整 Xcode 与 Apple 开发者签名环境。
# 2026-09-06 17:54:00

## UniApp iOS 刘海屏与底部导航安全区适配

### 修改内容

- 底部胶囊导航改为使用 `env(safe-area-inset-bottom)`，避免 iPhone Home Indicator 区域遮挡导航。
- 共享页面底部留白增加安全区空间，避免表单按钮、固定操作栏和底部导航重叠。

### 验证结果

- UniApp Vitest：16 个测试文件、46 项测试通过。
- `npm run build:uni:app` 通过。
- 已执行 `git add`，未提交 commit。
# 2026-09-06 17:58:00

## UniApp iOS 共享入口跳转一致性审查

### 修改内容

- 检查所有 UniApp 页面中的 `navigator`、`navigateTo`、`reLaunch` 和 `switchTab` 目标。
- 将 My Account 页面中的 Apply、Under Review、Repayment 三个服务入口统一接入 `applicationNextPage`，根据当前用户状态进入身份验证、审核、提现或账单页面，避免 iOS/H5/Android 从不同入口绕过状态机。
- 扩展页面完整性测试，确保服务入口持续使用共享申请流。

### 验证结果

- UniApp Vitest：16 个测试文件、47 项测试通过。
- `npm run build:h5` 通过。
- `npm run build:uni:app` 通过。
- 已执行 `git add`，未提交 commit。
# 2026-09-06 18:02:00

## UniApp iOS/H5/Android 服务入口状态流统一

### 修改内容

- My Account 页面服务入口现在先读取用户借款状态，再通过统一 `applicationNextPage` 决定下一页。
- Apply、Under Review、Repayment 三个入口不会再分别固定跳转到提现、审核和账单页面，iOS、H5、Android 的业务流保持一致。
- 页面完整性测试新增服务入口约束。

### 验证结果

- UniApp Vitest：16 个测试文件、47 项测试通过。
- `npm run build:h5` 通过。
- `npm run build:uni:app` 通过。
- 已执行 `git add`，未提交 commit。
# 2026-09-06 18:03:00

## UniApp iOS My Account 网络容错

### 修改内容

- My Account 页面将用户资料与借款状态请求改为独立容错。
- 借款状态接口短暂失败时，账户资料仍正常显示，并提示状态暂不可用；服务入口回退到统一 INIT 申请流，用户可以稍后重试。
- 该行为在 iOS、H5、Android 共享页面中一致。

### 验证结果

- UniApp Vitest：16 个测试文件、47 项测试通过。
- `npm run build:h5` 通过。
- `npm run build:uni:app` 通过。
- 已执行 `git add`，未提交 commit。
# 2026-09-06 18:05:00

## UniApp iOS 键盘弹起布局适配

### 修改内容

- 在 `pages.json` 全局 app-plus 配置中启用 `softinputMode: adjustResize`。
- 覆盖紧急联系人、修改密码、邀请码、还款确认等所有共享输入页面，iPhone 键盘弹起时自动调整可视区域。
- 新增 iOS 配置测试，锁定全局键盘适配配置。

### 验证结果

- UniApp Vitest：16 个测试文件、48 项测试通过。
- `npm run build:uni:app` 通过。
- 已执行 `git add`，未提交 commit。
# 2026-09-06 18:10:47

# 2026-09-06 18:12:30

# 2026-09-06 18:14:10

# 2026-09-06 18:16:50

# 2026-09-06 18:19:05

# 2026-09-06 18:21:00

# 2026-09-06 18:27:00

## 主机磁盘空间清理

### 执行内容

- 只读盘点后确认用户缓存、npm 缓存和开发工具缓存是主要可安全清理项。
- 将 `~/Library/Caches`、npm 缓存、用户级通用缓存移入废纸篓，并清空废纸篓。
- 执行 `brew cleanup --prune=all`，清理旧 Homebrew 版本和下载缓存。
- 未删除项目源码、数据库、证书、`/Library/Developer` 系统开发工具目录或系统临时目录。

### 结果

- 可用空间从约 20 GiB 增加到约 29 GiB，释放约 9 GiB。
- `~/Library/Caches` 与 `~/.cache` 当前为 0B，`~/.npm` 约 52KB。


## 当前 UniApp iOS 目标阶段性验收

### 当前已验证

- UniApp app-plus 构建产物存在，H5 构建产物存在。
- 工作树通过 `git diff --check`，本轮构建生成的前端产物已准备暂存。
- 页面路由、申请状态流、底部胶囊导航、iOS 权限配置、平台能力隔离和应用内升级逻辑均已有自动化检查或构建证据。

### 尚未完成的验收

- 当前 macOS 只有 CommandLineTools，没有完整 Xcode、iOS Simulator 或可用 iPhone，因此无法完成 IPA 签名、安装、截图和真机逐页点击验收。
- 这属于外部环境条件，不代表源码或构建失败；获得 Apple Team/证书/设备后需按 `doc/ios_build.md` 继续验收。


## H5 与 app-plus 底部胶囊导航对齐

### 修改内容

- 修复 H5 底部导航首项仍显示 `GalaCredit` 且使用不同图标的问题。
- H5 与 app-plus 现在统一使用 `Home`、`My Applications`、`My Account` 三项，首项统一使用 Home 图标。
- 新增底部导航一致性测试，覆盖标签、路由和 iOS 安全区留白。

### 验证结果

- Vitest：17 个测试文件、51 项测试通过。
- `npm run build:h5`：通过。
- `npm run build:uni:app`：通过。


## iOS 发布配置一致性回归

### 修改内容

- 增加 `tests/ios-config.test.js` 回归检查，确保根目录 `manifest.json` 与 `src/manifest.json` 的 `versionName`、`versionCode` 和 iOS 隐私用途声明保持一致，避免云打包读取不同配置。

### 验证结果

- Vitest：16 个测试文件、50 项测试通过。
- `npm run build:h5`：通过。
- `npm run build:uni:app`：通过。
- `git diff --check`：通过。


## UniApp 页面路由与 iOS 平台隔离审计

### 审计结果

- 扫描所有页面及组件中的静态 `navigateTo`、`switchTab`、`reLaunch` 目标，未发现未在 `pages.json` 声明的页面路径。
- 检查定位、图片选择、联系人选择、短信和 APK 更新等平台能力：Android 专用短信/联系人逻辑均有平台或原生桥接隔离；iOS 使用定位、图片和联系人桥接，不读取 Android SMS。
- 未发现需要本轮修改的路由或平台隔离问题。


## 页面流回归测试补强

### 修改内容

- 为 `tests/pages-integrity.test.js` 增加已完成 Face Verification 后继续进入紧急联系人页面的回归断言。

### 验证结果

- Vitest：16 个测试文件、49 项测试通过。
- `npm run build:uni:app`：通过。
- 期间一次在 `frontend_uniapp` 子目录执行 git add 时使用了错误的相对路径，未改变文件内容；随后从仓库根目录重新暂存。


## UniApp 页面跳转审查：已完成的人脸认证可继续申请

### 修改内容

- 修复 `frontend_uniapp/src/pages/face/index.vue`：用户已经通过人脸认证后再次进入该页面时，原按钮被禁用，无法进入紧急联系人页面。
- 认证成功状态下按钮改为 `Continue Application`，点击统一进入 `/pages/application/index`；认证进行中仍保持禁用，避免重复提交。

### 验证结果

- `npm test`：16 个测试文件、48 项测试全部通过。
- `npm run build:h5`：构建通过。
- `npm run build:uni:app`：app-plus 构建通过。

### 当前边界

- 当前机器仍未安装完整 Xcode，且没有可用 iPhone/模拟器，因此本轮完成的是跨端源码、构建和自动化测试验证，未宣称 iOS IPA 签名或真机交互验收完成。

# 2026-06-01 22:08:31

## 修改说明

- 补齐本地 `frontend_h5` 的 H5 前端源码，恢复 `src/`、`public/`、入口 HTML、依赖清单、Vite/Vitest 配置等开发所需文件。
- 恢复来源优先使用 Git 历史中 2026-05-25 删除源码前的提交 `ff3d63bf7dd5a3f56426bd396634ba5df88efe4b`，避免从本机备份带入旧 `.env`、证书等敏感配置。
- 保留当前已有的 `frontend_h5/dist` 发布产物，未用恢复源码覆盖现有生产发布包。
- 补充 `frontend_h5/src/test/setup.js` 并在 `vitest.config.js` 配置测试环境变量，使恢复后的原有 H5 测试可重复执行。

## 修改后的代码

- `frontend_h5/.env.production.example`
- `frontend_h5/.gitignore`
- `frontend_h5/.vscode/extensions.json`
- `frontend_h5/README.md`
- `frontend_h5/index.html`
- `frontend_h5/package-lock.json`
- `frontend_h5/package.json`
- `frontend_h5/pnpm-lock.yaml`
- `frontend_h5/public/*`
- `frontend_h5/src/*`
- `frontend_h5/vite.config.js`
- `frontend_h5/vite.https.config.js`
- `frontend_h5/vitest.config.js`
- `doc/work_result/20260601.md`

## 单元测试代码

- 恢复并验证 `frontend_h5/src/**/*.test.js`。
- 新增恢复缺失的测试环境文件 `frontend_h5/src/test/setup.js`。

## 集成测试代码

- 本次未新增后端集成测试；通过 H5 本地开发服务启动和首页 HTTP 可达性验证开发入口。

## 测试执行结果

- H5 依赖安装：`npm ci` 通过。
- H5 单元测试：`npm test` 通过，`7 files / 16 tests` 全部通过。
- H5 临时构建：`npx vite build --outDir dist-restore-check` 通过；构建后已删除临时目录，未覆盖现有 `dist`。
- H5 本地开发服务：`npm run dev -- --host 127.0.0.1 --port 2201` 启动成功；访问 `http://127.0.0.1:2201/` 返回 `200 OK`；验证后已停止临时服务。
- `npm ci` 提示依赖审计存在 `9 vulnerabilities`，本次未执行 `npm audit fix`，避免引入无关依赖升级和行为变化。

## 操作步骤

- 检查当前 `frontend_h5` 仅有 `dist`，缺少源码。
- 从 Git 历史确认 H5 源码在 2026-05-25 同步 baseline 时被删除。
- 从删除前提交恢复源码、配置、依赖清单和 public 资源。
- 仅从本机备份补回测试环境 setup 文件，不带入旧 `.env` 或证书。
- 执行依赖安装、单元测试、临时构建和本地服务可达性验证。

# 2026-06-01 21:42:56

## 修改说明

- 根据 iOS 真机截图调整风控报告区域小卡片内部文案的垂直位置。
- 将报告时间、报告评估结论、申请准入分、信用行为分、最近放款、探查结果、正常还款比例以及风险维度格子内的内容，从顶部对齐改为左对齐且上下居中。
- 保持现有卡片尺寸、颜色、边框和字体不变，仅修正内容在区域内偏上的视觉问题。

## 修改后的代码

- `ios_native/Sources/Views/Screens/DetailView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次通过 iPhone 17 模拟器执行 iOS 测试。
- 本次通过 Release 真机构建并安装启动到已连接 iPhone。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- `xcodebuild -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -configuration Release -destination 'generic/platform=iOS' build` 通过。
- 已通过 `devicectl` 将 Release 包安装到真机 `temp`，并成功启动 `com.juxin.xiaohebao.ios`。

## 操作步骤

- 对照截图定位风控报告小卡片内容偏上的原因。
- 将相关小卡片和风险维度格子的 `frame` 对齐方式由顶部对齐调整为垂直居中。
- 执行 iOS 测试、Release 构建、真机安装与启动。

# 2026-06-01 21:28:09

## 修改说明

- 针对 iOS 页面加载、上下滑动时的轻微颤抖和卡顿做滚动性能优化。
- 详情页主内容从一次性渲染 `VStack` 调整为 `LazyVStack`，降低长详情页首次渲染和滚动过程中的布局压力。
- 认证照片加载增加内存缓存，并使用 `ImageIO` 按场景降采样：列表缩略图使用较小尺寸，照片预览使用较大尺寸，避免原图直接参与滚动渲染。
- 将详情页右滑退出手势从全屏监听调整为左侧边缘手势区，减少与纵向滚动手势竞争导致的抖动。
- 对通用卡片和底部操作胶囊增加合成分组，减少多层阴影在滚动时反复离屏渲染。

## 修改后的代码

- `ios_native/Sources/Views/Components/AppTheme.swift`
- `ios_native/Sources/Views/Screens/DetailView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次通过 iPhone 17 模拟器执行 iOS 测试。
- 本次通过 Release 真机构建并安装启动到已连接 iPhone。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- `xcodebuild -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -configuration Release -destination 'generic/platform=iOS' build` 通过。
- 已通过 `devicectl` 将 Release 包安装到真机 `temp`，并成功启动 `com.juxin.xiaohebao.ios`。

## 操作步骤

- 检查详情页滚动容器、认证照片加载、右滑退出手势和阴影渲染路径。
- 调整详情页懒加载渲染、图片缓存与降采样、边缘右滑退出手势和卡片合成策略。
- 执行 iOS 测试、Release 构建、真机安装与启动。

# 2026-06-01 21:07:17

## 修改说明

- 根据 iOS 真机截图修正详情页底部多操作胶囊宽度。
- 多按钮场景下底部胶囊从可露出第三个按钮的一截，收窄为只完整展示两个按钮；其余操作通过左右滑动查看，避免出现右侧半截按钮影响美观和识别。

## 修改后的代码

- `ios_native/Sources/Views/Screens/DetailView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次通过 iPhone 17 模拟器执行 iOS 测试。
- 本次通过 Release 真机构建并安装启动到已连接 iPhone。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- `xcodebuild -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -configuration Release -destination 'generic/platform=iOS' build` 通过。
- 已通过 `devicectl` 将 Release 包安装到真机 `temp`，并成功启动 `com.juxin.xiaohebao.ios`。

## 操作步骤

- 根据真机截图定位底部操作胶囊露出第三个按钮的问题。
- 收窄多操作胶囊最大可视宽度。
- 执行 iOS 测试、Release 构建、真机安装与启动。

# 2026-06-01 21:01:56

## 修改说明

- iOS 用户详情页新增从左往右拖动/滑动退出能力，保留左上角返回按钮，方便真机单手操作。
- 回款 Tab 的开始日期、结束日期输入框改为点击弹出日历选择器，不再弹出键盘，选择后自动刷新列表。
- 平账 Tab 检索区收窄输入框，将 `查询`、`清空` 放到同一行；`全部`、`已逾期`、`未逾期` 三个快捷标签改为等宽占满一行。
- 风控报告顶部前三行卡片收紧最小高度和内边距，减少下半部分空白，保持当前 iOS 视觉风格。

## 修改后的代码

- `ios_native/Sources/Views/Screens/DetailView.swift`
- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift` 验证工作台关键逻辑。

## 集成测试代码

- 本次通过 iPhone 17 模拟器执行 iOS 测试。
- 本次通过 Release 真机构建并安装启动到已连接 iPhone。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- `xcodebuild -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -configuration Release -destination 'generic/platform=iOS' build` 通过。
- 已通过 `devicectl` 将 Release 包安装到真机 `temp`，并成功启动 `com.juxin.xiaohebao.ios`。

## 操作步骤

- 复核详情页手势退出、回款日期输入、平账检索布局和风控报告卡片空白问题。
- 在详情页补充右滑退出手势。
- 将回款日期输入替换为日历选择弹窗。
- 调整平账检索行与逾期筛选标签宽度。
- 收紧风控报告顶部指标卡片高度和内边距。
- 执行 iOS 测试、Release 构建、真机安装与启动。

# 2026-06-01 20:21:17

## 修改说明

- 根据 iOS 真机截图继续优化前端可读性和详情页交互。
- 将页面底色从接近纯白调整为更柔和的浅灰蓝，通用卡片保持浅白并增加边框和阴影层次，提升长时间浏览时的区分度。
- 认证照片缩略图增加严格裁切，避免照片内容超出缩略区域遮挡其他内容。
- 认证照片支持点击后以弹窗形式查看完整图片。
- 风控报告中各小卡片增加淡灰边框和分隔线，风险维度区改为紧凑的 2x2 分组，缩小上下间距。
- 底部操作区在只有两个操作时改为自适应居中胶囊，按钮与底层胶囊使用不同颜色层级，提升识别度。

## 修改后的代码

- `ios_native/Sources/Views/Components/AppTheme.swift`
- `ios_native/Sources/Views/Components/PrimaryControls.swift`
- `ios_native/Sources/Views/Screens/DetailView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次通过 iPhone 17 模拟器执行测试。
- 本次通过 Release 真机构建并安装启动到已连接 iPhone。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- `xcodebuild -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -configuration Release -destination 'generic/platform=iOS' build` 通过。
- 已通过 `devicectl` 将 Release 包安装到真机 `temp`，并成功启动 `com.juxin.xiaohebao.ios`。

## 操作步骤

- 对照真机截图定位卡片区分度、照片溢出、风控报告松散、底部操作胶囊识别度问题。
- 调整主题背景与通用卡片样式。
- 接入照片预览弹窗并限制缩略图裁切。
- 将风控维度从松散网格改为带分隔线的紧凑分组。
- 调整底部操作胶囊在两个按钮场景下的布局。
- 执行测试、Release 构建、真机安装与启动。

# 2026-06-01 19:51:36

## 修改说明

- 根据真机生产环境详情页截图，修复 iOS 详情页卡片和底部操作栏的配色问题。
- 全局强制浅色模式，避免用户系统深色模式影响业务页面文字和色块。
- 将通用 `glassCard` 从系统毛玻璃改为稳定浅白实体玻璃卡片，避免真机渲染成灰色硬块。
- 认证照片加载从普通 `AsyncImage` 改为带登录 token 的图片加载，并兼容绝对 URL 与相对路径 URL。
- 为详情页各区块标题显式设置深色文字，避免出现白字不可识别。
- 调整风控报告卡片内标签、说明文字、分值字号和最小高度，使同一行卡片高度更一致。
- 底部多操作胶囊宽度改为最多约包住两个操作按钮，并保留横向滑动能力。

## 修改后的代码

- `ios_native/App/XiaoHeBaoIOSApp.swift`
- `ios_native/Sources/Views/Components/AppTheme.swift`
- `ios_native/Sources/Views/Screens/DetailView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次通过 iPhone 17 模拟器执行测试。
- 本次通过 Release 真机构建并安装启动到已连接 iPhone。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- `xcodebuild -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -configuration Release -destination 'generic/platform=iOS' build` 通过。
- 已通过 `devicectl` 将 Release 包安装到真机 `temp`，并成功启动 `com.juxin.xiaohebao.ios`。

## 操作步骤

- 对照真机截图定位详情页灰色卡片、白字、照片占位、风控报告卡片高度和底部操作胶囊问题。
- 修改 App 入口为浅色模式。
- 修改通用卡片材质和详情页区块文字颜色。
- 新增带 token 的远程图片加载视图并接入认证照片。
- 调整风控报告卡片排版和底部操作胶囊宽度。
- 执行测试、Release 构建、真机安装与启动。

# 2026-06-01 19:30:23

## 修改说明

- 修复 iOS 真机登录页视觉问题：去掉登录卡片上的系统 `ultraThinMaterial`，避免真机渲染成灰色硬块。
- 将登录页卡片改为浅白柔和背景，降低尺寸和按钮高度，使整体更轻、更软。
- 为账号和密码输入框显式设置深色文字与主色光标，避免真机输入文字发白不可见。
- 修复真机连接服务失败：`xcconfig` 中的 URL 原写法 `https://` 被 Xcode 当作注释截断为 `https:`，现改为 Xcode 兼容写法。
- 为 Release 包增加代码侧生产环境 fallback，即使 Info.plist 未注入配置，也会使用 `https://xhbadmin.juxin.pro/api`。

## 修改后的代码

- `ios_native/Config/Debug.xcconfig`
- `ios_native/Config/Release.xcconfig`
- `ios_native/Sources/Config/AppConfig.swift`
- `ios_native/Sources/Views/Screens/LoginView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次通过 iPhone 17 模拟器截图验证登录页视觉。
- 本次通过真机 Release 构建并安装到已连接 iPhone。

## 测试执行结果

- `xcodebuild -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -configuration Release -showBuildSettings` 显示 `XHB_API_BASE = https://xhbadmin.juxin.pro/api`。
- `curl https://xhbadmin.juxin.pro/api/admin/me` 返回未登录响应，证明生产域名和 HTTPS 可访问。
- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- `xcodebuild -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -configuration Release -destination 'generic/platform=iOS' build` 通过。
- 已通过 `devicectl` 将 Release 包安装到真机 `temp`，并成功启动 `com.juxin.xiaohebao.ios`。

## 操作步骤

- 对照真机截图定位登录页材质、输入文字颜色和尺寸问题。
- 修正 `xcconfig` URL 写法，确认 Release 构建获得完整生产地址。
- 调整登录页卡片、输入框和按钮样式。
- 执行测试、Release 真机构建、真机安装启动和模拟器截图验证。

# 2026-06-01 18:41:57

## 修改说明

- 按用户附件要求调整 iOS 详情页底部可执行操作区域。
- 单个操作按钮时，底层磨砂胶囊不再撑满屏幕宽度，改为刚好包住按钮并整体居中。
- 多个操作按钮时，保留横向滚动，但对底层胶囊做裁切，超出边界的按钮内容不再浮在底层区域上方。
- 提取 `actionButton` 复用现有按钮逻辑，避免重复实现和误改权限逻辑。

## 修改后的代码

- `ios_native/Sources/Views/Screens/DetailView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次重新安装并启动 iPhone 17 模拟器 App；由于模拟器当前回到登录页，未能直接进入详情页做业务画面截图。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- 已重新安装并启动 `com.juxin.xiaohebao.ios` 到 iPhone 17 模拟器，启动成功。

## 操作步骤

- 定位 iOS 详情页底部 `actionDock`。
- 区分单按钮和多按钮两种展示场景。
- 单按钮使用内容自适应宽度并居中展示；多按钮使用横向滚动和圆角裁切。
- 执行 iOS 测试并重新安装模拟器 App。

# 2026-06-01 18:22:33

## 修改说明

- 按用户要求将 iOS 底部 tab 组件整体继续向屏幕底部移动。
- 仅调整底部 dock 的底部留白，从 `24` 降为 `10`，保持浅色磨砂胶囊、选中凸起胶囊和横向滚动能力不变。

## 修改后的代码

- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次通过 iPhone 17 模拟器重新安装并截图验证底部 tab 位置。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- 已重新安装并启动 `com.juxin.xiaohebao.ios` 到 iPhone 17 模拟器。
- 模拟器截图验证：底部 tab 已进一步靠近屏幕底部，5 个入口完整可见。

## 操作步骤

- 定位 iOS 工作台底部 tab 的底部留白。
- 同步下调 tab 内容和背景胶囊的底部 padding。
- 执行 iOS 测试、重新安装模拟器 App 并截图核验。

# 2026-06-01 18:18:32

## 修改说明

- 按用户补充要求，将 iOS 底部 tab 统一为白天模式效果，不使用附件深色模式中的黑色底栏。
- 保留参考图的结构语言：浅色磨砂圆角 dock、选中 tab 为凸起胶囊、图标和文字居中展示。
- 收窄单个 tab 宽度，使当前 5 个 tab 在 iPhone 17 模拟器上完整可见；外层仍保留横向滚动能力，兼容后续权限导致的更多 tab。

## 修改后的代码

- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次通过 iPhone 17 模拟器重新安装并截图验证底部 tab 展示。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- 已重新安装并启动 `com.juxin.xiaohebao.ios` 到 iPhone 17 模拟器。
- 模拟器截图验证：底部 tab 为浅色白天模式，5 个 tab 完整可见，选中项为凸起胶囊。

## 操作步骤

- 复核 iOS 工作台底部 tab 代码。
- 将选中态和 dock 保持浅色磨砂玻璃风格。
- 收窄 tab item 宽度，避免当前 5 个入口在屏幕右侧被裁切。
- 执行 iOS 测试、重新安装模拟器 App 并截图核验。

# 2026-06-01 16:55:50

## 修改说明

- 按用户截图要求调整 iOS 申请详情页顶部审核员入口位置。
- 将 `审核员 未分配` 从姓名下方移动到顶部 header 右侧，靠近屏幕右边显示。
- 左侧保留姓名、状态标签、手机号和订单状态，右侧作为审核员入口，保持可点击转单能力不变。

## 修改后的代码

- `ios_native/Sources/Views/Screens/DetailView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次通过 iPhone 17 模拟器重新安装并启动 App，验证构建产物可运行。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- 已重新安装并启动 `com.juxin.xiaohebao.ios` 到 iPhone 17 模拟器，启动成功。

## 操作步骤

- 定位 iOS 详情页顶部 header 布局。
- 将审核员 badge 从左侧文字栈迁移到 header 右侧。
- 执行 iOS 测试并重新安装模拟器 App。

# 2026-06-01 16:51:50

## 修改说明

- 按用户截图要求调整 iOS 工作台底部 tab 栏。
- 将底部 tab 从悬浮胶囊改为贴屏幕底部的整条底栏，背景延伸到 Home 指示条区域。
- tab 按钮保留当前柔光玻璃风格与选中胶囊效果，但去掉外层悬浮边距。
- 底部 tab 容器改为横向滚动，若后续权限导致 tab 数量过多，可通过左右滑动选择。

## 修改后的代码

- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次通过 iPhone 17 模拟器重新安装并截图验证底部 tab 展示。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- 已重新安装并启动 `com.juxin.xiaohebao.ios` 到 iPhone 17 模拟器。
- 模拟器截图验证：底部 tab 栏已贴近屏幕底部，5 个 tab 完整可见。

## 操作步骤

- 定位 iOS 工作台底部 tab 栏布局。
- 删除外层悬浮胶囊和底部负边距，改为贴底整条背景。
- 使用横向 `ScrollView` 承载 tab 按钮，兼容更多 tab 场景。
- 执行 iOS 测试、重新安装模拟器 App 并截图核验。

# 2026-06-01 16:33:48

## 修改说明

- 按用户截图要求调整 iOS 用户详情页顶部标签位置。
- 将 `正常`/`黑名单`/`位置风控` 等状态标签从姓名下方移动到姓名右侧，同一行展示。
- 保留手机号与订单状态作为姓名下方副标题；申请页审核员标签继续放在下一行，避免顶部信息过度拥挤。

## 修改后的代码

- `ios_native/Sources/Views/Screens/DetailView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次通过 iPhone 17 模拟器重新安装并启动 App，验证构建产物可运行。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- 已重新安装并启动 `com.juxin.xiaohebao.ios` 到 iPhone 17 模拟器，启动成功。

## 操作步骤

- 定位详情页顶部 header 的姓名、副标题和状态标签布局。
- 将状态标签并入姓名行，保留现有标签样式。
- 执行 iOS 测试并重新安装模拟器 App。

# 2026-06-01 16:28:55

## 修改说明

- 根据用户截图反馈修正 iOS 详情页对 iPhone 灵动岛、状态栏和底部 Home 指示条的适配。
- 保留详情页背景铺满全屏的效果，但取消详情层整体忽略安全区，避免左上角返回按钮、姓名和状态信息进入灵动岛区域。
- 详情页滚动内容增加顶部安全区留白，确保顶部信息从状态栏下方开始展示。
- 底部可执行操作区增加底部安全区留白，避免贴近或遮挡 Home 指示条。

## 修改后的代码

- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `ios_native/Sources/Views/Screens/DetailView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次通过 iPhone 17 模拟器重新安装并启动 App，验证构建产物可运行。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- 已重新安装并启动 `com.juxin.xiaohebao.ios` 到 iPhone 17 模拟器，启动成功。

## 操作步骤

- 对照用户截图确认顶部内容被灵动岛/状态栏覆盖。
- 调整详情页全屏覆盖方式，让背景全屏但内容遵守安全区。
- 为顶部滚动内容和底部操作区分别补充安全区间距。
- 执行 iOS 测试并重新安装模拟器 App。

# 2026-06-01 16:19:27

## 修改说明

- 按用户要求调整 iOS 用户详情页进入与退出动效。
- 将详情页从系统 `sheet` 弹出改为工作台内全屏覆盖层，点击任意 tab 的用户卡片后，详情页从屏幕右侧向左滑入并覆盖全屏。
- 将详情页左上角返回按钮改为调用工作台自定义关闭逻辑，点击后详情页从左向右滑出退出。
- 保持详情页现有内容、按钮、弹窗和 iOS 柔光玻璃视觉风格不变。

## 修改后的代码

- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `ios_native/Sources/Views/Screens/DetailView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift` 验证核心业务逻辑。

## 集成测试代码

- 本次通过 iPhone 17 模拟器重新安装并启动 App，验证应用可正常进入工作台。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- 已重新安装并启动 `com.juxin.xiaohebao.ios` 到 iPhone 17 模拟器，启动成功。

## 操作步骤

- 定位 iOS 工作台中用户卡片点开详情的系统 sheet 入口。
- 改为 `overlay` 全屏详情层，并使用右侧 `move` transition 控制进入和退出方向。
- 将详情页返回按钮从系统 dismiss 改为调用父页面关闭回调。
- 执行 iOS 测试并重新安装模拟器 App。

# 2026-06-01 16:12:35

## 修改说明

- 按用户要求以生产环境安卓 App 为参照，对齐 iOS 原生 App 的用户列表卡片与详情页个人信息卡片。
- 已核对公网版本清单 `https://xhbadmin.juxin.pro/download/android-version.json`，生产安卓版本为 `0.1.23(24)`，与本地安卓 `AndroidManifest.xml` 当前版本一致。
- iOS 各 tab 的普通用户卡片改为与安卓一致的两项核心展示：金额项 + 日期项；回款/平账列表不再额外展示安卓没有的 `实际还款时间` 卡片格。
- iOS 列表日期文案按安卓统一：档案为 `注册时间`，其他 tab 有 `due_date` 时为 `还款日`，否则为 `提交时间`。
- iOS 详情页删除安卓没有的 `订单信息` 卡片，保留并对齐安卓详情页卡片结构：认证照片、核心信息、风控报告、紧急联系人、地理位置、IP记录、订单状态及审核批注。
- iOS 详情页字段文案对齐安卓：`IP记录` 去掉空格，`联系人1/联系人2` 去掉空格，订单状态卡片统一展示 `提交时间` 与 `还款日`。
- 保留 iOS 当前柔光玻璃、圆角卡片、底部导航等既有设计风格，仅调整字段和卡片结构。

## 修改后的代码

- `ios_native/Sources/Models/WorkspaceLogic.swift`
- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `ios_native/Sources/Views/Screens/DetailView.swift`
- `ios_native/Tests/WorkspaceLogicTests.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- `ios_native/Tests/WorkspaceLogicTests.swift`
  - 新增 `testListCardInfoCellsShouldMatchAndroidProductionCard`，覆盖回款/发卡列表卡片字段与安卓生产版口径一致。

## 集成测试代码

- 本次未新增独立集成测试文件；通过 iPhone 17 模拟器安装启动验证 iOS App 页面可正常运行。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`14 tests, 0 failures`。
- 已重新安装并启动 `com.juxin.xiaohebao.ios` 到 iPhone 17 模拟器，工作台页面正常显示。
- 公网安卓版本清单校验通过：`versionCode=24`、`versionName=0.1.23`。

## 操作步骤

- 对照安卓 `MainActivity.java` 中 `addCard`、`addApplicationCard`、`coreRows`、`emergencyRows`、`locationRows`、`ipRows`、`orderAuditRows`。
- 将 iOS 列表卡片展示口径抽到 `WorkspaceLogic.listCardInfoCells`，避免各页面重复分叉。
- 调整 iOS 详情页卡片标题、联系人文案和订单状态字段，删除与安卓不一致的冗余卡片。
- 执行 iOS 测试并安装模拟器核验。

# 2026-06-01 16:02:31

## 修改说明

- 按用户反馈调整 iOS 登录页顶部品牌区域。
- 将登录页顶部原有蓝色圆形符号替换为与当前 iOS AppIcon、安卓启动图标一致的橙色钱包标识。
- 删除登录页中容易造成混淆的辅助文案：`iOS 原生管理端 · Soft UI 工作流`、`环境：LOCAL`。
- 删除登录页底部 `当前定位` 调试说明卡片，避免生产/测试使用时出现无业务价值的信息干扰。

## 修改后的代码

- `ios_native/Sources/Views/Screens/LoginView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift` 覆盖 iOS 核心业务逻辑。

## 集成测试代码

- 本次通过 iPhone 17 模拟器重新安装并启动 App，人工验证登录页展示。

## 测试执行结果

- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`13 tests, 0 failures`。
- 已卸载并重新安装 `com.juxin.xiaohebao.ios` 到 iPhone 17 模拟器，启动成功。
- 模拟器截图验证：登录页顶部为橙色钱包 logo；副标题、环境标签、底部当前定位说明卡片均已删除。

## 操作步骤

- 定位 iOS 登录页 SwiftUI 结构。
- 在原页面上做最小 UI 补丁，新增钱包 logo 视图并删除无用调试/说明内容。
- 运行 iOS 测试、重新安装模拟器 App，并截图核验登录页。

# 2026-06-01 15:57:45

## 修改说明

- 按用户反馈重新参照安卓 App 图标修正 iOS AppIcon。
- 对照发现当前安卓实际启动图标来自 `android_native/app/src/main/res/drawable/ic_launcher.xml`，是橙色钱包图形；`design/UX_normal.py` 中的蓝绿播放/数据柱形标识与当前安卓启动图标不一致。
- 将 iOS AppIcon 改为与安卓启动图标一致的钱包视觉：橙色满底、白色钱包主体、橙色侧袋和白色圆点。
- 修正 iOS 图标为满底 RGB 图，避免透明角在 iOS 图标中出现黑角。

## 修改后的代码

- `ios_native/Resources/Assets.xcassets/AppIcon.appiconset/*.png`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增单元测试；属于 iOS 图标资源替换。

## 集成测试代码

- 本次通过 iPhone 17 模拟器重新构建、卸载旧包、安装新包并启动验证。

## 测试执行结果

- `xcodebuild build -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -sdk iphonesimulator -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`BUILD SUCCEEDED`。
- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`13 tests, 0 failures`。
- 构建产物已包含新的 `Assets.car` 和 `AppIcon60x60@2x.png`。
- 已对 `com.juxin.xiaohebao.ios` 执行卸载、安装并启动成功。

## 操作步骤

- 对照 `design/UX_normal.py`、`android/public/app-icon.svg` 与安卓原生 `ic_launcher.xml`。
- 以安卓原生启动图标为准重新生成 iOS AppIcon 多尺寸 PNG。
- 重新构建、测试、卸载旧包并安装到模拟器。

# 2026-06-01 15:34:52

## 修改说明

- 修复 iOS App 模拟器图标空白问题。
- 原因是 `AppIcon.appiconset` 只有空 `Contents.json`，没有 PNG 图标文件；同时 Xcode 工程未将 `Resources/Assets.xcassets` 接入 App target 的资源编译阶段，导致图标资源不会进入构建产物。
- 新增一套与当前 iOS 柔光玻璃风格一致的 AppIcon PNG 资源，并把 `Assets.xcassets` 加入 `XiaoHeBaoIOS` 的 Resources build phase。
- 卸载模拟器旧包后重新安装新包，避免 SpringBoard 使用旧缓存。

## 修改后的代码

- `ios_native/Resources/Assets.xcassets/AppIcon.appiconset/Contents.json`
- `ios_native/Resources/Assets.xcassets/AppIcon.appiconset/*.png`
- `ios_native/XiaoHeBaoIOS.xcodeproj/project.pbxproj`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增单元测试；属于 iOS 资源与工程配置修复。

## 集成测试代码

- 本次通过 iPhone 17 模拟器重新构建、卸载旧包、安装新包并启动验证。

## 测试执行结果

- `xcodebuild build -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -sdk iphonesimulator -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`BUILD SUCCEEDED`。
- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`13 tests, 0 failures`。
- 构建产物已包含 `Assets.car` 和 `AppIcon60x60@2x.png`。
- 已对 `com.juxin.xiaohebao.ios` 执行卸载、安装并启动成功；测试后再次重新安装启动了一次，确保模拟器当前使用的是包含图标的新包。

## 操作步骤

- 检查 `AppIcon.appiconset`，确认图标文件缺失。
- 生成标准 iOS AppIcon 多尺寸 PNG。
- 修复 Xcode 工程资源阶段，让 asset catalog 正常编译进入 App 包。
- 重新构建并卸载旧版后安装到模拟器。

# 2026-06-01 15:18:12

## 修改说明

- 保持当前 iOS 原生 App 的柔光玻璃设计风格不变，对照已上线安卓 App 收敛页面信息与交互口径。
- 顶部统计卡片对齐安卓端：档案、申请、发卡、回款、平账按当前业务统计字段展示。
- 搜索与筛选区按安卓端规则做减法：档案/平账保留关键词查询，申请仅保留状态筛选，回款保留应还款日期区间和逾期状态筛选。
- 申请列表补齐申请时间、最新 IP、IP 位置、最新 GPS、GPS 位置；列表卡片补齐风险标签展示。
- 详情页核心信息对齐安卓端运营字段，保留状态、手机号、身份证、渠道、复购、主时间、其他费用和风险校验，删除会分散判断的冗余字段。
- 状态与筛选文案对齐安卓端，如 `审核中`、`已通过`、`未通过`、`待下单`、`拒发卡`。

## 修改后的代码

- `ios_native/Sources/Models/AppModels.swift`
- `ios_native/Sources/Models/WorkspaceLogic.swift`
- `ios_native/Sources/ViewModels/WorkspaceViewModel.swift`
- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `ios_native/Sources/Views/Screens/DetailView.swift`
- `ios_native/Tests/WorkspaceLogicTests.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- `ios_native/Tests/WorkspaceLogicTests.swift`
  - 新增顶部统计卡片安卓口径测试。
  - 新增回款全部/已逾期统计口径测试。
  - 新增申请状态筛选文案测试。

## 集成测试代码

- 本次通过 iPhone 17 模拟器人工验证档案、申请、回款、平账和详情页展示。

## 测试执行结果

- `xcodebuild build -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -sdk iphonesimulator -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`BUILD SUCCEEDED`。
- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`13 tests, 0 failures`。
- 模拟器目测验证：档案页保留关键词查询；申请页无多余搜索框且展示 IP/GPS 信息；回款页展示日期区间查询与逾期筛选；平账页保留关键词查询与逾期筛选；详情页核心信息字段已按安卓口径收敛。

## 操作步骤

- 对照安卓端 `MainActivity.java` 的统计卡片、筛选区、申请列表、状态文案和详情字段。
- 在 iOS 现有 SwiftUI 页面上做最小补丁，不改动整体视觉风格。
- 运行 iOS 构建、单元测试并安装到模拟器进行页面检查。

# 2026-06-01 15:01:53

## 修改说明

- 按截图反馈调整 iOS 工作台页面视觉结构。
- 删除工作台每个 tab 顶部的 `iOS 原生工作台` 副标题，减少无效信息。
- 压缩顶部两张统计卡片，去掉强制撑高导致的大块空白区域，让卡片高度跟随内容。
- 将底部 tab 下移贴近屏幕底部，减少悬浮感，同时保留必要可点击安全边距。

## 修改后的代码

- `ios_native/Sources/Views/Components/SummaryCards.swift`
- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift`。

## 集成测试代码

- 本次通过 iPhone 17 模拟器人工验证档案页视觉效果。

## 测试执行结果

- 重新构建并安装 iOS App 到 iPhone 17 模拟器成功。
- 模拟器目测验证：`iOS 原生工作台` 已删除，顶部统计卡片空白已收缩，底部 tab 已贴近屏幕底部。
- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`10 tests, 0 failures`。

## 操作步骤

- 定位工作台 header、统计卡片和底部 tab 的 SwiftUI 结构。
- 删除副标题文本。
- 移除统计卡片 `minHeight` 强制高度并降低内部间距。
- 调整底部 tab 的底部偏移。
- 重新构建安装并在模拟器中验证。

# 2026-06-01 14:30:17

## 修改说明

- 接管 iOS 模拟器进行实际测试，先启动本地后端后完成 iOS App 登录。
- 逐页测试档案、申请、发卡、回款、平账五个 tab，进入申请/发卡/回款/平账详情并检查主要操作弹窗。
- 测试确认申请详情中的审核员转单弹窗可正常打开，具备取消、确定按钮，未执行真实转单提交。
- 测试确认回款详情的账单展期弹窗、平账详情的登记平账弹窗可正常打开，未点击最终提交。
- 发现列表卡片中长时间字段在申请、发卡、回款、平账页存在省略号截断；已修正为列表显示日期，详情保留完整时间，并降低长字段字号允许两行展示。

## 修改后的代码

- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试文件；沿用 `ios_native/Tests/WorkspaceLogicTests.swift` 覆盖 iOS 核心权限、接口、统计与地址去重逻辑。

## 集成测试代码

- 本次通过 iOS 模拟器人工集成验证登录、tab 切换、详情页、审核员弹窗、展期弹窗、平账弹窗。

## 测试执行结果

- 本地后端使用 Python 3.12 启动成功：`Uvicorn running on http://0.0.0.0:8001`。
- iOS 模拟器登录成功，进入工作台。
- 人工验证页面：档案、申请、发卡、回款、平账均可加载。
- 人工验证详情与弹窗：申请审核员转单弹窗、发卡详情操作、回款账单展期弹窗、平账登记弹窗均可打开。
- 修正后重新构建并安装 iOS App，回款列表卡片日期已完整显示为 `2026-05-28`，不再出现省略号截断。
- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`10 tests, 0 failures`。

## 操作步骤

- 发现 App 登录报 `Could not connect to the server.`，确认 `8001` 端口未启动。
- 使用 Python 3.12 启动本地后端，绕过旧 `.venv` Python 3.9 不兼容当前代码的问题。
- 在 iPhone 17 模拟器中登录并逐页操作测试。
- 针对测试发现的列表日期截断问题做最小 UI 修复。
- 重新构建、安装、打开 App，并执行 iOS 测试。

# 2026-06-01 14:06:53

## 修改说明

- 参照当前安卓最新版管理 App，继续完善 iOS 原生 App 的运营作业能力。
- 申请详情顶部增加审核员展示；有申请权限的登录人员可点击审核员，进入审核员列表弹窗，选择后必须点击“确定”才会提交转单。
- 回款、平账相关列表卡片与详情页统一补齐 `应还款时间`、`实际还款时间` 口径，减少和 Web/安卓端的字段差异。
- GPS 定位地址与 IP 记录地址增加统一去重处理，避免出现“国家省市 + 完整地址”重复拼接。
- 平账表单默认带出实际还款日，并修正减免金额默认值，避免已有减免被误当成本次新增减免再次叠加。
- iOS API 客户端补充数组响应解析能力，用于 `/admin/loan-assignees` 这类列表型接口。

## 修改后的代码

- `ios_native/Sources/Infrastructure/APIClient.swift`
- `ios_native/Sources/Models/WorkspaceLogic.swift`
- `ios_native/Sources/Views/Screens/DetailView.swift`
- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `ios_native/Tests/WorkspaceLogicTests.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- `ios_native/Tests/WorkspaceLogicTests.swift`
  - 新增 GPS/IP 地址重复前缀去重测试。
  - 继续覆盖可见 tab、权限动作、平账 payload、接口路径、回款统计卡片等核心逻辑。

## 集成测试代码

- 本次未新增单独集成测试文件；通过 iOS Xcode 工程测试与模拟器构建验证 App 目标、测试目标和接口模型均可编译运行。

## 测试执行结果

- `xcodebuild build -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -sdk iphonesimulator -destination 'generic/platform=iOS Simulator'` 通过，`BUILD SUCCEEDED`。
- `xcodebuild test -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,id=F3BE2106-34CF-4663-A643-150C7FA44A48'` 通过，`10 tests, 0 failures`。
- 直接使用 `generic/platform=iOS Simulator` 执行测试时，Xcode 要求具体模拟器设备；已改用本机 `iPhone 17` 模拟器完成测试。

## 操作步骤

- 对比 iOS 已有骨架、安卓最新版 `MainActivity.java` 和后端接口契约。
- 补齐 iOS 审核员转单、时间口径、地址去重、平账默认值和列表卡片展示。
- 增加逻辑测试并运行 iOS 工程构建与测试。

# 2026-06-01 13:05:16

## 修改说明

- 按用户反馈“生产环境中没有看到变化”，将已验证的管理端完整构建发布到腾讯云生产环境。
- 发布方式改为完整 `dist` 同步到 `/data/www/xhbadmin.juxin.pro/dist/` 与 `/opt/xiaohebao/current/frontend_admin/dist/`，避免新旧入口和页面 chunk 混用。
- 同步时保留 `/download/` 目录和 `ecard-pool-enhance.js/css`，避免影响安卓固定下载地址和卡池增强逻辑。

## 修改后的代码

- 本次生产发布使用 `/tmp/xhb-admin-build-check/dist/` 完整构建产物，未额外修改源码。
- `doc/work_result/20260601.md`

## 单元测试代码

- 沿用 `frontend_admin/src/views/RepaymentsProgress.test.js`，覆盖三个页面的两类时间展示和两类时间筛选参数。

## 集成测试代码

- 本次未新增独立集成测试文件；通过生产静态资源发布、生产目录关键字检查和公网资源检查完成线上验证。

## 测试执行结果

- 生产备份目录：`/opt/xiaohebao/backups/admin-time-fields-20260601130342`。
- 生产入口已指向新主包：`/assets/index-CtCKsZpV.js`。
- 生产三个页面资源已更新：
  - `/assets/Repayments-Dv15wJ54.js`
  - `/assets/Collections-DpCPI8_t.js`
  - `/assets/FinancialReconciliation-CLDnVp13.js`
- 生产目录关键字验证通过：三个页面资源均包含 `应还款时间`、`实际还款时间`、`due_date_start`、`actual_repayment_start`。
- 公网入口验证：`https://xhbadmin.juxin.pro/repayments` 返回新入口，包含 `index-CtCKsZpV.js` 与 `ecard-pool-enhance.js/css`。
- 公网资源验证：三个新页面 JS 均可访问，并包含新增字段与参数。
- 公网下载保留验证：`https://xhbadmin.juxin.pro/download/xiaohebao.apk` 返回 `200 OK`，文件名仍为 `xiaohebao.apk`。

## 操作步骤

- 确认生产入口仍为旧包，解释生产无变化原因。
- 备份生产实际 Nginx 静态目录和 `/opt` 当前版本静态目录。
- 将完整构建产物同步到两个生产静态目录，排除并保留下载目录和卡池增强脚本。
- 校验生产入口、新页面包、关键字、公网页面与 APK 下载地址。

# 2026-06-01 12:44:30

## 修改说明

- 将管理端三个还款相关页面的时间口径拆清：`应还款时间` 对应 `Loan.due_date`，`实际还款时间` 对应 `Loan.actual_repayment_date`。
- 还款管理、催收管理、财务平账列表均增加 `实际还款时间` 列，并将原 `还款日` 展示文案改为 `应还款时间`。
- 三个页面详情区同步展示 `应还款时间` 与 `实际还款时间`，避免点开详情后仍出现旧口径。
- 三个页面筛选区均补充 `应还款时间` 与 `实际还款时间` 日期区间查询，分别传递 `due_date_start/end` 与 `actual_repayment_start/end`。
- 补回财务平账页面源码文件，并保留既有额外收款、实际还款日、冲抵逾期费与其他费用预览逻辑。

## 修改后的代码

- `frontend_admin/src/views/Repayments.vue`
- `frontend_admin/src/views/Collections.vue`
- `frontend_admin/src/views/FinancialReconciliation.vue`
- `frontend_admin/src/views/RepaymentsProgress.test.js`
- `doc/work_result/20260601.md`

## 单元测试代码

- `frontend_admin/src/views/RepaymentsProgress.test.js`
  - 覆盖还款管理页关键词、应还款时间、实际还款时间三类查询参数互不混淆。
  - 覆盖还款管理、催收管理、财务平账三个页面都具备两类时间展示和两类时间筛选参数。

## 集成测试代码

- 本次未新增独立集成测试文件；使用完整前端工程临时构建验证三个页面可参与生产打包。

## 测试执行结果

- 源码级前端测试：`android/node_modules/.bin/vitest run frontend_admin/src/views/RepaymentsProgress.test.js` 通过，`1 file passed, 3 tests passed`。
- 完整前端工程临时测试：`npm test -- --run src/views/RepaymentsProgress.test.js` 通过，`1 file passed, 3 tests passed`。
- 完整前端工程临时构建：首次构建被备份工程缺少 `unlockUserLocationRisk` 导出阻塞；仅在 `/tmp/xhb-admin-build-check` 临时构建目录补齐该导出后，`npm run build` 通过。
- 本次未发布生产静态资源，避免与当前生产回滚后的稳定前端包混用。

## 操作步骤

- 复核后端 `/admin/loans` 与 `/admin/repayment-stats` 已支持 `due_date_start/end`、`actual_repayment_start/end`。
- 修改还款管理页字段文案、列表字段、详情字段和查询参数。
- 修改催收管理页字段文案、列表字段、详情字段和查询参数。
- 补回并升级财务平账源码，增加两类时间字段与两类时间区间查询。
- 更新源码级测试并执行 targeted test。
- 使用完整前端备份工程叠加当前源码进行临时打包验证。

# 2026-06-01 12:12:48

## 修改说明

- 按要求验证并启用安卓端云端升级方式：不再让用户手动重新下载新包，而是通过云端 `android-version.json` 触发 App 内升级弹窗。
- 将安卓版本递增为 `0.1.23(24)`，发布到固定下载地址 `https://xhbadmin.juxin.pro/download/xiaohebao.apk`。
- 云端版本清单已更新为 `versionCode=24`，低于 24 的已安装 App 会在启动、回到前台、登录页或工作台渲染后检查到新版本并弹窗；用户点击“升级”后进入百分比进度条下载流程。
- 本次 24 版同时包含安卓端 GPS 地址重复前缀去重修复。

## 修改后的代码

- `android_native/app/src/main/AndroidManifest.xml`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次为原生 Android 发布与云端版本清单触发验证，当前原生 Android 目录没有 JVM 单元测试框架，未新增单元测试文件。

## 集成测试代码

- 本次未新增独立集成测试文件；通过正式 APK 构建、签名校验、公网版本清单与公网 APK 包信息校验完成验证。

## 测试执行结果

- 安卓 release 构建：`./build_apk.sh release` 通过。
- APK 包信息：`versionCode=24`，`versionName=0.1.23`。
- APK 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 release APK SHA256：`7232442f43c1c08b73130e021a7722611a483cc2a466bf8d22493b89335e1ad1`。
- 已备份云端旧 APK、旧版本清单到 `/opt/xiaohebao/backups/android-cloud-update-test-20260601121136/`。
- 版本清单已更新：`versionCode=24`、`versionName=0.1.23`、`apkUrl=https://xhbadmin.juxin.pro/download/xiaohebao.apk`、`sha256=7232442f43c1c08b73130e021a7722611a483cc2a466bf8d22493b89335e1ad1`。
- 公网固定下载 APK 包信息为 `versionCode=24`、`versionName=0.1.23`，SHA256 与本地一致。
- 本机当前未连接安卓设备或模拟器，无法在本机直接截图验证弹窗；但云端触发条件与公网包信息已满足。

## 操作步骤

- 复核当前云端版本清单为 `versionCode=23`，本地待发布版本递增到 `24`。
- 使用正式签名构建 release APK。
- 校验 APK 包名、版本号、签名与 SHA256。
- 备份腾讯云旧 APK 和旧版本清单。
- 上传 24 版 APK 到固定下载地址，并更新云端版本清单。
- 从公网重新下载版本清单和 APK，核验版本号与 SHA256。

# 2026-06-01 12:05:27

## 修改说明

- 按安卓端用户卡片截图要求，修复 GPS 位置展示中“省市区”重复拼接的问题。
- 将 GPS 地址拼接改为全局去重规则：如果详细地址已经包含前面拼过的省、市、区或街道，只追加后续差异部分。
- 修复范围覆盖安卓端用户卡片的“GPS位置”以及详情页“定位地址”等共用 GPS 地址展示。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次安卓原生项目暂无独立单元测试框架，未新增单元测试文件。

## 集成测试代码

- 本次未新增独立集成测试文件；通过安卓 APK debug 构建完成编译级验证。

## 测试执行结果

- 安卓 debug 构建：`./build_apk.sh debug` 通过。
- 构建产物：`android_native/build/xiaohebao-debug.apk`。
- 编译提示：仅存在 Java 8 引导类路径警告和既有过时 API 提示，无编译错误。

## 操作步骤

- 定位安卓端用户卡片与详情页共用的 GPS 地址格式化方法。
- 新增统一地址拼接去重方法。
- 将用户卡片 `GPS位置` 与详情页 `定位地址` 改为使用统一去重方法。
- 执行安卓 debug 构建验证。

# 2026-06-01 11:42:32

## 修改说明

- 紧急处理生产后台 `/channels` 白屏：原因是临时将旧渠道页产物接到新入口后，旧渠道页与新公共依赖版本不一致，触发前端渲染异常。
- 按用户要求优先恢复生产可用性，已回滚到今天本轮完整前端发布前的生产静态版本。
- 回滚后渠道管理入口、渠道二维码模块、公共依赖重新回到同一批构建产物，避免继续白屏。
- 安卓下载目录保留，未修改后端、数据库和渠道数据。

## 修改后的代码

- `doc/work_result/20260601.md`

## 单元测试代码

- 本次为生产紧急回滚，未新增单元测试。

## 集成测试代码

- 本次未新增独立集成测试文件；通过公网静态入口与生产目录资源校验完成验证。

## 测试执行结果

- 公网入口验证：`https://xhbadmin.juxin.pro/channels` 已重新指向 `index-WVgexbX7.js`。
- 生产入口验证：`index.html` 加载 `es-B-O9bK2G.js`，与渠道页 `Channels-Bu-2IDj1.js` 属于同一批旧构建依赖。
- 渠道页验证：`Channels-Bu-2IDj1.js` 包含“二维码”文案，并引用 `channelQr-D3drrmT3.js`。
- 生产目录验证：`download/xiaohebao.apk` 仍存在。

## 操作步骤

- 备份出问题时的当前生产静态目录到 `/opt/xiaohebao/backups/emergency-before-rollback-20260601114050/`。
- 将 `/data/www/xhbadmin.juxin.pro/dist/` 回滚到 `/opt/xiaohebao/backups/admin-clean-repayment-query-20260601112247/www-dist/`。
- 将 `/opt/xiaohebao/current/frontend_admin/dist/` 回滚到 `/opt/xiaohebao/backups/admin-clean-repayment-query-20260601112247/opt-dist/`。
- 回滚同步时排除 `download/` 目录，保留 APK 下载文件。
- 清理本地错误构建产物的暂存记录，避免后续误发。

# 2026-06-01 11:36:56

## 修改说明

- 排查渠道管理页二维码缺失：数据未丢失，原因是上一轮完整前端构建使用的渠道页源码版本较旧，导致新入口把渠道管理路由指向了无二维码能力的渠道页产物。
- 按最小改动先恢复生产渠道页路由：将当前入口中的渠道管理页面指回发布前备份中带二维码能力的渠道页产物。
- 为避免浏览器继续缓存旧入口，新增入口文件 `index-CnQr91xF.js` 并更新 `index.html` 指向新入口。
- 未修改后端、数据库和渠道数据。

## 修改后的代码

- `frontend_admin/dist/assets/index-DqzIRG6W.js`
- `frontend_admin/dist/assets/index-DqzIRG6W.js.gz`
- `frontend_admin/dist/assets/index-CnQr91xF.js`
- `frontend_admin/dist/assets/index-CnQr91xF.js.gz`
- `frontend_admin/dist/index.html`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试；为生产静态入口路由修正。

## 集成测试代码

- 本次未新增独立集成测试文件；通过公网静态资源链路验证渠道页加载目标。

## 测试执行结果

- 公网入口验证：`index-DqzIRG6W.js` 中旧渠道页 `Channels-CVN3tfBr.js` 引用数为 `0`，带二维码渠道页 `Channels-Bu-2IDj1.js` 引用数为 `2`。
- 公网缓存验证：`https://xhbadmin.juxin.pro/channels` 已指向新入口 `index-CnQr91xF.js`。
- 新入口验证：`index-CnQr91xF.js` 中旧渠道页 `Channels-CVN3tfBr.js` 引用数为 `0`，带二维码渠道页 `Channels-Bu-2IDj1.js` 引用数为 `2`。
- 公网渠道页验证：`Channels-Bu-2IDj1.js` 包含“二维码”文案，并引用 `channelQr-D3drrmT3.js`。
- 公网二维码模块验证：`/assets/channelQr-D3drrmT3.js` 返回 `200 OK`。
- 生产双目录验证：`/data/www/xhbadmin.juxin.pro/dist/` 与 `/opt/xiaohebao/current/frontend_admin/dist/` 均已指向带二维码渠道页。

## 操作步骤

- 对比当前生产入口与发布前备份入口，确认渠道管理路由从 `Channels-Bu-2IDj1.js` 被替换为 `Channels-CVN3tfBr.js`。
- 备份当前入口到 `/opt/xiaohebao/backups/channels-qr-restore-20260601113556/`。
- 将当前入口中的渠道管理页 JS/CSS 指回发布前带二维码版本。
- 重建入口 gzip，并同步实际 Nginx 服务目录与应用目录。
- 新增入口文件名并更新 `index.html`，避免浏览器继续使用旧入口缓存。
- 使用公网资源请求验证二维码页面和二维码生成模块可加载。

# 2026-06-01 11:25:58

## 修改说明

- 已停止使用手工修改压缩后静态包的临时方案，先将生产入口恢复到可用状态，避免还款管理页继续白屏。
- 使用完整前端工程重新构建并发布还款管理页，确保入口、业务页面、公共 API、公共依赖为同一批构建产物。
- 明确还款管理页查询关系：左侧搜索按钮只按文本输入检索；还款时间右侧查询按钮只按选定还款时间区间检索；还款日快捷标签继续按还款日口径检索。
- 保留卡池增强静态资源与安卓下载目录，避免修复还款页时影响其他生产页面和 APK 下载。
- 清理本地与生产中上一轮临时缓存文件，避免后续发布继续引用无效临时包。

## 修改后的代码

- `frontend_admin/src/views/Repayments.vue`
- `frontend_admin/src/views/RepaymentsProgress.test.js`
- `frontend_admin/dist/index.html`
- `frontend_admin/dist/assets/index-DqzIRG6W.js`
- `frontend_admin/dist/assets/index-DqzIRG6W.js.gz`
- `frontend_admin/dist/assets/Repayments-B5fxgCLY.js`
- `frontend_admin/dist/assets/Repayments-B5fxgCLY.js.gz`
- `frontend_admin/dist/assets/api-D6iIcyjw.js`
- `frontend_admin/dist/assets/api-D6iIcyjw.js.gz`
- `frontend_admin/dist/assets/Users-BvhkgCbK.js`
- `frontend_admin/dist/assets/Users-BvhkgCbK.js.gz`
- `doc/work_result/20260601.md`

## 单元测试代码

- `frontend_admin/src/views/RepaymentsProgress.test.js`
  - 覆盖两个查询按钮存在。
  - 覆盖文本查询与还款时间查询参数互不混用。

## 集成测试代码

- 本次未新增独立集成测试文件；通过完整前端构建、生产静态资源链路和生产目录保留项校验完成线上验证。

## 测试执行结果

- 完整前端工程验证：`npm test -- --run src/views/RepaymentsProgress.test.js` 通过，`1 file passed, 2 tests passed`。
- 完整前端工程构建：`npm run build` 通过。
- 公网入口验证：`https://xhbadmin.juxin.pro/repayments` 已指向 `index-DqzIRG6W.js`。
- 公网还款页资源验证：新入口已加载 `Repayments-B5fxgCLY.js`，资源内包含 `actual_repayment_start`、`actual_repayment_end` 和还款时间区间提示。
- 生产保留项验证：`/download/xiaohebao.apk` 所在下载目录仍存在；`ecard-pool-enhance.js/css` 仍在入口中加载。
- 生产清理验证：上一轮 `v202606011103`、`v202606011112` 临时静态文件已从生产目录移除。

## 操作步骤

- 先恢复生产入口，确认页面不再处于白屏风险状态。
- 从完整前端工程重新构建，并补齐完整构建所需的既有 API 导出。
- 重新发布完整 `dist` 到 `/data/www/xhbadmin.juxin.pro/dist/` 与 `/opt/xiaohebao/current/frontend_admin/dist/`。
- 同步时排除并保留 `download/` 目录，入口中继续保留卡池增强资源。
- 删除上一轮临时静态文件，仅保留本次完整构建产物作为当前生产版本。
- 通过公网入口、新还款页资源、下载目录和增强资源进行复核。

# 2026-06-01 10:56:38

## 修改说明

- 按生产还款管理页面截图要求，在“还款时间”日期区间右侧新增独立“查询”按钮。
- 调整筛选口径：搜索输入框旁的“查询”只按左侧手机号/姓名/身份证输入检索，不再带入还款时间区间；还款时间右侧的“查询”只按选定的还款时间区间检索，不再带入左侧搜索输入。
- 保留“还款日”快捷标签的独立筛选行为，点击今日/明日/全部时仍按还款日口径刷新。
- 已同步生产静态资源到 `/opt/xiaohebao/current/frontend_admin/dist/assets/` 与实际 Nginx 服务目录 `/data/www/xhbadmin.juxin.pro/dist/assets/`。

## 修改后的代码

- `frontend_admin/src/views/Repayments.vue`
- `frontend_admin/src/views/RepaymentsProgress.test.js`
- `frontend_admin/dist/assets/Repayments-Bxsm43Bs.js`
- `frontend_admin/dist/assets/Repayments-Bxsm43Bs.js.gz`
- `doc/work_result/20260601.md`

## 单元测试代码

- `frontend_admin/src/views/RepaymentsProgress.test.js`
  - 增加还款页存在两个独立查询入口、且文本查询与还款时间查询参数口径分离的断言。

## 集成测试代码

- 本次未新增独立集成测试文件；通过生产静态资源同步后公网资源内容校验完成线上验证。

## 测试执行结果

- 前端针对性测试：`android/node_modules/.bin/vitest run frontend_admin/src/views/RepaymentsProgress.test.js`，结果 `1 file passed, 2 tests passed`。
- 本地静态资源语法检查：`node --check frontend_admin/dist/assets/Repayments-Bxsm43Bs.js` 通过。
- 本地 gzip 校验：`gzip -t frontend_admin/dist/assets/Repayments-Bxsm43Bs.js.gz` 通过。
- 生产 ES Module 语法检查：`node --input-type=module --check < Repayments-Bxsm43Bs.js` 通过。
- 生产 gzip 校验：`gzip -t Repayments-Bxsm43Bs.js.gz` 通过。
- 公网验证：`https://xhbadmin.juxin.pro/repayments` 返回 `200`；公网 JS 包包含 `onClick:It`、`onClick:fn`、`sn=S(\`due\`)` 与 `actual_repayment_start`。

## 操作步骤

- 定位还款管理页筛选区域与当前 `fetchData` 参数组装逻辑。
- 拆分当前生效筛选口径为文本查询、还款时间查询、还款日快捷筛选。
- 给还款时间区间右侧新增独立查询按钮。
- 更新测试并对本地静态包做窄补丁、重建 gzip。
- 备份生产还款页资源后同步到生产实际服务目录，并验证公网资源已更新。

# 2026-06-01 10:38:32

## 修改说明

- 排查生产后台用户详情弹窗空白问题：`GET /api/admin/users/360` 在生产日志中返回 500，根因是用户事件记录中 `ip_detail`、`lon_lat_detail` 存在 `None`，但接口响应模型要求字符串，导致 FastAPI 响应校验失败。
- 按最小改动修复用户详情序列化：事件中的 IP/GPS 文本字段为空时统一返回空字符串，避免历史或新事件空值导致详情接口 500。
- 已同步生产环境并重启后端服务，确认服务状态正常。

## 修改后的代码

- `backend/app/services/admin_service.py`
- `backend/tests/test_admin_user_profile_rules.py`
- `doc/work_result/20260601.md`

## 单元测试代码

- `backend/tests/test_admin_user_profile_rules.py`
  - 新增用户详情事件地址字段为空值时，响应模型仍可校验通过的测试。

## 集成测试代码

- 本次未新增独立集成测试文件；使用生产环境真实用户 `360` 直接执行详情服务与响应模型校验，覆盖本次线上 500 的真实数据路径。

## 测试执行结果

- 本地语法检查：`python -m py_compile backend/app/services/admin_service.py backend/app/services/location.py` 通过。
- 本地后端测试：`python -m pytest backend/tests/test_admin_user_profile_rules.py backend/tests/test_async_http_services.py`，结果 `12 passed`。
- 生产后端语法检查：`.venv/bin/python -m py_compile app/services/admin_service.py` 通过。
- 生产服务状态：`xiaohebao-backend.service` 重启后为 `active`。
- 生产真实数据验证：用户 `360` / `13865333339` 详情响应模型校验通过，事件数量 `106`，空 GPS 详情字段已返回为空字符串。
- 生产 HTTP 接口验证：`GET /api/admin/users/360` 返回 `200`，响应大小 `63056`，用户 `13865333339` 与 `106` 条事件正常返回。

## 操作步骤

- 检索生产日志中 `/api/admin/users/360` 的 500 堆栈。
- 定位到响应模型校验失败字段 `events[].ip_detail` 与 `events[].lon_lat_detail`。
- 本地修复序列化空值并补充单元测试。
- 备份生产 `admin_service.py`，同步单文件到生产，语法检查后重启服务。
- 使用生产库真实用户详情数据执行服务层与响应模型校验。
- 使用生产本机 HTTP 请求验证用户详情接口返回 `200`。

# 2026-06-01 10:32:59

## 修改说明

- 按要求对生产环境 `2026-05-31` 和 `2026-06-01` 有 GPS 经纬度但未成功解析行政地址的用户重新解析。
- 重新解析严格使用 GPS 经纬度解析，不使用 IP 地址兜底。
- 对无效 GPS 坐标 `0,0` 的用户保留 `GPS无法解析`。
- 确认今天开始新申请/新登录的用户仍会通过现有登录定位与 `/api/user/location` 链路继续触发 GPS 解析；若接口失败，则按当前规则显示 `GPS无法解析`。

## 修改后的代码

- 本次未新增代码文件，仅执行生产数据修正与复核。
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次未新增测试；沿用上一轮 `backend/tests/test_async_http_services.py` 对 GPS 解析失败口径的覆盖。

## 集成测试代码

- 本次未新增独立集成测试文件；通过生产数据库复核完成线上验证。

## 测试执行结果

- 重跑解析前：昨天和今天共有 `9` 个用户有 GPS 经纬度但无有效行政地址。
- 重跑解析结果：`8` 个成功解析，`1` 个因 GPS 坐标为 `0,0`，标记为 `GPS无法解析`。
- 复核结果：
  - `2026-06-01`：有 GPS 用户 `1` 人，已有地址 `1` 人，`GPS无法解析` `0` 人。
  - `2026-05-31`：有 GPS 用户 `16` 人，已有地址 `16` 人，其中 `GPS无法解析` `1` 人。
- 当前唯一 `GPS无法解析` 用户：`18159495914`，原因是经纬度为 `0.0,0.0`。

## 操作步骤

- 查询生产环境 `2026-05-31` 至 `2026-06-01` 未成功解析 GPS 的用户清单。
- 逐个使用现有 GPS 经纬度重新调用解析逻辑。
- 将解析成功的省/市/区/街道/地址写回用户定位字段。
- 对无效坐标写入 `GPS无法解析`。
- 查询每日 GPS 用户数、已有地址数和未解析用户清单进行复核。

# 2026-06-01 10:22:01

## 修改说明

- 排查生产用户 `13865333339` 后台 GPS 位置未解析问题：该用户已提交 GPS 经纬度，不是手机未授权；生产日志显示阿里云云市场 GPS 反解析接口从 `2026-06-01 09:50` 起持续返回 `403 Forbidden`，导致行政地址字段为空。
- 按业务要求修正 GPS 解析失败口径：不使用 IP 地址解析兜底，也不使用其他地址源冒充 GPS 行政地址；当配置的 GPS 反解析接口失败或返回空地址时，统一返回并展示 `GPS无法解析`。
- 已将用户 `13865333339` 的生产定位地址修正为 `GPS无法解析`，保留原 GPS 经纬度、定位精度和更新时间，便于后续复核。

## 修改后的代码

- `backend/app/services/location.py`
- `backend/tests/test_async_http_services.py`
- `doc/work_result/20260601.md`

## 单元测试代码

- `backend/tests/test_async_http_services.py`
  - 新增阿里云 GPS 反解析失败时不调用其他地址源、直接返回 `GPS无法解析` 的测试。

## 集成测试代码

- 本次未新增独立集成测试文件；通过生产环境只读诊断、单用户数据修正和后端服务重启状态验证完成线上核验。

## 测试执行结果

- 本地后端测试：`python -m pytest backend/tests/test_async_http_services.py`，结果 `5 passed`。
- 生产后端语法检查：`.venv/bin/python -m py_compile app/services/location.py` 通过。
- 生产后端服务：`xiaohebao-backend.service` 重启后状态为 `active`。
- 生产 GPS 解析验证：当前阿里云接口仍返回 `403 Forbidden`，服务返回 `{'address': 'GPS无法解析', 'province': None, 'city': None, 'district': None, 'street': None}`。
- 生产用户 `13865333339` 当前定位字段：经纬度保留，`location_address='GPS无法解析'`，省/市/区/街道为空。

## 操作步骤

- 查询生产用户 `13865333339` 的定位字段与最近用户事件。
- 检索生产日志确认阿里云云市场 GPS 反解析接口返回 `403`。
- 修改本地 GPS 解析失败逻辑并补充测试。
- 同步生产后端单文件，重启服务并验证状态。
- 修正该用户定位地址为 `GPS无法解析`，不使用 IP 或其他地址兜底。

# 2026-06-01 09:56:46

## 修改说明

- 继续完善原生 iOS 管理端工程，使其与当前安卓端和云端后台的权限口径保持一致。
- iOS 工作台 Tab 与详情页底部操作改为优先按后端返回的 `permissions` 控制；旧账号缺少权限字段时保留角色兜底。
- iOS 回款 Tab 移除容易混淆的“当日还款/逾期催收”分段，回款列表固定使用 `REPAYMENTS` scope，通过“全部/已逾期/未逾期”筛选表达当前业务口径。
- iOS 顶部统计卡片同步安卓端最新口径：回款全部展示今日实际/应回款进度，已逾期展示未转催收逾期人数/金额，未逾期展示待回款人数/金额，平账展示累计结清/部分结清订单。
- iOS 详情页风控报告卡片调整为移动端快速审核视图：报告时间/评估结论、申请准入分/信用行为分、最近放款/探查结果/正常还款比例、风险维度、命中原因；去掉展开完整报告入口。
- iOS 账单展期表单将展期类型从手输改为 `免费展期`、`付费展期` 两个快捷按钮，分别提交 `FREE`、`FEE`。
- iOS 档案详情底部 `解除位移风控` 仅在账号具备 `user-location-risk-unlock` 权限且用户命中 4 小时位移类位置风控时展示。

## 修改后的代码

- `ios_native/Sources/Models/AppModels.swift`
- `ios_native/Sources/Models/WorkspaceLogic.swift`
- `ios_native/Sources/ViewModels/WorkspaceViewModel.swift`
- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `ios_native/Sources/Views/Screens/DetailView.swift`
- `ios_native/Tests/WorkspaceLogicTests.swift`
- `doc/work_result/20260601.md`

## 单元测试代码

- `ios_native/Tests/WorkspaceLogicTests.swift`
  - 新增/更新权限优先 Tab 展示、回款 scope 固定、位移风控权限、未逾期统计卡片、平账动作集合等用例。

## 集成测试代码

- 本次未改动后端接口；iOS 端通过 Xcode 工程构建与 XCTest 进行本地集成级编译验证。

## 测试执行结果

- iOS 模拟器构建：`xcodebuild -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -sdk iphonesimulator -destination 'platform=iOS Simulator,OS=18.6,name=iPhone 16 Pro' build`，结果 `BUILD SUCCEEDED`。
- iOS 单元测试：`xcodebuild -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -sdk iphonesimulator -destination 'platform=iOS Simulator,OS=18.6,name=iPhone 16 Pro' test`，结果 `TEST SUCCEEDED`，共 `8 tests, 0 failures`。
- iOS 真机 Release 无签名编译：`xcodebuild -project ios_native/XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -configuration Release -sdk iphoneos CODE_SIGNING_ALLOWED=NO build`，结果 `BUILD SUCCEEDED`。

## 操作步骤

- 复核安卓端当前权限、回款 scope、统计卡片、展期弹窗和位移风控解除逻辑。
- 补齐 iOS 工作台权限透传、详情页操作权限判断、回款筛选与摘要卡片口径。
- 调整 iOS 风控报告卡片结构和展期表单交互。
- 更新 XCTest 覆盖核心规则。
- 执行 iOS 构建与测试，确认工程可编译、核心逻辑可验证。

# 2026-06-01 09:34:41

## 修改说明

- 复核线上版本清单与上一版 APK：线上清单为 `versionCode=22`，上一版备份 APK 为 `versionCode=21`，且内置更新地址为 `https://xhbadmin.juxin.pro/download/android-version.json`。
- 为解决部分手机仍未看到升级弹窗的问题，进一步强化升级触发时机：除启动后延迟检查外，登录页渲染完成、工作台渲染完成后也会再次延迟检查更新。
- 安卓版本递增为 `0.1.22(23)` 并发布到腾讯云固定下载地址，确保当前 `21` 或 `22` 版本均满足升级弹窗条件。

## 修改后的代码

- `android_native/app/src/main/AndroidManifest.xml`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次为原生 Android 升级触发时机调整，当前原生 Android 目录没有 JVM 单元测试框架，未新增测试文件。

## 集成测试代码

- 本次未修改后端接口逻辑，未新增后端集成测试。

## 测试执行结果

- 安卓 debug 构建：`cd android_native && ./build_apk.sh debug` 通过。
- 安卓 release 构建：`cd android_native && ./build_apk.sh release` 通过。
- APK 包信息：`versionCode=23`，`versionName=0.1.22`。
- APK 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 release APK SHA256：`9d67decd4e0067ad99e3a8c3d1c7db5b31cd022f53b7de8b492a8a7062cdabd6`。
- 已备份云端旧 APK、旧版本清单到 `/opt/xiaohebao/backups/android-update-popup-20260601093401`。
- 版本清单已更新：`versionCode=23`、`versionName=0.1.22`、`apkUrl=https://xhbadmin.juxin.pro/download/xiaohebao.apk`、`sha256=9d67decd4e0067ad99e3a8c3d1c7db5b31cd022f53b7de8b492a8a7062cdabd6`。
- 公网固定下载 APK 包信息为 `versionCode=23`、`versionName=0.1.22`，SHA256 与本地一致。

## 操作步骤

- 校验公网版本清单、下载响应头和上一版 APK 内置更新地址。
- 补充登录页/工作台渲染后的升级检查。
- 构建 debug/release APK 并完成签名校验。
- 备份腾讯云旧版本，上传新版 APK，更新版本清单。
- 从公网固定下载地址下载 APK 并核验版本、哈希。

# 2026-06-01 09:28:56

## 修改说明

- 安卓档案 Tab 用户详情底部新增 `解除位移风控` 操作展示逻辑，识别 4 小时内位移超过配置公里数触发的登录位置异常风控。
- 所有新增/调整的安卓可执行操作按当前登录人员权限展示：优先读取后端返回的 `permissions`，无权限不展示按钮；旧数据缺少权限字段时才按角色做兼容兜底。
- 安卓回款 Tab 的 `账单展期` 弹窗将展期类型从手输 `FREE/FEE` 改为两个快捷标签：`免费展期`、`付费展期`，分别提交 `FREE` 和 `FEE`。
- 升级检测改为页面启动/恢复后延迟触发，并记录本轮已提示版本，避免页面还未渲染时弹窗被覆盖，同时避免同一版本反复弹。
- 安卓版本递增为 `0.1.21(22)` 并发布到腾讯云固定下载地址。

## 修改后的代码

- `android_native/app/src/main/AndroidManifest.xml`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `doc/work_result/20260601.md`

## 单元测试代码

- 本次为原生 Android UI/交互权限展示调整，当前原生 Android 目录没有 JVM 单元测试框架，未新增测试文件。

## 集成测试代码

- 本次未修改后端接口逻辑，未新增后端集成测试。

## 测试执行结果

- 安卓 debug 构建：`cd android_native && ./build_apk.sh debug` 通过。
- 安卓 release 构建：`cd android_native && ./build_apk.sh release` 通过。
- APK 包信息：`versionCode=22`，`versionName=0.1.21`。
- APK 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 release APK SHA256：`98e83a4c96fc808e334724f843ff2ea5cb7bc47aea37223941b657060fd562dc`。
- 已备份云端旧 APK、旧版本清单到 `/opt/xiaohebao/backups/android-action-permissions-20260601092805`。
- 版本清单已更新：`versionCode=22`、`versionName=0.1.21`、`apkUrl=https://xhbadmin.juxin.pro/download/xiaohebao.apk`、`sha256=98e83a4c96fc808e334724f843ff2ea5cb7bc47aea37223941b657060fd562dc`。
- 公网固定下载 APK 包信息为 `versionCode=22`、`versionName=0.1.21`，SHA256 与本地一致。

## 操作步骤

- 复核安卓详情页底部操作生成逻辑和当前后台权限字段。
- 补充权限判断工具，按后端 `permissions` 控制按钮可见性。
- 修改位移风控解除按钮、账单展期类型快捷标签和升级弹窗触发时机。
- 执行安卓 debug/release 构建、APK 签名校验。
- 备份腾讯云旧版本，上传新版 APK，更新版本清单。
- 从公网固定下载地址下载 APK 并核验版本、哈希。

# 2026-06-01 00:09:06

## 修改说明

- 核实并修正回款与催收口径：催收页 `scope=OVERDUE` 仍只展示逾期超过 14 天的订单；回款页“已逾期”恢复为未转催收阶段的逾期订单，即逾期未超过 14 天。
- 后端 `/api/admin/repayment-stats` 新增待回款人数、待回总金额、累计结清用户数、部分结清未结清用户数等统计字段。
- 安卓回款 Tab 顶部卡片调整：
  - 点击“全部”：卡片 1 展示今日实际/应回款人数，卡片 2 展示今日实际/应回款金额。
  - 点击“已逾期”：卡片 1 展示累计逾期人数，卡片 2 展示累计逾期金额，口径为未转催收逾期。
  - 点击“未逾期”：卡片 1 展示待回款人数，卡片 2 展示待回总金额。
- 安卓平账 Tab 顶部卡片 1 改为 `累计结清/部分结清订单`，展示累计结清用户数/累计有还款但未结清用户数。
- 移除登录页和工作台顶部的 `Google Cloud` / `Google Cloud style workspace` 文案。
- 安卓版本递增为 `0.1.20(21)` 并发布到腾讯云固定下载地址，确保低版本 App 启动时能看到升级弹窗。

## 修改后的代码

- `backend/app/api/endpoints/admin.py`
- `backend/app/schemas/loan.py`
- `backend/tests/test_admin_loans_filters.py`
- `android_native/app/src/main/AndroidManifest.xml`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `doc/work_result/20260601.md`

## 单元测试代码

- `backend/tests/test_admin_loans_filters.py`

## 集成测试代码

- 本次未新增独立集成测试文件；通过本地启动后端服务验证接口路由可达且鉴权仍生效。

## 测试执行结果

- 后端测试：`python -m pytest backend/tests/test_admin_loans_filters.py`，结果 `6 passed`。
- 后端本地启动：`cd backend && python -m app.main` 可正常启动 Uvicorn。
- 本地接口可达性：未登录访问 `http://127.0.0.1:8001/api/admin/stats` 返回 `401`。
- 安卓 debug 构建：`cd android_native && ./build_apk.sh debug` 通过。
- 安卓 release 构建：`cd android_native && ./build_apk.sh release` 通过。
- APK 包信息：`versionCode=21`，`versionName=0.1.20`。
- APK 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 release APK SHA256：`4e1a4c5053b24984228066629a39701cc1d130b64785c4919399ade91be5389d`。
- 已备份云端旧后端接口文件、旧 APK、旧版本清单到 `/opt/xiaohebao/backups/android-repayment-cards-20260601000745`。
- 腾讯云后端已更新并重启，`xiaohebao-backend.service` 状态为 `active`。
- 版本清单已更新：`versionCode=21`、`versionName=0.1.20`、`apkUrl=https://xhbadmin.juxin.pro/download/xiaohebao.apk`、`sha256=4e1a4c5053b24984228066629a39701cc1d130b64785c4919399ade91be5389d`。
- 公网固定下载 APK 包信息为 `versionCode=21`、`versionName=0.1.20`，SHA256 与本地一致。
- 未登录访问生产 `https://xhbadmin.juxin.pro/api/admin/stats` 返回 `401`，说明路由可达且仍受鉴权保护。

## 操作步骤

- 复核 Web/Android 共同使用的后端列表与统计接口，确认催收超过 14 天规则保留。
- 收窄上次回款页逾期特例，避免超过 14 天订单进入回款页“已逾期”列表。
- 补充还款统计字段与后端回归测试。
- 调整安卓顶部统计卡片和登录/工作台文案。
- 执行后端测试、后端启动验证、安卓 debug/release 构建和签名校验。
- 备份腾讯云旧版本，上传后端修正文件和新版 APK，更新版本清单。
- 从公网固定下载地址下载 APK 并核验版本、哈希。

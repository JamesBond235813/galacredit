# 2026-04-28 17:26:25

## 1. 修改说明（做了什么、为什么）
- 按要求将 `backend` 服务端口改为从配置读取，配置项为 `APP_PORT`。
- 在配置模型中新增 `APP_PORT`，并将启动时 `uvicorn.run` 的 `port` 从硬编码改为 `settings.APP_PORT`。
- 在 `backend/.env` 增加 `APP_PORT=8001`，保证默认启动可直接读取。

## 2. 修改后的代码
- `backend/app/core/config.py`
  - 新增：`APP_PORT: int = 8001`
- `backend/app/main.py`
  - `uvicorn.run(..., port=settings.APP_PORT, ...)`
- `backend/.env`
  - 新增：`APP_PORT=8001`

## 3. 单元测试代码
- `backend/tests/test_profile_config.py`
  - 新增测试：`test_app_port_from_settings`
  - 验证 `Settings(APP_PORT=8012)` 可正确读取端口值。

## 4. 集成测试代码（如适用）
- 本次未新增专门集成测试文件；通过配置读取验证。

## 5. 测试执行结果或执行方式
- 执行：`pytest -q backend/tests/test_profile_config.py`
  - 结果：`6 passed`
- 执行：`cd backend && python - << 'PY' ...` 输出 `settings.APP_PORT`
  - 结果：`APP_PORT=8001`
# 2026-04-28 17:18:42

## 1. 修改说明（做了什么、为什么）
- 服务端新增启动参数支持：`--profile=<PROFILE>`。
- 配置加载规则调整为：
  - 指定 `--profile=<PROFILE>` 时加载 `backend/.env.<PROFILE>`。
  - 未指定时加载 `backend/.env`。
- 为兼容 `uvicorn reload` 子进程，增加环境变量桥接：启动时若解析到 profile，会写入 `APP_PROFILE`，子进程优先读取该变量。
- 禁用 FastAPI 文档接口：关闭 OpenAPI JSON、Swagger UI、ReDoc。

## 2. 修改后的代码
- `backend/app/core/config.py`
  - 新增 `resolve_profile`：优先 `APP_PROFILE`，其次解析 `--profile=`。
  - 新增 `resolve_env_file`：按 profile 生成 `.env` 或 `.env.<profile>`。
  - `Settings` 使用动态 `ENV_FILE`。
- `backend/app/main.py`
  - `FastAPI(...)` 改为：
    - `openapi_url=None`
    - `docs_url=None`
    - `redoc_url=None`
  - `__main__` 启动前解析 profile 并注入 `APP_PROFILE`。
- 新增单元测试：`backend/tests/test_profile_config.py`。

## 3. 单元测试代码
- `backend/tests/test_profile_config.py`
  - 覆盖：profile 解析优先级、argv 解析、默认行为、env 文件路径映射。

## 4. 集成测试代码（如适用）
- 本次未新增专门集成测试文件；通过启动验证与应用属性检查验证行为。

## 5. 测试执行结果或执行方式
- 执行：`pytest -q backend/tests/test_profile_config.py`
  - 结果：`5 passed`。
- 执行：`cd backend && python - <<'PY' ...` 检查 FastAPI 配置
  - 结果：`openapi_url=None, docs_url=None, redoc_url=None`。
- 启动验证：
  - `python -m app.main --profile=dev` 可启动。
  - 默认启动在当前环境下出现端口占用提示（`[Errno 48] Address already in use`），非本次改动逻辑错误。
# 2026-04-28 16:33:30

## 1. 修改说明（做了什么、为什么）
- 按要求在 `frontend_h5/src/views/OCR.vue` 中将“《信用消费服务协议》”点击行为改为不执行任何动作。
- 保留文案与样式，不再绑定跳转事件。

## 2. 修改后的代码
- 文件：`frontend_h5/src/views/OCR.vue`
- 关键变更：
  - 将
    `@click.stop="goUserAgreement"`
    移除。
  - 删除未再使用的 `goUserAgreement` 方法。

## 3. 单元测试代码
- 本次未新增单元测试代码。

## 4. 集成测试代码（如适用）
- 本次未新增集成测试代码；采用构建验证。

## 5. 测试执行结果或执行方式
- 在 `frontend_h5` 执行 `npx vite build --mode development`：通过。
# 2026-04-28 16:30:23

## 1. 修改说明（做了什么、为什么）
- 新增“个人信息授权协议”静态页面，并在 `实名认证` 页的“《个人信息授权协议》”点击后跳转到该页面。
- 协议页样式复用现有 `UserAgreement.vue` 结构与风格，保证与当前站点视觉一致。
- `实名认证` 页中将“同意勾选区”和“协议链接区”点击行为拆分：
  - 点击“我已阅读并同意”仅切换选中/不选中。
  - 点击“《个人信息授权协议》”仅跳转协议页面。
  - 点击“《信用消费服务协议》”仅跳转用户协议页面。
  - 两者点击范围互不交叉，链接点击不触发勾选。

## 2. 修改后的代码
- 新增协议静态文件：`frontend_h5/public/personal-info-authorization.txt`（来源：`doc/个人信息授权协议.txt`）
- 新增协议页面：`frontend_h5/src/views/PersonalInfoAuthorization.vue`
  - 通过 `fetch('/personal-info-authorization.txt')` 加载静态文本。
- 路由新增：`frontend_h5/src/router/index.js`
  - 新增路径：`/personal-info-authorization`
- 实名认证页交互调整：`frontend_h5/src/views/OCR.vue`
  - `van-checkbox` 增加 `label-disabled`。
  - 新增 `toggleAgreed`、`goPersonalAgreement`、`goUserAgreement`。
  - 文案点击区与协议链接点击区拆分并加 `@click.stop`。

## 3. 单元测试代码
- 本次未新增单元测试代码（现有测试集全部通过）。

## 4. 集成测试代码（如适用）
- 本次未新增集成测试代码；采用前端测试与构建验证。

## 5. 测试执行结果或执行方式
- 在 `frontend_h5` 执行 `npm test`：通过（4 files, 10 tests）。
- 在 `frontend_h5` 执行 `npx vite build --mode development`：通过。
# 2026-04-28 15:54:24

## 1. 修改说明（做了什么、为什么）
- 在 `frontend_h5` 增加 `client id` 配置：新增环境变量 `VITE_CLIENT_ID`。
- 按服务端解析规则传输 `client id`：服务端在登录接口通过请求头 `client-id` 读取客户端标识，因此前端统一在 axios 请求拦截器中注入 `client-id` 请求头。
- 为避免隐式行为，`VITE_CLIENT_ID` 设为必填，缺失时启动报错。
- 这样可保证通过 `request` 发起的全部 HTTP API 请求均携带 `client-id`。

## 2. 修改后的代码
- `frontend_h5/src/api/request.js` 关键变更：
```js
const clientId = import.meta.env.VITE_CLIENT_ID;
if (!clientId) {
  throw new Error('Missing required env: VITE_CLIENT_ID');
}

request.interceptors.request.use(
  config => {
    config.headers = config.headers || {};
    config.headers['client-id'] = clientId;

    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  error => Promise.reject(error)
);
```
- `frontend_h5/.env.development` 新增配置：
```env
VITE_CLIENT_ID=h5-web
```
- `frontend_h5/.env.test`（测试环境变量文件）：
```env
VITE_API_BASE_URL=/api
VITE_CLIENT_ID=h5-web
```

## 3. 单元测试代码
- 新增文件：`frontend_h5/src/api/request.test.js`
- 覆盖点：
  - 每次请求都会注入 `client-id`。
  - 已登录场景下，`Authorization` 与 `client-id` 同时存在。

## 4. 集成测试代码（如适用）
- 本次未新增专门集成测试代码；通过构建和现有测试集验证。

## 5. 测试执行结果或执行方式
- 在 `frontend_h5` 执行 `npm test`：通过（4 个文件，10 个测试全部通过）。
- 在 `frontend_h5` 执行 `npx vite build --mode development`：通过。
# 2026-04-28 15:41:12

## 1. 修改说明（做了什么、为什么）
- 按要求将 `VITE_API_PROXY_TARGET` 调整为非必填。
- 在 `frontend_admin`、`frontend_h5` 的 `vite.config.js` 中，删除必填报错逻辑。
- 改为：仅当存在 `VITE_API_PROXY_TARGET` 时启用 `/api` 代理；未配置时不注入 `server.proxy`。
- `VITE_API_BASE_URL` 仍保持必填（请求地址仍必须显式配置）。

## 2. 修改后的代码
- `frontend_admin/vite.config.js` 关键变更：
```js
const apiProxyTarget = env.VITE_API_PROXY_TARGET
const proxy = apiProxyTarget
  ? {
      '/api': {
        target: apiProxyTarget,
        changeOrigin: true,
        ws: true
      }
    }
  : undefined
...
server: {
  ...,
  proxy
}
```
- `frontend_h5/vite.config.js` 同步上述改法。

## 3. 单元测试代码
- 本次未新增单元测试代码（配置行为调整）。

## 4. 集成测试代码
- 本次未新增集成测试代码；采用构建验证配置生效。

## 5. 测试执行结果或执行方式
- `frontend_admin`：`npx vite build --mode development`，成功。
- `frontend_h5`：`npx vite build --mode development`，成功。
# 2026-04-28 15:36:49

## 1. 修改说明（做了什么、为什么）
- 目标：`frontend_admin`、`frontend_h5` 的服务端地址必须从配置文件读取，代码中不设置默认值。
- 调整点：
  - `vite.config.js` 中原写死的代理目标地址 `http://127.0.0.1:8001` 改为读取 `VITE_API_PROXY_TARGET`。
  - `src/api/request.js` 中原写死的 `baseURL: '/api'` 改为读取 `VITE_API_BASE_URL`。
  - 对上述两个变量均增加必填校验：未配置时直接抛错，避免隐式默认值。
  - 新增两端 `.env.development` 配置文件，提供开发环境示例值。

## 2. 修改后的代码
- frontend_admin/vite.config.js
```js
import { defineConfig, loadEnv } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig(({ mode }) => {
  const env = loadEnv(mode, process.cwd(), '')
  const apiProxyTarget = env.VITE_API_PROXY_TARGET
  if (!apiProxyTarget) {
    throw new Error('Missing required env: VITE_API_PROXY_TARGET')
  }

  return {
    plugins: [vue()],
    server: {
      port: 2002,
      strictPort: true,
      host: '0.0.0.0',
      proxy: {
        '/api': {
          target: apiProxyTarget,
          changeOrigin: true,
          ws: true
        }
      }
    }
  }
})
```
- frontend_h5/vite.config.js
```js
import { defineConfig, loadEnv } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig(({ mode }) => {
  const env = loadEnv(mode, process.cwd(), '')
  const apiProxyTarget = env.VITE_API_PROXY_TARGET
  if (!apiProxyTarget) {
    throw new Error('Missing required env: VITE_API_PROXY_TARGET')
  }

  return {
    plugins: [
      vue({
        template: {
          compilerOptions: {
            isCustomElement: (tag) =>
              tag === 'AppHeader' ||
              tag === 'AppFooter' ||
              tag === 'app-header' ||
              tag === 'app-footer'
          }
        }
      })
    ],
    server: {
      port: 2001,
      host: '0.0.0.0',
      proxy: {
        '/api': {
          target: apiProxyTarget,
          changeOrigin: true,
          ws: true
        }
      }
    }
  }
})
```
- frontend_admin/src/api/request.js（关键变更）
```js
const apiBaseUrl = import.meta.env.VITE_API_BASE_URL;
if (!apiBaseUrl) {
  throw new Error('Missing required env: VITE_API_BASE_URL');
}

const request = axios.create({
  baseURL: apiBaseUrl,
  timeout: 10000
});
```
- frontend_h5/src/api/request.js（关键变更）
```js
const apiBaseUrl = import.meta.env.VITE_API_BASE_URL;
if (!apiBaseUrl) {
  throw new Error('Missing required env: VITE_API_BASE_URL');
}

const request = axios.create({
  baseURL: apiBaseUrl,
  timeout: 60000
});
```
- frontend_admin/.env.development
```env
VITE_API_PROXY_TARGET=http://127.0.0.1:8001
VITE_API_BASE_URL=/api
```
- frontend_h5/.env.development
```env
VITE_API_PROXY_TARGET=http://127.0.0.1:8001
VITE_API_BASE_URL=/api
```

## 3. 单元测试代码
- 本次为配置读取改造，未新增业务逻辑函数，未新增单元测试代码。

## 4. 集成测试代码
- 本次未新增集成测试代码；通过前端构建验证配置生效。

## 5. 测试执行结果或执行方式
- 执行：`frontend_admin` 下 `npm run build`（默认 production）
  - 结果：失败
  - 原因：严格要求变量存在，`production` 模式未提供 `VITE_API_PROXY_TARGET`。
- 执行：`frontend_h5` 下 `npm run build`（默认 production）
  - 结果：失败
  - 原因同上。
- 执行：`frontend_admin` 下 `npx vite build --mode development`
  - 结果：成功。
- 执行：`frontend_h5` 下 `npx vite build --mode development`
  - 结果：成功。

# 2026-04-28 13:47:41

## 重置密码后解除冻结限制（补充）
- 需求：在 `frontend-admin` 端重置用户密码后，解除该用户登录冻结限制。
- 实现：在后台接口 `POST /api/admin/users/{user_id}/reset-password` 成功重置密码后，立即调用登录风控管理器清理该手机号状态。
  - 文件：`backend/app/api/endpoints/admin.py`
  - 逻辑：`await auth_endpoints.password_login_guard.on_success(user.phone)`

## 单元测试
- 新增：`backend/tests/test_admin_reset_password_unfreeze.py`
- 覆盖场景：
  1. 先制造手机号冻结（5次错误）。
  2. 调用后台重置密码逻辑。
  3. 断言冻结状态已清除（可立即尝试登录）。

## 验证结果
- 测试通过：
  - `pytest -q tests/test_admin_reset_password_unfreeze.py` -> `1 passed`
  - `pytest -q tests/test_auth_password_login.py tests/test_admin_stats_ws.py` -> `3 passed`
- 冒烟通过（接口联调）：
  1. 错密5次后提示冻结。
  2. 冻结期间正确旧密码仍被拒绝。
  3. Admin 重置密码成功。
  4. 用户使用新密码立即登录成功（`200`）。
# 2026-04-28 12:35:16

## 联调验证补充
- 问题修复：`/api/admin/login` 响应模型与实际返回不一致，导致 500。
  - 已新增 `AdminTokenResponse`，并将 `admin/login` 的 `response_model` 调整为该模型。
- 环境数据修复：当前数据库 `users` 表缺少 `password_hash` 列，触发 `admin/channels` 联表查询报错。
  - 已执行数据库 DDL：`ALTER TABLE users ADD COLUMN password_hash VARCHAR(255) NULL COMMENT '登录密码哈希' AFTER phone;`

## 冒烟链路（已通过）
1. 管理员登录 -> 获取管理员信息 -> 查询启用中的渠道。
2. 后台新增用户（手机号/密码/来源渠道）。
3. H5 使用手机号+密码登录成功。
4. H5 修改密码成功，旧密码失效，新密码可登录。
5. 后台重置该用户密码成功，重置后密码可登录。
6. 登录风控验证：同手机号连续 5 次错误密码后冻结 30 分钟；冻结期间正确密码仍被拒绝。

## 关键结果摘录
- `h5_login_b_wrong_5`: `由于密码输入错误次数过多，请在30分钟后再输入密码。`
- `h5_login_b_correct_when_frozen`: `由于密码输入错误次数过多，请在30分钟后再输入密码。`
- 最终标记：`SMOKE_OK`

# 2026-04-28 12:26:43

## 1. 修改说明（做了什么、为什么）
- 服务端登录改造为手机号+密码：
  - `POST /api/auth/login` 从短信验证码登录改为密码登录。
  - 取消“自动注册用户”逻辑；用户不存在或密码错误统一返回 `用户或密码不正确`。
  - 复用现有 OAuth 持久化模型，继续保存 `client_id/access_token/refresh_token`。
- 服务端新增登录安全措施（手机号维度）：
  - 5 分钟内最多 5 次密码尝试；达到阈值后冻结 30 分钟。
  - 冻结期间无论密码是否正确都直接拒绝，并返回：`由于密码输入错误次数过多，请在xx分钟后再输入密码。`
  - 通过内存状态自动清理避免泄漏。
- 服务端新增改密能力：
  - `POST /api/user/change-password`，支持原密码校验、新密码二次确认。
- 服务端新增后台用户管理能力（给 `frontend_admin` 用）：
  - `POST /api/admin/users`：新增用户（手机号、密码、来源渠道）。
  - `POST /api/admin/users/{user_id}/reset-password`：重置用户密码。
  - 来源渠道强制为“启用中”。
- `frontend_h5` 改造：
  - 登录页改为手机号+密码登录，去除注册文案。
  - “我的-更多服务”新增“修改密码”入口。
  - 新增修改密码页面并接后端接口。
- `frontend_admin` 改造：
  - 用户档案页增加“新增用户”按钮（右侧抽屉表单：手机号/密码/来源渠道）。
  - 来源渠道使用带搜索下拉，搜索字段为“业务员/渠道名称”，仅取启用中渠道。
  - 操作列增加“重置密码”，弹窗展示用户信息并做二次密码输入比对。

## 2. 修改后的代码
- Backend
  - `backend/app/services/password_login_guard.py`
  - `backend/app/api/endpoints/auth.py`
  - `backend/app/api/endpoints/user.py`
  - `backend/app/api/endpoints/admin.py`
  - `backend/app/models/user.py`
  - `backend/app/schemas/user.py`
  - `backend/app/schemas/admin.py`
  - `backend/app/core/config.py`
  - `backend/app/services/sms_auth.py`
  - `backend/seed_mock_data.py`
  - `backend/app/api/endpoints/loan.py`
- Frontend H5
  - `frontend_h5/src/views/Login.vue`
  - `frontend_h5/src/views/ChangePassword.vue`
  - `frontend_h5/src/views/Profile.vue`
  - `frontend_h5/src/router/index.js`
  - `frontend_h5/src/api/index.js`
  - `frontend_h5/src/utils/passwordAuth.js`
- Frontend Admin
  - `frontend_admin/src/views/Users.vue`
  - `frontend_admin/src/api/index.js`
- DDL
  - `doc/ddl.sql`（`users` 表新增 `password_hash` 字段）

关键代码片段：
```python
# backend/app/api/endpoints/auth.py
frozen_minutes = await password_login_guard.before_verify(req.phone)
if frozen_minutes > 0:
    raise HTTPException(status_code=400, detail=_build_login_frozen_message(frozen_minutes))

user = (await db.execute(select(User).where(User.phone == req.phone))).scalar_one_or_none()
if not user or not user.password_hash or not verify_password(req.password, user.password_hash):
    frozen_minutes = await password_login_guard.on_failure(req.phone)
    if frozen_minutes > 0:
        raise HTTPException(status_code=400, detail=_build_login_frozen_message(frozen_minutes))
    raise HTTPException(status_code=400, detail="用户或密码不正确")
```

```python
# backend/app/services/password_login_guard.py
if len(state.failures) >= self.max_attempts:
    state.freeze_until = now + self.freeze_seconds
    state.failures.clear()
    return max(1, int(ceil(self.freeze_seconds / 60.0)))
```

## 3. 单元测试代码
- `backend/tests/test_password_login_guard.py`
  - 覆盖失败次数冻结、冻结倒计时、状态清理（防内存泄漏）。
- `frontend_h5/src/utils/passwordAuth.test.js`
  - 覆盖手机号归一化、手机号校验、密码长度校验。

## 4. 集成测试代码（涉及模块交互）
- `backend/tests/test_auth_password_login.py`
  - 通过 FastAPI TestClient 验证：
    - 用户不存在不自动注册。
    - 5 次失败触发冻结，冻结期间正确密码也被拒绝。

## 5. 测试执行结果或执行方式
- 后端全量测试：
  - 命令：`cd backend && pytest -q`
  - 结果：`20 passed`
- H5 前端单元测试：
  - 命令：`cd frontend_h5 && pnpm test`
  - 结果：`3 files passed, 8 tests passed`
- H5 前端构建：
  - 命令：`cd frontend_h5 && pnpm build`
  - 结果：构建成功
- Admin 前端构建：
  - 命令：`cd frontend_admin && pnpm build`
  - 结果：构建成功
- 服务端启动验证：
  - 命令：`cd backend && python -m uvicorn app.main:app --host 127.0.0.1 --port 8001 --no-access-log`
  - 结果：应用启动流程完成（含 scheduler startup），但当前沙箱环境禁止端口绑定，报 `operation not permitted`。

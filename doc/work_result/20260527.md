# 2026-05-27 21:00:18

## 修改说明

- 按用户确认的现行业务口径，修正生产环境用户“冷军”（用户 ID `8`，订单 ID `9`）的财务明细。
- 修正目标为：实际还款日 `2026-05-23`，应还款 `1600.00` 元，逾期 2 天应收逾期费 `20.00` 元，实际收款 `1700.00` 元，其中 `1600.00` 元记登记收款，`20.00` 元冲抵逾期费，`80.00` 元记其他费用。
- 修正后订单状态应为 `SETTLED`，同时保留“因逾期自动进入黑名单”的状态不变。

## 修改后的代码

- 本次未修改本地业务代码，仅修正生产数据并补充工作记录。

## 单元测试代码

- 不适用，本次为生产数据修正动作。

## 集成测试代码

- 不适用，本次未修改接口逻辑。

## 测试执行结果

- 已在腾讯云生产环境备份修正前数据：
- 备份目录：`/opt/xiaohebao/backups/loan9-finance-fix-20260527205322`
- 修正后订单主记录：
- `status=SETTLED`
- `penalty_amount=20`
- `paid_penalty_amount=20`
- `repaid_amount=1620`
- `other_fee_amount=80`
- `actual_repayment_date=2026-05-23`
- 修正后交易流水：
- `REPAYMENT=1620`（其中逾期费 `20`）
- `OTHER_FEE=80`
- 修正后分期台账：
- 第 1 期 `status=SETTLED`
- `paid_amount=1600`
- 修正后事件文案已同步更新，保留修正留痕。
- 用户黑名单状态复核：
- `blacklist_hit=1`
- `blacklist_reason=订单逾期自动进入黑名单`
- `overdue_credit_locked=1`

## 操作步骤

- 查询冷军订单主表、交易流水、分期台账、事件记录的线上现状。
- 在腾讯云生产环境生成修正前文本备份。
- 仅针对订单 `9` 执行最小范围数据修正：
- 更新 `loans`
- 更新 `loan_transactions`
- 更新 `loan_installments`
- 更新两条 `user_events` 文案
- 再次查询生产环境，确认“已结清 + 其他费用 80 + 黑名单保留”结果成立。

# 2026-05-27 18:27:04

## 修改说明

- 将最新安卓原生管理端 release APK 发布到腾讯云生产下载地址。
- 发布前已备份云端旧 APK，发布后已完成云端文件与公网下载文件双重哈希校验。
- 本次发布版本包含：详情页风控报告完整折叠区、回款 tab 顶部统计卡片按逾期状态切换口径。

## 修改后的代码

- 本次未新增业务代码修改，仅补充发布记录文件。

## 单元测试代码

- 不适用，本次为生产 APK 发布动作。

## 集成测试代码

- 不适用，本次未修改接口逻辑。

## 测试执行结果

- 本地 release APK：`android_native/build/xiaohebao-release.apk`。
- 本地 release APK SHA256：`bd7ef222ec7fe2ebe24f4ae8e64f400da2ffb8cdcdf5cbf4b4e51812a3dcfefa`。
- 云端旧 APK 备份目录：`/opt/xiaohebao/backups/android-apk-full-risk-report-20260527182628/xiaohebao.apk`。
- 已上传到生产下载路径：`/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 云端 APK SHA256：`bd7ef222ec7fe2ebe24f4ae8e64f400da2ffb8cdcdf5cbf4b4e51812a3dcfefa`。
- 公网下载地址：`https://xhbadmin.juxin.pro/download/xiaohebao.apk`。
- 公网下载 APK SHA256：`bd7ef222ec7fe2ebe24f4ae8e64f400da2ffb8cdcdf5cbf4b4e51812a3dcfefa`。

## 操作步骤

- 备份腾讯云生产下载目录中的旧 APK。
- 上传本地最新 release APK 到生产下载地址。
- 校验云端 APK SHA256。
- 从公网下载 APK 并再次校验 SHA256，确认与本地一致。

# 2026-05-27 18:19:51

## 修改说明

- 按用户要求，继续扩展安卓原生管理端详情页中的“风控报告”卡片：保留现有摘要结论卡片，并新增可展开/收起的完整报告区域。
- 完整报告区域整理并接入了 Web 端综合风控报告的主要内容，包括：客户摘要、系统风险核查、当前订单摘要、全景雷达摘要、探针C摘要、最近访问记录。
- 为适配移动端浏览习惯，首屏只保留关键结论与风险维度，完整内容默认折叠，点击“展开完整报告”后查看详细字段，再次点击可收起。
- 按用户要求调整安卓回款 tab 顶部两个统计卡片的口径：
- `未逾期` 时：
- 第一张卡片主值为“今日到期客户数量”，副值展示“实际还款人数”。
- 第二张卡片主值为“今日到期金额”，副值展示“实际还款金额”。
- `已逾期` 时：
- 第一张卡片展示“截止此时累计逾期人数”。
- 第二张卡片展示“截止此时累计逾期金额”。
- 为支持上述统计口径，最小改动扩展了后端 `/admin/repayment-stats` 返回字段，并补充对应测试。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `backend/app/api/endpoints/admin.py`
- `backend/app/schemas/loan.py`
- `backend/tests/test_admin_loans_filters.py`

## 单元测试代码

- `backend/tests/test_admin_loans_filters.py`
- `backend/tests/test_composite_risk_report.py`

## 集成测试代码

- 本次未新增独立集成测试文件；安卓端通过真实构建验证，后端通过路由统计相关测试验证。

## 测试执行结果

- 已执行后端测试：`pytest backend/tests/test_admin_loans_filters.py backend/tests/test_composite_risk_report.py`，结果 `9 passed`。
- 已执行后端应用启动验证：`python -m app.main`，应用成功完成 startup；随后因本机 `8001` 端口已被占用退出，说明代码可启动，但当前机器已有服务占用该端口。
- 已执行安卓 debug 包构建：通过，产物 `android_native/build/xiaohebao-debug.apk`。
- 已执行安卓 release 包构建：通过，产物 `android_native/build/xiaohebao-release.apk`。
- 本地 release APK SHA256：`bd7ef222ec7fe2ebe24f4ae8e64f400da2ffb8cdcdf5cbf4b4e51812a3dcfefa`。
- 本次尚未发布到腾讯云下载地址。

## 操作步骤

- 复用现有综合风控报告接口与 Web 端字段映射，拆分为安卓端“摘要区 + 折叠完整报告区”。
- 在安卓详情页中新增完整风控报告的展开/收起交互与详细字段区域。
- 扩展后端回款统计接口返回字段，满足 `未逾期 / 已逾期` 不同口径展示。
- 调整安卓回款 tab 顶部卡片展示逻辑。
- 执行后端测试、后端启动验证、安卓 debug/release 构建验证。

# 2026-05-27 16:02:07

## 修改说明

- 将包含“风控报告”方案 2 卡片的安卓原生新版本 APK 发布到腾讯云生产下载地址。
- 发布前先备份云端旧 APK，避免回滚时无基线可用。
- 本次 APK 包含详情页新增风控报告卡片、真实综合风控接口接入，以及此前详情时间字段与手机号明文展示修复。

## 修改后的代码

- 本次未新增代码修改，仅执行生产 APK 发布。

## 单元测试代码

- 不适用，本次为 APK 发布动作。

## 集成测试代码

- 不适用，本次未修改接口逻辑。

## 测试执行结果

- 本地 release APK SHA256：`04cdab43c6a530494bc9b852b8020d4c0e0fa09dd1d59149a99f79c9760976f0`。
- 已备份云端旧 APK：`/opt/xiaohebao/backups/android-apk-risk-card-20260527160207/xiaohebao.apk`。
- 已上传新 APK 到：`/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 云端与公网哈希校验结果待上传完成后复核。

## 操作步骤

- 校验本地 release 包哈希。
- 备份腾讯云生产下载目录中的旧 APK。
- 上传新 APK 到生产下载地址。
- 复核云端文件与公网下载文件哈希。


# 2026-05-27 15:28:18

## 修改说明

- 按用户选定的方案 2，在安卓原生管理端“个人信息/详情页”中新增“风控报告”卡片，位置放在“核心信息”和“紧急联系人”之间。
- 风控报告卡片采用数据强化版结构：左侧高亮展示申请准入分，右侧展示信用行为分、探针结果、报告时间；下方展示四个风险维度卡片与命中原因摘要。
- 详情页异步加载用户详情时，新增调用 `/admin/risk/composite-report` 综合风控接口，并将结果挂载到详情数据中复用，不修改后端接口协议。
- 风控报告卡片优先展示系统核查、位置/IP、履约行为、探针C四类维度；当风控接口失败或暂无数据时，展示对应的加载态或空态卡片，避免页面空白。
- 本次仅修改安卓原生 `MainActivity.java`，保持最小改动原则。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`

## 单元测试代码

- 本次为安卓原生 UI 与接口展示整合，项目当前没有 Android 单元测试框架；未新增测试文件。

## 集成测试代码

- 本次未修改后端接口逻辑，未新增集成测试文件。

## 测试执行结果

- 已执行安卓 debug 包构建：通过，产物 `android_native/build/xiaohebao-debug.apk`。
- debug APK SHA256：`612852020d00f4b04fd3545f2d272c46b43a235dd3d5bf0f62c98a928f984dd8`。
- 已执行安卓 release 包构建：通过，产物 `android_native/build/xiaohebao-release.apk`。
- 本次未上传生产环境 APK。

## 操作步骤

- 复用现有综合风控报告接口与 Web 端字段映射思路，确定安卓卡片展示字段。
- 在详情页中新增“风控报告”卡片与加载/空态展示。
- 接入综合风控接口异步加载逻辑。
- 构建 debug 与 release APK 验证通过。


# 2026-05-27 14:49:30

## 修改说明

- 按用户最新要求，对安卓原生管理端详情页做最小改动修复，补齐不同业务 tab 中首个时间字段的真实来源与文案。
- 申请 tab 详情页的首个时间字段改为“申请时间”，取 `application_submitted_at`，为空时回退 `created_at`。
- 发卡 tab 详情页的首个时间字段改为“下单时间”，优先取 `ordered_at`，为空时回退 `application_submitted_at` 与 `created_at`。
- 回款 tab 与平账 tab 详情页的首个时间字段文案统一改为“应还款时间”，值取 `due_date`。
- 安卓 app 任一 tab 打开用户详情后，用户本人手机号与两个紧急联系人手机号均改为明文展示；详情页标题副标题中的手机号也同步改为明文。
- 本次仅调整 `MainActivity.java` 中详情页字段映射与展示逻辑，未改动接口与数据库逻辑。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`

## 单元测试代码

- 本次为安卓原生 UI 详情展示修复，项目当前没有 Android 单元测试框架；未新增测试文件。

## 集成测试代码

- 本次未修改后端接口或 Web 前端逻辑，未新增集成测试文件。

## 测试执行结果

- 已执行安卓 debug 包构建：通过，产物 `android_native/build/xiaohebao-debug.apk`。
- 已执行安卓 release 包构建：通过，产物 `android_native/build/xiaohebao-release.apk`。
- 本地 release APK SHA256：`9b5c7ff5f505674ed5e1870a282c798b7cbb19cda27f27956144c4b1c85e61ed`。
- 已备份云端旧 APK：`/opt/xiaohebao/backups/android-apk-detail-fix-20260527144901/xiaohebao.apk`。
- 已上传新 APK 到：`/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 线上文件校验结果待本次上传完成后复核。

## 操作步骤

- 检查安卓详情页核心字段生成逻辑，确认时间字段与手机号展示入口。
- 按 tab 区分申请、发卡、回款/平账详情页的时间文案与取值来源。
- 去除详情页内手机号掩码，统一改为明文展示。
- 构建 debug 与 release APK。
- 备份并替换腾讯云生产下载包，随后校验线上文件哈希。


# 2026-05-27 13:12:51

## 修改说明

- 按用户最新要求继续改造安卓管理端 UI，整体风格从柔光拟态进一步收敛为接近 Google Cloud Android App 的样式。
- 底部 Tab 去掉大色块选中态，改为白底导航栏 + 蓝/灰文字图标对比形式。
- 列表卡片、统计卡片、详情卡片、弹窗卡片、照片卡片、登录卡片统一改为白底、浅灰描边、轻阴影、较克制圆角的 GCP 风格。
- 主按钮改为 Google 蓝纯色按钮，输入框改为白底细边，系统状态栏与导航栏配色同步调整。
- 已重新打包生产 APK 并上传腾讯云生产下载路径。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/Ui.java`
- `android_native/app/src/main/res/drawable/card_bg.xml`
- `android_native/app/src/main/res/drawable/input_bg.xml`
- `android_native/app/src/main/res/drawable/primary_button.xml`
- `android_native/app/src/main/res/values/styles.xml`

## 单元测试代码

- 本次为安卓原生 UI 样式与布局调整，项目当前没有 Android 单元测试框架；未新增测试文件。

## 集成测试代码

- 本次未修改后端接口或 Web 前端逻辑，未新增集成测试文件。

## 测试执行结果

- 已执行安卓 debug 包构建：通过，产物 `android_native/build/xiaohebao-debug.apk`。
- 已执行安卓 release 包构建：通过，产物 `android_native/build/xiaohebao-release.apk`。
- 本地 release APK SHA256：`3bf51fcacde063ab04daab5fe523be2e8c51305b8551561061d38d5847fb2d4d`。
- 已备份云端旧 APK：`/opt/xiaohebao/backups/android-apk-gcp-style-20260527131228/xiaohebao.apk`。
- 已上传新 APK 到：`/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 云端 APK SHA256：`3bf51fcacde063ab04daab5fe523be2e8c51305b8551561061d38d5847fb2d4d`，与本地一致。
- 公网下载地址 `https://xhbadmin.juxin.pro/download/xiaohebao.apk` 返回 `200 OK`，公网下载文件 SHA256 与本地一致。

## 操作步骤

- 收口此前进行中的安卓 GCP 风格改造，统一 `MainActivity` 中残留的玻璃拟态组件。
- 调整底部导航、卡片、按钮、输入框与系统栏样式。
- 构建 debug 包验证编译。
- 构建 release 包并上传腾讯云生产环境。
- 校验云端文件与公网下载文件哈希一致。

# 2026-05-27 12:53:20

## 修改说明

- 临时处理生产用户 `13037315459` 因最新申请被拒绝后无法继续申请的问题。
- 查验生产库确认该用户未命中黑名单、未命中风险名单，身份证 OCR 与人脸认证均已完成；最新普通订单为 `loan_id=287`，状态为 `REJECTED`。
- 按“恢复为可再次申请”原则，仅将该用户最新订单 `287` 从 `REJECTED` 恢复为 `INIT`，并清空本次拒绝相关的 `review_note`、`approved_at`、`review_admin_id`、`risk_report_checked_at`、`risk_report_checked_by`。
- 未清除用户身份证、人脸认证、联系人资料、资料提交时间，也未影响上一笔已结清订单。

## 修改后的代码

- 本次未修改代码，仅执行生产数据修复。

## 单元测试代码

- 不适用，本次为单用户生产数据修复。

## 集成测试代码

- 不适用，本次未修改接口逻辑。

## 测试执行结果

- 更新前确认：用户 `13037315459` 最新订单 `287` 状态为 `REJECTED`，上一笔普通订单为 `SETTLED`，用户 `blacklist_hit=0`、`risk_list_hit=0`。
- 执行更新：影响行数 `1`。
- 更新后复查：订单 `287` 状态为 `INIT`，`review_note=NULL`，`approved_at=NULL`，`review_admin_id=NULL`，`risk_report_checked_at=NULL`，`risk_report_checked_by=NULL`；`identity_ocr_submitted_at` 与 `identity_face_auth_at` 仍保留。

## 操作步骤

- 查询后端 H5 申请逻辑，确认 `REJECTED` 会硬拦截用户继续申请。
- 通过腾讯云服务器连接生产 MySQL，查询手机号 `13037315459` 的用户与订单状态。
- 使用带用户与订单限定条件的 SQL 更新最新拒绝订单。
- 再次查询生产库确认恢复结果。

# 2026-05-27 09:29:43

## 修改说明

- 修复安卓管理端顶部两张统计卡片数字底部被裁切的问题：统计卡片高度从 `76dp` 增加到 `98dp`，并调整内部留白、字号与文字行高。
- 放大底部 Tab 图标：图标与文字使用分段字号，Tab 高度增加，当前选中态改为更柔和的大圆角胶囊。
- 按用户要求将安卓 App 设计风格从高饱和 Stripe 玻璃风调整为浅灰白 Soft UI / Neumorphism / Apple Vision Pro 柔光拟态方向：低饱和淡紫与浅蓝渐变、白色磨砂玻璃卡片、超大圆角、轻柔阴影、低对比度背景、柔和按钮与输入框。
- 已重新打包生产 APK 并上传到腾讯云生产下载路径。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/Ui.java`
- `android_native/app/src/main/res/drawable/card_bg.xml`
- `android_native/app/src/main/res/drawable/input_bg.xml`
- `android_native/app/src/main/res/drawable/primary_button.xml`
- `android_native/app/src/main/res/values/styles.xml`

## 单元测试代码

- 本次为安卓原生 UI 样式与布局调整，项目当前没有 Android 单元测试框架；未新增测试文件。

## 集成测试代码

- 本次未修改后端接口或 Web 前端逻辑，未新增集成测试文件。

## 测试执行结果

- 已执行安卓 debug 包构建：通过，产物 `android_native/build/xiaohebao-debug.apk`。
- 已执行安卓 release 包构建：通过，产物 `android_native/build/xiaohebao-release.apk`。
- 本地 release APK SHA256：`7355aaa0a1573887dce87b18dce58122c5ed6eea263e75fae1d13820a45f478f`。
- 已备份云端旧 APK：`/opt/xiaohebao/backups/android-apk-soft-ui-20260527092754/xiaohebao.apk`。
- 已上传新 APK 到：`/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 云端 APK SHA256：`7355aaa0a1573887dce87b18dce58122c5ed6eea263e75fae1d13820a45f478f`，与本地一致。
- 公网下载地址 `https://xhbadmin.juxin.pro/download/xiaohebao.apk` 返回 `200 OK`，公网下载文件 SHA256 与本地一致。

## 操作步骤

- 根据用户反馈定位顶部统计卡片高度不足与底部 Tab 图标字号过小问题。
- 调整安卓原生统计卡片、底部导航、背景、卡片、输入框与主按钮样式。
- 构建 debug 包验证编译。
- 构建 release 包并上传腾讯云生产环境。
- 校验云端文件与公网下载文件哈希一致。

# 2026-05-27 09:08:23

## 修改说明

- 审查并修复安卓原生管理端列表页、详情页、照片区、底部操作区与业务弹窗的展示问题，避免底部导航或固定操作区遮挡卡片内容。
- 将主要业务弹窗从系统默认白框改为统一玻璃卡片样式，补齐发卡、展期、增加可用额度、调整授信、登记提醒、登记催收、拉黑等移动端可独立作业所需字段。
- 优化输入框高度、备注多行输入、照片预览和照片卡片高度，减少文字与图片被截断的风险。
- 已重新打包生产 APK 并上传到腾讯云生产下载路径。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`

## 单元测试代码

- 本次为安卓原生单文件 UI 与打包配置内行为调整，项目当前没有 Android 单元测试框架；未新增测试文件。

## 集成测试代码

- 本次未修改后端接口或前端 Web 逻辑，未新增集成测试文件。

## 测试执行结果

- 已执行安卓 debug 包构建：通过，产物 `android_native/build/xiaohebao-debug.apk`。
- 已执行安卓 release 包构建：通过，产物 `android_native/build/xiaohebao-release.apk`。
- 本地 release APK SHA256：`f0324ab09d81081c6298d262ba0184886451e30e782a54cbd7aaabaae6284f21`。
- 已备份云端旧 APK：`/opt/xiaohebao/backups/android-apk-ui-complete-20260527090720/xiaohebao.apk`。
- 已上传新 APK 到：`/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 云端 APK SHA256：`f0324ab09d81081c6298d262ba0184886451e30e782a54cbd7aaabaae6284f21`，与本地一致。
- 公网下载地址 `https://xhbadmin.juxin.pro/download/xiaohebao.apk` 返回 `200 OK`，公网下载文件 SHA256 与本地一致。

## 操作步骤

- 审查用户截图中的遮挡、弹窗粗糙和操作字段不足问题。
- 修改安卓原生页面滚动留白、照片展示、业务弹窗和操作表单。
- 构建 debug 包验证编译。
- 构建 release 包并上传腾讯云生产环境。
- 校验云端文件与公网下载文件哈希一致。

# 2026-05-22 23:54:00

## 修改说明

- 根据用户授权，新增真正的安卓原生 App 工程 `android_native/`，不覆盖现有网页安卓端 `android/`。
- App 名称为“小荷包”，包名为 `com.juxin.xiaohebao.admin`，生产接口固定连接 `https://xhbadmin.juxin.pro/api`。
- 原生 App 已实现管理后台账号登录，登录后通过 `/api/admin/me` 获取角色，并按角色展示档案、申请、发卡、回款、平账入口。
- 原生 App 已实现客户/订单列表搜索，以及审批、发卡、回款跟进、催收、平账、授信调整、拉黑、解除位置风控等核心操作入口。
- 新增命令行 APK 构建脚本 `android_native/build_apk.sh`，可在本机 Android SDK 环境直接生成安装包。
- 新增生产下载入口模板：安卓手机访问 `xhbadmin.juxin.pro` 时可跳转到下载页并下载 `xiaohebao.apk`。

## 修改文件

- `android_native/.gitignore`
- `android_native/app/src/main/AndroidManifest.xml`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/ApiClient.java`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/Ui.java`
- `android_native/app/src/main/res/drawable/card_bg.xml`
- `android_native/app/src/main/res/drawable/ic_launcher.xml`
- `android_native/app/src/main/res/drawable/input_bg.xml`
- `android_native/app/src/main/res/drawable/primary_button.xml`
- `android_native/app/src/main/res/values/strings.xml`
- `android_native/app/src/main/res/values/styles.xml`
- `android_native/build_apk.sh`
- `android_native/build/xiaohebao-debug.apk`
- `android_native/docs/android-download.html`
- `android_native/docs/logo.svg`
- `android_native/docs/native-app.md`
- `scripts/nginx/xhbadmin_android_apk_download.conf`
- `doc/work_result/20260522.md`

## 测试结果

- `cd /Volumes/littlejiang02/VibeCoding/xiaohebao / && ./android_native/build_apk.sh`
  - 结果：构建成功，生成 `android_native/build/xiaohebao-debug.apk`。
- `aapt dump badging android_native/build/xiaohebao-debug.apk`
  - 结果：包名 `com.juxin.xiaohebao.admin`，应用名“小荷包”，`minSdkVersion=23`，`targetSdkVersion=34`，仅声明 `INTERNET` 权限。
- `apksigner verify --verbose android_native/build/xiaohebao-debug.apk`
  - 结果：签名校验通过，v1/v2/v3 签名均有效。
- `adb devices`
  - 结果：当前未连接安卓真机或模拟器，因此未执行安装运行测试。

## 说明

- 当前 APK 使用 debug 签名，仅用于本地验证，不应直接作为生产正式包发布。
- 本次仅完成本地原生 App 工程与下载入口模板，未修改腾讯云生产环境配置，未上传 APK 到生产服务器。
- 正式上线前需要生成 release keystore，产出正式签名 APK，并经用户确认后再发布到腾讯云。

# 2026-05-22 23:20:34

## 修改说明

- 在当前项目中查找小荷包 logo，发现管理端与 H5 打包资源中存在同一份钱包图标。
- 将该钱包图标整理为安卓端公共资源 `android/public/logo.svg`，登录页改为使用该 logo。
- 登录页登录框以外的文案仅保留“小荷包移动工作台”，并居中展示。
- 删除登录页英文 `ANDROID WORKSPACE` 和原说明文案，降低冗余信息。

## 修改文件

- `android/public/logo.svg`
- `android/src/main.js`
- `android/src/styles.css`
- `doc/work_result/20260522.md`

## 测试结果

- `cd /Volumes/littlejiang02/VibeCoding/xiaohebao /android && pnpm test`
  - 结果：`3` 个测试文件，`16` 条测试全部通过。
- `cd /Volumes/littlejiang02/VibeCoding/xiaohebao /android && pnpm build`
  - 结果：构建通过。
- 已检查构建产物中登录页仅保留 `/logo.svg` 与“小荷包移动工作台”，无 `ANDROID WORKSPACE` 和原说明文案残留。

# 2026-05-22 23:12:15

## 修改说明

- 安卓端登录页移除默认账号密码，用户名和密码输入框默认保持空白。
- 右上角个人按钮不再直接登出，点击后先弹出确认面板，提供“登出 / 取消”。
- 点击取消或遮罩会关闭确认面板，只有点击“登出”才清除登录状态。

## 修改文件

- `android/src/main.js`
- `android/src/styles.css`
- `doc/work_result/20260522.md`

## 测试结果

- `cd /Volumes/littlejiang02/VibeCoding/xiaohebao /android && pnpm test`
  - 结果：`3` 个测试文件，`16` 条测试全部通过。
- `cd /Volumes/littlejiang02/VibeCoding/xiaohebao /android && pnpm build`
  - 结果：构建通过。
- 已检查安卓登录页代码中无 `value="admin"` 与 `admin123` 预填内容。

# 2026-05-22 23:08:12

## 修改说明

- 删除安卓端列表页右下角悬浮“+”按钮，避免被误解为新增客户或新增申请。
- 保留顶部刷新按钮作为页面刷新入口，减少重复且含义不清的操作。
- 清理悬浮按钮相关样式和事件绑定，确认代码中无 `fab` / `fabBtn` 残留。

## 修改文件

- `android/src/main.js`
- `android/src/styles.css`
- `doc/work_result/20260522.md`

## 测试结果

- `cd /Volumes/littlejiang02/VibeCoding/xiaohebao /android && pnpm test`
  - 结果：`3` 个测试文件，`16` 条测试全部通过。
- `cd /Volumes/littlejiang02/VibeCoding/xiaohebao /android && pnpm build`
  - 结果：构建通过。
- `rg -n "fab|fabBtn" android/src` 无残留结果。

# 2026-05-22 23:05:21

## 修改说明

- 根据安卓端截图反馈，将页面顶部标题区调整为固定顶部显示，列表上下滑动时保持可见。
- 删除标题下方的说明文案，减少每页顶部重复冗余信息。
- 顶部栏增加白色半透明背景和细分隔线，避免滚动内容穿透影响可读性。
- 本次仅调整安卓端页面展示，不改变业务逻辑和接口行为。

## 修改文件

- `android/src/main.js`
- `android/src/styles.css`
- `doc/work_result/20260522.md`

## 测试结果

- `cd /Volumes/littlejiang02/VibeCoding/xiaohebao /android && pnpm test`
  - 结果：`3` 个测试文件，`16` 条测试全部通过。
- `cd /Volumes/littlejiang02/VibeCoding/xiaohebao /android && pnpm build`
  - 结果：构建通过。

# 2026-05-22 22:58:18

## 修改说明

- 根据安卓端截图反馈，将底部导航的文字方块图标替换为统一线条风格简笔画 SVG 图标。
- 五个入口分别使用档案夹、审批清单、银行卡、回款箭头、账本图标，保留原有底栏文字，降低识别成本。
- 仅调整安卓端底部导航展示，不改变现有页面结构、业务入口和接口逻辑。

## 修改文件

- `android/src/main.js`
- `android/src/styles.css`
- `android/src/viewModel.js`
- `doc/work_result/20260522.md`

## 测试结果

- `cd /Volumes/littlejiang02/VibeCoding/xiaohebao /android && pnpm test`
  - 结果：`3` 个测试文件，`16` 条测试全部通过。
- `cd /Volumes/littlejiang02/VibeCoding/xiaohebao /android && pnpm build`
  - 结果：构建通过。

# 2026-05-22 22:46:12

## 修改说明

- 在现有安卓管理端框架上补齐了管理后台的核心移动能力，保留底部五页签：档案、申请、发卡、回款、平账。
- 将详情页改为手机单页操作面板，补上返回按钮、认证照片、核心信息、订单信息和底部固定操作区，避免手机端来回跳页。
- 补齐安卓端可执行的后台操作：解除位置风控、一键拉黑、移出黑名单、审批通过/拒绝、调整授信、增加可用额度、确认发卡、拒绝发卡、退回待下单、保存备注、登记提醒、登记催收、确认还款申请、账单展期、登记平账、确认结清。
- 安卓端操作表单改为内嵌填写，不再依赖 `prompt`，更适合手机屏幕。
- 修正安卓端请求地址构造，兼容本地代理模式和真机/云端完整后端地址模式。

## 修改文件

- `android/src/api.js`
- `android/src/api.test.js`
- `android/src/format.js`
- `android/src/format.test.js`
- `android/src/main.js`
- `android/src/styles.css`
- `android/src/viewModel.js`
- `android/src/viewModel.test.js`

## 测试结果

- `cd /Volumes/littlejiang02/VibeCoding/xiaohebao /android && pnpm test`
  - 结果：`3` 个测试文件，`16` 条测试全部通过。
- `cd /Volumes/littlejiang02/VibeCoding/xiaohebao /android && pnpm build`
  - 结果：构建通过，生成安卓端静态产物。

## 本地接口验证

- 本地登录接口可用，`admin/admin123` 可获取管理端 token。
- 已只读冒烟验证以下接口均返回 `200`：
  - `/api/admin/me`
  - `/api/admin/users?skip=0&limit=1`
  - `/api/admin/loans?scope=REVIEWING&status=REVIEWING&skip=0&limit=1`
  - `/api/admin/loans?scope=WITHDRAWING&skip=0&limit=1`
  - `/api/admin/loans?scope=REPAYMENTS&due_date_preset=TODAY&skip=0&limit=1`
  - `/api/admin/loans?scope=FINANCE&skip=0&limit=1`
- 安卓开发页可访问，当前本地地址：`http://127.0.0.1:2004/`

## 说明

- 本次仅完成本地改造与验证，未触碰腾讯云生产环境数据。
- 后续可在用户确认后，再推进云端发布或打包成可安装安卓 App。

# 2026-05-22 21:52:33

## 修改说明

- 按要求固定本地前端端口：用户 H5 `2001`、管理前端 `2002`、安卓端 `2003`。
- 清理上述端口占用后重新启用本地服务。
- 将安卓 Vite 开发端口配置从 `2101` 调整为 `2003`，包括 `android/vite.config.js` 和 `android/package.json`。
- 本地 Nginx 仅保留 H5 `2001` 与管理前端 `2002` 两个入口，均代理 `/api` 和 `/uploads` 到本地后端 `8001`。

## 修改文件

- `android/vite.config.js`
- `android/package.json`
- `doc/work_result/20260522.md`

## 验证结果

- `http://127.0.0.1:2001/` 返回 `200 OK`。
- `http://127.0.0.1:2002/` 返回 `200 OK`。
- `http://127.0.0.1:2003/` 返回 `200 OK`。
- 三个入口的 `/api` 请求均能到达本地后端，未登录接口返回鉴权提示。
- 旧端口 `2017`、`2103` 已无小荷包监听进程。

# 2026-05-22 21:47:55

## 操作说明

- 根据浏览器截图定位到当前访问地址为 `http://127.0.0.1:2103/`，该端口此前未监听，因此浏览器报 `ERR_CONNECTION_REFUSED`。
- 已在本地 Nginx 配置中新增管理后台兼容入口 `2103`，继续代理 `/api`、`/uploads` 到本地后端 `8001`。

## 验证结果

- `nginx -t` 配置检查通过。
- `http://127.0.0.1:2103/` 已返回 `200 OK`。
- 当前可继续使用 `http://127.0.0.1:2103/` 访问本地管理后台。

# 2026-05-22 21:43:52

## 操作说明

- 清理并确认本地小荷包相关端口：后端 `8001`、H5 `2001`、管理后台 `2017`。
- 使用当前项目目录启动本地后端服务，后端监听 `http://127.0.0.1:8001`。
- 使用本机 Nginx 启动 H5 与管理后台静态入口，并将 `/api`、`/uploads` 反向代理到本地后端。

## 本地访问地址

- H5 前端：`http://127.0.0.1:2001/`
- 管理后台：`http://127.0.0.1:2017/`
- 后端接口：`http://127.0.0.1:8001/`

## 验证结果

- 后端根路径访问成功，返回服务存活信息。
- H5 首页访问返回 `200`。
- 管理后台首页访问返回 `200`。
- H5 与管理后台的 `/api` 请求均已转发到本地后端，未登录接口返回鉴权提示，说明代理链路正常。

# 2026-05-22 21:35:30

## 腾讯云部署

- 按用户明确授权，将“位置风控升级”同步发布到腾讯云生产环境。
- 发布前备份目录：`/opt/xiaohebao/backups/location-risk-upgrade-20260522212831`
- 本次发布范围严格限定为位置风控相关文件：
  - 后端：`/opt/xiaohebao/current/backend/app/services/admin_permissions.py`
  - 后端：`/opt/xiaohebao/current/backend/app/services/admin_service.py`
  - 后端：`/opt/xiaohebao/current/backend/app/api/endpoints/admin.py`
  - 后端：`/opt/xiaohebao/current/backend/app/schemas/user.py`
  - 管理端静态资源：`/data/www/xhbadmin.juxin.pro/dist/assets/api-B_waDxrx.js` 与 `.gz`
  - 管理端静态资源：`/data/www/xhbadmin.juxin.pro/dist/assets/Users-DwuoAh5t.js` 与 `.gz`
  - 管理端静态资源：`/data/www/xhbadmin.juxin.pro/dist/assets/Users-DVyjHDaO.css` 与 `.gz`
- 发布策略采用最小变更：未替换管理端 `Layout-CH_kxrrU.js`，避免把本地联调过的 WebSocket 地址误带到生产。
- 生产后端服务已重启：`xiaohebao-backend.service = active`

## 生产验证

- 生产管理端入口：`https://xhbadmin.juxin.pro/` 返回 `200`。
- 生产管理端路由：`https://xhbadmin.juxin.pro/users` 返回 `200`。
- 生产后端启动日志正常，`Uvicorn running on http://0.0.0.0:28001`。
- 已确认生产文件包含本次升级关键标识：
  - 独立权限：`user-location-risk-unlock`
  - 用户列表筛选字段：`location_risk_blocked`
  - 详情/列表权限字段：`can_unlock_location_risk`
  - 解除接口：`/admin/users/{user_id}/location-risk/unlock`
  - 前端按钮文案：`解除位置风控`
- 本次未修改生产真实业务数据，仅发布代码与静态资源。

# 2026-05-22 18:35:00

## 收尾确认

- 复核“解除位置风控”后端权限检查实现，`ensure_admin_permission(...)` 已在 `backend/app/services/admin_service.py` 中存在，本次新增接口可直接复用，无需额外补函数。
- 重新执行本次能力相关后端测试，结果保持通过：
  `/opt/homebrew/anaconda3/envs/py312/bin/python3 -m pytest backend/tests/test_async_db_routes.py backend/tests/test_admin_loans_filters.py backend/tests/test_admin_user_location_risk.py -q`
  结果：`8 passed, 8 warnings`
- 重新执行管理端静态产物语法检查，结果通过：
  - `node --input-type=module --check < frontend_admin/dist/assets/Users-DwuoAh5t.js`
  - `node --input-type=module --check < frontend_admin/dist/assets/api-B_waDxrx.js`
- 本次能力的实现结论保持不变：
  - 管理后台可在“用户档案”页主动筛出位置风控锁定用户
  - “解除位置风控”使用独立权限 `user-location-risk-unlock`
  - 解除时只清锁定状态字段，历史位置数据与历史事件记录保持原样

# 2026-05-22 18:01:50

## 修改说明

- 为管理后台新增“主动发现位置风控锁定用户”的能力。
- 用户档案列表接口 `/api/admin/users` 支持新筛选参数 `location_risk_blocked`，可按“仅已锁定 / 仅未锁定”筛选。
- 用户档案列表返回新增字段：
  - `location_risk_blocked`
  - `location_risk_reason`
  - `location_risk_at`
  - `can_unlock_location_risk`
- 用户档案详情返回补充位置风控字段，并返回 `can_unlock_location_risk` 供前端控制按钮显示。
- 新增后台解除位置风控接口：`POST /api/admin/users/{user_id}/location-risk/unlock`
  - 仅清除 `location_risk_blocked / location_risk_reason / location_risk_at`
  - 保留经纬度、定位时间、地址和历史事件记录不变
  - 新增审计事件：`ADMIN_LOCATION_RISK_UNLOCK`
- 新增独立权限键：`user-location-risk-unlock`
  - 设计上与 `users` 分离
  - `users` 负责查看
  - `user-location-risk-unlock` 负责解除
  - 避免“能看用户档案的人默认都能解除风控”
- 本地管理端运行产物同步修改：
  - 用户档案列表页增加“位置风控”筛选
  - 用户姓名右侧增加“位置风控”标签
  - 风险管理列增加“解除位置风控”按钮
  - 用户档案详情“地理位置（授权）”区块增加风控状态、风控时间、风控原因及解除按钮

## 验证结果

- 后端测试：
  `/opt/homebrew/anaconda3/envs/py312/bin/python3 -m pytest backend/tests/test_async_db_routes.py backend/tests/test_admin_loans_filters.py backend/tests/test_admin_user_location_risk.py -q`
  结果：`8 passed, 8 warnings`
- 前端静态产物 ESM 语法检查通过：
  - `node --input-type=module --check < frontend_admin/dist/assets/Users-DwuoAh5t.js`
  - `node --input-type=module --check < frontend_admin/dist/assets/api-B_waDxrx.js`
- 本地接口核验：
  - `/api/admin/users?limit=1` 已返回 `location_risk_blocked / location_risk_reason / location_risk_at / can_unlock_location_risk`
  - `/api/admin/users/{id}` 已返回相同字段
- 本地后端已重启到新代码，端口 `8001` 正常监听。

# 2026-05-22 17:31:32

## 生产数据处理

- 按用户明确确认，对手机号 `18601143372` 执行“彻底解除并重置位置基准”。
- 处理前：用户 ID `194`，姓名 `王辉`，`location_risk_blocked=1`，锁定原因 `登录位置异常：4小时内距离上次登录约52.5公里`，位置基准时间 `2026-05-22 13:31:48`，基准经纬度 `38.4080079 / 114.8064156`。
- 执行生产库更新：
  `location_risk_blocked/location_risk_reason/location_risk_at` 清空；
  `location_latitude/location_longitude/location_accuracy/location_address/location_province/location_city/location_district/location_street/location_source/location_updated_at` 全部清空。
- 处理后复查：位置风控锁定字段已清空，位置基准字段已清空。
- 当前后端服务状态：`xiaohebao-backend.service = active`。
- 本次仅修改该手机号对应用户的位置风控与位置基准字段，未修改其他用户或其他业务数据。

# 2026-05-22 16:39:39

## 生产问题修复

- 根据用户截图，生产管理端控制台报错：`Identifier 'Lt' has already been declared`，导致还款页静态 chunk 解析失败。
- 原因：还款页生产压缩产物中，“当前区间选项”计算字段与“切换还款日”函数被压缩成同名变量 `Lt`。
- 修复：将还款页静态资源中的“当前区间选项”变量调整为稳定变量名 `dueSelectedOption`，避免重复声明。
- 发布文件：`/data/www/xhbadmin.juxin.pro/dist/assets/Repayments-Bxsm43Bs.js` 与对应 `.gz`。
- 发布前备份：`/opt/xiaohebao/backups/fix-repayments-syntax-20260522163659/`。

## 验证结果

- 腾讯云服务器 ESM 语法检查通过：`node --input-type=module --check < /data/www/xhbadmin.juxin.pro/dist/assets/Repayments-Bxsm43Bs.js`。
- 生产域名静态资源复查：包含 `dueSelectedOption`，不再包含旧重复声明片段 `Lt=c(()=>At.find`，`Lt=` 只剩 1 处。
- 本次只修复管理端还款页静态资源语法问题，未修改业务数据。

# 2026-05-22 16:11:14

## 生产数据处理

- 按用户明确确认，解除手机号 `18601143372` 的位置风控锁定。
- 处理前：用户 ID `194`，姓名 `王辉`，`location_risk_blocked=1`，原因 `登录位置异常：4小时内距离上次登录约52.5公里`，锁定时间 `2026-05-22 15:42:40`。
- 执行生产库更新：
  `UPDATE users SET location_risk_blocked=0, location_risk_reason=NULL, location_risk_at=NULL WHERE phone='18601143372';`
- 处理后复查：`location_risk_blocked=0`，`location_risk_reason=NULL`，`location_risk_at=NULL`。
- 本次仅修改该手机号对应用户的位置风控锁定字段，未修改其他用户或业务数据。

# 2026-05-22 16:00:27

## 修改说明

- 按用户确认，将本地已验证的还款管理四卡片口径发布到腾讯云生产环境。
- 发布范围：
  - 后端：`/opt/xiaohebao/current/backend/app/api/endpoints/admin.py`
  - 后端：`/opt/xiaohebao/current/backend/app/schemas/loan.py`
  - 管理端静态资源：`/data/www/xhbadmin.juxin.pro/dist/assets/Repayments-Bxsm43Bs.js`
  - 管理端静态资源：`/data/www/xhbadmin.juxin.pro/dist/assets/api-B_waDxrx.js`
- 发布前已备份原生产文件到腾讯云：`/opt/xiaohebao/backups/deploy-20260522155315/`。
- 发布后重新生成生产静态资源 `.gz` 文件，并保持生产 API 基址为 `https://xhb.juxin.pro/api`。
- 已重启 `xiaohebao-backend.service`。

## 生产验证

- `xiaohebao-backend.service` 当前状态：`active`。
- 生产管理端静态资源已确认：
  - API 基址仍为 `https://xhb.juxin.pro/api`。
  - `/admin/repayment-stats` 已支持参数透传。
  - 还款页资源包含 `due_date_preset` 与 `receivable_order_count`。
- 生产服务日志显示重启后管理端接口请求正常返回 `200`。

## 用户风控锁定检查

- 手机号：`18601143372`，用户姓名：王辉，用户 ID：`194`。
- 当前 `location_risk_blocked=1`。
- 当前锁定原因：`登录位置异常：4小时内距离上次登录约52.5公里`。
- 锁定时间：`2026-05-22 15:42:40`。
- 上一次保存定位时间：`2026-05-22 13:31:48`。
- 当前系统阈值：`LOGIN_DISTANCE_RISK_HOURS=4`，`LOGIN_DISTANCE_RISK_KM=30.0`。
- 最近访问 IP 为 `101.16.167.101`，IP 归属地为河北石家庄裕华；本次拦截由 GPS 定位距离触发，不是单纯 IP 归属地触发。

## 解除方式

- 如需解除该单个用户的位置风控锁定，可在腾讯云生产数据库执行：
  `UPDATE users SET location_risk_blocked=0, location_risk_reason=NULL, location_risk_at=NULL WHERE phone='18601143372';`
- 本次仅检查并说明解除方式，未直接修改该用户真实业务数据。

# 2026-05-22 15:32:55

## 修改说明

- 修改管理端还款管理顶部四个卡片口径，使卡片数据随还款日时间区间筛选变化。
- 后端 `/admin/repayment-stats` 新增 `due_date_preset` 参数，支持 `TODAY`、`TOMORROW`，并按所选还款日区间过滤统计范围。
- 回款率口径调整为：当前选定时间区间内 `received_amount / receivable_amount`，即实际回款金额占应还款额比例。
- 返回结构新增 `receivable_order_count`，用于前端展示选定区间内的应还订单数。
- 管理端还款页四个卡片改为：应还订单、应还客户、实收金额、回款率，并统一使用 `/admin/repayment-stats` 的当前区间统计结果。
- 同步修正本地管理端 `dist` 产物及 `.gz` 文件，保证本地正在运行的管理端页面使用新口径。

## 本地接口验证

- 全部：应还订单 `5`，应还客户 `5`，应还金额 `9780.0`，实收金额 `2532.5`，回款率 `25.89%`。
- 今日：应还订单 `0`，应还客户 `0`，应还金额 `0.0`，实收金额 `0.0`，回款率 `0.0%`。
- 明日：应还订单 `0`，应还客户 `0`，应还金额 `0.0`，实收金额 `0.0`，回款率 `0.0%`。
- 浏览器页面视觉核验未执行：当前会话的浏览器工具拒绝访问 `http://127.0.0.1:2103`，未绕过该限制。

## 测试结果

- 后端定向测试：`/opt/homebrew/anaconda3/envs/py312/bin/python3 -m pytest backend/tests/test_async_db_routes.py backend/tests/test_admin_loans_filters.py -q`，结果 `6 passed, 8 warnings`。
- Android 单元测试：`cd android && /opt/homebrew/bin/pnpm test`，结果 `2 files passed, 8 tests passed`。
- 本次未向腾讯云推送、部署或写回任何本地改动。

# 2026-05-22 14:15:30

## 修改说明

- 核对 `http://127.0.0.1:2103/applications` 页面数据来源：页面当前展示的 4 条申请记录与本地 MySQL、本地后端接口一致。
- 修复本地管理端统计 WebSocket 仍请求 `ws://127.0.0.1:2103/api/admin/ws/stats` 的问题，改为直连本地后端 `ws://127.0.0.1:8001/api/admin/ws/stats`。
- 修复本地 H5 订单状态 WebSocket 同样经静态服务端口的问题，改为直连本地后端 `ws://127.0.0.1:8001/api/loan/ws/status`。
- 同步重新生成相关 `.gz` 文件。

## 验证结果

- 本地 MySQL：`loans.status='REVIEWING'` 为 4 条。
- 本地接口：`/api/admin/loans?status=REVIEWING&scope=REVIEWING` 返回 `total: 4`。
- 管理端静态产物：未发现 `ws://127.0.0.1:2103`、`xhb.juxin.pro/api` 等非本地后端连接。
- H5 静态产物：未发现 `ws://127.0.0.1:2104`、`xhb.juxin.pro/api` 等非本地后端连接。

# 2026-05-22 13:53:37

## 修改说明

- 修复本地管理端登录时跨端口 CORS 报错的问题。
- 将本地管理端/H5 静态产物中的 API 基址从 `http://127.0.0.1:8001/api` 调整为同源 `/api`。
- 扩展 `scripts/local_spa_static.py`：本地静态服务现在会将 `/api/*` 和 `/uploads/*` 转发到本地后端 `http://127.0.0.1:8001`，避免浏览器跨域请求。
- 重新生成管理端/H5 静态产物对应的 `.gz` 文件，确保压缩资源也使用本地同源 API。

## 验证结果

- `curl -X POST http://127.0.0.1:2103/api/admin/login` 使用 `admin/admin123` 返回 `200 OK` 和本地 token。
- `curl -I http://127.0.0.1:2103/applications` 返回 `200 OK`。
- `curl -I http://127.0.0.1:2104/orders` 返回 `200 OK`。
- 扫描普通静态文件和 gzip 静态文件，未发现 `xhb.juxin.pro/api` 或 `http://127.0.0.1:8001/api` 旧基址。
- `python -m py_compile scripts/local_spa_static.py` 通过。
- Android 单元测试：`cd android && pnpm test`，结果 `2 files passed, 8 tests passed`。

## 数据核对

- 当前本地 MySQL `xiaohebao.loans` 中 `status='REVIEWING'` 为 4 条。
- 当前本地后端 `/api/admin/loans?status=REVIEWING&scope=REVIEWING` 返回 `total: 4`。
- 如果浏览器页面仍显示 5 条申请数据，属于浏览器旧缓存或旧会话资源，需要强制刷新后重新登录。

# 2026-05-22 13:35:06

## 修改说明

- 修正本地管理端、用户 H5 静态产物仍指向生产 API 的问题。
- 将本地 `frontend_admin/dist/assets/*.js`、`frontend_h5/dist/assets/*.js` 中的生产 API 基址 `https://xhb.juxin.pro/api` 替换为 `http://127.0.0.1:8001/api`。
- 同步重新生成对应 `.gz` 文件，避免本地静态服务或后续本地代理读取压缩文件时仍访问生产 API。
- 新增 `scripts/local_spa_static.py`，用于本地启动管理端/H5 的 SPA 静态服务，支持 `/applications`、`/orders` 等前端路由回退到 `index.html`。

## 本地启动结果

- 后端：`http://127.0.0.1:8001/`。
- Android 本地开发服务：`http://127.0.0.1:2101/`。
- 管理端本地 SPA 静态服务：`http://127.0.0.1:2103/`。
- 用户 H5 本地 SPA 静态服务：`http://127.0.0.1:2104/`。

## 验证结果

- 扫描普通静态文件：`rg "https://xhb\\.juxin\\.pro/api|juxin\\.pro/api" frontend_admin frontend_h5 android backend`，无生产 API 命中。
- 扫描 gzip 静态文件：逐个 `gzip -cd` 后检查生产 API，无命中。
- 管理端前端路由：`curl -I http://127.0.0.1:2103/applications` 返回 `200 OK`。
- H5 前端路由：`curl -I http://127.0.0.1:2104/orders` 返回 `200 OK`。
- `python -m py_compile scripts/local_spa_static.py` 通过。
- Android 单元测试：`cd android && pnpm test`，结果 `2 files passed, 8 tests passed`。

## 环境边界说明

- 本地管理端和 H5 现在访问本地后端 `http://127.0.0.1:8001/api`，不会再请求腾讯云生产 API。
- 本地后端继续使用本机 MySQL 配置。
- 本次未向腾讯云推送、部署或写回任何本地改动。

# 2026-05-22 12:34:12

## 修改说明

- 以腾讯云当前保留并继续运行的生产代码为基准，重新覆盖同步到本地。
- 后端来源：`tencent-superapi:/opt/xiaohebao/current/backend/`，本地保留 `.env`、`.env.xhb-prod`、`.venv`、`uploads` 等本机运行配置与运行时目录，避免覆盖本地 MySQL 配置。
- 管理端来源：`tencent-superapi:/opt/xiaohebao/current/frontend_admin/`，并用当前生产运行目录 `tencent-superapi:/data/www/xhbadmin.juxin.pro/dist/` 覆盖本地 `frontend_admin/dist/`。
- 用户 H5 来源：当前生产运行目录 `tencent-superapi:/data/www/xhb.juxin.pro/dist/`，覆盖本地 `frontend_h5/dist/`；本地 H5 仅保留生产运行产物。
- 文档来源：`tencent-superapi:/opt/xiaohebao/current/doc/`。
- Android 端继续保留在本地 `android/`，接口和业务逻辑按更新后的本地后端管理端接口保持一致：
  - 底部五标签：档案、申请、发卡、回款、平账。
  - 不展示用户编号。
  - 风险名单、黑名单、风险区域标签展示在姓名右侧。
  - 申请详情包含三张认证照片、客户资料、授信审批表单。
  - 回款列表继续使用管理端贷款列表筛选口径：`scope=REPAYMENTS&due_date_preset=TODAY`；逾期催收使用 `scope=OVERDUE`。

## 一致性校验

- `frontend_admin/dist/index.html` 与云端 `/data/www/xhbadmin.juxin.pro/dist/index.html` SHA256 一致：`f6dee959444a7064de013e948489243c7f40f46de8b37cd6a98aa959f4265d86`。
- `frontend_h5/dist/index.html` 与云端 `/data/www/xhb.juxin.pro/dist/index.html` SHA256 一致：`f3a56523c2aedb66f5c26d8df24a377359ff5cbd4670e57ae17c279f268cab0b`。

## 本地启动结果

- 后端：`http://127.0.0.1:8001/`，返回 `hello world xhb. [default][127.0.0.1]`。
- Android 本地开发服务：`http://127.0.0.1:2101/`，Vite 代理 `/api` 到 `http://127.0.0.1:8001`。
- 管理端静态服务：`http://127.0.0.1:2103/`。
- 用户 H5 静态服务：`http://127.0.0.1:2104/`。

说明：云端生产打包产物中的管理端和 H5 API 地址为生产域名。本次为了保持本地文件与云端生产产物一致，未修改打包产物中的 API 地址。

## 测试结果

- Android 单元测试：`cd android && pnpm test`，结果 `2 files passed, 8 tests passed`。
- Android 构建：`cd android && pnpm build`，构建通过。
- 后端定向测试：`python -m pytest backend/tests/test_async_db_routes.py backend/tests/test_admin_loans_filters.py -q`，结果 `5 passed, 8 warnings`。
- 浏览器冒烟：已打开 `http://127.0.0.1:2101/`，页面显示 Android 移动工作台登录页。

## 云端同步说明

- 本次只从腾讯云拉取到本地，并启动本地服务。
- 未向腾讯云推送、部署或写回任何本地改动。

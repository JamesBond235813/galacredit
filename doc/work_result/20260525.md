# 2026-05-25 19:34:43

## 修改说明

- 按“云端和本地配置必须隔离”的要求，重新定义一致性判断口径：本地 `/api`、本地 WebSocket、云端生产域名等环境配置差异不再视为业务逻辑不一致。
- 排查本地与云端是否存在配置串线：云端运行产物未发现实际误带本地 API/WebSocket 地址；本地 H5 和 Web 管理端未发现误连云端 API。
- 发现本地原生 Android 管理端源码硬编码生产接口 `https://xhbadmin.juxin.pro/api`，存在本地调试误连云端风险。
- 最小改动修正原生 Android 构建方式：debug 默认使用本地模拟器访问宿主机地址 `http://10.0.2.2:8001/api` 和 `http://10.0.2.2:8001`；release 构建必须显式设置 `XHB_NATIVE_API_BASE` 和 `XHB_NATIVE_ASSET_BASE`，避免本地和云端配置互相混淆。

## 修改后的代码

- 修改 `android_native/build_apk.sh`：按 debug/release 注入 `AppConfig.java`，release 未显式配置时直接失败。
- 修改 `android_native/app/src/main/java/com/juxin/xiaohebao/admin/ApiClient.java`：API 基址改为读取 `AppConfig.API_BASE`。
- 修改 `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`：图片资源相对路径基址改为读取 `AppConfig.ASSET_BASE`。

## 测试与验证

- 已执行 `./build_apk.sh debug`，构建成功，产物为 `android_native/build/xiaohebao-debug.apk`。
- 构建过程中仅出现 Java 8 bootclasspath 和过时 API 警告，不影响本次配置隔离验证。
- 已扫描本地源码，实际生产 API 硬编码已移除；仅保留 release 构建参数示例文本。
- 已扫描 debug APK 字符串，未发现 `xhbadmin.juxin.pro/api` 或 `xhb.juxin.pro/api`。
- 已复查云端运行产物中本地地址命中，确认命中来自第三方库内部 `localhost` 字符串，不是业务 API 或 WebSocket 配置。

# 2026-05-25 12:16:30

## 修改说明

- 根据反馈补充 `doc/xiaohebao_rebuild_prompt.md` 中缺失的 UI 描述。
- 新增独立章节“UI/UX 设计规范”，覆盖总体视觉方向、设计 Token、H5 用户端 UI、H5 关键页面视觉、Web 管理后台 UI、Android 管理端 UI、通用交互状态、响应式与可访问性、UI 验收标准。
- 调整后续章节编号，并在“生成系统时的执行要求”中加入“先定义 UI/UX 设计系统”的步骤，确保后续投喂 Codex 时不会只生成无设计感的功能页面。

## 修改后的代码

- 更新文档：`doc/xiaohebao_rebuild_prompt.md`
- 更新工作记录：`doc/work_result/20260525.md`
- 本次仅补充产品说明书/提示词内容，无业务代码修改。

## 测试与验证

- 已检查文档章节编号，确认顶层章节从 1 到 19 连续。
- 未执行单元测试或集成测试：本次为纯文档修改，不涉及后端、前端或数据库逻辑变更。

# 2026-05-25 12:00:33

## 修改说明

- 根据当前云端业务系统和本地代码结构，撰写一份可直接交给 Codex 使用的“小荷包业务系统重构提示词 / 产品说明书”。
- 新增文档 `doc/xiaohebao_rebuild_prompt.md`，覆盖 C 端 H5、Android 管理工作台、Web 管理后台、后端 API、数据库模型、权限、状态机、风控、审计、合同、验收标准。
- 文档重点纠正旧版 `PRODUCT_DOC.md` 中偏早期的描述，补充当前线上真实业务能力：渠道专属链接、滑块短信登录、e签宝 OCR/人脸、位置风控、风险名单、黑名单、商品/卡池、待发卡、财务平账、催收和 Android 移动工作台。

## 修改后的代码

- 新增文档：`doc/xiaohebao_rebuild_prompt.md`
- 本次仅撰写产品说明书/提示词，无业务代码修改。

## 测试与验证

- 已检查文档文件可正常读取，全文约 643 行。
- 未执行单元测试或集成测试：本次为纯文档修改，不涉及后端、前端或数据库逻辑变更。

# 2026-05-25 10:41:58

## 修改说明

- 排查腾讯云生产环境用户 `15805655619` 仍反馈无法上传身份证的问题。
- 确认用户 `id=251` 当前状态正常：未实名、未拉黑、未命中风险名单、位置风控未锁定，订单 `id=254` 状态为 `INIT`。
- 确认今天该用户已登录并进入首页，后端收到 `/api/user/location`、`/api/user/info` 和 `/api/loan/ws/status`，但没有收到 `/api/user/ocr` 上传请求。
- 确认线上 H5 WebSocket 返回 `status=INIT`，首页理论上可显示“立即申请”。
- 结合用户使用小米/MiuiBrowser，且后端完全没有上传请求，按最小改动处理疑似手机浏览器拦截 `display:none` 文件输入框触发的问题。
- 修改 H5 入口页 `frontend_h5/dist/index.html`，增加针对 `.hidden-input[type="file"]` 的覆盖样式，将文件输入框改为视觉隐藏但保留在页面中，提升部分手机浏览器对拍照/相册选择的兼容性。

## 修改后的代码

```html
<style>
  .hidden-input[type="file"] {
    position: fixed !important;
    left: -9999px !important;
    top: 0 !important;
    width: 1px !important;
    height: 1px !important;
    opacity: 0 !important;
    pointer-events: none !important;
  }
</style>
```

## 测试与验证

- 生产环境已备份原 H5 入口文件：`/opt/xiaohebao/backups/h5-index-file-input-20260525104603/index.html`。
- 已同步 `frontend_h5/dist/index.html` 到腾讯云 `/data/www/xhb.juxin.pro/dist/index.html`。
- 已执行 `nginx -t`，结果通过：`syntax is ok`、`test is successful`。
- 已通过线上地址 `https://xhb.juxin.pro/000004ab0585703e8766ac72` 拉取页面，确认返回内容包含 `.hidden-input[type="file"]` 和 `left: -9999px`。
- 未执行前端 Vitest：当前工作区 `frontend_h5/src` 与 `package.json` 被既有变更删除，仅保留 `dist` 构建产物，本次为生产静态入口最小兼容修复。

## 后续建议

- 让用户关闭小米浏览器当前页面后重新打开专属链接，再点击“立即申请”进入实名认证，优先选择“从相册选择”或使用微信/系统浏览器打开。
- 若仍无法弹出拍照/相册，下一步建议补充 H5 客户端埋点，记录进入 `/ocr`、点击拍照/相册、图片处理失败等前端事件，避免只能从后端请求倒推。

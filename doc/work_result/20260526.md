# 2026-05-26 21:00:30

## 修改说明

- 修复用户反馈的生产安卓 App 下载后只能看到登录页、登录后进不去并闪退的问题。
- 根因：本次 UI 升级给统计卡增加了渐变高光条和提示文案，但 `addSummaryStrip` 仍按旧结构使用 `card.getChildAt(1)` 获取统计数字 TextView；新结构中 `childAt(1)` 是间距 View，登录后进入工作台时发生类型转换崩溃。
- 修复：将统计数字 TextView 索引调整为新结构中的 `childAt(3)`。
- 已重新构建 debug 和 release APK，并覆盖腾讯云生产下载包。

## 修改后的代码

- 修改 `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`：修复统计卡数字 TextView 获取索引。

## 测试与验证

- 已执行 `android_native/build_apk.sh debug`，构建成功。
- 已执行 `android_native/build_apk.sh release`，构建成功。
- 构建过程中仅出现 Java 8 bootclasspath 和既有过时 API 警告。
- 已备份闪退版本 APK 到 `/opt/xiaohebao/backups/android-apk-ui-crash-20260526205902/xiaohebao.apk`。
- 已将修复后的正式签名 APK 上传覆盖 `/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 线上下载地址 `https://xhbadmin.juxin.pro/download/xiaohebao.apk` 返回 `HTTP 200`，响应类型为 `application/vnd.android.package-archive`。
- 线上 APK sha256 为 `7365ea6f439aa446153b49426d854c7befe70dbeca8b1a19558cf86b10f8999b`，与本地 release 包一致。

# 2026-05-26 20:48:24

## 修改说明

- 按用户要求回到安卓 App 生产更新任务，重新确认 `awesome` 指的是 GitHub 仓库 `sindresorhus/awesome`。
- 结合联网检索和 Stripe 官方 Android 外观能力，确认 `awesome` 本身不是可直接套用的 Stripe UI 组件库；本次以 Stripe 常见视觉语言落地：蓝紫青渐变、玻璃拟态、半透明卡片、明亮留白、圆角胶囊组件和柔和阴影。
- 针对原生 Android 管理端 `android_native` 做最小 UI 升级，不改业务接口、不改登录/审批/发卡/回款/平账逻辑。
- 已构建正式签名 APK，并替换腾讯云生产下载包。

## 修改后的代码

- 修改 `android_native/app/src/main/java/com/juxin/xiaohebao/admin/Ui.java`：扩展 Stripe 风格色板，补充紫色、天蓝、深灰、半透明玻璃色。
- 修改 `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`：登录页增加玻璃标签，工作台 Header 增加 Stripe 副标题，统计卡增加渐变高光条和提示文案，列表卡增加渐变高光条，按钮/分段控件/背景统一强化为 Stripe glass 风格。
- 修改 `android_native/app/src/main/res/drawable/card_bg.xml`：卡片背景改为更明显的半透明玻璃效果。
- 修改 `android_native/app/src/main/res/drawable/input_bg.xml`：输入框改为半透明玻璃输入底。
- 修改 `android_native/app/src/main/res/values/styles.xml`：状态栏和导航栏色彩对齐新视觉。

## 测试与验证

- 已执行 `android_native/build_apk.sh debug`，构建成功，产物为 `android_native/build/xiaohebao-debug.apk`。
- 已执行正式签名构建：`android_native/build_apk.sh release`，构建成功，产物为 `android_native/build/xiaohebao-release.apk`。
- 构建过程中仅出现 Java 8 bootclasspath 和既有过时 API 警告，不影响本次 UI 验证。
- 已备份生产旧 APK 到 `/opt/xiaohebao/backups/android-apk-ui-20260526204608/xiaohebao.apk`。
- 已将正式签名 APK 上传覆盖 `/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 线上下载地址 `https://xhbadmin.juxin.pro/download/xiaohebao.apk` 返回 `HTTP 200`，响应类型为 `application/vnd.android.package-archive`。
- 线上 APK sha256 为 `4477465275d38a0dafc0727bbe4fb0c9e38d9a033ec16a4fcd0ad49905047a85`，与本地 release 包一致。

# 2026-05-26 20:25:58

## 修改说明

- 根据用户截图反馈，继续修复卡池管理页右侧操作列异常和“拷贝密码时间”展示问题。
- 根因判断：Element 表格内部维护列宽和列结构，外部动态新增真实列仍会破坏最右侧操作列布局。
- 调整方案：增强脚本不再新增任何真实表格列，只复用原有列展示新增信息。
- 将原“绑定订单”列复用为“领取人手机号”。
- 将原“备注”列复用为“发放/拷贝密码时间”，单元格内显示两行：发放时间、拷贝密码时间；无记录时显示 `--`。
- 增强脚本保留清理前一次动态插列残留的逻辑，避免用户页面缓存或热更新后残留旧列。
- 已发布腾讯云生产环境，增强脚本版本号更新为 `v=202605262023`。

## 修改后的代码

- 修改 `frontend_admin/dist/assets/ecard-pool-enhance.js`：去除真实插列逻辑，改为复用原列并清理旧动态列残留。
- 修改 `frontend_admin/dist/assets/ecard-pool-enhance.css`：调整复用列和按钮不换行样式。
- 修改 `frontend_admin/dist/index.html`：刷新增强脚本与样式版本参数。

## 测试与验证

- 本地执行 `node --check frontend_admin/dist/assets/ecard-pool-enhance.js`，通过。
- 云端执行 `node --check /data/www/xhbadmin.juxin.pro/dist/assets/ecard-pool-enhance.js`，通过。
- 线上首页已引用 `ecard-pool-enhance.js?v=202605262023` 和 `ecard-pool-enhance.css?v=202605262023`。
- 线上增强脚本可检索到“领取人手机号”“发放/拷贝密码时间”“拷贝密码：”。

# 2026-05-26 20:19:19

## 修改说明

- 按用户截图反馈修复卡池管理页右侧“编辑”列被挤压成竖排的问题。
- 将额外新增列从三列减少为两列：仅新增“领取人手机号”“发放时间”。
- 将原“备注”列复用为“拷贝密码时间”，无记录时显示 `--`，确保该字段在当前可视区域内显示出来。
- 增强样式增加表格按钮不换行规则，避免最右侧操作按钮被挤压折行。
- 已发布腾讯云生产环境，增强脚本版本号更新为 `v=202605262017`。

## 修改后的代码

- 修改 `frontend_admin/dist/assets/ecard-pool-enhance.js`：将“备注”列改为“拷贝密码时间”，并填充 `secret_copied_at`。
- 修改 `frontend_admin/dist/assets/ecard-pool-enhance.css`：增加拷贝时间列和操作按钮不换行样式。
- 修改 `frontend_admin/dist/index.html`：刷新增强脚本与样式版本参数。

## 测试与验证

- 本地执行 `node --check frontend_admin/dist/assets/ecard-pool-enhance.js`，通过。
- 云端执行 `node --check /data/www/xhbadmin.juxin.pro/dist/assets/ecard-pool-enhance.js`，通过。
- 线上首页已引用 `ecard-pool-enhance.js?v=202605262017` 和 `ecard-pool-enhance.css?v=202605262017`。
- 线上增强脚本可检索到“拷贝密码时间”，已无旧文案“用户拷贝时间”。
- 线上增强样式已包含 `el-button` 不换行规则。

# 2026-05-26 20:07:29

## 修改说明

- 按用户反馈调整卡池管理统计卡片与列表字段展示。
- 去掉原第一张“在库卡片”统计卡，改为四张卡片：累计发放、在库可用、今日入库、今日发卡。
- 将四张统计卡移动到搜索条件区域上方。
- 将新增列表列稳定插入到“备注”列之前，顺序为：领取人手机号、发放时间、拷贝密码时间、备注，避免字段挤到最右侧不可见。
- 后端补充统计口径：累计发放按 `assigned_at IS NOT NULL` 统计；在库可用按 `AVAILABLE` 且有效期满足发卡逻辑统计。
- 已同步腾讯云生产环境，重启后端服务，并更新增强脚本版本号为 `v=202605262004`。

## 修改后的代码

- 修改 `backend/app/services/admin_service.py`：新增累计发放和在库可用统计。
- 修改 `backend/app/schemas/loan.py`：新增统计响应字段。
- 修改 `backend/tests/test_ecard_pool_management_fields.py`：覆盖新增统计口径。
- 修改 `frontend_admin/dist/assets/ecard-pool-enhance.js`：调整统计卡内容、位置和新增列插入位置。
- 修改 `frontend_admin/dist/assets/ecard-pool-enhance.css`：统计卡改为四列布局。
- 修改 `frontend_admin/dist/index.html`：刷新增强脚本与样式版本参数。

## 测试与验证

- 本地执行 `cd backend && .venv/bin/python -m pytest tests/test_ecard_pool_management_fields.py tests/test_async_db_routes.py -q`，结果 `8 passed`。
- 本地执行 `node --check frontend_admin/dist/assets/ecard-pool-enhance.js`，通过。
- 云端执行 `py_compile app/services/admin_service.py app/schemas/loan.py`，通过。
- 云端执行 `node --check /data/www/xhbadmin.juxin.pro/dist/assets/ecard-pool-enhance.js`，通过。
- 云端 `xiaohebao-backend.service` 重启后状态为 `active`。
- 线上首页已引用 `ecard-pool-enhance.js?v=202605262004` 和 `ecard-pool-enhance.css?v=202605262004`。
- 云端服务层验证：新增统计字段均存在，当前样例值为累计发放 `46` 张/`38000.0`，在库可用 `34` 张/`27000.0`，今日入库 `30` 张/`25000.0`，今日发卡 `6` 张/`3000.0`。
- 云端服务层验证：已发放列表项包含 `recipient_phone`、`assigned_at`、`secret_copied_at` 三个字段。

# 2026-05-26 19:52:17

## 修改说明

- 根据生产卡池页再次空白的问题，先将生产前端恢复到稳定状态，再重新检查云端系统。
- 云端检查结果：后端服务 `active`，卡池接口服务层已经返回 `stats`、`recipient_phone`、`assigned_at`、`secret_copied_at`；已发放卡样本能查到领取人手机号和发放时间。
- 云端检查结果：`/opt/xiaohebao` 与本地 `frontend_admin` 均未发现完整 Web 管理端源码和构建配置，卡池页面只有 Vite 构建后的压缩产物；继续手改 Vue 压缩渲染函数风险高，已停止该路线。
- 改用独立增强脚本方案：不修改卡池 Vue chunk，不修改路由，不新建入口 chunk；仅在 `index.html` 中挂载普通 `defer` 脚本和普通 CSS，仅在 `/ecard-pool` 路由生效。
- 增强脚本复用当前后台登录态读取 `/api/admin/ecard-pool`，在筛选区下方补三张统计卡，并按当前表格可见 ID 给列表补“领取人手机号”“发放时间”“用户拷贝时间”三列。
- 增强脚本请求接口时使用 `cache: "no-store"`，避免读到旧接口结果。
- 已发布到腾讯云生产环境，并同步到 `/opt/xiaohebao/current/frontend_admin/dist`。

## 修改后的代码

- 新增 `frontend_admin/dist/assets/ecard-pool-enhance.js`：卡池页统计卡和字段列增强逻辑。
- 新增 `frontend_admin/dist/assets/ecard-pool-enhance.css`：统计卡和新增列样式。
- 修改 `frontend_admin/dist/index.html`：加载增强脚本与样式，版本参数为 `v=202605261950`。

## 测试与验证

- 本地执行 `cd backend && .venv/bin/python -m pytest tests/test_ecard_pool_management_fields.py tests/test_async_db_routes.py -q`，结果 `8 passed`。
- 本地执行 `node --check frontend_admin/dist/assets/ecard-pool-enhance.js`，通过。
- 云端执行 `node --check /data/www/xhbadmin.juxin.pro/dist/assets/ecard-pool-enhance.js`，通过。
- 线上 `https://xhbadmin.juxin.pro/` 已加载 `ecard-pool-enhance.js?v=202605261950` 和 `ecard-pool-enhance.css?v=202605261950`。
- 线上增强 JS 响应类型为 `application/javascript`，增强 CSS 响应类型为 `text/css`。
- 线上首页无旧错误版本参数 `202605261914`、`202605261927` 和 `index-XhbCards` 残留。
- 线上原卡池 Vue chunk 保持稳定版本，不含手工新增字段，新增展示由独立增强脚本负责。

# 2026-05-26 19:29:09

## 修改说明

- 在生产页面恢复稳定后，重新以最小改动补回 Web 管理端卡池展示，不再新建入口文件，不改其他页面。
- 仅修改稳定卡池 chunk：保存接口返回的 `stats`，在筛选区下方展示三张统计卡片，并在列表中增加“领取人手机号”“发放时间”“用户拷贝时间”三列。
- 因生产 `/assets/` 已启用长期缓存，稳定入口 `index-WVgexbX7.js` 仅增加卡池 JS/CSS 的版本参数，`index.html` 仅增加稳定入口版本参数，确保浏览器刷新后拿到新卡池资源。
- 已在云端替换 `EcardPool-CDlqwN21.js`、`EcardPool-D-FlL_KT.css`、`index-WVgexbX7.js`、`index.html`，并同步到 `/opt/xiaohebao/current/frontend_admin/dist`。
- 已重新生成线上 `.gz` 压缩副本，避免 Nginx 返回旧压缩内容。

## 修改后的代码

- 修改 `frontend_admin/dist/assets/EcardPool-CDlqwN21.js`：增加统计卡片渲染和三列展示。
- 修改 `frontend_admin/dist/assets/EcardPool-D-FlL_KT.css`：增加统计卡片样式。
- 修改 `frontend_admin/dist/assets/index-WVgexbX7.js`：仅为卡池 JS/CSS 资源增加 `v=202605261927`。
- 修改 `frontend_admin/dist/index.html`：仅为稳定入口增加 `v=202605261927`。

## 测试与验证

- 本地已执行 `node --check frontend_admin/dist/assets/EcardPool-CDlqwN21.js`，通过。
- 本地已执行 `node --check frontend_admin/dist/assets/index-WVgexbX7.js`，通过。
- 线上 `https://xhbadmin.juxin.pro/` 已引用 `index-WVgexbX7.js?v=202605261927`。
- 线上稳定入口已引用 `EcardPool-CDlqwN21.js?v=202605261927` 和 `EcardPool-D-FlL_KT.css?v=202605261927`。
- 线上卡池 JS 可检索到“领取人手机号”“用户拷贝时间”“在库卡片”。
- 线上压缩入口中无 `index-XhbCards` 残留，后端服务状态为 `active`。

# 2026-05-26 19:19:55

## 修改说明

- 根据生产环境卡池管理页面异常反馈，优先执行生产止血。
- 判断前端异常由手工修改 Vite 构建产物导致，存在破坏 Vue 运行时渲染结构的风险；因此撤回本次 Web 管理端卡池页构建产物展示改动。
- 保留后端已上线的数据能力：卡池接口仍返回统计数据、领取人手机号、发放时间、用户首次复制密码时间；H5 复制卡密事件记录逻辑仍保留。
- 生产 Web 管理端已恢复到改动前稳定入口和卡池静态资源，同时重新生成入口 `.gz` 压缩副本，避免 Nginx 命中旧压缩文件。
- 本地已同步撤回对应前端构建产物改动，并清理前端入口文件的错误暂存，避免后续误提交。

## 修改后的代码

- 恢复生产 `/data/www/xhbadmin.juxin.pro/dist/index.html`。
- 恢复生产 `/data/www/xhbadmin.juxin.pro/dist/assets/index-WVgexbX7.js`。
- 恢复生产 `/data/www/xhbadmin.juxin.pro/dist/assets/EcardPool-CDlqwN21.js`。
- 恢复生产 `/data/www/xhbadmin.juxin.pro/dist/assets/EcardPool-D-FlL_KT.css`。
- 同步恢复云端 `/opt/xiaohebao/current/frontend_admin/dist` 下对应文件。
- 同步恢复本地 `frontend_admin/dist` 下对应文件。

## 测试与验证

- 线上首页 `https://xhbadmin.juxin.pro/` 返回 `HTTP 200`。
- 线上首页入口已恢复引用 `index-WVgexbX7.js`，不再携带本次手工缓存参数。
- 线上卡池 chunk 中已无“领取人手机号”文本，确认前端展示改动已撤回。
- 线上 `index-WVgexbX7.js.gz` 中已无 `202605261914`、`index-XhbCards`、`领取人手机号` 残留。
- 本地 `git status` 已确认前端构建产物不再处于改动状态，剩余改动仅为后端能力、测试与本工作记录。
- 本地重新执行 `cd backend && .venv/bin/python -m pytest tests/test_ecard_pool_management_fields.py tests/test_async_db_routes.py -q`，结果 `8 passed`。

# 2026-05-26 19:02:39

## 修改说明

- 按用户要求补充本地与腾讯云 Web 管理端卡池管理信息展示。
- 后端 `/api/admin/ecard-pool` 新增列表字段：领取人手机号 `recipient_phone`、发放时间 `assigned_at`、用户首次拷贝密码时间 `secret_copied_at`。
- 后端同一接口新增统计卡片数据：在库卡片总数/总金额、当日入库数量/总金额、当日发卡数量/总金额。
- H5 复制 E 卡卡号/密码接口新增用户事件 `USER_ECARD_SECRET_COPIED`，后续以首次复制密码事件作为卡池列表的“拷贝时间”。历史访问审计未记录具体字段和卡池 ID，因此历史已复制数据无法反推到单卡。
- Web 管理端卡池页增加三张统计卡，并在列表中增加“领取人手机号”“发放时间”“拷贝时间”三列。
- 腾讯云端已同步相同后端代码和管理端静态文件。
- 19:06 发现新主入口 `index-XhbCards.js` 在生产管理端会导致非卡池页面前端运行时空白，已立即切回上一版稳定主入口 `index-WVgexbX7.js`，并清理未使用的新入口文件；卡池页改动保留在原卡池 chunk 中。
- 19:14 发现卡池页仍显示旧内容，原因是浏览器仍命中旧卡池 chunk 缓存；已在不更换主入口文件名的前提下，为 `index-WVgexbX7.js` 和卡池页动态资源增加 `v=202605261914` 版本参数。

## 修改后的代码

- 修改 `backend/app/services/admin_service.py`：补充卡池统计、领取人手机号映射、复制密码时间解析与接口返回。
- 修改 `backend/app/api/endpoints/loan.py`：H5 获取 E 卡卡密时记录复制事件。
- 修改 `backend/app/schemas/loan.py`：补充卡池响应字段和统计响应结构。
- 修改 `frontend_admin/dist/assets/EcardPool-CDlqwN21.js`、`frontend_admin/dist/assets/EcardPool-D-FlL_KT.css`：更新管理端卡池页面展示。
- 修改 `frontend_admin/dist/index.html`、`frontend_admin/dist/assets/index-WVgexbX7.js`：为卡池页资源添加版本参数，绕过浏览器旧缓存。
- 新增 `backend/tests/test_ecard_pool_management_fields.py`：覆盖新增字段序列化、复制事件详情、统计口径。

## 测试与验证

- 本地已执行 `python3 -m py_compile backend/app/services/admin_service.py backend/app/api/endpoints/loan.py backend/app/schemas/loan.py`，通过。
- 本地已执行 `node --check frontend_admin/dist/assets/EcardPool-CDlqwN21.js`、`node --check frontend_admin/dist/assets/EcardPool-Bv9qCards.js`、`node --check frontend_admin/dist/assets/index-XhbCards.js`，通过。
- 本地已执行 `cd backend && .venv/bin/python -m pytest tests/test_ecard_pool_management_fields.py tests/test_async_db_routes.py -q`，结果 `8 passed`。
- 本地尝试执行完整后端测试 `cd backend && .venv/bin/python -m pytest -q`，因当前 `.venv` 为 Python 3.9，项目既有 `str | None` 类型语法在收集 `tests/test_root_profile_endpoint.py` 时失败，未完成全量测试。
- 本地改用当前可启动项目的 Python 3.12 执行完整后端测试，结果 `87 passed, 11 failed`；失败集中在既有测试桩与当前代码不匹配、短信真实服务流控、日志捕获配置和事件循环复用问题，本次新增测试均已通过。
- 本地临时使用 Python 3.12 在 `18001` 端口启动后端并验证 `/api/admin/ecard-pool`，返回新增 `stats`、`recipient_phone`、`assigned_at`、`secret_copied_at` 字段。
- 云端已执行 `py_compile`，通过；已重启 `xiaohebao-backend.service`，状态 `active/running`。
- 云端接口验证：`/api/admin/ecard-pool?status=ALL` 返回统计数据；`status=ASSIGNED` 返回领取人手机号和发放时间。
- 云端静态资源验证：`https://xhbadmin.juxin.pro/` 已恢复引用 `index-WVgexbX7.js`；线上日志显示申请审批页 `/api/admin/loans?scope=REVIEWING...` 持续返回 `200`。
- 云端缓存刷新验证：`https://xhbadmin.juxin.pro/` 已引用 `index-WVgexbX7.js?v=202605261914`，主入口已引用 `EcardPool-CDlqwN21.js?v=202605261914`，该资源可检索到“领取人手机号”。

# 2026-05-26 18:27:47

## 修改说明

- 按用户要求升级安卓 App 端 UI，参考 Stripe 风格做移动管理端视觉改造。
- 本次针对原生 Android 管理端 `android_native` 实现，不改业务接口、不改业务状态流转、不触碰腾讯云生产环境。
- 视觉方向调整为：紫蓝渐变、柔和冷色背景、半透明玻璃拟态卡片、轻阴影、圆角输入框、渐变主按钮、胶囊式底部 Tab。
- 为兼容当前最低 Android 版本，毛玻璃采用半透明玻璃卡片 + 渐变背景 + 细描边的方式表达，不引入额外依赖和真实模糊渲染。

## 修改后的代码

- 修改 `android_native/app/src/main/java/com/juxin/xiaohebao/admin/Ui.java`：更新 Stripe 风格色板，增加紫蓝、青色、薄荷绿、玻璃背景等设计色。
- 修改 `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`：登录页、工作台、统计卡、列表卡、详情页、照片区、底部操作栏、底部 Tab、按钮和输入框统一切换为 Stripe / glass 风格。
- 修改 `android_native/app/src/main/res/drawable/card_bg.xml`：卡片改为半透明白色、圆角和浅色描边。
- 修改 `android_native/app/src/main/res/drawable/input_bg.xml`：输入框改为半透明白色、圆角和柔和描边。
- 修改 `android_native/app/src/main/res/drawable/primary_button.xml`：主按钮改为紫蓝青渐变。
- 修改 `android_native/app/src/main/res/values/styles.xml`：状态栏、导航栏和 accent 色对齐新视觉风格。

## 测试与验证

- 已执行 `android_native/build_apk.sh debug`，构建成功，产物为 `android_native/build/xiaohebao-debug.apk`。
- 构建过程中仅出现 Java 8 bootclasspath 和既有过时 API 警告，不影响本次 UI 改造验证。
- 未新增原生 Android 单元测试：当前原生 Android 工程无独立测试框架，本次为纯 UI 样式改造，已通过 debug APK 编译验证。

# 2026-05-26 10:40:46

## 修改说明

- 排查后台用户反馈 `xhbadmin.juxin.pro` 访问非常慢的问题。
- 服务器资源检查正常：CPU 负载低、磁盘空间充足，`nginx` 与 `xiaohebao-backend.service` 均为 `active`。
- 外网测速发现管理端公共静态资源传输慢，其中 `assets/es-B-O9bK2G.js` 未压缩时约 1.1MB，弱网情况下下载可明显拖慢首屏。
- 确认生产 Nginx 对 `xhbadmin.juxin.pro` 的 `location /` 给所有静态资源设置了 `no-cache`，且没有单独 `/assets/` 规则，导致哈希静态资源不能长期缓存，也未使用已有 `.gz` 预压缩文件。
- 按最小改动修改腾讯云生产 Nginx：仅新增 `location ^~ /assets/`，启用 `gzip_static on`，并将哈希资源缓存策略设置为 `public, max-age=31536000, immutable`；`index.html` 继续保持禁缓存，避免前端入口更新不生效。

## 修改后的配置

生产配置文件：`/etc/nginx/conf.d/xhbadmin.juxin.pro.conf`

```nginx
location ^~ /assets/ {
    gzip_static on;
    add_header Cache-Control "public, max-age=31536000, immutable" always;
    try_files $uri =404;
}
```

生产备份目录：`/opt/xiaohebao/backups/xhbadmin-nginx-assets-cache-20260526103231`

## 测试与验证

- 已执行 `nginx -t`，结果通过：`syntax is ok`、`test is successful`。
- 已执行 `nginx -s reload`，生产 Nginx 已重新加载配置。
- 线上响应头验证：
  - `https://xhbadmin.juxin.pro/assets/es-B-O9bK2G.js` 返回 `Content-Encoding: gzip`，下载大小从约 `1095831` 字节降为约 `339500` 字节。
  - `https://xhbadmin.juxin.pro/assets/index-4obcwz8x.css` 返回 `Content-Encoding: gzip`，下载大小从约 `353852` 字节降为约 `47506` 字节。
  - 静态资源返回 `Cache-Control: public, max-age=31536000, immutable`。
  - 首页 `index.html` 仍返回 `Cache-Control: no-cache, no-store, must-revalidate`。
  - `/api/admin/me` 未登录返回 `401`，接口链路正常。
- 服务器本机通过 Nginx 访问静态资源耗时约 `0.005s`，说明 Nginx、磁盘和后端服务本身不慢。
- 外网到腾讯云的下载耗时仍存在较大波动，说明除原静态资源策略问题外，还可能存在公网链路或带宽波动；本次已完成不改业务代码的首屏静态资源优化。

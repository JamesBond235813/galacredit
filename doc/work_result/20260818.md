# 2026-08-18 23:04:31

## 本轮紧急联系人页面调整

- 将 H5 借款申请第 3 步的紧急联系人页面做了瘦身，抬高了标题和联系人卡片字号，减少页面拥挤感。
- 把每个联系人卡片改成三个固定展示框：
  - `Relatives`：下拉选择关系
  - `PhoneNumber`：通过通讯录选择回填，只展示 9 位本地号码
  - `Full Name`：通过通讯录选择回填，禁止手动输入
- 联系人手机号继续沿用系统内部的 Ghana 国际号码规范，页面仅做本地 9 位展示，不改接口提交格式。
- 删除了原来的“单独联系人选择说明卡”和底部提示文案，让页面更聚焦。
- 保持当前联系方式仍必须从通讯录选择，避免手工录入导致信息不可核验。

## 修改文件

- `frontend_h5/src/views/ApplicationForm.vue`
- `frontend_h5/src/utils/contactPicker.js`
- `frontend_h5/src/utils/contactPicker.test.js`

## 测试与验证

- `cd frontend_h5 && npm run test`：通过，13 个测试文件、31 个测试全部通过。
- `cd frontend_h5 && npm run build`：通过，Vite 生产构建成功。
- 本地服务状态检查：
  - `http://127.0.0.1:8001`：后端监听中
  - `http://127.0.0.1:2001/application`：H5 页面可访问，返回 `200 OK`

## 备注

- 本轮未提交 commit。
- 仓库内存在其他已有改动，本轮未触碰无关文件。

# 2026-08-18 20:33:44

## H5 首页截图文案与布局调整

- 首页品牌区移除 `Credit Service`，改为信用卡线条 logo + `GalaCredit` 横向组合。
- 移除现金贷 hero 卡片顶部的三个标签：`Ghana cash loan`、`MoMo disbursement`、`Flexible repayment`。
- 移除授信额度下方的申请提示文案。
- 将 hero 卡片底部说明改为两行左右对齐内容：
  - `0.05%` / `180days`
  - `Min daily interest rate` / `Max loan period.`
- 将 `Customer Support` 与 `My Applications` 两个服务入口从左右并排改为上下排列，保留原有点击跳转。
- 仅修改首页展示层和样式，未改变登录、授信、放款或还款业务逻辑。

## 验证结果

- `cd frontend_h5 && npm run test`：通过，11 个测试文件、25 个测试全部通过。
- `cd frontend_h5 && npm run build`：通过，Vite 生产构建成功。
- 已按 `390 x 844` 移动端尺寸检查首页布局代码和登录页入口；当前浏览器会因未登录跳转至登录页，未绕过登录状态执行首页截图。
- 本轮未提交 commit；修改文件已执行 `git add`。

# 2026-08-18 20:25:03

## 品牌替换收尾与服务重启

- 清理了后端剩余的旧风险报告函数命名和文档注释，统一使用 GalaCredit 语义；历史报告类型值 `XIAOHEBAO_RISK` 仍保留作为兼容标识。
- 检查数据库中的合同签名和复合风险报告数据：当前没有包含旧品牌名称的记录，因此无需执行数据迁移。
- 重启本地后端服务使合同和风险报告新文案生效，服务继续监听 `8001`。
- 使用 `xiaojiang` 管理员账号验证 `/api/admin/login` 返回 HTTP 200。

## 最终验证

- 管理后台测试：28 个测试通过。
- 管理后台构建：通过。
- 后端风险报告与现金贷测试：11 个测试通过。
- Android Web 工作台：安装依赖后构建通过。
- 未构建 Android 原生 APK 和 iOS 原生 App；仅更新用户可见品牌名称。

# 2026-08-18 20:18:26

## GalaCredit 品牌名称统一

- 将管理后台登录页、渠道二维码、用户合同标题、风险报告标题和空状态文案更新为 GalaCredit。
- 将后端新生成的贷款合同标题、风险报告标题、审核事件标题和接口文案更新为 GalaCredit。
- 将 Android Web 工作台、Android 原生工作台、下载页和 iOS 工作台的用户可见品牌更新为 GalaCredit。
- 将 README 和当前产品说明文档的产品名称更新为 GalaCredit，并将产品定位描述调整为面向 Ghana 市场的现金贷服务。
- 保留数据库名 `xiaohebao`、历史风险报告类型值 `XIAOHEBAO_RISK`、旧 APK 文件名、Android/iOS 包名、旧渠道签名默认值和部署目录等技术兼容标识，避免影响历史数据、已安装客户端和已有链接。

## 验证结果

- 管理后台 Vitest：12 个测试文件、28 个测试全部通过。
- 管理后台 Vite 构建：通过。
- 后端风险报告与现金贷测试：11 个测试全部通过。
- Android Web 工作台首次构建因未安装依赖出现 `vite: command not found`；执行 `pnpm install --frozen-lockfile` 后重新构建成功。
- 未执行 Android 原生 APK 和 iOS 原生 App 构建，本轮只更新了用户可见品牌文本。
- 本轮未提交 commit；修改内容已准备执行 `git add`。

# 2026-08-18 20:08:41

## 管理员账号密码更新

- 将管理后台超级管理员账号 `xiaojiang` 的密码更新为用户指定的新密码。
- 保留账号原有 `ADMIN` 角色和全部后台权限；未删除或修改其他管理员账号。
- 通过数据库中的密码哈希方式更新，没有保存明文密码到项目文件或配置文件。

## 验证结果

- `xiaojiang` 使用新密码调用 `/api/admin/login`：HTTP 200，登录成功。
- `xiaojiang` 使用旧密码 `admin123` 调用登录接口：HTTP 400，旧密码已失效。
- 数据库密码哈希校验通过。
- 运行过程中出现 aiomysql 在 Python 事件循环关闭时的清理警告，但不影响密码更新、数据库提交或登录验证结果。

## 当前登录信息

- 管理后台地址：`http://127.0.0.1:2002`
- 管理员账号：`xiaojiang`
- 新密码：用户本轮指定的密码

# 2026-08-18 20:05:12

## 本轮管理后台升级

- 将管理后台核心业务从 E-card 语义平移到 Ghana cash-loan 语义：名义本金、上扣费用、MoMo 实际到账、应还总额、分期和逾期费用。
- 保留历史 E-card / 旅游权益数据、表结构和页面入口，统一标记为 Legacy compatibility，不删除历史数据，也不参与新的 MoMo 放款主流程。
- 放款确认页改为 MoMo disbursement：确认金额校验改为名义本金、实际到账金额和还款总额，去除主流程中的京东 E 卡发放确认文案。
- 产品页面支持现金贷产品参数展示和编辑：上扣费率、审核费、风控费、系统费、利息、起息日、到期日、分期期数、每期比例和每日逾期费。
- 用户新增表单改为 Ghana 手机号格式：固定 `+233`，输入 9 位本地号码后以 `233` 国际格式提交。
- 管理后台新增 `中文 / English` 全局切换，默认中文；语言选择写入 `galacredit_admin_locale`，刷新页面后保持。Element Plus 控件、菜单、页面标题、产品页、放款页、状态、日期和金额展示已接入切换。
- 管理后台金额统一使用 `GHS`，手机号支持 `+233 24 0000001` 展示格式。

## 验证结果

- `cd frontend_admin && npm run test`：通过，12 个测试文件、28 个测试全部通过。
- `cd frontend_admin && npm run build`：通过，Vite 生产构建成功；仅保留既有大包体积提示。
- `cd backend && .venv/bin/python -m pytest tests/test_cash_loan_amounts.py tests/test_ghana_auth_phone_schema.py -q`：通过，10 个测试全部通过。
- 服务状态已核实：后端 `8001`、H5 `2001`、管理后台 `2002` 均处于监听状态。
- 浏览器实际验证：使用 `admin / admin123` 登录管理后台，中文切换为 English 后菜单、标题、产品页、放款页汇总卡片和放款列表主要表头均更新；刷新后 English 仍保持。

## 交付注意事项

- 当前 MoMo 使用可替换的 `MockMomoProvider`，真实支付公司接入时只需替换 provider 实现并保留放款服务接口。
- 个别非核心历史统计说明和旧数据中的中文产品名称仍按原始数据保留；历史 E-card 页面仅作为兼容查询入口。
- 本轮没有执行 git commit；新增测试、双语模块和本轮修改文件已准备执行 `git add`，供人工 Review。

# 2026-08-18 19:32:52

## 本轮修正

- 根据截图修正手机号输入区域的视觉错位。
- 将 Ghana 国旗与 `+233` 左侧前缀垂直居中，使其与手机号数字处于同一条水平输入线。
- 取消“输入后隐藏前面灰色零、导致后续零向右移动”的实现方式。
- 改为 9 个固定字符位置：输入的真实数字直接覆盖对应位置的灰色零，未输入的位置继续显示灰色零。
- 保留 `x/9` 计数器和手机号输入末尾的真实光标。

## 视觉验证

- 在 `390 x 844` 手机视口下实际验证。
- 输入 `240` 后显示连续的九位位置：前三位为真实数字，后六位为灰色零。
- 计数显示为 `3/9`。
- 国旗、`+233` 与号码输入垂直位置已对齐；输入光标位于第三位数字之后。

## 测试结果

- `cd frontend_h5 && npm run test`：通过，11 个测试文件、25 个测试全部通过。
- `cd frontend_h5 && npm run build`：通过，Vite 生产构建成功。
- `cd backend && .venv/bin/python -m pytest tests/test_ghana_auth_phone_schema.py -q`：通过，7 个测试全部通过。
- 中文文案扫描：无匹配结果。
- `git diff --check`：通过。

# 2026-08-18 19:19:42

## 修改说明

- 将 H5 登录页手机号输入改为 Ghana 本地号码形式。
- 手机号输入框左侧固定显示 Ghana 国旗小图标与国家区号 `+233`。
- 本地号码输入区限制为 9 位，初始显示 9 个浅灰色 `0`。
- 每输入一位数字，对应位置的灰色 `0` 被真实数字覆盖，剩余位置继续显示灰色 `0`。
- 输入框右侧新增实时计数器 `x/9`，从 `0/9` 递增到 `9/9`。
- H5 向认证接口发送时自动将 9 位本地号码转换为 `233 + 9 位号码` 的国际格式。
- 后端认证请求新增 Ghana 国际号码校验，同时继续兼容历史 11 位测试数据。

## 修改后的代码

- `frontend_h5/src/views/Login.vue`：新增国旗、`+233`、逐位灰色零、输入计数器及对应布局样式。
- `frontend_h5/src/utils/passwordAuth.js`：手机号规范调整为 9 位 Ghana 本地号码，并新增国际号码转换函数。
- `frontend_h5/src/utils/passwordAuth.test.js`：覆盖 9 位规范化、校验和 `+233` 拼接。
- `backend/app/schemas/user.py`：认证相关请求接受 `233` 开头的 12 位 Ghana 国际号码，并兼容历史 11 位号码。
- `backend/tests/test_ghana_auth_phone_schema.py`：新增 Ghana 认证号码 schema 单元测试。

## 测试执行结果

- `cd frontend_h5 && npm run test`：通过，11 个测试文件、25 个测试全部通过。
- `cd frontend_h5 && npm run build`：通过，Vite 生产构建成功。
- `cd backend && .venv/bin/python -m pytest tests/test_ghana_auth_phone_schema.py -q`：通过，7 个测试全部通过。
- Ghana 完整登录接口验证：`233240000001` 已成功完成滑块创建、滑块验证、验证码发送、短信登录和用户信息读取。
- 后端已按项目启动方式重新启动，监听 `8001`；H5 继续监听 `2001`。
- 浏览器以 `390 x 844` 手机视口检查：初始显示国旗、`+233`、9 个灰色零和 `0/9`；输入 `24012` 后显示 5 位真实数字、4 位灰色零和 `5/9`，光标位于已输入数字末尾。
- H5 与后端 HTTP 检查均返回 `200`。

## 已知测试现状

- 既有 `test_auth_sms_login.py` 中两个用例仍因历史测试使用长度不符合当前规则的邀请码而返回 `422`，与本轮 Ghana 手机号修改无关。
- 既有 `test_auth_sms_send_code.py` 的转发 IP 用例仍存在跨 event loop 复用异步数据库连接的问题，与本轮修改无关。

# 2026-08-18 19:07:01

## 修改说明

- 通过当前开发环境的真实认证接口创建 H5 测试账号，而不是直接绕过认证逻辑写入一条不可登录的用户记录。
- 使用滑块验证码、发送验证码、短信验证码登录完整链路完成账号初始化。
- 测试账号手机号：`02400000001`
- 开发环境固定验证码：`635147`

## 测试账号初始状态

- 用户 ID：`1`
- 授信额度：`GHS 0`
- 实名状态：`UNVERIFIED`
- 人脸认证状态：`PENDING`
- 黑名单命中：否
- 风险名单命中：否
- 当前没有历史借款订单

## 测试执行结果

- `POST /api/auth/slider-captcha/create`：成功。
- `POST /api/auth/slider-captcha/verify`：成功取得验证码票据。
- `POST /api/auth/send-code`：成功，返回 60 秒冷却时间。
- `POST /api/auth/sms-login`：成功返回 access token 和 refresh token。
- `GET /api/user/info`：成功返回测试账号信息。

## 使用方式

- 打开 H5：`http://127.0.0.1:2001/login`
- 手机号输入：`02400000001`
- 点击 `Send code`，完成滑块验证。
- 验证码输入：`635147`
- 点击 `Sign In`。

## 注意事项

- 该验证码仅适用于当前本地开发环境，生产环境不会使用固定验证码。
- 账号目前为干净新用户，登录后授信额度为 0，需要继续完成资料、身份核验和授信申请流程才能查看后续业务状态。

# 2026-08-18 18:40:41

## 修改说明

- 将 H5 借款用户端的可见文案统一调整为英文，面向加纳用户使用。
- 将主要金额展示从人民币符号/中文格式调整为 `GHS` 与 `en-GH` 格式。
- 更新登录、首页、个人中心、借款申请、OCR、活体/人脸校验、订单、账单、客服、关于我们、协议及授权页面的英文文案。
- 更新请求拦截提示、短信验证码按钮、订单短信及旧 E-card 展示工具中的用户可见文本。
- 新增英文版用户协议与个人信息授权草案：
  - `frontend_h5/public/user-agreement.txt`
  - `frontend_h5/public/personal-info-authorization.txt`
- 增加 H5 英文内容扫描测试，防止 `src` 与 `public` 中重新出现中文可见文案。

## 设计与业务说明

- H5 品牌继续使用 GalaCredit 及当前橙色信用卡抽象线条 Logo。
- 手机登录继续采用手机号 + 6 位验证码的单一登录流程，页面文案使用 Ghana mobile number 等英文表达。
- 货币统一使用 Ghanaian cedi 的 `GHS` 展示；后台与接口字段仍保持现有业务兼容性。
- 协议内容属于面向加纳市场的运营草案，正式上线前仍需由当地法律、隐私和信贷合规顾问审核。

## 测试代码

- 新增 `frontend_h5/src/config/englishContent.test.js`：递归扫描 H5 `src` 与 `public` 中的 `.vue`、`.js`、`.txt` 文件，断言不包含中文字符。
- 保留并执行已有 H5 工具函数测试，覆盖请求、短信登录、订单短信、E-card 展示、OCR 图片压缩、WebSocket 等逻辑。

## 测试执行结果

- `cd frontend_h5 && npm run test`：通过，11 个测试文件、24 个测试全部通过。
- `cd frontend_h5 && npm run build`：通过，Vite 生产构建成功。
- 中文文案扫描：`rg -n "[\\p{Han}]" frontend_h5/src frontend_h5/public --glob '!dist/**' --glob '!*.svg'` 无匹配结果。
- 浏览器实际检查 `http://127.0.0.1:2001/login`：页面正常打开，DOM 可见内容为英文，包括手机号输入、6 位验证码输入、Send code、Sign In、GalaCredit 和品牌副标题。
- 本地服务监听检查：后端 `8001`、H5 `2001`、管理端 `2002` 均正常监听。
- H5 首页 HTTP 验证：`http://127.0.0.1:2001/login` 返回 `200 text/html`。

## 当前结果与后续注意事项

- H5 用户端已达到全英文交付状态，适合作为加纳市场的英文交互基础版本。
- 管理端仍保持中文，未在本轮修改。
- 后端及数据库中的历史 E-card 结构和兼容流程仍保留，后续可继续按现金贷映射方案逐步收敛。
- MoMo 当前仍是本地 Mock provider，正式接入第三方支付公司时需要补充真实签名、回调验签、幂等、超时重试和对账流程。
- 当前协议文本不能视为最终法律文件，必须完成 Ghana 当地法律和数据保护合规审查后才能用于生产。
# 2026-08-18 21:03:00

## Ghana Card 身份核验页面本地化与识别服务预留

### 本轮完成内容

- 将 H5 `/ocr` 页面从双面中国身份证上传流程调整为 Ghana Card 本地流程：
  - 标题改为 `ID Verification`。
  - 页面主体改为 `Front of your` + `Ghana Card`，参考加纳贷款类 App 的单一正面上传路径。
  - 使用 GalaCredit 当前蓝色主色、浅灰背景和圆形上传按钮，不直接复制参考 App 的红色品牌色。
  - 增加 Ghana Card 示意卡片、取景框、Upload/Change 操作、Note 提示和拒绝图片示例区域。
  - Ghana Card 正面为必传；底层接口仍兼容可选 `back_image`，便于以后第三方接口要求双面时扩展。
  - 识别结果弹窗将证件字段显示为 `Ghana Card No.`。

- 新增 `backend/app/services/ghana_identity.py`：
  - 提供 `GhanaCardIdentityProvider`，统一承接 Ghana Card OCR 和人脸比对。
  - `GHANA_IDENTITY_MOCK_ENABLED=true` 时返回 Ghana 语义的开发数据：Ama Mensah、GHA 卡号、Accra 地址和 Ghana 日期格式。
  - `GHANA_IDENTITY_ENABLED=true` 时暂时明确返回“provider reserved but not configured”，不会误调用 e签宝，也不会伪造第三方协议。
  - 保留已有 e签宝实现作为 legacy 兼容路径，仅在 Ghana provider 未启用时才可能使用。

- 后端 `/api/user/ocr` 和 `/api/user/face-auth` 已优先接入 Ghana provider，并更新事件日志为 Ghana Card 语义；原有用户字段继续复用，避免本轮扩大数据库改造范围。
- 新增配置项：`GHANA_IDENTITY_ENABLED`、`GHANA_IDENTITY_MOCK_ENABLED`、`GHANA_IDENTITY_API_URL`、`GHANA_IDENTITY_API_KEY`、超时和人脸阈值配置。
- 新增后端 provider 单元测试和 H5 OCR 页面源代码测试。

### 验证结果

- 后端定向测试：13 passed。
- H5 Vitest：12 个测试文件、27 个测试全部通过。
- H5 production build：通过。
- Python compileall：通过。
- 后端已重启，`http://127.0.0.1:8001/` 返回 HTTP 200。
- H5 `http://127.0.0.1:2001/` 返回 HTTP 200。
- 说明：本项目主动关闭了 FastAPI `/docs`、`/openapi.json`，访问 404 属于既有配置，不是本轮故障。

### 需要后续记录

- 当前 Ghana Card 真实 OCR、证件库比对和人脸服务尚未拿到供应商接口协议，因此生产模式仍需补充请求字段、签名、回调、超时重试、脱敏日志和供应商错误码映射。
- 当前本地 `.env` 使用 Ghana mock，不能代表真实身份核验结果；上线前必须关闭 mock 并完成第三方联调。
- 本轮没有删除 e签宝代码或数据库字段，便于历史数据和旧流程兼容。
# 2026-08-18 22:06:00

## 紧急联系人改为仅通讯录选择

### 本轮完成内容

- H5 `Additional Information` 页面已移除联系人姓名和手机号的手工输入框。
- 每位联系人现在只有一个 `Select from contacts` 入口，点击后调用设备 Contact Picker：
  - 自动读取姓名和手机号。
  - 姓名与手机号均为只读展示，不能键盘修改。
  - 支持 Ghana 本地号码 `024...` 和国际号码 `+233...`，统一提交为 `233XXXXXXXXX`。
  - 联系人取消选择、设备不支持通讯录 API、联系人缺少姓名或有效 Ghana 手机号时显示英文提示。
- 关系字段仍然使用原有下拉选择。
- 提交请求为每位联系人增加 `source: CONTACT_PICKER`。
- 后端 `EmergencyContactRequest` 强制要求 `source=CONTACT_PICKER`，直接通过接口手工提交的联系人会被 schema 拒绝；保留旧 11 位号码格式用于历史数据兼容。
- 新增 `frontend_h5/src/utils/contactPicker.js` 及单元测试。
- 新增 `backend/tests/test_ghana_emergency_contact_schema.py`，覆盖国际号码、本地历史号码、非法号码和手工来源拒绝。

### 验证结果

- H5 Vitest：13 个测试文件、30 个测试全部通过。
- H5 production build：通过。
- 后端定向测试：19 passed。
- Python compileall 和 `git diff --check`：通过。
- H5 `http://127.0.0.1:2001/`：HTTP 200，开发服务保持运行。

### 兼容与限制

- Contact Picker API 依赖设备/浏览器支持，通常需要移动端安全上下文；不支持时页面不会开放手工输入，只会提示使用支持通讯录能力的 GalaCredit App 或移动浏览器。
- 服务器只信任 `CONTACT_PICKER` 来源标记，无法替代原生系统层面的通讯录权限审计；正式 App 仍需在 Android/iOS 原生层补充联系人权限声明、拒绝权限处理和隐私授权记录。
# 2026-08-18 22:18:00

## 紧急联系人家庭/社会关系分类与已选信息展示

### 本轮完成内容

- 第一位联系人固定为 `Emergency contact 1 (family)`，关系下拉选项严格限定为：
  - `Parents`
  - `Brothers or sisters`
  - `Grandparents`
  - `Couple`
  - `Children`
- 第二位联系人固定为 `Emergency contact 2 (friend)`，关系下拉选项严格限定为：
  - `Friends`
  - `Classmates`
  - `Colleagues`
- 联系人仍然只能通过通讯录选择；选中后新增独立的只读信息展示：
  - `Phone Number`
  - `Full Name`
  - 同时保留 `Change selected contact`，便于发现错误后重新选择。
- 后端申请 schema 新增联系人 `category` 字段，并强制校验：第一位必须是 `FAMILY` 及家庭关系，第二位必须是 `SOCIAL` 及朋友/同学/同事关系。
- 页面载入历史联系人时，如果旧关系不属于新分组，会清空关系并要求重新选择，避免错误关系被继续提交。

### 验证结果

- H5 Vitest：13 个测试文件、30 个测试全部通过。
- H5 production build：通过。
- 后端定向测试：21 passed。
- Python compileall 和 `git diff --check`：通过。
- 后端重启后根接口返回 HTTP 200；H5 `2001` 返回 HTTP 200。
# 2026-08-18 22:25:00

## 手机局域网登录测试说明

- 当前电脑局域网 IP：`192.168.1.132`。
- H5 局域网访问地址：`http://192.168.1.132:2001/login`。
- H5 服务监听 `0.0.0.0:2001`，API 通过 H5 同源代理转发到后端 `8001`，手机不需要直接访问后端端口。
- 开发测试账号对应的 Ghana 标准输入：本地 9 位号码 `240000001`，页面自动拼接 `+233`。
- 开发环境固定验证码：`635147`。
- 电脑本机和局域网访问检查均返回 HTTP 200；手机端若无法打开，优先检查是否连接到同一个 `192.168.1.x` Wi-Fi 以及 macOS 防火墙是否允许 Node 接收局域网连接。
# 2026-08-18 22:36:00

## 手机局域网 HTTPS 本地测试入口

### 问题

- 手机通过 `http://192.168.1.132:2001` 可以打开 H5，但浏览器禁止非 HTTPS 页面调用定位权限，因此出现 `Location Permission Required`。

### 本轮处理

- 生成 GalaCredit 本地开发 CA 和局域网服务器证书，服务器证书包含 `192.168.1.132`、`127.0.0.1` 和 `localhost`。
- 启动 HTTPS H5 服务：`https://192.168.1.132:2443`。
- HTTPS 入口继续使用 H5 `/api` 同源代理连接本地后端 `http://127.0.0.1:8001`，手机不需要直连后端端口。
- 提供 CA 下载文件：`http://192.168.1.132:2001/galacredit-local-ca.crt`。

### 验证结果

- `https://192.168.1.132:2443/login`：HTTP 200。
- HTTPS H5 根页面：HTTP 200。
- HTTPS H5 `/api` 代理可达；未授权接口返回业务预期状态。
- 本地 CA 可下载并通过 OpenSSL 校验。

### 手机操作

1. 手机连接与电脑相同 Wi-Fi。
2. 用手机浏览器打开 `http://192.168.1.132:2001/galacredit-local-ca.crt`，下载证书。
3. 在 Android 设置中进入“安全与隐私 / 更多安全设置 / 从设备存储安装证书 / CA 证书”，选择下载的 `galacredit-local-ca.crt` 并完成系统验证。
4. 重新打开 Chrome，访问 `https://192.168.1.132:2443/login`。
5. 允许 Chrome 和系统的定位权限，然后使用测试账号登录。

### 注意事项

- 证书仅用于当前局域网开发测试，不可用于生产环境；服务器证书有效期约 90 天，本地 CA 有效期约 1 年。
- 测试结束后，可以在手机“用户安装的 CA 证书”中删除 `GalaCredit Local Development CA`。
# 2026-08-18 22:49:00

## 通讯录 9 位 Ghana 手机号识别修复

### 问题原因

- 手机通讯录返回测试号码 `241234567` 和 `591234567`，这是不带本地前导 `0`、也不带国际区号 `233` 的 9 位 Ghana 手机号。
- 原前端仅接受 `024...` 形式的 10 位本地号码或 `+233...` 国际号码，因此误报 `Select a contact with a name and a valid Ghana mobile number.`。

### 修复内容

- 联系人号码规范化新增 9 位本地号码支持：
  - `241234567` 转换为 `233241234567`，展示为 `+233 24 123 4567`。
  - `591234567` 转换为 `233591234567`，展示为 `+233 59 123 4567`。
- 原有 `024...`、`+233...`、带空格或横线的通讯录号码格式继续支持。
- 新增两个用户实际测试号码的单元测试，防止回归。

### 验证结果

- H5 Vitest：13 个测试文件、30 个测试全部通过。
- H5 production build：通过。
- HTTPS H5 服务重启后 `https://192.168.1.132:2443/application-form` 返回 HTTP 200。

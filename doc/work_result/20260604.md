# 2026-06-04 18:33:42

## 修改说明

- 优化 Web 管理端“申请审批”的审核转单入口，让审核角色更直观看到并使用“转给我”。
- 原有入口只在详情抽屉内出现，且生产前端包未发布到包含该入口的版本，导致审核账号在页面上看不到“转给我”。
- 最小改动调整：状态筛选中为有权限的审核角色增加“可转入”选项；列表操作列对符合条件的审核中申请直接显示“转给我”；点击后先弹出确认框，确认后才执行转入当前账号。
- 保持权限边界不变：仅非超级管理员、审核角色、拥有 `loan-review-takeover` 权限、且订单状态为 `REVIEWING`、当前审核员不是本人的申请，才显示并允许转入。
- 本次仅替换 Web 管理端静态资源，未修改后端业务逻辑、未重启后端、未改数据库。

## 修改后的代码

- `frontend_admin/src/views/Applications.vue`
  - 状态下拉增加审核角色专用“可转入”。
  - 操作列增加“转给我”按钮和确认弹窗。
  - 详情抽屉内“转给我”复用同一确认与转入逻辑。
- `frontend_admin/src/utils/applicationsFilters.js`
  - 将前端虚拟状态 `TAKEOVER` 映射为后端 `status=REVIEWING&takeover_pool=true`。
- `frontend_admin/src/utils/applicationsFilters.test.js`
  - 增加可转入状态和无权限场景的查询参数测试。
- `frontend_admin/dist`
  - 已重新构建并发布 Web 管理端生产静态资源。

## 单元测试代码

- `frontend_admin/src/utils/applicationsFilters.test.js`
  - 覆盖“可转入”状态会映射为审核中转入池。
  - 覆盖无转入权限时不会携带 `takeover_pool`。

## 集成测试代码

- 本次未新增独立集成测试文件；通过管理端完整构建、线上静态资源内容和线上 API 鉴权验证覆盖发布链路。

## 测试执行结果

- `frontend_admin`: `pnpm test -- applicationsFilters.test.js`，10 files / 23 tests passed。
- `frontend_admin`: `pnpm build`，构建成功；存在第三方 `@vueuse/core` pure annotation warning，不影响构建产物。
- 生产管理端备份：`/opt/xiaohebao/backups/admin-review-takeover-visible-20260604183245`。
- 生产管理端：`https://xhbadmin.juxin.pro/` 已引用新主包 `index-CcFatljk.js`。
- 生产申请审批资源：`https://xhbadmin.juxin.pro/assets/Applications-DTxg6Xbj.js` 已确认包含“可转入”“转给我”“确认转入”和 `takeover_pool`。
- 生产 API 鉴权：`https://xhbadmin.juxin.pro/api/admin/stats` 未登录返回 `401 Unauthorized`。
- 安卓下载入口保留：`https://xhbadmin.juxin.pro/download/xiaohebao.apk` 返回 `200 OK`。

# 2026-06-04 17:05:20

## 修改说明

- 继续排查渠道扫码跳转登录后 `/login` 白屏的问题。
- 线上浏览器控制台报错为 `Missing required env: VITE_API_BASE_URL`，说明生产构建环境未注入 H5 API 基础地址，前端模块初始化时直接抛错，导致登录页白屏。
- 最小改动修复：H5 请求模块为生产同源部署增加默认值，`VITE_API_BASE_URL` 缺失时默认使用 `/api`，`VITE_CLIENT_ID` 缺失时默认使用 `h5-web`。
- 保持已有环境变量优先级不变；本地或其他环境显式配置后仍按配置值运行。

## 修改后的代码

- `frontend_h5/src/api/request.js`
  - `apiBaseUrl` 改为 `import.meta.env.VITE_API_BASE_URL || '/api'`。
  - `clientId` 改为 `import.meta.env.VITE_CLIENT_ID || 'h5-web'`。
- `frontend_h5/dist`
  - 已重新构建并发布 H5 生产静态资源。

## 单元测试代码

- 本次未新增独立测试；复用 H5 现有 `request.test.js`、`channel.test.js`、`ocrImageCompression.test.js` 覆盖请求配置、渠道码与 OCR 压缩相关逻辑。

## 集成测试代码

- 本次未新增独立集成测试文件；通过 H5 构建、线上登录页静态资源、线上 API 代理链路验证。

## 测试执行结果

- `frontend_h5`: `pnpm test -- request.test.js channel.test.js ocrImageCompression.test.js`，8 files / 18 tests passed。
- `frontend_h5`: `pnpm build`，构建成功。
- 生产 H5 备份：`/opt/xiaohebao/backups/h5-api-env-default-20260604170337`。
- 生产 H5：`https://xhb.juxin.pro/login` 返回新版入口，引用 `index-CCvbcyO8.js`。
- 生产 API 静态资源：`https://xhb.juxin.pro/assets/api-BCYrDDI0.js` 已确认不再包含 `Missing required env`，并包含默认 `/api` 与 `h5-web`。
- 生产渠道入口资源：`https://xhb.juxin.pro/assets/ChannelEntry-B7vbcY_U.js` 已确认包含 `window.location.replace('/login')`。
- 生产公网 API：`https://xhb.juxin.pro/api/user/info` 未登录返回 `401 Unauthorized`，说明 H5 到后端代理链路正常。

# 2026-06-04 16:53:21

## 修改说明

- 继续处理用户扫码后卡在“专属邀请通道：已识别专属邀请码，正在跳转登录...”的问题。
- 现象说明 24 位动态渠道码已经被前端识别，但 `ChannelEntry` 使用 `router.replace('/login')` 在微信内置浏览器中未完成组件切换，导致停留在中间页。
- 最小改动修复：渠道入口保存邀请码后，改用 `window.location.replace('/login')` 做整页跳转，绕开微信内置浏览器里路由懒加载/组件切换卡住的风险。
- 同时为渠道码本地缓存写入增加容错，避免微信内置浏览器偶发存储异常时阻断跳转。

## 修改后的代码

- `frontend_h5/src/views/ChannelEntry.vue`
  - 去掉 `useRouter` 与 `router.replace('/login')`。
  - 新增 `redirectToLogin()`，用 `window.location.replace('/login')` 跳转登录页。
- `frontend_h5/src/utils/channel.js`
  - `saveEntryInviteCode()` 写入 `localStorage` 增加 `try/catch`。
- `frontend_h5/dist`
  - 已重新构建并发布 H5 生产静态资源。

## 单元测试代码

- 本次未新增独立测试；复用 `frontend_h5/src/utils/channel.test.js` 与 `frontend_h5/src/utils/ocrImageCompression.test.js` 覆盖渠道码和 OCR 压缩相关逻辑。

## 集成测试代码

- 本次未新增独立集成测试文件；通过 H5 构建、线上渠道入口资源和线上渠道校验接口验证。

## 测试执行结果

- `frontend_h5`: `pnpm test -- channel.test.js ocrImageCompression.test.js`，8 files / 18 tests passed。
- `frontend_h5`: `pnpm build`，构建成功。
- 生产 H5 备份：`/opt/xiaohebao/backups/h5-channel-entry-location-redirect-20260604165152`。
- 生产 H5：`https://xhb.juxin.pro/0000049bad855673c404cdfe` 返回新版 H5 入口，引用 `index-T8_aWzK8.js`。
- 生产渠道入口资源：`https://xhb.juxin.pro/assets/ChannelEntry-CmrLvrzy.js` 已确认包含 `window.location.replace('/login')`、`setTimeout`，且不再包含 `router`。
- 生产渠道工具资源：`https://xhb.juxin.pro/assets/channel-CRZ5zUGk.js` 已确认 `localStorage.setItem` 包含异常容错。
- 生产后端渠道校验：`GET https://xhb.juxin.pro/api/auth/channel-invites/0000049bad855673c404cdfe` 返回 `200 OK`，渠道为 `金多多 / jinduoduo / ACTIVE`。

# 2026-06-04 16:41:37

## 修改说明

- 排查用户扫码渠道二维码后 H5 白屏的问题。
- 生产日志和线上验证确认当前渠道二维码使用的是 24 位动态邀请码，例如 `0000049bad855673c404cdfe`。
- 当前 H5 路由和本地渠道码工具仍只接受 16 位邀请码，导致扫码进入 `/0000049bad855673c404cdfe` 时前端路由无法匹配页面，微信内表现为白屏。
- 最小改动修复：H5 路由扩展为可接收 16 到 24 位小写字母数字路径；渠道码工具严格允许 16 位旧码或 24 位动态码。
- 保留旧 16 位码兼容，不改变登录、短信验证码、渠道绑定后端逻辑。

## 修改后的代码

- `frontend_h5/src/router/index.js`
  - 渠道入口路由由 `/:inviteCode([a-z0-9]{16})` 扩展为 `/:inviteCode([a-z0-9]{16,24})`。
- `frontend_h5/src/utils/channel.js`
  - 邀请码校验规则扩展为 16 位旧码或 24 位动态码。
- `frontend_h5/src/utils/channel.test.js`
  - 增加 24 位动态邀请码校验与持久化测试。
- `frontend_h5/dist`
  - 已重新构建并发布 H5 生产静态资源。

## 单元测试代码

- `frontend_h5/src/utils/channel.test.js`
  - 覆盖 `0000049bad855673c404cdfe` 这类 24 位动态渠道码为合法。
  - 覆盖 24 位动态渠道码可写入并从本地缓存读取。

## 集成测试代码

- 本次未新增独立集成测试文件；通过 H5 构建、线上渠道码 URL、线上后端渠道校验接口和线上静态资源内容验证。

## 测试执行结果

- `frontend_h5`: `pnpm test -- channel.test.js ocrImageCompression.test.js`，8 files / 18 tests passed。
- `frontend_h5`: `pnpm build`，构建成功。
- 生产 H5 备份：`/opt/xiaohebao/backups/h5-channel-24-code-20260604163925`。
- 生产 H5：`https://xhb.juxin.pro/0000049bad855673c404cdfe` 返回新版 H5 入口，引用 `index-CNgPF1u2.js`。
- 生产 H5 主包：已确认包含路由规则 `a-z0-9]{16,24}`。
- 生产渠道工具包：`https://xhb.juxin.pro/assets/channel-DVN9TkE4.js` 已确认包含 24 位动态码校验规则。
- 生产后端渠道校验：`GET https://xhb.juxin.pro/api/auth/channel-invites/0000049bad855673c404cdfe` 返回 `200 OK`，渠道为 `金多多 / jinduoduo / ACTIVE`。

# 2026-06-04 15:54:34

## 修改说明

- 排查 H5 前端用户 `13617141035` 在实名认证上传身份证后提示“身份证识别失败：网络请求失败”的问题。
- 生产日志确认该用户在 `2026-06-04 15:36:35`、`15:37:01`、`15:38:27`、`15:39:49` 连续提交 `/api/user/ocr`，每次约 20 秒后返回 `400`。
- 同时段另一个用户在 `15:22` 调用同一 OCR 接口成功，说明不是 H5 整体不可用，也不是 e签宝服务完全不可用。
- 根因判断：用户上传的身份证图片请求体约 `1.1MB~1.4MB`，前端旧逻辑低于 `2.5MB` 不压缩；后端转 base64 后调用 e签宝 OCR，生产 `ESIGN_HTTP_TIMEOUT_SECONDS=20`，第三方请求在 20 秒内未返回，被后端转换为“网络请求失败”。
- 已将生产 e签宝 OCR 超时从 `20` 秒提高到 `45` 秒并重启后端，避免慢请求在 20 秒被截断。
- 已优化 H5 身份证图片压缩策略：超过 `600KB` 或长边超过 `1280px` 的图片都会压缩后再提交 OCR，降低第三方识别超时概率。
- 查询用户当前状态：用户 ID `394`，订单 ID `400`，仍为新用户未实名，订单状态 `INIT`，未写入身份证识别成功时间，未发现被风控/黑名单错误拦截。

## 修改后的代码

- `backend/app/core/config.py`
  - e签宝 HTTP 超时默认值调整为 `45` 秒。
- `backend/.env.example`
  - 示例配置同步为 `ESIGN_HTTP_TIMEOUT_SECONDS=45`。
- `frontend_h5/src/views/OCR.vue`
  - 身份证照片提交前改为按 OCR 场景压缩策略处理。
- `frontend_h5/src/utils/ocrImageCompression.js`
  - 新增 OCR 图片压缩策略工具。
- `frontend_h5/src/utils/ocrImageCompression.test.js`
  - 新增压缩策略单元测试。

## 单元测试代码

- `frontend_h5/src/utils/ocrImageCompression.test.js`
  - 覆盖 `1.1MB` 中等身份证图片应压缩。
  - 覆盖小图片不做无意义压缩。

## 集成测试代码

- 本次未新增独立集成测试文件；通过 H5 构建、生产静态资源发布、生产后端重启和公网接口验证覆盖链路。

## 测试执行结果

- `frontend_h5`: `pnpm test -- ocrImageCompression.test.js`，8 files / 18 tests passed。
- `frontend_h5`: `pnpm build`，构建成功，生成新 OCR 资源 `OCR-C41HfcF2.js`。
- `backend`: `python -m compileall app/core/config.py app/services/esign_identity.py app/api/endpoints/user.py`，通过。
- 生产后端配置备份：`/opt/xiaohebao/backups/esign-timeout-20260604154638`。
- 生产后端：`ESIGN_HTTP_TIMEOUT_SECONDS` 已生效为 `45`。
- 生产后端服务：`xiaohebao-backend.service` 为 `active (running)`，实际监听端口为 `28001`。
- 生产 H5 备份：`/opt/xiaohebao/backups/h5-ocr-compress-20260604155053`。
- 生产 H5：`https://xhb.juxin.pro/` 已引用新入口 `index-e99TMj_9.js`。
- 生产 OCR 静态资源：`https://xhb.juxin.pro/assets/OCR-C41HfcF2.js` 返回 `200 OK`，并确认包含 `614400`、`1280`、`shouldCompress` 等新压缩策略内容。
- 生产公网 API：`https://xhb.juxin.pro/api/user/info` 未登录返回 `401 Unauthorized`，说明 H5 到后端代理链路正常。

# 2026-06-04 14:43:07

## 修改说明

- 修复 Web 管理端“用户档案 -> 查询风控报告”偶发 500 的问题。
- 生产日志确认根因：综合风控报告构造 JSON 时，报告内容中存在 `date` 类型字段，原 `_json_default()` 统一调用 `isoformat(timespec="seconds")`，而 `date.isoformat()` 不支持 `timespec`，触发 `TypeError: date.isoformat() takes no keyword arguments`。
- 最小改动修复：`datetime` 继续输出秒级时间；`date` 输出 `YYYY-MM-DD`；其他对象保持原有兜底字符串化。
- 同步兼容历史综合报告缓存中三要素或时间字段为空的情况，避免响应模型校验导致 500。
- 已按单文件方式发布到腾讯云生产，未执行全量同步，避免带入当前工作区其他未完成改动。

## 修改后的代码

- `backend/app/services/composite_risk_report.py`
  - 引入 `date` 类型判断。
  - `_dt()` 区分 `datetime` 与 `date`。
  - `_json_default()` 区分 `datetime` 与 `date`。
- `backend/app/schemas/risk.py`
  - `CompositeRiskReportResponse` 的 `name`、`id_card`、`phone`、`query_time`、`created_at`、`updated_at` 改为可空。
- `backend/tests/test_composite_risk_report.py`
  - 新增日期序列化回归测试。
  - 新增历史空字段响应兼容测试。

## 单元测试代码

- `backend/tests/test_composite_risk_report.py`
  - `test_composite_report_json_default_should_accept_date`
  - `test_composite_response_should_accept_legacy_null_fields`

## 集成测试代码

- 本次未新增独立集成测试文件；按后端服务级验证执行本地应用启动，并在腾讯云生产重启后通过公网接口和服务日志验证链路。

## 测试执行结果

- 本地：`pytest tests/test_composite_risk_report.py tests/test_admin_risk_single_query.py`，11 passed。
- 本地：`python -m compileall app/schemas/risk.py app/services/composite_risk_report.py app/services/admin_service.py`，通过。
- 本地：`python -m app.main`，启动到 `Application startup complete` 后停止。
- 生产备份目录：`/opt/xiaohebao/backups/composite-risk-report-500-20260604144050`。
- 生产发布文件：
  - `/opt/xiaohebao/current/backend/app/services/composite_risk_report.py`
  - `/opt/xiaohebao/current/backend/app/schemas/risk.py`
- 生产：`.venv/bin/python -m py_compile app/services/composite_risk_report.py app/schemas/risk.py`，通过。
- 生产：日期序列化验证输出 `{"due_date": "2026-06-04", "created_at": "2026-06-04T14:29:25"}`。
- 生产：`systemctl restart xiaohebao-backend.service` 后状态为 `active (running)`。
- 生产公网：未登录 `POST https://xhbadmin.juxin.pro/api/admin/risk/composite-report` 返回 `401 Unauthorized`，未再出现 500。
- 生产日志：重启后未发现新的 `composite-report` 相关 `request_exception`、`TypeError` 或 `ResponseValidationError`。

# 2026-06-04 12:22:47

## 修改说明

- 新增后台操作权限 `loan-review-takeover`，默认授予审核角色；超管仍保持完整改派能力。
- 后端订单列表新增 `takeover_pool` 查询口径：仅允许非超管审核角色在申请审批审核中列表查看“可转入”的审核中申请。
- 后端转单接口保持原接口不变：超管可继续完整改派；审核员仅能把审核中的申请转给自己，不能转给其他人，不能改派催收。
- Web 管理端申请审批页新增“只看可转入申请”筛选；申请详情中对符合条件的审核员展示“转给我”按钮。
- 安卓管理 App 申请页新增“可转入”快捷筛选；详情顶部审核员标签与底部操作区均支持“转给我”。
- iOS 管理 App 申请页新增“可转入”快捷筛选；详情页审核员标签在权限允许时支持“转给我”，超管仍打开完整审核员列表。

## 修改后的代码

- `backend/app/services/admin_permissions.py`
- `backend/app/api/endpoints/admin.py`
- `backend/app/services/admin_service.py`
- `backend/tests/test_admin_review_takeover.py`
- `frontend_admin/src/constants/adminPages.js`
- `frontend_admin/src/utils/applicationsFilters.js`
- `frontend_admin/src/utils/applicationsFilters.test.js`
- `frontend_admin/src/views/Applications.vue`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `ios_native/Sources/Models/AppModels.swift`
- `ios_native/Sources/ViewModels/WorkspaceViewModel.swift`
- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `ios_native/Sources/Views/Screens/DetailView.swift`
- `ios_native/Tests/WorkspaceLogicTests.swift`

## 单元测试代码

- 新增 `backend/tests/test_admin_review_takeover.py`，覆盖审核员转入自己、禁止转给他人、禁止转入非审核中订单、审核角色默认权限。
- 更新 `frontend_admin/src/utils/applicationsFilters.test.js`，覆盖 `takeover_pool` 参数。
- 更新 `ios_native/Tests/WorkspaceLogicTests.swift`，覆盖 iOS “可转入”筛选标签。

## 集成测试代码

- 本次未新增独立集成测试文件；通过现有 Web 构建、安卓打包、iOS Xcode 构建/测试以及后端应用启动验证三端集成可用性。

## 测试执行结果

- `backend`: `pytest tests/test_admin_review_takeover.py`，4 passed。
- `backend`: `python -m app.main`，服务启动到 `Application startup complete` 后正常停止。
- `frontend_admin`: `pnpm test -- applicationsFilters.test.js adminPages.test.js`，10 files / 21 tests passed。
- `frontend_admin`: `pnpm build`，构建成功；仅存在第三方依赖 pure annotation 与 chunk size warning。
- `android_native`: `./build_apk.sh debug`，构建成功，产物 `android_native/build/xiaohebao-debug.apk`。
- `ios_native`: `xcodebuild -project XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,name=iPhone 16,OS=18.6' build`，BUILD SUCCEEDED。
- `ios_native`: `xcodebuild test -project XiaoHeBaoIOS.xcodeproj -scheme XiaoHeBaoIOS -destination 'platform=iOS Simulator,name=iPhone 16,OS=18.6'`，14 tests passed。

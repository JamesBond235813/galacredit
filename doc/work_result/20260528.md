# 2026-05-28 22:38:34

## 修改说明

- 按用户截图反馈继续优化安卓 App 申请详情页与档案列表：
- 申请详情页顶部 `审核员` 胶囊改为底色色块内居中展示。
- 审核员弹窗改为先选择审核员，再点击 `确定` 后才执行转单。
- 审核员弹窗新增 `取消`、`确定` 两个按钮。
- IP 记录地址统一去重和规范化，只展示国家/省/市/区层级，例如 `中国/广东/深圳`，不再出现 `中国广东深圳中国/广东/深圳` 这类重复文本。
- 档案 Tab 用户卡片底部去掉 `点击查看处理动作` 文案。
- 详情页 ScrollView 底部预留高度从 `248dp` 缩小到 `18dp`，减少订单状态卡片与底部 `可执行操作` 区域之间的大块空白。
- 安卓版本递增为 `0.1.16(17)` 并发布上线。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `android_native/app/src/main/AndroidManifest.xml`

## 单元测试代码

- 本次为安卓原生 UI 与交互细节调整，项目当前没有 Android 单元测试框架；未新增测试文件。

## 集成测试代码

- 本次未修改后端接口逻辑，未新增集成测试文件。

## 测试执行结果

- 安卓 debug 构建：`cd android_native && ./build_apk.sh debug` 通过。
- 安卓 release 构建：`cd android_native && ./build_apk.sh release` 通过。
- APK 包信息：`versionCode=17`，`versionName=0.1.16`。
- APK 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 APK SHA256：`b422f929ba6f2c319cd68b488a74993e0f18f68d83444dd942f0965af86e1f10`。
- 已备份生产旧 APK、下载页和版本清单到 `/opt/xiaohebao/backups/android-apk-20260528223732`。
- 新版 APK 已上传到固定下载地址：`https://xhbadmin.juxin.pro/download/xiaohebao.apk`。
- 版本清单已更新：`versionCode=17`、`versionName=0.1.16`、`apkUrl=https://xhbadmin.juxin.pro/download/xiaohebao.apk`。
- 公网下载 APK 包信息为 `versionCode=17`、`versionName=0.1.16`，SHA256 与本地一致。
- 生产下载页仍为无后缀链接 `/download/xiaohebao.apk`。

## 操作步骤

- 根据截图定位审核员胶囊、审核员弹窗、IP 记录、档案卡片和详情页底部留白代码。
- 修改审核员展示样式与二次确认转单流程。
- 修改 IP 地址拼接口径。
- 删除档案卡片默认提示文案。
- 缩小详情页底部 ScrollView 预留高度。
- 构建并发布新版 APK，更新生产版本清单。

# 2026-05-28 22:21:16

## 修改说明

- 按用户要求，将安卓 App 更新方式调整为老版体验：
- App 启动后先正常进入登录页或工作台，不再停留在“正在检查应用版本”页面。
- 后台异步检查版本，有新版时弹出升级提示弹窗。
- 弹窗按钮文案改为 `升级`。
- 点击 `升级` 后在 App 内下载升级包。
- 下载弹窗改为自定义百分比进度条，明确显示 `0%` 到 `100%`。
- 下载完成后直接调起 Android 系统安装确认页。
- 安卓版本递增为 `0.1.15(16)` 并发布上线。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `android_native/app/src/main/AndroidManifest.xml`

## 单元测试代码

- 本次为安卓原生更新体验调整，项目当前没有 Android 单元测试框架；未新增测试文件。

## 集成测试代码

- 本次未修改后端接口逻辑，未新增集成测试文件。

## 测试执行结果

- 安卓 debug 构建：`cd android_native && ./build_apk.sh debug` 通过。
- 安卓 release 构建：`cd android_native && ./build_apk.sh release` 通过。
- APK 包信息：`versionCode=16`，`versionName=0.1.15`。
- APK 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 APK SHA256：`f863e4a25d94ba08b975e834fa3586db51ad2bf8c52ffe088ddf84548ff68932`。
- 已备份生产旧 APK、下载页和版本清单到 `/opt/xiaohebao/backups/android-apk-20260528221939`。
- 新版 APK 已上传到固定下载地址：`https://xhbadmin.juxin.pro/download/xiaohebao.apk`。
- 版本清单已更新：`versionCode=16`、`versionName=0.1.15`、`apkUrl=https://xhbadmin.juxin.pro/download/xiaohebao.apk`。
- 公网固定下载地址返回 `200 OK`。
- 公网下载 APK 包信息为 `versionCode=16`、`versionName=0.1.15`，SHA256 与本地一致。
- 生产下载页仍为无后缀链接 `/download/xiaohebao.apk`。

## 操作步骤

- 调整启动流程，移除版本检查中间页。
- 调整升级弹窗按钮与取消行为。
- 将系统 `ProgressDialog` 替换为自定义百分比进度条弹窗。
- 构建并签名新版 APK。
- 发布到腾讯云固定下载地址并更新版本清单。
- 从公网无后缀地址下载 APK 并核验版本与哈希。

# 2026-05-28 22:14:06

## 修改说明

- 按用户要求调整安卓 App 个人详情页风控报告卡片：
- 第 1 行展示 `报告时间`、`报告评估结论`。
- 第 2 行展示 `申请准入分`、`信用行为分` 两个卡片。
- 第 3 行展示 `最近放款`、`探查结果`、`正常还款比例`。
- 保留原有 `风险维度` 区域。
- 保留原有 `命中原因` 区域。
- 去掉 `展开完整报告` 按钮及展开后的完整报告内容展示。
- 按用户要求调整申请 Tab 详情页：
- 在申请人姓名下方显示 `渠道` 和 `审核员`。
- 超级管理员可点击 `审核员` 打开审核员列表弹窗。
- 点击列表内其他审核员后调用生产已有转单接口完成审核转单。
- 复用后端现有权限控制：无权限账号无法获取审核员列表或执行转单。
- 安卓版本递增为 `0.1.14(15)` 并发布上线。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/ApiClient.java`
- `android_native/app/src/main/AndroidManifest.xml`

## 单元测试代码

- 本次为安卓原生 UI 与接口调用逻辑调整，项目当前没有 Android 单元测试框架；未新增测试文件。

## 集成测试代码

- 本次未修改后端接口逻辑，未新增集成测试文件。

## 测试执行结果

- 安卓 debug 构建：`cd android_native && ./build_apk.sh debug` 通过。
- 安卓 release 构建：`cd android_native && ./build_apk.sh release` 通过。
- APK 包信息：`versionCode=15`，`versionName=0.1.14`。
- APK 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 APK SHA256：`d984f374dd20e31ec2ecac46ffd45c57067419121981f396988460562f41a198`。
- 生产审核员列表接口验证：`GET /api/admin/loan-assignees?stage=review` 可返回审核员列表，当前数量 `2`。
- 本次未在生产执行真实转单，避免误改业务数据。
- 已备份生产旧 APK、下载页和版本清单到 `/opt/xiaohebao/backups/android-apk-20260528221226`。
- 新版 APK 已上传到固定下载地址：`https://xhbadmin.juxin.pro/download/xiaohebao.apk`。
- 版本清单已更新：`versionCode=15`、`versionName=0.1.14`、`apkUrl=https://xhbadmin.juxin.pro/download/xiaohebao.apk`。
- 公网固定下载地址返回 `200 OK`。
- 公网下载 APK 包信息为 `versionCode=15`、`versionName=0.1.14`，SHA256 与本地一致。
- 生产下载页仍为无后缀链接 `/download/xiaohebao.apk`。

## 操作步骤

- 阅读安卓风控报告卡片、申请详情页头部、审核员接口相关代码。
- 使用现有后端 `loan-assignees` 与 `loans/{loan_id}/assign` 接口实现移动端转单。
- 给 `ApiClient` 补充 JSON 数组读取能力，以适配审核员列表接口。
- 调整风控报告卡片布局并删除完整报告展开区域。
- 构建并发布新版 APK，更新生产版本清单。

# 2026-05-28 19:59:36

## 修改说明

- 按用户要求，去掉新版安卓下载地址中的 `?v=` 后缀，恢复为固定地址 `https://xhbadmin.juxin.pro/download/xiaohebao.apk`。
- 新增安卓 App 内更新能力：
- App 启动后先请求 `https://xhbadmin.juxin.pro/download/android-version.json` 检查版本。
- 当线上 `versionCode` 高于本机版本时，在 App 内弹出更新提示。
- 用户点击“立即更新”后，由 App 内部下载 APK，并显示百分比进度条。
- 下载完成后直接调起 Android 系统安装确认页，不再通过登录页面或浏览器下载页更新。
- 新增 APK 安装用内部 `ContentProvider`，避免 Android 7+ 直接暴露 `file://` 安装路径的问题。
- 安卓版本递增为 `0.1.13(14)` 并重新发布。

## 修改后的代码

- `android_native/app/src/main/AndroidManifest.xml`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/ApkInstallProvider.java`

## 单元测试代码

- 本次为安卓原生更新流程，项目当前没有 Android 单元测试框架；未新增测试文件。

## 集成测试代码

- 本次未修改后端接口逻辑，未新增集成测试文件。

## 测试执行结果

- 安卓 debug 构建：`cd android_native && ./build_apk.sh debug` 通过。
- 安卓 release 构建：`cd android_native && ./build_apk.sh release` 通过。
- APK 包信息：`versionCode=14`，`versionName=0.1.13`。
- APK 权限校验：包含 `INTERNET` 与 `REQUEST_INSTALL_PACKAGES`。
- APK 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 APK SHA256：`7966d72c9f129fc8a211553bae7d9b1c0a0f306ad461965abcca013851277c31`。
- 已备份生产旧 APK、下载页和版本清单到 `/opt/xiaohebao/backups/android-apk-20260528195728`。
- 新版 APK 已上传到 `/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 已上线版本清单 `/data/www/xhbadmin.juxin.pro/dist/download/android-version.json`：
- `versionCode=14`
- `versionName=0.1.13`
- `apkUrl=https://xhbadmin.juxin.pro/download/xiaohebao.apk`
- 生产下载页 `android.html` 已改为无后缀链接 `/download/xiaohebao.apk`。
- 公网固定下载地址 `https://xhbadmin.juxin.pro/download/xiaohebao.apk` 返回 `200 OK`。
- 公网下载 APK 包信息为 `versionCode=14`、`versionName=0.1.13`，SHA256 与本地一致。
- 当前未连接 Android 设备或模拟器，无法执行真机安装弹窗流程测试；`adb devices` 返回空设备列表。

## 操作步骤

- 在 App 启动流程前增加版本检查入口。
- 新增版本清单读取、更新弹窗、APK 下载进度、安装调起逻辑。
- 新增内部 APK 安装 Provider。
- 构建并签名新版 APK。
- 发布到腾讯云固定下载地址并更新版本清单。
- 从公网无后缀地址下载 APK 并核验版本与哈希。

# 2026-05-28 19:48:26

## 修改说明

- 处理用户反馈：下载新版安卓 App 后，回款 Tab 左上角卡片仍没有变化。
- 复查确认公网 APK 已是上一版 `0.1.11(12)`，不是下载地址未更新。
- 根因确认：上一版只在回款 Tab 的“未逾期”筛选态展示 `N/M`，默认回款页仍使用旧 `/admin/stats` 口径；生产旧口径 `due_today_loans=1`，因此默认页面看起来没有变化。
- 修复安卓默认回款页：
- 默认回款页左上角也改为 `N/M`。
- `N` 取 `due_today_actual_repayment_user_count`。
- `M` 取 `due_today_user_count`。
- 默认回款页和“未逾期”筛选态现在使用同一套 `N/M` 口径。
- 安卓版本递增为 `0.1.12(13)` 并重新发布。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `android_native/app/src/main/AndroidManifest.xml`

## 单元测试代码

- 本次为安卓原生展示逻辑修正，项目当前没有 Android 单元测试框架；未新增测试文件。

## 集成测试代码

- 本次未修改后端接口逻辑，未新增集成测试文件。

## 测试执行结果

- 生产接口复核：
- `/api/admin/stats` 当前旧口径返回 `due_today_loans=1`。
- `/api/admin/repayment-stats` 当前新口径返回 `due_today_user_count=3`、`due_today_actual_repayment_user_count=2`。
- 安卓 debug 构建：串行执行 `cd android_native && ./build_apk.sh debug` 通过。
- 安卓 release 构建：串行执行 `cd android_native && ./build_apk.sh release` 通过。
- APK 包信息：`versionCode=13`，`versionName=0.1.12`。
- APK 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 APK SHA256：`36848c7e3506540bba90d0d44298ef7c55a638c0c6af3e919fe89a6e69dec4fb`。
- 新版 APK 已上传到 `/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 公网下载地址 `https://xhbadmin.juxin.pro/download/xiaohebao.apk?v=202605281948` 返回 `200 OK`。
- 公网下载 APK 包信息为 `versionCode=13`、`versionName=0.1.12`，SHA256 与本地一致。
- 下载页已更新为 `xiaohebao.apk?v=202605281948`。

## 操作步骤

- 校验公网 APK 版本与哈希。
- 对比安卓默认回款页与“未逾期”筛选态的统计映射。
- 将默认回款页左上角改为新 `N/M` 口径。
- 重新构建、签名、发布 APK，并校验公网下载产物。

# 2026-05-28 19:18:45

## 修改说明

- 修复安卓 App 回款 Tab 顶部左上角统计卡口径不准确的问题。
- 原因确认：生产今日到期列表实际有 `3` 人，其中 `1` 人状态为 `OVERDUE`，`2` 人状态为 `SETTLED`；旧统计口径只统计 `DISBURSED`，导致今日到期人数少算。
- 后端新增今日到期集合内的实际回款统计：
- `due_today_actual_repayment_user_count`
- `due_today_actual_repayment_amount`
- 同时将 `due_today_user_count` 调整为“到期日为今天的人数”，不再仅限 `DISBURSED` 状态。
- 安卓回款 Tab 未逾期分类下，左上角卡片改为 `N/M` 展示：
- `N` 为今天应回款客户中今天实际回款人数。
- `M` 为到期日为今天的人数。
- 本次生产验证结果：`N/M = 2/3`。

## 修改后的代码

- `backend/app/api/endpoints/admin.py`
- `backend/app/schemas/loan.py`
- `backend/tests/test_admin_loans_filters.py`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `android_native/app/src/main/AndroidManifest.xml`

## 单元测试代码

- 修改 `backend/tests/test_admin_loans_filters.py`，覆盖今日到期统计应包含已结清订单，并校验今日到期集合内实际回款人数与金额。

## 集成测试代码

- 本次未新增独立集成测试文件。
- 已通过生产接口验证后端与真实数据联动结果。

## 测试执行结果

- 后端单测：`pytest backend/tests/test_admin_loans_filters.py -q`
- 结果：`3 passed`。
- Python 语法检查：`python -m py_compile backend/app/api/endpoints/admin.py backend/app/schemas/loan.py` 通过。
- 本地后端启动冒烟：`cd backend && python -m app.main` 可正常启动并完成应用初始化。
- 安卓 debug 构建：`cd android_native && ./build_apk.sh debug` 通过。
- 安卓 release 构建：`cd android_native && ./build_apk.sh release` 通过。
- APK 包信息：`versionCode=12`，`versionName=0.1.11`。
- APK 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 APK SHA256：`02b560b550fba5b9bf341d0b7a81e92b89cafcf3d2bf8487a47f97d0b720eeff`。
- 腾讯云后端已更新并重启，`xiaohebao-backend.service` 状态为 `active`。
- 生产 `/api/admin/repayment-stats` 验证：
- `due_today_user_count=3`
- `due_today_actual_repayment_user_count=2`
- `due_today_amount=5600.0`
- `due_today_actual_repayment_amount=4000.0`
- 今日到期列表验证：`total=3`。
- 新版 APK 已上传到 `/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 公网下载地址 `https://xhbadmin.juxin.pro/download/xiaohebao.apk?v=202605281917` 返回 `200 OK`，公网下载文件 SHA256 与本地一致。
- 下载页已更新为 `xiaohebao.apk?v=202605281917`。

## 操作步骤

- 排查安卓端回款统计卡展示逻辑。
- 对比生产 `/repayment-stats` 和今日到期订单列表数据。
- 修正后端统计口径并补充测试。
- 修正安卓卡片展示为 `N/M`。
- 构建并发布新版后端与 Android APK。

# 2026-05-28 18:53:16

## 生产数据处理

- 按用户要求，再次解除手机号 `15612759789` 的位置风控锁定。
- 处理前查询结果：
- 用户 ID：`201`
- 用户姓名：`胡华`
- `location_risk_blocked=true`
- 锁定原因：`登录位置异常：4小时内距离上次登录约126.8公里`
- 锁定时间：`2026-05-28T17:10:58`
- 处理方式：通过生产正式后台接口执行解除，而非直接改库，以保留后台审计链路。
- 调用接口：`POST https://xhbadmin.juxin.pro/api/admin/users/201/location-risk/unlock`
- 接口返回：`{"msg":"位置风控已解除"}`
- 处理后复查：
- `location_risk_blocked=false`
- `location_risk_reason=null`
- `location_risk_at=null`
- 本次仅解除该用户的位置风控锁定，未修改其他用户或其他业务数据。

## 修改后的代码

- 本次未修改本地业务代码，仅执行生产后台接口操作并补充工作记录。

## 单元测试代码

- 不适用，本次为生产数据处理动作。

## 集成测试代码

- 不适用，本次未修改接口逻辑。

## 测试执行结果

- 已通过生产后台用户列表接口复查，手机号 `15612759789` 对应用户的位置风控状态已解除。

# 2026-05-28 18:44:37

## 修改说明

- 继续处理安卓端加载慢、发卡入口误导问题的发布工作。
- 将安卓原生 App 版本从 `0.1.9(10)` 递增为 `0.1.10(11)`，确保用户手机下载安装时能够明确识别为新版本。
- 使用本机私有 release 签名配置构建正式 APK。
- 已将新版 APK 发布到腾讯云生产下载地址，并同步更新下载页缓存参数。

## 修改后的代码

- `android_native/app/src/main/AndroidManifest.xml`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`

## 单元测试代码

- 本次为安卓原生 App 行为收口与发布，项目当前没有 Android 单元测试框架；未新增测试文件。

## 集成测试代码

- 本次未修改后端接口逻辑，未新增集成测试文件。

## 测试执行结果

- 已执行安卓 release 构建：`cd android_native && ./build_apk.sh release`
- 结果：构建通过，产物 `android_native/build/xiaohebao-release.apk`。
- 包信息校验：包名 `com.juxin.xiaohebao.admin`，版本 `versionCode=11`、`versionName=0.1.10`。
- 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 APK SHA256：`0c2c935c5717caab7fed6b38b6b51648d32d38fe0178f0ca0fb745d27bedc0e7`。
- 已备份生产旧 APK 和下载页到 `/opt/xiaohebao/backups/android-apk-20260528184143`。
- 已上传新版 APK 到 `/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 云端 APK SHA256 与本地一致：`0c2c935c5717caab7fed6b38b6b51648d32d38fe0178f0ca0fb745d27bedc0e7`。
- 公网下载地址 `https://xhbadmin.juxin.pro/download/xiaohebao.apk?v=202605281842` 返回 `200 OK`，公网下载文件 SHA256 与本地一致。
- 下载页 `https://xhbadmin.juxin.pro/download/android.html` 已更新为 `xiaohebao.apk?v=202605281842`。
- Android UA 访问 `https://xhbadmin.juxin.pro/` 返回 `302`，仍跳转到下载页。

## 操作步骤

- 查找并加载本机私有 release 签名配置。
- 递增安卓版本号。
- 构建正式签名包并校验包信息、签名和哈希。
- 通过 `tencent-superapi` 备份生产旧文件、上传新版 APK、修正文件权限。
- 校验公网下载地址和下载页版本参数。

# 2026-05-28 18:27:53

## 修改说明

- 排查安卓原生管理端加载慢、发卡点击后状态未变化的问题。
- 生产接口复核结果：
- 当前生产统计 `withdrawing_loans=0`，即云端没有真正处于待发卡 `WITHDRAWING` 状态的订单。
- 当前卡池仍有可用库存：`ecard_pool_available_count=24`，`ecard_pool_available_amount=18000.0`。
- 串行复核 `/api/admin/loans?scope=WITHDRAWING&skip=0&limit=20` 返回 `total=0`、`items=[]`。
- 结论：当前“发卡不成功”的直接原因不是卡池无库存，而是没有待发卡订单；安卓端若在非待发卡状态仍能看到发卡/退回相关入口，会造成误导。
- 修复安卓端加载慢问题：将原生 App 后台网络任务从单线程执行器调整为 4 线程执行器，避免统计、列表、详情、风控报告请求互相排队。
- 修复安卓端发卡动作展示过宽问题：
- 档案页仅在 `CARD_REJECTED` 时展示“开启二次发卡”。
- 档案页仅在 `WITHDRAWING` 或 `CARD_REJECTED` 时展示“退回待下单”。
- 发卡页仅在订单状态为 `WITHDRAWING` 时展示“确认发卡 / 拒绝发卡 / 退回待下单”。
- 提交前新增状态校验，即使旧页面或旧缓存残留按钮，也会提示当前订单状态不支持该操作。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`

## 单元测试代码

- 本次为安卓原生 UI 与动作入口收口，项目当前没有 Android 单元测试框架；未新增测试文件。

## 集成测试代码

- 本次未修改后端接口逻辑，未新增集成测试文件。

## 测试执行结果

- 已执行安卓 debug 构建：`cd android_native && ./build_apk.sh debug`
- 结果：构建通过，产物 `android_native/build/xiaohebao-debug.apk`。
- 已复核生产待发卡列表：`total=0`。
- 已复核生产卡池统计：可用卡 `24` 张，可用总额 `18000.0`。
- release 构建未执行：当前 shell 未配置 `XHB_RELEASE_KEYSTORE / XHB_RELEASE_STORE_PASS / XHB_RELEASE_KEY_ALIAS / XHB_RELEASE_KEY_PASS` 等签名环境变量。

## 操作步骤

- 阅读安卓原生 `ApiClient`、工作台加载、详情加载、发卡动作提交代码。
- 阅读后端发卡接口 `_disburse_loan`，确认后端只允许 `WITHDRAWING` 订单发卡。
- 调用生产后台接口复核统计、待发卡列表、卡池可用库存。
- 对安卓端做最小修复：并发加载网络任务、按订单真实状态收口发卡相关入口、提交前补状态校验。
- 执行安卓 debug 构建验证。

# 2026-05-28 17:04:40

## 生产数据处理

- 按用户要求，解除手机号 `15612759789` 的位置风控锁定。
- 处理前查询结果：
- 用户 ID：`201`
- 用户姓名：`胡华`
- `location_risk_blocked=true`
- 锁定原因：`登录位置异常：4小时内距离上次登录约55.6公里`
- 锁定时间：`2026-05-28T14:56:18`
- 处理方式：通过生产正式后台接口执行解除，而非直接改库，以保留后台审计链路。
- 调用接口：`POST https://xhbadmin.juxin.pro/api/admin/users/201/location-risk/unlock`
- 接口返回：`{"msg":"位置风控已解除"}`
- 处理后复查：
- `location_risk_blocked=false`
- `location_risk_reason=null`
- `location_risk_at=null`
- 本次仅解除该用户的位置风控锁定，未修改其他用户或其他业务数据。

## 修改后的代码

- 本次未修改本地业务代码，仅执行生产后台接口操作并补充工作记录。

## 单元测试代码

- 不适用，本次为生产数据处理动作。

## 集成测试代码

- 不适用，本次未修改接口逻辑。

## 测试执行结果

- 已验证生产登录接口可用：`admin/admin123` 可正常获取后台 token。
- 已验证生产用户档案查询接口返回该用户当前锁定状态。
- 已验证生产解除接口执行成功，且复查返回字段已变更为未锁定。

## 操作步骤

- 调用生产后台登录接口获取管理员 token。
- 调用生产用户档案查询接口，确认手机号 `15612759789` 的用户 ID 与当前锁定状态。
- 调用生产“解除位置风控”正式接口执行解锁。
- 再次查询生产用户档案接口，复核解锁结果。

# 2026-05-28 10:35:25

## 修改说明

- 继续推进原生 iOS 管理端与安卓原生管理端的功能对齐，重点补齐详情页字段展示与业务操作表单。
- 在 `DetailView.swift` 中补充了安卓端已有但 iOS 骨架版缺失的业务信息：
- 详情页新增“订单信息”卡片，并将“订单状态及审核批注”拆分展示，补齐商品、订单金额、其他费用、催收备注、复购、提交时间等字段。
- 位置区域补齐定位地址、经纬度、定位精度、定位来源、位置风控说明。
- IP 记录改为读取最近 3 条审计记录，与安卓端查看习惯保持一致。
- 将 `ActionFormSheet` 从单输入框升级为按动作动态渲染的表单：
- 审批通过：授信额度、减免额度、期限天数
- 确认发卡：账期天数 + 订单摘要
- 账单展期：展期类型、天数、减免金额、备注
- 登记平账：登记收款、减免金额、其他费用、备注、实际还款日、平账预览
- 调整授信 / 增加可用额度 / 各类备注动作：分别补齐对应字段
- 补充 iOS 侧 `WorkspaceLogicTests`，新增回款统计卡片口径与平账 tab 动作集测试。

## 修改后的代码

- `ios_native/Sources/Views/Screens/DetailView.swift`
- `ios_native/Tests/WorkspaceLogicTests.swift`

## 单元测试代码

- `ios_native/Tests/WorkspaceLogicTests.swift`
- 本次新增覆盖：
- 回款 tab 在“未逾期”筛选下的顶部统计卡片口径
- 平账 tab 的动作集合是否保持为 `reconcile / settle / extend`

## 集成测试代码

- 本次未新增独立 iOS 集成测试文件。
- 仍通过 iOS 模拟器构建与后端启动验证做联调级自检。

## 测试执行结果

- 已执行 iOS 模拟器构建：
- `xcodebuild -project '/Volumes/littlejiang02/VibeCoding/xiaohebao /ios_native/XiaoHeBaoIOS.xcodeproj' -scheme XiaoHeBaoIOS -sdk iphonesimulator -destination 'platform=iOS Simulator,OS=18.6,name=iPhone 16 Pro' build`
- 结果：`BUILD SUCCEEDED`
- 已尝试执行 iOS 模拟器测试两次：
- `xcodebuild -project '/Volumes/littlejiang02/VibeCoding/xiaohebao /ios_native/XiaoHeBaoIOS.xcodeproj' -scheme XiaoHeBaoIOS -sdk iphonesimulator -destination 'platform=iOS Simulator,OS=18.6,name=iPhone 16 Pro' test`
- 结果：未能完成执行，原因是本机模拟器资源不足，系统报错 `Unable to boot device due to insufficient system resources`，属于环境限制，不是代码编译错误。
- 已执行后端启动验证：`cd backend && python -m app.main`
- 结果：应用完成 startup，随后因本机 `8001` 端口已被占用退出；说明后端可正常启动，但当前机器已有服务占用该端口。

## 操作步骤

- 对照安卓原生详情页字段与动作弹窗，梳理 iOS 端缺口。
- 在不改动现有工程结构的前提下，直接扩展 `DetailView` 的字段分组与 `ActionFormSheet` 的动态表单能力。
- 补充工作台映射测试，确认统计口径与动作集合未被这轮改动带偏。
- 执行 iOS 构建验证、iOS 测试尝试、后端启动验证，并回写结果。

# 2026-05-28 10:15:02

## 修改说明

- 新增原生 iOS 管理端工程 `ios_native`，采用 `Swift + SwiftUI + XcodeGen`，用于与当前安卓原生管理端同步开发。
- 第一阶段已完成可运行骨架，覆盖登录、会话管理、五个业务 tab、列表/详情页、风控报告区域、操作入口、网络层、环境配置隔离。
- 配置已按本地与云端隔离：
- `Debug` 指向本地 `http://127.0.0.1:8001/api`
- `Release` 指向云端 `https://xhbadmin.juxin.pro/api`
- 本次对 `ios_native/project.yml` 做最小修正，补齐测试 target 的 `Info.plist` 自动生成与独立模块命名，解决测试编译失败问题。

## 修改后的代码

- 新增 iOS 原生工程与主要代码文件：
- `ios_native/project.yml`
- `ios_native/Config/Debug.xcconfig`
- `ios_native/Config/Release.xcconfig`
- `ios_native/App/XiaoHeBaoIOSApp.swift`
- `ios_native/Sources/Config/AppConfig.swift`
- `ios_native/Sources/Infrastructure/APIClient.swift`
- `ios_native/Sources/Infrastructure/JSONValue.swift`
- `ios_native/Sources/Infrastructure/Formatters.swift`
- `ios_native/Sources/Models/AppModels.swift`
- `ios_native/Sources/Models/WorkspaceLogic.swift`
- `ios_native/Sources/ViewModels/SessionStore.swift`
- `ios_native/Sources/ViewModels/WorkspaceViewModel.swift`
- `ios_native/Sources/Views/Components/AppTheme.swift`
- `ios_native/Sources/Views/Components/PrimaryControls.swift`
- `ios_native/Sources/Views/Components/SummaryCards.swift`
- `ios_native/Sources/Views/Screens/RootView.swift`
- `ios_native/Sources/Views/Screens/LoginView.swift`
- `ios_native/Sources/Views/Screens/WorkspaceView.swift`
- `ios_native/Sources/Views/Screens/DetailView.swift`
- `ios_native/Resources/Assets.xcassets/AppIcon.appiconset/Contents.json`
- `ios_native/Resources/Assets.xcassets/Contents.json`
- `ios_native/Resources/Preview Assets.xcassets/Contents.json`
- `ios_native/XiaoHeBaoIOS.xcodeproj/project.pbxproj`
- `ios_native/XiaoHeBaoIOS.xcodeproj/project.xcworkspace/contents.xcworkspacedata`
- `ios_native/XiaoHeBaoIOS.xcodeproj/xcshareddata/xcschemes/XiaoHeBaoIOS.xcscheme`
- 本次补充的关键配置修正：
- `ios_native/project.yml`

## 单元测试代码

- `ios_native/Tests/WorkspaceLogicTests.swift`
- 覆盖内容：
- 角色与 tab 可见性映射
- 平账默认 payload 计算
- 用户/订单动作接口路径映射

## 集成测试代码

- 本次未新增独立 iOS 集成测试文件。
- 采用以下方式完成可运行性验证：
- 生成 Xcode 工程
- 模拟器构建 app
- 模拟器执行单元测试
- 启动后端服务验证应用可启动

## 测试执行结果

- 已执行工程生成：`cd ios_native && /opt/homebrew/bin/xcodegen generate`，结果成功生成 `XiaoHeBaoIOS.xcodeproj`。
- 已执行 iOS 模拟器构建：
- `xcodebuild -project '/Volumes/littlejiang02/VibeCoding/xiaohebao /ios_native/XiaoHeBaoIOS.xcodeproj' -scheme XiaoHeBaoIOS -sdk iphonesimulator -destination 'platform=iOS Simulator,OS=18.6,name=iPhone 16 Pro' build`
- 结果：`BUILD SUCCEEDED`
- 已执行 iOS 模拟器测试：
- `xcodebuild -project '/Volumes/littlejiang02/VibeCoding/xiaohebao /ios_native/XiaoHeBaoIOS.xcodeproj' -scheme XiaoHeBaoIOS -sdk iphonesimulator -destination 'platform=iOS Simulator,OS=18.6,name=iPhone 16 Pro' test`
- 结果：`3 tests, 0 failures`
- 已执行后端启动验证：`cd backend && python -m app.main`
- 结果：应用完成 startup，随后因本机 `8001` 端口已被占用退出；说明后端可正常启动，但当前机器已有服务占用该端口。

## 操作步骤

- 梳理安卓原生管理端的 tab 结构、详情结构、统计卡片与动作接口。
- 以最小改动原则新增 `ios_native` 原生工程，先完成与安卓端一致的主工作台骨架。
- 通过 `Debug/Release` 配置完成本地与云端接口地址隔离。
- 接入基础网络层、会话层、动态 JSON 数据模型与页面视图模型。
- 补充 `WorkspaceLogicTests` 校验核心业务映射逻辑。
- 修正 iOS 测试 target 配置，解决 `Info.plist` 缺失和测试模块产物重名问题。
- 重新生成工程，并完成构建、测试、后端启动验证。

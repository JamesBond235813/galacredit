# 2026-05-31 23:38:09

## 修改说明

- 核实安卓 App 回款 Tab “全部”与“已逾期”显示不一致的原因：顶部逾期数原本统计当前全部逾期订单，但“已逾期”列表和部分统计复用了催收阶段 `OVERDUE` 口径，额外要求逾期超过 14 天。
- 后端 `/api/admin/repayment-stats` 的逾期统计改为回款页业务口径：当前仍为 `OVERDUE` 且存在应还款日的订单，不再套用催收转移阈值。
- 后端 `/api/admin/loans` 在 `scope=REPAYMENTS&status=OVERDUE` 时展示全部当前逾期订单，不再使用催收阶段的 14 天筛选条件。
- 安卓回款 Tab 顶部右侧卡片改为 `逾期订单/金额`，数值展示为 `X/Y`，其中 `X` 为逾期订单数，`Y` 为逾期总金额。
- 安卓回款 Tab 点击“已逾期”后仍使用回款页 Scope，仅通过 `status=OVERDUE` 筛选，避免误进入催收 Scope。
- 安卓版本递增为 `0.1.19(20)`，按固定下载地址发布到腾讯云。

## 修改后的代码

- `backend/app/api/endpoints/admin.py`
- `backend/app/schemas/loan.py`
- `backend/tests/test_admin_loans_filters.py`
- `android_native/app/src/main/AndroidManifest.xml`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `doc/work_result/20260531.md`

## 单元测试代码

- `backend/tests/test_admin_loans_filters.py`

## 集成测试代码

- 本次未新增独立集成测试文件；通过本地启动后端服务验证接口路由可达且鉴权仍生效。

## 测试执行结果

- 后端测试：`python -m pytest backend/tests/test_admin_loans_filters.py`，结果 `5 passed`。
- 后端本地启动：`cd backend && python -m app.main` 可正常启动 Uvicorn。
- 接口可达性：未登录访问 `http://127.0.0.1:8001/api/admin/stats` 返回 `401`，说明服务可达且鉴权保护仍生效。
- 安卓 debug 构建：`cd android_native && ./build_apk.sh debug` 通过，生成 `android_native/build/xiaohebao-debug.apk`。
- 安卓 release 构建：`cd android_native && ./build_apk.sh release` 通过。
- APK 包信息：`versionCode=20`，`versionName=0.1.19`。
- APK 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 release APK SHA256：`d50a0803eec8e25b86ed218d62497c5b2cb5fd689dc318ddfde5ae5d63ed09dd`。
- 已备份云端旧后端接口文件、旧 APK、旧版本清单到 `/opt/xiaohebao/backups/repayment-overdue-scope-20260531234157`。
- 腾讯云后端已更新并重启，`xiaohebao-backend.service` 状态为 `active`。
- 版本清单已更新：`versionCode=20`、`versionName=0.1.19`、`apkUrl=https://xhbadmin.juxin.pro/download/xiaohebao.apk`、`sha256=d50a0803eec8e25b86ed218d62497c5b2cb5fd689dc318ddfde5ae5d63ed09dd`。
- 公网固定下载 APK 包信息为 `versionCode=20`、`versionName=0.1.19`，SHA256 与本地一致。
- 未登录访问生产 `https://xhbadmin.juxin.pro/api/admin/stats` 返回 `401`，说明路由可达且仍受鉴权保护。

## 操作步骤

- 对照回款 Tab 统计和列表查询链路，确认“当前逾期”和“催收阶段逾期”口径混用。
- 修改后端回款统计与列表 Scope 处理逻辑。
- 修改安卓回款 Tab 顶部右侧卡片文案、数值格式和筛选 Scope。
- 补充后端回归测试，覆盖逾期统计与回款页已逾期列表不再使用 `datediff` 阈值。
- 执行后端测试、后端启动验证和安卓 debug/release 构建验证。
- 备份腾讯云旧版本，上传后端修正文件和新版 APK，更新版本清单。
- 从公网固定下载地址下载 APK 并核验版本、哈希。

# 2026-05-31 23:11:43

## 修改说明

- 落实安卓 App 后续建议第一项：平账弹窗新增 `实际还款日期`，默认当天，可点选日期，并随平账请求提交给后端用于冻结逾期费计算口径。
- 落实安卓 App 后续建议第二项：后端 `/api/admin/loans` 与 `/api/admin/repayment-stats` 新增 `due_date_start`、`due_date_end` 应还款日期范围参数。
- 安卓回款 Tab 的日期范围查询改为后端筛选，不再只在当前页数据上做端侧过滤。
- 回款 Tab 顶部统计在选择日期范围后同步使用同一组后端筛选参数，列表与卡片统计口径保持一致。
- 安卓版本递增为 `0.1.18(19)` 并发布到腾讯云固定下载地址。

## 修改后的代码

- `backend/app/api/endpoints/admin.py`
- `backend/tests/test_admin_loans_filters.py`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `android_native/app/src/main/AndroidManifest.xml`
- `doc/work_result/20260531.md`

## 单元测试代码

- `backend/tests/test_admin_loans_filters.py`
- `backend/tests/test_finance_other_fee.py`

## 集成测试代码

- 本次未新增独立集成测试文件；使用现有后端接口单元测试覆盖新增日期范围条件、平账实际还款日期口径。

## 测试执行结果

- 后端测试：`pytest backend/tests/test_admin_loans_filters.py backend/tests/test_finance_other_fee.py`，结果 `8 passed`。
- 本地服务启动：`python -m app.main` 可启动 Uvicorn 并正常关闭。
- 安卓 debug 构建：`cd android_native && ./build_apk.sh debug` 通过。
- 安卓 release 构建：`cd android_native && ./build_apk.sh release` 通过。
- APK 包信息：`versionCode=19`，`versionName=0.1.18`。
- APK 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 release APK SHA256：`3da6e562d91142d1d54ead0ab35435bff8a29e178eeeb380356e04e319cea941`。
- 已备份生产旧 APK、下载页、版本清单到 `/opt/xiaohebao/backups/android-apk-20260531230942`。
- 已备份生产后端接口文件到 `/opt/xiaohebao/backups/backend-admin-endpoint-20260531230942`。
- 腾讯云后端已更新并重启，`xiaohebao-backend.service` 状态为 `active`。
- 版本清单已更新：`versionCode=19`、`versionName=0.1.18`、`apkUrl=https://xhbadmin.juxin.pro/download/xiaohebao.apk`、`sha256=3da6e562d91142d1d54ead0ab35435bff8a29e178eeeb380356e04e319cea941`。
- 公网下载 APK 包信息为 `versionCode=19`、`versionName=0.1.18`，SHA256 与本地一致。
- 未携带 token 访问新增日期参数接口返回 `401 Not authenticated`，说明路由可达且仍受鉴权保护。

## 操作步骤

- 修改后端查询参数与日期范围过滤。
- 修改安卓回款日期范围查询与统计请求参数。
- 修改安卓平账弹窗，补充实际还款日期字段。
- 执行后端测试、本地服务启动、安卓 debug/release 构建。
- 备份生产旧 APK 和后端接口文件。
- 上传新版后端接口文件、新版 APK，更新版本清单并重启腾讯云后端。
- 从公网下载 APK 并核验版本、哈希。

# 2026-05-31 22:45:05

## 修改说明

- 深入审查安卓 App 当前五个业务 Tab、详情页、顶部统计、操作按钮、弹窗交互和 App 内更新机制。
- 新增审查记录 `doc/android_native_audit_20260531.md`，记录运营视角发现的问题、已处理项和后续建议。
- 发卡 Tab 顶部第二张卡片从“今日发卡额”改为“可用卡池”，展示可用卡数量，并在说明中展示可用卡池金额。
- 平账 Tab 顶部第一张卡片从无效的 `--` 改为“待平账订单”，第二张卡片保留已收金额，并补充其他费用说明。
- 申请 Tab 操作按钮按订单状态收口：只有审核中订单展示审批通过、审批拒绝、调授信、增额动作。
- 回款 Tab 操作按钮按订单状态和还款申请次数收口，避免不适合处理的订单继续展示确认还款、展期、增额等按钮。
- 平账 Tab 仅对待付款、已逾期订单展示平账、结清、展期动作；无动作时显示“当前状态暂无可执行操作”。
- 移除档案详情中的无效“刷新档案”动作。
- 增强 App 内升级安全：下载 APK 前校验 HTTPS 同域来源，下载后校验包名、版本号和可选 SHA256。
- 优化升级权限流程：如果用户需要先允许安装未知应用，返回 App 后继续调起系统安装确认页。
- 安卓版本递增为 `0.1.17(18)` 并发布到腾讯云固定下载地址。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `android_native/app/src/main/AndroidManifest.xml`
- `doc/android_native_audit_20260531.md`
- `doc/work_result/20260531.md`

## 单元测试代码

- 本次主要为原生 Android UI/交互/更新流程调整，当前原生 Android 目录没有 JVM 单元测试框架，未新增测试文件。

## 集成测试代码

- 本次未修改后端接口逻辑，未新增后端集成测试。

## 测试执行结果

- 安卓 debug 构建：`cd android_native && ./build_apk.sh debug` 通过。
- 安卓 release 构建：`cd android_native && ./build_apk.sh release` 通过。
- APK 包信息：`versionCode=18`，`versionName=0.1.17`。
- APK 权限校验：包含 `INTERNET` 与 `REQUEST_INSTALL_PACKAGES`。
- APK 签名校验：`apksigner verify --verbose` 通过，v1/v2/v3 签名均有效。
- 本地 release APK SHA256：`4e722db86ae5303f1ea979eae1a494a2d4cbb92445823e287ee8efb21bd82889`。
- 已备份生产旧 APK、下载页和版本清单到 `/opt/xiaohebao/backups/android-apk-20260531224323`。
- 新版 APK 已上传到固定下载地址：`https://xhbadmin.juxin.pro/download/xiaohebao.apk`。
- 版本清单已更新：`versionCode=18`、`versionName=0.1.17`、`apkUrl=https://xhbadmin.juxin.pro/download/xiaohebao.apk`、`sha256=4e722db86ae5303f1ea979eae1a494a2d4cbb92445823e287ee8efb21bd82889`。
- 公网固定下载地址返回 `200 OK`，文件名仍为 `xiaohebao.apk`，无后缀。
- 公网下载 APK 包信息为 `versionCode=18`、`versionName=0.1.17`，SHA256 与本地一致。

## 操作步骤

- 阅读安卓 App 入口、五个业务 Tab、详情页、风控报告卡片、操作弹窗、更新下载与安装逻辑。
- 对照后端统计字段和现有生产发布记录，梳理运营侧不匹配点。
- 按最小改动原则修正顶部统计、按钮状态边界、升级包校验与安装续接。
- 构建 debug 和 release APK。
- 备份云端旧版本，上传新版 APK，更新版本清单。
- 从公网下载 APK 并核验版本、哈希和 HTTP 响应头。

# 2026-04-26 23:11:45

## 修改说明（做了什么、为什么）
- 修复 `frontend_admin` 调用 `POST /api/admin/loans/125/disburse` 返回 500。
- 根因：卡池查询使用 `scalar_one_or_none()`，当存在多条满足条件的可用卡时抛 `MultipleResultsFound`，导致接口 500。
- 最小改动：改为按原排序规则取第一条卡记录（`scalars().first()`）。

## 修改后的代码
- `backend/app/api/endpoints/admin.py`
  - `_disburse_loan` 中 `ecard_item` 查询结果处理从 `scalar_one_or_none()` 改为 `scalars().first()`。

## 单元测试代码
- 本次未新增测试文件（接口行为通过后端全量测试+真实联调验证）。

## 集成测试代码（如适用）
- 后端 + 前端联调验证该接口：
  - 登录后台账号获取 token
  - 通过前端代理调用 `POST /api/admin/loans/125/disburse`。

## 测试执行结果或执行方式
- `cd backend && pytest -q` -> `11 passed`
- 实际联调：
  - 启动后端：`uvicorn app.main:app --host 0.0.0.0 --port 8001 --no-access-log`
  - 启动前端：`cd frontend_admin && pnpm dev`
  - 请求：`POST http://127.0.0.1:2002/api/admin/loans/125/disburse`（`term_days=14`）
  - 结果：`HTTP 200`，返回 `{"msg":"发卡成功", ...}`。

## 操作步骤
1. 复现 `/api/admin/loans/125/disburse` 报错并抓取 traceback。
2. 定位到卡池查询 `scalar_one_or_none()` 引发 `MultipleResultsFound`。
3. 最小修改为 `scalars().first()`。
4. 执行后端测试与前后端联调验证。
# 2026-04-26 22:46:17

## 修改说明（做了什么、为什么）
- 处理 `api/admin/users`、`api/admin/loans?scope=REVIEWING...`、`api/admin/stats` 间歇性报错/无响应问题。
- 根因1（直接导致 500）：去除外键后，`/users` 与 `/loans` 中的隐式 `join` 失去推断依据，SQLAlchemy 抛 `InvalidRequestError`。
- 根因2（导致“有时无返回/很慢”）：`/stats`、`/loans` 读接口内执行了分配写操作并 `flush`，与调度任务 `process_overdue_loans` 并发时产生锁等待，表现为请求长时间卡住（日志只有 request_data，延后很久才 response_data，前端报网络异常）。

## 修改后的代码
- `backend/app/api/endpoints/admin.py`
  - `get_loans`：`.join(User, Loan.user_id == User.id)` 显式 join 条件。
  - `get_users`：`.outerjoin(Channel, User.source_channel_id == Channel.id)` 显式 join 条件。
  - `get_stats`、`get_loans`：移除请求路径中的分配写入逻辑（只读接口不再做分配与 flush）。

## 单元测试代码
- 复用现有后端测试集（本次未新增测试文件）。

## 集成测试代码（如适用）
- 实际接口循环压测（后端直连）：连续 8 轮调用三接口，全部 `200`。
- 前后端联调（后端 + frontend_admin）：通过前端代理地址请求三接口，全部 `200`。

## 测试执行结果或执行方式
- `cd backend && pytest -q` -> `11 passed`
- 临时服务复测（后端）：
  - `/api/admin/users?skip=0&limit=20`：8轮均 `200`，约 `0.68-0.74s`
  - `/api/admin/loans?scope=REVIEWING&status=REVIEWING&skip=0&limit=10`：8轮均 `200`，约 `0.58-0.63s`
  - `/api/admin/stats`：8轮均 `200`，约 `0.67-0.70s`
- 启动服务端、前端联调：
  - 后端：`uvicorn app.main:app --host 0.0.0.0 --port 8001`
  - 前端：`cd frontend_admin && pnpm dev`
  - 代理请求三接口均返回 `HTTP 200`

## 操作步骤
1. 复现三接口异常与无响应现象，抓取后端完整 traceback。
2. 定位 SQLAlchemy join 歧义与读接口写入锁等待两个根因。
3. 实施最小修复（显式 join + 移除读接口写入）。
4. 执行后端测试与前后端联调验证。
# 2026-04-26 22:36:01

## 修改说明（做了什么、为什么）
- 针对持续出现的 `MissingGreenlet: greenlet_spawn has not been called` 做系统性修复，目标是减少/消除隐式懒加载触发的异步 IO。
- 修复分为三层：
  1. 模型关系默认改为 `lazy="selectin"`，避免默认 `lazy=select` 在属性访问时触发隐式查询。
  2. `admin` 接口中所有 `joinedload(集合关系)` 的结果读取统一补 `unique()`，避免 SQLAlchemy 2.x 结果集异常。
  3. `loan_ledger` / `loan_amounts` 的关键同步逻辑改为优先读取已加载关系（`__dict__`），避免在同步函数里触发关系懒加载。

## 修改后的代码
- `backend/app/models/loan.py`
- `backend/app/models/user.py`
- `backend/app/models/channel.py`
- `backend/app/models/loan_installment.py`
- `backend/app/models/loan_transaction.py`
- `backend/app/models/user_event.py`
- `backend/app/api/endpoints/admin.py`
- `backend/app/services/loan_ledger.py`
- `backend/app/services/loan_amounts.py`

## 单元测试代码
- 使用现有测试与新增测试共同验证：
  - `backend/tests/test_admin_project_cash_insights.py`
  - `backend/tests/test_loan_status_async_loading.py`

## 集成测试代码（如适用）
- 通过实际调用服务逻辑链路验证：
  - 手动触发 `process_overdue_loans()`
  - 启动后端应用验证启动链路

## 测试执行结果或执行方式
- `cd backend && pytest -q` -> `11 passed`
- `cd backend && python - <<'PY' ... await process_overdue_loans() ... PY` -> 输出 `process_overdue_loans: ok`
- 服务启动校验：`cd backend && python -m app.main`，日志包含 `Application startup complete` 与 `Application shutdown complete`

## 操作步骤
1. 扫描后端关系访问与 `joinedload` 使用点。
2. 统一配置关系加载策略为 `selectin`。
3. 修复 `joinedload(集合)` 对应结果读取的 `unique()` 要求。
4. 修复同步函数里的关系读取，避免隐式懒加载。
5. 执行测试、任务链路校验、服务启动校验。
# 2026-04-26 22:04:11

## 修改说明（做了什么、为什么）
- 修复 `GET /api/admin/project-cash-insights` 返回 500 的问题。
- 根因：查询使用了 `joinedload(Loan.installments)`（集合关系），在 SQLAlchemy 2.x 中结果集必须先 `unique()` 再 `scalars().all()`，否则抛 `InvalidRequestError`。
- 采用最小改动：仅在该接口的查询结果处理中补 `unique()`。

## 修改后的代码
- `backend/app/api/endpoints/admin.py`

## 单元测试代码
- 新增：`backend/tests/test_admin_project_cash_insights.py`
- 用例：`test_project_cash_insights_should_call_unique_before_scalars_all`

## 集成测试代码（如适用）
- 使用真实服务联调验证（后端 + 前端代理）

## 测试执行结果或执行方式
- 后端单测：`cd backend && pytest -q tests/test_admin_project_cash_insights.py` -> `1 passed`
- 后端全量：`cd backend && pytest -q` -> `11 passed`
- 实际联调（使用提供账号）：
  - 启动后端：`uvicorn app.main:app --host 0.0.0.0 --port 8001 --no-access-log`
  - 启动前端：`cd frontend_admin && pnpm dev`（2002）
  - 登录：`POST /api/admin/login`（`xiaojiang` / `Cn12345678`）成功拿到 token
  - 直连后端请求：`GET http://127.0.0.1:8001/api/admin/project-cash-insights` -> `HTTP 200`
  - 前端代理请求：`GET http://127.0.0.1:2002/api/admin/project-cash-insights` -> `HTTP 200`

## 操作步骤
1. 复现接口 500 并抓取后端堆栈。
2. 定位 `joinedload(collection)` + `.scalars().all()` 缺少 `.unique()`。
3. 最小修改接口实现并补回归测试。
4. 启动后端、前端进行真实账号联调并验证通过。
# 2026-04-26 21:57:22

## 修改说明（做了什么、为什么）
- 修复 `frontend_admin` 启动时报错 `Failed to resolve import "@element-plus/icons-vue"`。
- 原因是 `src/main.js` 引用了 `@element-plus/icons-vue`，但依赖声明缺失。
- 最小改动：仅补充依赖，不改业务代码。

## 修改后的代码
- `frontend_admin/package.json`
- `frontend_admin/pnpm-lock.yaml`

## 单元测试代码
- 本次未新增单元测试（仅依赖声明修复）。

## 集成测试代码（如适用）
- 本次未新增集成测试（仅依赖声明修复）。

## 测试执行结果或执行方式
- 执行：`cd frontend_admin && pnpm dev`
- 结果：Vite 启动成功，输出 `VITE ... ready`，页面可正常加载到开发服务器。

## 操作步骤
1. 读取报错并定位到 `src/main.js` 的 `@element-plus/icons-vue` 导入。
2. 执行 `pnpm add @element-plus/icons-vue` 补齐依赖。
3. 启动 `pnpm dev` 验证启动成功。
# 2026-04-26 21:45:59

## 修改说明（做了什么、为什么）
- 按“方案2”修复 APScheduler 场景的 `MissingGreenlet`：在 `ensure_installment_records_async` 中不再通过 `loan.installments` 触发关系懒加载，改为显式查询 `loan_installments`。
- 为避免调度任务内部再次触发懒加载，`process_overdue_loans` 改为使用 `ensure_installment_records_async` 返回的 `installments`，不直接读 `loan.installments`。
- 在调度任务查询中增加 `selectinload(Loan.owner)`，避免 `loan.owner` 在事件写入时出现隐式懒加载。

## 修改后的代码
- `backend/app/services/loan_ledger.py`
- `backend/app/services/scheduler.py`

## 单元测试代码
- 复用现有测试：`backend/tests/test_loan_status_async_loading.py`

## 集成测试代码
- 复用现有测试：`backend/tests/test_loan_status_async_loading.py`

## 测试执行结果或执行方式
- `cd backend && pytest -q tests/test_loan_status_async_loading.py` -> `2 passed`
- `cd backend && pytest -q` -> `10 passed`
- 服务启动校验：
  - `python -m app.main` 首次失败：`[Errno 48] Address already in use`（8001 端口被占用）
  - 改用临时端口校验：`uvicorn app.main:app --host 0.0.0.0 --port 8011 --reload --no-access-log`
  - 结果：启动和关闭均正常，日志包含 `Application startup complete`。

## 操作步骤
1. 定位报错链路到 `ensure_installment_records_async` 的关系懒加载。
2. 将分期读取改为显式 SQL 查询。
3. 调整调度任务避免访问懒加载关系属性。
4. 执行测试并完成服务启动验证。
# 2026-04-26 21:30:43

## 修改说明（做了什么、为什么）
- 修复 `GET /api/loan/status` 在异步会话下访问 `loan.installments` 触发懒加载导致 `MissingGreenlet` 的问题。
- 在 `get_latest_loan_async` 查询中增加关系预加载：`installments`、`review_admin`、`collection_admin`，避免在同步序列化流程触发异步 IO。
- 根据你“去除外键相关约束”的要求，移除模型中的 `ForeignKey(...)` 声明，并通过 `relationship(primaryjoin + foreign_keys)` 保持 ORM 关联能力。

## 修改后的代码（文件）
- `backend/app/services/loan_flow.py`
- `backend/app/models/loan.py`
- `backend/app/models/user.py`
- `backend/app/models/channel.py`
- `backend/app/models/loan_installment.py`
- `backend/app/models/loan_transaction.py`
- `backend/app/models/user_event.py`

## 单元测试代码
- 新增 `backend/tests/test_loan_status_async_loading.py`
- 用例：`test_get_latest_loan_async_should_eager_load_relations`

## 集成测试代码
- 文件：`backend/tests/test_loan_status_async_loading.py`
- 用例：`test_loan_status_should_return_success_when_installments_exists`
- 覆盖链路：`/api/loan/status -> get_or_create_latest_loan -> sync_loan_repayment_state -> serialize_loan_snapshot`

## 测试执行结果
- `cd backend && pytest -q tests/test_loan_status_async_loading.py` -> `2 passed`
- `cd backend && pytest -q` -> `10 passed`
- 服务启动校验（按仓库要求）：
  - 执行：`cd backend && python -m app.main`
  - 结果：服务成功启动并正常关闭（日志包含 `Application startup complete` 与 `Application shutdown complete`）

## 操作步骤
1. 定位报错调用链，确认问题由异步场景下关系懒加载触发。
2. 在贷款查询中增加预加载，避免同步序列化时触发异步 DB IO。
3. 去除模型 `ForeignKey(...)` 声明，并改为显式关系连接条件。
4. 增加回归测试并执行全部后端测试。
5. 启动服务端进行启动验证。

# 2026-04-27 23:54:23

## 修改说明（做了什么、为什么）
- 新增“手机号短信验证码登录”能力，采用 OAuth2.0 简化模式：登录下发 `access_token + refresh_token`（JWT），并在服务端持久化 `client/access_token/refresh_token`。
- 新增短信下发 mock 与安全防护：
  - mock 由配置控制（`SMS_CODE_MOCK_ENABLED`），mock 模式验证码固定为 `635147`；
  - 手机号维度 60 秒冷却（内存态）；
  - IP 维度 1 分钟令牌桶限流（默认 10 次/分钟，可配置）；
  - 增加过期状态清理，避免内存泄漏。
- 前端 `frontend_h5` 登录页改造为“点击按钮发送短信验证码 + 60 秒倒计时禁用按钮”，并将验证码输入从 4 位改为 6 位。
- 按你的新增要求先完成 DDL 设计并落库执行：将 `oauth_clients`、`oauth_tokens` 追加到 `doc/ddl.sql` 末尾，并已连接 MySQL 执行成功。

## 修改后的代码
- 后端
  - `backend/app/api/endpoints/auth.py`
    - `send-code` 改为短信发送接口（mock 可配置）
    - `login` 改为 6 位短信验证码登录，签发 access/refresh token 并持久化
    - 新增 `refresh` 刷新令牌接口
  - `backend/app/api/deps.py`
    - 用户鉴权新增“access token 持久化记录”校验（`jti + token + revoked_at`）
  - `backend/app/core/config.py`
    - 新增短信与 refresh token 配置项
  - `backend/app/core/security.py`
    - `create_access_token` 支持 `typ/jti/cid`
    - 新增 `create_refresh_token`
  - `backend/app/models/oauth_client.py`（新增）
  - `backend/app/models/oauth_token.py`（新增）
  - `backend/app/models/__init__.py`
    - 注册新模型
  - `backend/app/schemas/user.py`
    - 登录验证码改为 6 位
    - Token 响应新增 `refresh_token` 与过期时间字段
    - 新增 `RefreshTokenRequest`
  - `backend/app/services/sms_auth.py`（新增）
    - 手机号冷却、IP 令牌桶、验证码校验与过期清理
- 前端（`frontend_h5`）
  - `frontend_h5/src/views/Login.vue`
    - 发送验证码按钮改为请求后端接口
    - 按钮在请求中与冷却期内禁用，冷却中显示倒计时
    - 验证码输入改为 6 位
    - 登录后额外保存 `refresh_token`
  - `frontend_h5/src/utils/smsLogin.js`（新增）
    - 短信登录交互工具函数
- DDL
  - `doc/ddl.sql`
    - 追加 `oauth_clients`、`oauth_tokens`（含表注释/字段注释）

## 单元测试代码
- 后端新增
  - `backend/tests/test_sms_auth_manager.py`
  - `backend/tests/test_auth_sms_send_code.py`
- 前端新增（Vitest）
  - `frontend_h5/src/utils/smsLogin.test.js`

## 集成测试代码（适用）
- 后端启动：`cd backend && python -m app.main`
- 前端启动：`cd frontend_h5 && pnpm dev`
- 接口联调（示例）：
  - `POST /api/auth/send-code`
  - `POST /api/auth/login`
  - `POST /api/auth/refresh`
  - 经前端代理：`http://127.0.0.1:2003/api/auth/*`

## 测试执行结果
- 后端测试通过：
  - `cd backend && pytest -q tests/test_sms_auth_manager.py tests/test_auth_sms_send_code.py tests/test_loan_status_ws.py tests/test_admin_stats_ws.py`
  - 结果：`5 passed`
- 前端测试通过：
  - `cd frontend_h5 && pnpm test`
  - 结果：`2 files passed, 5 tests passed`
- DDL 已执行成功：
  - 已在 MySQL 创建 `oauth_clients`、`oauth_tokens` 表（`SHOW TABLES LIKE 'oauth_%'` 可见）
- 实际联调通过：
  - 短信发送成功，重复发送命中 429 冷却限制
  - 使用 `635147` 登录成功并返回 `access_token + refresh_token`
  - `refresh_token` 可成功刷新
  - `oauth_clients/oauth_tokens` 表中可查询到持久化记录

## 操作步骤记录
1. 阅读现有 `auth/JWT/frontend_h5` 登录流程，确定最小改造点。
2. 设计短信登录 DDL 并追加 `doc/ddl.sql`。
3. 连接 MySQL 执行新增 DDL，确认新表创建成功。
4. 完成后端短信限流、验证码校验、token 签发与持久化改造。
5. 完成前端短信发送按钮禁用与 60 秒倒计时改造。
6. 增加后端与前端单元测试并执行通过。
7. 启动前后端进行接口实测，验证功能链路。
# 2026-04-27 18:59:32

## 修改说明（做了什么、为什么）
- 问题：`/api/loan/ws/status` 报 `MissingGreenlet`，栈中触发点在 `loan_status_ws -> get_latest_loan_snapshot_async(db, current_user.id)`。
- 根因：上一版为了避免旧事务快照，在循环里加入了 `rollback`。但 `rollback` 会使会话中的 ORM 实例过期，再访问 `current_user.id` 时触发属性懒加载，进而在非 greenlet 上下文报 `MissingGreenlet`。
- 修复：在进入循环前先把 `current_user.id` 提前提取为纯值 `current_user_id`，循环中只使用该纯值，不再访问已过期 ORM 对象。

## 修改后的代码
- `backend/app/api/endpoints/loan.py`
  - `loan_status_ws` 中新增 `current_user_id = int(current_user.id)`。
  - 循环内将 `get_latest_loan_snapshot_async(db, current_user.id)` 改为 `get_latest_loan_snapshot_async(db, current_user_id)`。

## 单元测试代码
- 复用现有：`backend/tests/test_loan_status_ws.py`、`backend/tests/test_admin_stats_ws.py`

## 集成测试代码（适用）
- 启动后端：`cd backend && python -m app.main`
- 启动前端：`cd frontend_h5 && pnpm dev`
- 使用账号 `15088790007` 通过 `send-code/login` 获取 token，连接：
  - `ws://127.0.0.1:2001/api/loan/ws/status?token=...`

## 测试执行结果
- `cd backend && pytest -q tests/test_loan_status_ws.py tests/test_admin_stats_ws.py` -> `2 passed`
- 实际联调输出：
  - `ws_open_h5_2001`
  - `msg_1 loan_snapshot DISBURSED 126`
  - `msg_2 loan_snapshot DISBURSED 126`
  - `ws_h5_ok`
- 服务端日志中未再出现该次 `MissingGreenlet` 异常。

## 操作步骤记录
1. 依据报错栈定位到 `rollback` 后访问 ORM 过期属性。
2. 改为在循环外缓存 `current_user_id` 纯值。
3. 执行后端回归测试并通过。
4. 启动后端 + H5 前端，使用指定账号完成 WS 实测。
# 2026-04-27 18:52:36

## 修改说明（做了什么、为什么）
- 问题：`frontend_admin` 在“待发卡”确认发卡成功后，`frontend_h5` 的 `/api/loan/ws/status` 未及时返回新状态。
- 根因：`/api/loan/ws/status` 是长连接循环查询，复用了同一个 `AsyncSession`。在 MySQL 默认事务隔离级别下，若不结束上一轮事务，后续读取可能持续命中旧事务快照，导致状态看起来“不变”。
- 修复：在每轮 WS 推送前先结束上一轮事务（`rollback`），确保读取最新状态；同时保留测试桩兼容（仅在 `db` 存在 `rollback` 时调用）。

## 修改后的代码
- `backend/app/api/endpoints/loan.py`
  - `loan_status_ws` 循环中新增“每轮查询前 rollback”逻辑，防止长连接事务快照陈旧。

## 单元测试代码
- 复用现有：`backend/tests/test_loan_status_ws.py`

## 集成测试代码（适用）
- 本次以后端 WS 单元回归为主；实服联调需在你现网流程下验证“发卡后下一帧 ws 状态更新”。

## 测试执行结果
- `cd backend && pytest -q tests/test_loan_status_ws.py tests/test_admin_stats_ws.py` -> `2 passed`

## 操作步骤记录
1. 根据现象定位到 `loan_status_ws` 长连接循环查询。
2. 分析事务快照导致的陈旧读风险并实施最小修复。
3. 处理测试桩兼容后回归测试通过。
# 2026-04-27 14:40:07

## 修改说明（做了什么、为什么）
- 目标：确认并修正 `frontend_h5`、`frontend_admin` 在“未登录/登录过期”与“WebSocket 鉴权失败”场景下的行为。
- 结论与修正：
  - 未登录：两个项目原有路由守卫已会跳转登录页（保留）。
  - 登录过期（HTTP）：两个项目原有 axios 401 拦截器已会清理 token 并跳转登录（保留）。
  - 登录过期（WS）：
    - `frontend_h5`：原先 1008 时默认停止重连但不一定跳登录；已改为清理 token 并默认跳登录。
    - `frontend_admin`：原先 WS 断开统一走重连；已改为 1008 时清理登录态并跳登录，且不再重连。

## 修改后的代码
- `frontend_h5/src/api/loanSocket.js`
  - WebSocket `onclose` 收到 `1008` 时：`stopped=true`、清理 `token`、触发 `onAuthFailed` 或默认跳转 `/login`。
- `frontend_admin/src/views/Layout.vue`
  - 新增 `handleStatsSocketAuthFailed`。
  - `statsSocket.onclose` 收到 `1008` 时执行清理登录态并跳转 `/login`，不再触发重连。
- `frontend_h5/src/api/loanSocket.test.js`
  - 新增用例：验证 `1008` 场景会清理 token、触发 auth failed 回调、且不会创建新的重连连接。

## 单元测试代码
- `frontend_h5/src/api/loanSocket.test.js`
  - `should stop reconnecting and clear token when auth failed with 1008`

## 集成测试代码（适用）
- 本次以构建与前端单元测试为主，未新增后端集成逻辑。

## 测试执行结果
- `cd frontend_h5 && pnpm test` 通过（`2 passed`）
- `cd frontend_h5 && pnpm build` 通过
- `cd frontend_admin && pnpm build` 通过

## 操作步骤记录
1. 审核两个项目路由守卫与 401 拦截器行为。
2. 审核 WS 断线重连逻辑，定位 1008 场景处理差异。
3. 对 `frontend_h5` 与 `frontend_admin` 分别补齐 1008 场景处理。
4. 增补 H5 单元测试并执行构建验证。
# 2026-04-27 14:31:40

## 修改说明（做了什么、为什么）
- 目标：解决 `frontend_h5` 持续轮询 `api/loan/status`、`api/loan/bill` 导致的高频请求问题，改为 WebSocket 实时推送。
- 后端新增用户侧 WebSocket：`/api/loan/ws/status`，按固定间隔推送贷款快照（含账单字段）。
- `bill` 是否需要实时刷新评估结论：需要。原因是账单页面核心状态（`WITHDRAWING -> DISBURSED -> OVERDUE/SETTLED`）和分期明细会在后台异步变化，用户在账单页停留时应即时感知。
- 为避免异步懒加载触发 `MissingGreenlet`，后端快照查询统一预加载 `installments`，并在 WS 发送前使用 `jsonable_encoder` 处理 `datetime` 序列化。
- 前端 `Home/Orders/Profile/Review/Bill` 统一改为订阅 WS，不再使用 2~3 秒轮询。

## 修改后的代码
- 后端
  - `backend/app/api/deps.py`
    - 新增 `get_user_by_token_async`，复用 H5 token 解码与用户查询逻辑。
  - `backend/app/api/endpoints/loan.py`
    - 新增 `LOAN_STATUS_WS_PUSH_SECONDS`
    - 新增 `get_latest_loan_snapshot_async`（预加载 `installments`）
    - 新增 `/ws/status` WebSocket 推送路由
    - `/status` 与 `/bill` 改为复用快照函数
- 前端
  - `frontend_h5/src/api/loanSocket.js`（新增）
    - 新增统一订阅器 `createLoanSnapshotSubscriber`
    - 内置断线重连（3s）与 1008 鉴权失败处理
  - `frontend_h5/src/views/Home.vue`
  - `frontend_h5/src/views/Orders.vue`
  - `frontend_h5/src/views/Profile.vue`
  - `frontend_h5/src/views/Review.vue`
  - `frontend_h5/src/views/Bill.vue`
    - 以上页面均改为 WS 订阅快照，移除轮询逻辑
  - `frontend_h5/vite.config.js`
    - `/api` 代理新增 `ws: true`

## 单元测试代码
- 后端新增：`backend/tests/test_loan_status_ws.py`
  - 用例：`test_loan_status_ws_should_push_snapshot_payload`
  - 覆盖：WS 建连后推送 `loan_snapshot` 消息结构
- 前端新增（Vitest）：`frontend_h5/src/api/loanSocket.test.js`
  - 覆盖：订阅器收到 `loan_snapshot` 消息后触发回调

## 集成测试代码（适用）
- 本次为后端 + 前端实启联调：
  - `cd backend && python -m app.main`
  - `cd frontend_h5 && pnpm dev`
  - 通过 `send-code/login` 获取 H5 token 后，连接：
    - `ws://127.0.0.1:2003/api/loan/ws/status?token=...`（本机 2001/2002 被占用，Vite 实际启动在 2003）

## 测试执行结果
- 后端：`cd backend && pytest -q tests/test_loan_status_ws.py tests/test_admin_stats_ws.py` -> `2 passed`
- 前端：`cd frontend_h5 && pnpm test` -> `1 passed`
- 前端构建：`cd frontend_h5 && pnpm build` -> 成功
- 实际联调：
  - `ws_open_h5_proxy`
  - `msg_1 loan_snapshot DISBURSED 125 true`
  - `msg_2 loan_snapshot DISBURSED 125 true`
  - `ws_h5_proxy_ok`

## 操作步骤记录
1. 排查 H5 轮询点，确认 `Home/Orders/Profile/Review/Bill` 为高频来源。
2. 新增后端用户侧 `loan ws` 路由并统一快照查询。
3. 将上述 5 个页面改为 WS 订阅，移除轮询。
4. 补充后端 + 前端单元测试（Vitest）。
5. 实际启动后端与 H5 前端联调，验证持续推送正常。
# 2026-04-27 11:58:06

## 修改说明（做了什么、为什么）
- 目标：将 `frontend_admin` 对 `api/admin/stats` 的高频轮询改为 WebSocket 实时推送，减少重复 HTTP 请求并实现即时状态感知。
- 后端新增后台统计 WS 通道：`/api/admin/ws/stats`，鉴权后每 5 秒推送一次 `admin_stats`。
- 前端 `Layout` 侧边栏角标改为优先消费 WS 消息，保留首次 HTTP 同步作为首屏兜底，断线自动重连（3 秒）。
- Vite 代理开启 `ws: true`，确保本地开发环境可通过 `/api` 代理 WebSocket。
- 联调中发现 WS 握手失败根因是运行环境缺少 `websockets/wsproto`；按最小改动补充 `backend/requirements.txt` 的 `websockets==12.0`。

## 修改后的代码（关键文件）
- `backend/app/api/deps.py`
  - 新增 `get_admin_by_token_async(db, token)`，并在鉴权流程中复用。
- `backend/app/api/endpoints/admin.py`
  - 新增 `ADMIN_STATS_WS_PUSH_SECONDS = 5`
  - 新增 `_extract_ws_token`、`_get_ws_admin_by_token`
  - 新增 `@router.websocket("/ws/stats")` 推送逻辑
- `frontend_admin/src/views/Layout.vue`
  - 新增 `connectStatsSocket/closeStatsSocket/scheduleStatsSocketReconnect`
  - 监听 `admin_stats` 消息更新菜单角标
  - 移除固定 30s 轮询定时器，改为 WS 驱动
- `frontend_admin/vite.config.js`
  - `/api` 代理新增 `ws: true`
- `backend/requirements.txt`
  - 新增 `websockets==12.0`

## 单元测试代码
- 新增：`backend/tests/test_admin_stats_ws.py`
  - 用例：`test_admin_stats_ws_should_push_stats_payload`
  - 覆盖：WS 建连后是否按协议推送 `{"type":"admin_stats","data":...}`

## 集成测试代码（适用）
- 本次主要为端到端联调验证：
  - 启动后端：`cd backend && python -m app.main`
  - 启动前端：`cd frontend_admin && pnpm dev`
  - 使用管理员账号登录获取 token 后，验证：
    - `ws://127.0.0.1:8001/api/admin/ws/stats?token=...`
    - `ws://127.0.0.1:2002/api/admin/ws/stats?token=...`
  - 两条链路均连续收到两帧 `admin_stats`。

## 测试执行结果
- `cd backend && pytest -q tests/test_admin_stats_ws.py` 通过（`1 passed`）
- `cd frontend_admin && pnpm build` 通过
- 端到端联调通过：
  - 直连后端 WS：`ws_open -> msg_1 -> msg_2 -> ws_ok`
  - 经前端代理 WS：`ws_open_proxy -> msg_1 -> msg_2 -> ws_proxy_ok`

## 操作步骤记录
1. 审核现有改动并确认 WS 路由/前端连接代码已落地。
2. 启动后端与前端进行联调。
3. 发现握手失败，定位为缺少 WebSocket 协议库。
4. 增加依赖 `websockets==12.0` 并安装后重启后端。
5. 重新验证后端直连和前端代理两条 WS 通路，确认实时推送正常。

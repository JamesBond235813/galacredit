# 2026-05-23 21:00:53

## 修改说明

- 按用户要求，将“非档案页手机号明文显示”的安卓原生版本发布到腾讯云生产下载地址。
- 为避免安卓手机因同版本覆盖失败，本次将原生 App 版本从 `0.1.7` 升级为 `0.1.8`，`versionCode` 从 `8` 升级为 `9`。
- 更新生产下载页缓存参数为 `v=202605232101`，确保手机浏览器下载到新包。

## 修改后的代码

- [AndroidManifest.xml](</Volumes/littlejiang02/VibeCoding/xiaohebao /android_native/app/src/main/AndroidManifest.xml>)
  - 版本更新为 `versionName="0.1.8"`、`versionCode="9"`。
- [android-download.html](</Volumes/littlejiang02/VibeCoding/xiaohebao /android_native/docs/android-download.html>)
  - 下载链接版本参数更新为 `v=202605232101`。
- `android_native/build/xiaohebao-release.apk`
  - 已重新构建并发布到腾讯云生产目录。

## 单元测试代码

- 本次为生产发布操作，未新增独立单元测试文件。

## 集成测试代码

- 本次通过正式包构建、线上文件哈希比对及 HTTP 下载验证完成发布校验；未新增独立集成测试文件。

## 测试结果

- 执行 `source ~/.xiaohebao/android/release-signing.env && ./android_native/build_apk.sh release` 成功。
- 本地 APK SHA256：
  - `bdc868cc6ed79c42cb0ee5d10471a9a9823da0c2da01a580e4148133f653d625`
- 生产 APK SHA256：
  - `bdc868cc6ed79c42cb0ee5d10471a9a9823da0c2da01a580e4148133f653d625`
- 生产下载页验证：
  - `https://xhbadmin.juxin.pro/download/android.html` 返回 `200 OK`。
  - 页面内 APK 链接已更新为 `/download/xiaohebao.apk?v=202605232101`。
- 生产 APK 链接验证：
  - `https://xhbadmin.juxin.pro/download/xiaohebao.apk?v=202605232101` 返回 `200 OK`。
  - 响应类型为 `application/vnd.android.package-archive`。

# 2026-05-23 20:56:42

## 修改说明

- 按用户要求，调整原生安卓 App 的敏感信息展示规则。
- 保持“档案”页手机号继续脱敏不变。
- 将“申请 / 发卡 / 回款 / 平账”等非档案页中的用户手机号改为明文显示，身份证号继续按现有明文逻辑展示。

## 修改后的代码

- [MainActivity.java](</Volumes/littlejiang02/VibeCoding/xiaohebao /android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java>)
  - 新增 `displayPhoneByTab`，统一按当前页面决定手机号是否脱敏。
  - 列表卡片与详情页头部的 `subtitle` 在非档案页改为显示明文手机号。
  - 非档案页详情“核心信息”中的手机号改为明文显示。

## 单元测试代码

- 本次为安卓前端展示逻辑微调，未新增独立单元测试文件。

## 集成测试代码

- 本次通过本地原生 APK 构建校验改动有效；未新增独立集成测试文件。

## 测试结果

- 执行 `./android_native/build_apk.sh debug` 成功。
- 生成调试安装包：
  - `android_native/build/xiaohebao-debug.apk`

# 2026-05-23 15:22:14

## 修改说明

- 按用户要求，对生产环境中误操作结清的孙俊楠订单进行数据恢复。
- 确认误结清订单为 `loans.id=185`，误操作新增还款流水为 `loan_transactions.id=31`，误操作新增事件为 `user_events.id=12186`。
- 将订单恢复为已发卡待还状态，清空本次误结清产生的已收/减免金额，并将分期台账恢复为未结清。

## 修改后的数据

- 生产订单 `185`
  - `status`: `SETTLED` 恢复为 `DISBURSED`
  - `repaid_amount`: `1600.00` 恢复为 `0.00`
  - `reduction_amount`: `0.00`
  - `remaining_repayment_amount`: 恢复为 `1600.00`
- 生产分期 `loan_installments.id=19`
  - `status`: `SETTLED` 恢复为 `PENDING`
  - `paid_amount`: `1600.00` 恢复为 `0.00`
  - `settled_at`: 已清空
- 删除误操作产生的生产流水 `loan_transactions.id=31`。
- 删除误操作产生的生产事件 `user_events.id=12186`。

## 单元测试代码

- 本次为生产数据修复操作，不涉及代码逻辑变更；未新增单元测试文件。

## 集成测试代码

- 本次通过生产数据库只读校验完成恢复验证；未新增独立集成测试文件。

## 测试结果

- 恢复后生产校验结果：
  - 订单 `185` 状态为 `DISBURSED`。
  - 已登记收款为 `0.00`，已登记减免为 `0.00`。
  - 剩余待还金额为 `1600.00`。
  - 分期 `19` 状态为 `PENDING`，已还金额为 `0.00`，结清时间为空。
  - 误结清流水 `31` 不再存在。
  - 误结清事件 `12186` 不再存在。

# 2026-05-23 13:44:14

## 修改说明

- 按用户要求，将本地已验证通过的原生 Android App `0.1.7` 发布到腾讯云生产下载目录。
- 上传新版 APK 到生产路径 `/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 更新生产下载页 `/data/www/xhbadmin.juxin.pro/dist/download/android.html`，将 APK 下载链接缓存参数更新为 `v=202605231337`，避免手机浏览器继续下载旧包。

## 修改后的代码

- `android_native/docs/android-download.html`
  - APK 下载链接版本参数更新为 `v=202605231337`。
- `android_native/build/xiaohebao-release.apk`
  - 已发布到腾讯云生产下载目录。

## 单元测试代码

- 本次为生产发布操作，不涉及业务逻辑代码新增；未新增单元测试文件。

## 集成测试代码

- 本次通过线上文件哈希、下载页内容和 HTTP 响应进行发布验证；未新增独立集成测试文件。

## 测试结果

- 本地 APK SHA256：
  - `6bb378e8f7f39b8dd321ce2f8d25337620bdc52ebafd7b55d1942efb43f5cbd3`
- 生产 APK SHA256：
  - `6bb378e8f7f39b8dd321ce2f8d25337620bdc52ebafd7b55d1942efb43f5cbd3`
- 生产下载页验证：
  - `https://xhbadmin.juxin.pro/download/android.html` 返回 `200 OK`。
  - 页面内 APK 链接已更新为 `/download/xiaohebao.apk?v=202605231337`。
- 生产 APK 链接验证：
  - `https://xhbadmin.juxin.pro/download/xiaohebao.apk?v=202605231337` 返回 `200 OK`。
  - 响应类型为 `application/vnd.android.package-archive`。

# 2026-05-23 13:36:54

## 修改说明

- 按用户要求继续完善原生 Android App。
- 登录页整体上移，并将登录内容放入可滚动容器；同时给 Activity 增加软键盘避让配置，键盘弹出时登录框不会被遮挡。
- 增加页面级缓存与详情缓存：同一筛选条件下返回已访问页面时直接使用缓存数据；点击刷新或执行业务操作后清空缓存并重新拉取。
- 申请页列表卡片改为业务字段展示：姓名、未脱敏手机号、渠道、申请时间、最新 IP、IP 位置、最新 GPS、GPS 位置；去掉待处理金额、还款日和空备注。
- 申请页卡片会补拉用户详情与 IP 审计数据，用于展示最新 IP/GPS 信息；位置解析失败时位置行保持空白。
- 申请页“审批通过”弹窗改为授信额度、减免额度、期限三个输入项，并提供关闭、提交两个按钮。
- 申请页“审批拒绝”弹窗改为审批备注输入框，并提供关闭、提交两个按钮。
- 申请页详情操作新增“审批备注”按钮，弹窗内填写审批意见后提交保存。
- App 版本升级为 `0.1.7`，`versionCode=8`。

## 修改后的代码

- `android_native/app/src/main/AndroidManifest.xml`
  - 增加 `android:windowSoftInputMode="adjustResize"`。
  - 版本更新为 `0.1.7` / `versionCode=8`。
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
  - 登录页改为可滚动上移布局。
  - 新增列表缓存、统计缓存、用户详情缓存。
  - 申请页新增独立卡片渲染逻辑。
  - 新增审批通过、审批拒绝、审批备注弹窗。
  - 业务操作完成后自动清空缓存并刷新页面。
- `android_native/build/xiaohebao-release.apk`
  - 已重新生成正式签名 APK。

## 单元测试代码

- 本次修改位于当前手写原生 Android 工程，项目暂未配置独立 Java 单元测试框架；未新增可运行的单元测试文件。

## 集成测试代码

- 本次通过真实 APK 构建、签名校验、模拟器安装、登录页键盘截图验证进行集成验证；未新增独立集成测试文件。

## 测试结果

- `source ~/.xiaohebao/android/release-signing.env && ./android_native/build_apk.sh release`
  - 结果：构建成功，输出 `android_native/build/xiaohebao-release.apk`。
- `~/Library/Android/sdk/build-tools/36.1.0/apksigner verify --verbose android_native/build/xiaohebao-release.apk`
  - 结果：签名校验通过，v1/v2/v3 均为 true。
- `adb install -r android_native/build/xiaohebao-release.apk`
  - 结果：安装成功。
- `adb shell dumpsys package com.juxin.xiaohebao.admin | rg "versionCode|versionName"`
  - 结果：`versionCode=8`，`versionName=0.1.7`。
- 模拟器页面验证：
  - 登录页内容位于屏幕中部偏上。
  - 点击输入框弹出键盘时，账号、密码、登录按钮未被键盘遮挡。
- APK SHA256：
  - `6bb378e8f7f39b8dd321ce2f8d25337620bdc52ebafd7b55d1942efb43f5cbd3`

# 2026-05-23 12:46:34

## 修改说明

- 按用户授权，将本地已验证通过的原生 Android App `0.1.6` 发布到腾讯云生产下载目录。
- 上传新版 APK 到生产路径 `/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 更新生产下载页 `/data/www/xhbadmin.juxin.pro/dist/download/android.html`，将 APK 下载链接缓存参数更新为 `v=202605231243`，避免手机浏览器继续下载旧包。

## 修改后的代码

- `android_native/docs/android-download.html`
  - APK 下载链接版本参数更新为 `v=202605231243`。
- `android_native/build/xiaohebao-release.apk`
  - 已发布到腾讯云生产下载目录。

## 单元测试代码

- 本次为生产发布操作，不涉及业务逻辑代码新增；未新增单元测试文件。

## 集成测试代码

- 本次通过线上文件哈希、下载页内容和 HTTP 响应进行发布验证；未新增独立集成测试文件。

## 测试结果

- 本地 APK SHA256：
  - `c75fb3f6dc5d3569e220d618e0d18e6ddfcee716e2fea3bbacf7a039c093c6ab`
- 生产 APK SHA256：
  - `c75fb3f6dc5d3569e220d618e0d18e6ddfcee716e2fea3bbacf7a039c093c6ab`
- 生产下载页验证：
  - `https://xhbadmin.juxin.pro/download/android.html` 返回 `200 OK`。
  - 页面内 APK 链接已更新为 `/download/xiaohebao.apk?v=202605231243`。
- 生产 APK 链接验证：
  - `https://xhbadmin.juxin.pro/download/xiaohebao.apk?v=202605231243` 返回 `200 OK`。
  - 响应类型为 `application/vnd.android.package-archive`。

# 2026-05-23 12:43:25

## 修改说明

- 按用户要求继续完善原生 Android App 页面交互。
- 将每个工作台页面顶部区域（标题、统计卡、搜索/筛选条件）从名单滚动区中拆出，保持固定不动；下方名单单独滚动。
- 将申请页、回款页、平账页等快捷筛选标签的选中态改为圆角蓝色色块，未选中态使用圆角浅边框，整体更柔和。
- 回款页日期输入框改为点击弹出原生日期选择器，不再直接弹键盘。
- 认证照片支持点击弹窗大图预览，弹窗内可点“上一张 / 下一张 / 关闭”，也可在图片区域左右滑动切换照片。
- App 版本升级为 `0.1.6`，`versionCode=7`。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
  - `showWorkspace(...)`：顶部固定区与名单滚动区拆分。
  - `segment(...)`：快捷标签按钮改为圆角背景。
  - `bindDatePicker(...)`、`showDatePicker(...)`：日期输入接入原生日期选择器。
  - `addPhotosSection(...)`、`photoBox(...)`、`showPhotoPreview(...)`：认证照片支持弹窗预览、按钮切换与左右滑动。
- `android_native/app/src/main/AndroidManifest.xml`
  - 版本更新为 `0.1.6` / `versionCode=7`。
- `android_native/build/xiaohebao-release.apk`
  - 已重新生成正式签名 APK。

## 单元测试代码

- 本次修改位于当前手写原生 Android 工程，项目暂未配置独立 Java 单元测试框架；未新增可运行的单元测试文件。

## 集成测试代码

- 本次通过真实 APK 构建、签名校验、模拟器安装、页面操作与截图验证进行集成验证；未新增独立集成测试文件。

## 测试结果

- `source ~/.xiaohebao/android/release-signing.env && ./android_native/build_apk.sh release`
  - 结果：构建成功，输出 `android_native/build/xiaohebao-release.apk`。
- `~/Library/Android/sdk/build-tools/36.1.0/apksigner verify --verbose android_native/build/xiaohebao-release.apk`
  - 结果：签名校验通过，v1/v2/v3 均为 true。
- `adb install -r android_native/build/xiaohebao-release.apk`
  - 结果：安装成功。
- `adb shell dumpsys package com.juxin.xiaohebao.admin | rg "versionCode|versionName"`
  - 结果：`versionCode=7`，`versionName=0.1.6`。
- 模拟器页面验证：
  - 档案页名单滑动时，顶部标题、统计卡、搜索框保持固定。
  - 认证照片点击后可弹出大图预览，弹窗内上一张、下一张、关闭按钮可见，大图加载成功。
- APK SHA256：
  - `c75fb3f6dc5d3569e220d618e0d18e6ddfcee716e2fea3bbacf7a039c093c6ab`

# 2026-05-23 12:20:14

## 修改说明

- 按用户要求继续调整原生 Android App 各页面顶部统计卡与搜索筛选区。
- 顶部两个水平统计卡高度由 `104dp` 降为 `76dp`，去掉底部蓝色说明文案，改为更轻的浅底统计卡样式。
- 档案页搜索保持“手机号 / 姓名 / 身份证”不变。
- 申请页去掉文本搜索，改为状态筛选：全部、审核中、已通过、未通过。
- 发卡页取消搜索筛选区。
- 回款页改为两行筛选：第一行为日期区间，第二行为逾期状态（全部、已逾期、未逾期）。
- 平账页保留当前文本搜索，并新增逾期状态（全部、已逾期、未逾期）筛选。
- 回款页移除旧的“当日还款 / 逾期催收”分段入口，避免与新的日期区间和逾期状态筛选重复。
- App 版本升级为 `0.1.5`，`versionCode=6`。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
  - 新增搜索筛选状态字段：申请状态、回款逾期状态、平账逾期状态、回款日期区间。
  - 新增 `addSearchControls(...)`、`addKeywordSearch(...)`、`addDateRangeSearch(...)`、`addOverdueFilter(...)`、`addStatusFilter(...)`。
  - 调整 `loadList(...)` 查询参数组装，不同页面按不同筛选条件请求数据。
  - 新增 `filterByRepaymentDate(...)` 与 `inRepaymentDateRange(...)`，用于回款页日期区间前端过滤。
  - 重新设计 `metricCard(...)` 顶部统计卡样式。
- `android_native/app/src/main/AndroidManifest.xml`
  - 版本更新为 `0.1.5` / `versionCode=6`。
- `android_native/build/xiaohebao-release.apk`
  - 已重新生成正式签名 APK。

## 单元测试代码

- 本次修改位于当前手写原生 Android 工程，项目暂未配置独立 Java 单元测试框架；未新增可运行的单元测试文件。

## 集成测试代码

- 本次通过真实 APK 构建、签名校验、模拟器安装、页面截图查看进行集成验证；未新增独立集成测试文件。

## 测试结果

- `source ~/.xiaohebao/android/release-signing.env && ./android_native/build_apk.sh release`
  - 结果：构建成功，输出 `android_native/build/xiaohebao-release.apk`。
- `~/Library/Android/sdk/build-tools/36.1.0/apksigner verify --verbose android_native/build/xiaohebao-release.apk`
  - 结果：签名校验通过，v1/v2/v3 均为 true。
- `adb install -r android_native/build/xiaohebao-release.apk`
  - 结果：安装成功。
- `adb shell dumpsys package com.juxin.xiaohebao.admin | rg "versionCode|versionName"`
  - 结果：`versionCode=6`，`versionName=0.1.5`。
- 模拟器页面验证：
  - 档案页顶部统计卡变矮，底部蓝色文字已去掉。
  - 档案页原搜索框保留。
  - 列表中的风险色块标签仍正常显示。
- 注意：
  - 当前生产 `/admin/loans` 接口尚未提供日期区间查询参数，因此回款页日期区间先在 App 端对已取回列表做本地过滤；后续腾讯云端改造时建议同步补齐后端日期区间参数。
- APK SHA256：
  - `cf257d8e36f2799f89039e93f5a186bd4d8b6e701c16b67d4a09f53e3d2b3251`

# 2026-05-23 10:58:53

## 修改说明

- 按用户要求调整原生 Android App 审核/详情页展示。
- 去掉非档案详情中重复的“订单信息”卡片，避免与“订单状态及审核批注”卡片重复展示订单状态、备注等信息。
- 将用户风险状态从姓名后方的纯文字拼接，改为姓名右侧的色块标签形式，更接近管理端展示方式。
- 已覆盖标签类型：黑名单、风险名单、风险地区、位置风控。
- 列表卡片和详情页标题均使用同一套风险标签展示逻辑；无命中状态时不展示冗余标签。
- App 版本升级为 `0.1.4`，`versionCode=5`。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
  - 详情页移除“订单信息”卡片渲染。
  - 新增 `addRiskTags(...)`、`riskLabels(...)`、`riskPill(...)`，用于在用户姓名右侧渲染彩色风险标签。
  - 列表卡片标题行与详情标题行均改为“姓名 + 风险标签 + 状态”的展示结构。
- `android_native/app/src/main/AndroidManifest.xml`
  - 版本更新为 `0.1.4` / `versionCode=5`。
- `android_native/build/xiaohebao-release.apk`
  - 已重新生成正式签名 APK。

## 单元测试代码

- 本次修改位于当前手写原生 Android 工程，项目暂未配置独立 Java 单元测试框架；未新增可运行的单元测试文件。

## 集成测试代码

- 本次通过真实 APK 构建、签名校验、模拟器安装、进入 App 页面进行集成验证；未新增独立集成测试文件。

## 测试结果

- `source ~/.xiaohebao/android/release-signing.env && ./android_native/build_apk.sh release`
  - 结果：构建成功，输出 `android_native/build/xiaohebao-release.apk`。
- `~/Library/Android/sdk/build-tools/36.1.0/apksigner verify --verbose android_native/build/xiaohebao-release.apk`
  - 结果：签名校验通过，v1/v2/v3 均为 true。
- `adb install -r android_native/build/xiaohebao-release.apk`
  - 结果：安装成功。
- `adb shell dumpsys package com.juxin.xiaohebao.admin | rg "versionCode|versionName"`
  - 结果：`versionCode=5`，`versionName=0.1.4`。
- 模拟器页面验证：
  - 列表中命中风险状态的用户姓名右侧已展示红色风险标签。
  - 详情页标题不再拼接纯文字风险状态，改为色块标签。
  - 详情页不再展示重复的“订单信息”卡片。
- APK SHA256：
  - `09b928820a0e83bf582effc64355eca0b31e61a0ca9df56f9400798890608937`

# 2026-05-23 10:31:02

## 修改说明

- 按用户最新截图反馈继续完善纯原生 Android App 的客户详情页，保留当前已接受的整体页面展示效果。
- 详情页点击列表卡片后先展示已有数据，再异步拉取 `/admin/users/{user_id}` 与 `/admin/users/{user_id}/ip-audit`，用于补齐 Web 管理端里的详情信息。
- 新增并展示四个信息卡片：紧急联系人、地理位置、IP记录、订单状态及审核批注。
- 保持“可执行操作”区域在滚动内容外部，页面上下滑动时操作按钮固定在底部不随内容滚走。
- 调整详情卡片行布局：标签固定宽度，内容靠左并允许多行，避免紧急联系人、地址、IP、批注在手机屏幕上被压缩难读。
- IP记录增加国家、省、市、区、地址等字段拼接兜底，避免无地址时出现冗余占位。
- App 版本升级为 `0.1.3`，`versionCode=4`，方便区分本次本地验证包。

## 修改后的代码

- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
  - `openDetail(...)`：进入详情后补拉用户详情与 IP 审计数据。
  - `showDetail(...)`：详情页增加紧急联系人、地理位置、IP记录、订单状态及审核批注卡片。
  - `addInfoSection(...)`：优化手机端长文本展示。
  - 新增 `emergencyRows(...)`、`locationRows(...)`、`ipRows(...)`、`orderAuditRows(...)` 等数据组装方法。
- `android_native/app/src/main/AndroidManifest.xml`
  - 版本更新为 `0.1.3` / `versionCode=4`。
- `android_native/build/xiaohebao-release.apk`
  - 已重新生成正式签名 APK。

## 单元测试代码

- 本次修改位于当前手写原生 Android 工程，项目暂未配置独立 Java 单元测试框架；未新增可运行的单元测试文件。

## 集成测试代码

- 本次通过真实 APK 构建、模拟器安装、登录生产接口、进入客户详情页、滑动详情页进行集成验证；未新增独立集成测试文件。

## 测试结果

- `source ~/.xiaohebao/android/release-signing.env && ./android_native/build_apk.sh release`
  - 结果：构建成功，输出 `android_native/build/xiaohebao-release.apk`。
- `~/Library/Android/sdk/build-tools/36.1.0/apksigner verify --verbose android_native/build/xiaohebao-release.apk`
  - 结果：签名校验通过，v1/v2/v3 均为 true。
- `adb install -r android_native/build/xiaohebao-release.apk`
  - 结果：安装成功。
- `adb shell dumpsys package com.juxin.xiaohebao.admin | rg "versionCode|versionName"`
  - 结果：`versionCode=4`，`versionName=0.1.3`。
- 模拟器页面验证：
  - 登录页账号密码为空，符合前序要求。
  - 客户详情页已展示紧急联系人、地理位置、IP记录、订单状态及审核批注卡片。
  - 页面滑动过程中，“可执行操作”区域固定在底部不随详情内容滚动。
- APK SHA256：
  - `71069e9863f341766c9541d8fa6264d4accffc15d617225f273262fe518b366f`

# 2026-05-23 10:34:11

## 修改说明

- 升级后台登录会话规则：仅超级管理员 `xiaojiang` 允许 Web 管理端和移动端同时在线，其他后台用户仍保持全端单会话在线。
- 后端新增后台登录端类型 `client_type`，默认 `WEB`；移动端登录传 `MOBILE`。
- 管理员表新增 Web 与移动端独立会话字段：
  - `active_web_session_id`
  - `active_web_session_issued_at`
  - `active_mobile_session_id`
  - `active_mobile_session_issued_at`
- `xiaojiang` 登录时按端类型分别写入 Web/Mobile 会话；同一端再次登录仍会踢掉该端旧登录。
- 普通后台用户继续使用 `active_session_id`，任意端再次登录都会踢掉此前登录。
- Android Web 管理端与原生 Android App 登录请求均已补充 `client_type: MOBILE`。
- 已同步部署到腾讯云，并重启 `xiaohebao-backend.service`；云端数据库已确认新增会话字段存在。

## 修改文件

- `backend/app/services/admin_session.py`
- `backend/app/api/deps.py`
- `backend/app/api/endpoints/admin.py`
- `backend/app/models/admin.py`
- `backend/app/schemas/admin.py`
- `backend/app/core/database.py`
- `backend/tests/test_admin_dual_session.py`
- `android/src/api.js`
- `android/src/api.test.js`
- `android/dist/`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/ApiClient.java`
- `android_native/build/xiaohebao-release.apk`
- `android_native/docs/android-download.html`
- `doc/ddl.sql`
- `doc/work_result/20260523.md`

## 测试结果

- `python -m pytest backend/tests/test_admin_dual_session.py`
  - 结果：2 个用例通过。
- `python -m compileall -q backend/app/services/admin_session.py backend/app/api/deps.py backend/app/api/endpoints/admin.py backend/app/models/admin.py backend/app/schemas/admin.py`
  - 结果：通过。
- `pnpm test`（android）
  - 结果：3 个测试文件通过，17 个用例通过。
- `pnpm build`（android）
  - 结果：构建成功，输出 `android/dist/assets/index-DLwBeiIT.js`。
- `./android_native/build_apk.sh debug`
  - 结果：构建成功。
- `source ~/.xiaohebao/android/release-signing.env && ./android_native/build_apk.sh release`
  - 结果：构建成功，输出 `android_native/build/xiaohebao-release.apk`。
- 腾讯云：
  - `pytest tests/test_admin_dual_session.py` 通过。
  - `systemctl restart xiaohebao-backend.service` 后服务状态为 `active`。
  - `admins` 表已确认存在 `active_session_id`、`active_web_session_id`、`active_mobile_session_id`。
  - `nginx -t` 通过。
  - 线上 APK 下载包 SHA256：`71069e9863f341766c9541d8fa6264d4accffc15d617225f273262fe518b366f`。

# 2026-05-23 10:09:00

## 修改说明

- 修复还款管理页面“账单进度”中已结清订单仍同时显示“逾期”的问题。
- 增加 `isLoanSettled(row)` 判断，优先识别 `status === 'SETTLED'` 或剩余待还金额为 0 的账单。
- 已结清账单在列表中只渲染一个绿色“已结清”标签；未结清账单继续显示到期状态与支付状态两个标签。
- 同步更新本地管理端静态包及腾讯云管理端线上静态资源。

## 修改文件

- `frontend_admin/src/views/Repayments.vue`
- `frontend_admin/src/views/RepaymentsProgress.test.js`
- `frontend_admin/dist/assets/Repayments-Bxsm43Bs.js`
- `frontend_admin/dist/assets/Repayments-Bxsm43Bs.js.gz`
- `doc/work_result/20260523.md`

## 测试结果

- `npx vitest run frontend_admin/src/views/RepaymentsProgress.test.js`
  - 结果：1 个测试文件通过，1 个测试用例通过。
- `node --check frontend_admin/dist/assets/Repayments-Bxsm43Bs.js`
  - 结果：语法检查通过。
- `gzip -t frontend_admin/dist/assets/Repayments-Bxsm43Bs.js.gz`
  - 结果：gzip 文件检查通过。
- 腾讯云验证：
  - 已同步到 `/opt/xiaohebao/current/frontend_admin/dist/assets/` 和实际 Nginx 服务目录 `/data/www/xhbadmin.juxin.pro/dist/assets/`。
  - `nginx -t` 检查通过。
  - `https://xhbadmin.juxin.pro/assets/Repayments-Bxsm43Bs.js` 已确认包含已结清优先显示逻辑。

# 2026-05-23 08:52:48

## 操作说明

- 根据浏览器截图定位到本地 `http://127.0.0.1:2003/` 连接被拒绝。
- 检查发现安卓 Vite 进程存在残留，但 `2003` 端口没有实际监听。
- 为避免 Vite 后台进程因会话结束或半启动状态导致端口掉线，已改由本地 Nginx 托管 `android/dist`，并将 `/api`、`/uploads` 继续代理到本地后端 `8001`。

## 验证结果

- `nginx -t` 配置检查通过。
- `http://127.0.0.1:2001/` 返回 `200 OK`。
- `http://127.0.0.1:2002/` 返回 `200 OK`。
- `http://127.0.0.1:2003/` 返回 `200 OK`。

# 2026-05-23 09:25:00

## 修改说明

- 按用户要求，以 `127.0.0.1:2003` 移动端 Web 版为参照，继续更新纯原生 Android App。
- 使用本机安卓模拟器 `Medium_Phone_API_36.1` 安装并截图验证原生 App 页面。
- 对齐登录页：登录输入区域改为卡片容器，保持 logo、标题、账号、密码、登录按钮的居中工作台风格。
- 对齐工作台列表页：顶部按钮改为圆形阴影按钮，列表卡片保留摘要卡、搜索、状态标签、双列金额日期、底部导航等结构。
- 对齐详情页：增加认证照片区域，核心信息、订单信息、底部固定操作区继续向 Web 版靠拢。
- 认证照片支持原生加载后端图片 URL，不再只显示“已上传”占位。
- 修复空姓名显示 `null` 的问题。
- App 版本升级为 `0.1.2`，`versionCode=3`。
- 重新构建正式签名 APK，并上传替换腾讯云生产下载包。
- 下载页 APK 链接版本参数更新为 `v=202605230923`，避免手机浏览器缓存旧包。

## 修改文件

- `android_native/app/src/main/AndroidManifest.xml`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `android_native/build/xiaohebao-release.apk`
- `android_native/docs/android-download.html`
- `doc/work_result/20260523.md`

## 生产发布文件

- 新版 APK：`https://xhbadmin.juxin.pro/download/xiaohebao.apk?v=202605230923`
- 下载页：`https://xhbadmin.juxin.pro/download/android.html`

## 测试结果

- `source ~/.xiaohebao/android/release-signing.env && ./android_native/build_apk.sh release`
  - 结果：构建成功，输出 `android_native/build/xiaohebao-release.apk`。
- `apksigner verify --verbose android_native/build/xiaohebao-release.apk`
  - 结果：签名校验通过。
- `adb install -r android_native/build/xiaohebao-release.apk`
  - 结果：模拟器安装成功。
- `adb shell monkey -p com.juxin.xiaohebao.admin 1`
  - 结果：模拟器启动成功。
- 模拟器截图已生成用于对照：
  - `/tmp/xhb_compare/native-login.png`
  - `/tmp/xhb_compare/native-list.png`
  - `/tmp/xhb_compare/native-detail-0.1.2.png`
  - `/tmp/xhb_compare/native-0.1.2-final.png`
- 生产 APK 哈希校验：
  - 本地/线上：`0e9d8214064297cf409cc03262561ae8f561a595d46235fda7849d843f3dd99a`

## 说明

- 本次已完成纯原生方向的第一轮视觉与交互对齐，并发布生产 APK。
- 原生 App 与 Web 版仍不是同一套 UI 技术，后续如需做到逐像素一致，需要继续逐页复刻具体细节。

# 2026-05-23 09:03:00

## 修改说明

- 根据用户反馈，确认用户选择纯原生 Android 复刻方案，不使用 WebView。
- 将原生 App 的主界面向 `127.0.0.1:2003` 移动端 Web 版继续对齐：
  - 增加顶部固定标题区风格。
  - 增加每页摘要卡片。
  - 调整搜索区、列表卡片、状态标签、风险提示、金额/日期双列展示。
  - 点击列表卡片进入原生详情页，不再只弹出操作列表。
  - 详情页增加核心信息、订单信息和底部固定操作区。
  - 底部页签改为图标+文字双行展示。
  - 安卓系统返回键在详情页内先返回列表。
- App 版本升级为 `0.1.1`，`versionCode=2`。
- 重新构建正式签名 APK 并上传腾讯云生产下载目录。
- 下载页 APK 链接增加版本参数，减少手机浏览器缓存旧安装包的概率。

## 修改文件

- `android_native/app/src/main/AndroidManifest.xml`
- `android_native/app/src/main/java/com/juxin/xiaohebao/admin/MainActivity.java`
- `android_native/build/xiaohebao-release.apk`
- `android_native/docs/android-download.html`
- `doc/work_result/20260523.md`

## 生产发布文件

- 生产 APK：`https://xhbadmin.juxin.pro/download/xiaohebao.apk?v=202605230902`
- 生产下载页：`https://xhbadmin.juxin.pro/download/android.html`

## 测试结果

- `source ~/.xiaohebao/android/release-signing.env && ./android_native/build_apk.sh release`
  - 结果：构建成功，输出 `android_native/build/xiaohebao-release.apk`。
- `aapt dump badging android_native/build/xiaohebao-release.apk`
  - 结果：包名 `com.juxin.xiaohebao.admin`，应用名“小荷包”，`versionCode=2`，`versionName=0.1.1`，仅声明 `INTERNET` 权限。
- `apksigner verify` 由构建脚本执行通过。
- 生产 APK 哈希校验：
  - 本地/线上：`c24b2bf85e846cb55cd2d05551d7c6ca6416f772f497255f22c07619629488fb`
- Android UA 访问 `https://xhbadmin.juxin.pro/`
  - 结果：`302` 跳转到下载页。
- 下载页已更新为 `/download/xiaohebao.apk?v=202605230902`。

## 说明

- 当前仍未连接安卓真机或模拟器，因此无法在本机完成真实安装后的视觉截图验证。
- 本次已将生产 APK 替换为原生对齐版，用户需要重新下载并安装覆盖旧版。

# 2026-05-23 08:41:00

## 修改说明

- 正式推进小荷包原生安卓 App 生产发布链路。
- 为 `android_native/build_apk.sh` 增加 `release` 构建模式，正式签名参数通过本机私有环境变量读取，不写入仓库。
- 在本机私有目录生成 release keystore：`~/.xiaohebao/android/xiaohebao-release.jks`。
- 使用 release keystore 构建正式签名 APK：`android_native/build/xiaohebao-release.apk`。
- 将正式 APK 上传到腾讯云生产静态目录：`/data/www/xhbadmin.juxin.pro/dist/download/xiaohebao.apk`。
- 上传下载页与 logo 到腾讯云生产静态目录：
  - `/data/www/xhbadmin.juxin.pro/dist/download/android.html`
  - `/data/www/xhbadmin.juxin.pro/dist/download/logo.svg`
- 备份并更新生产 Nginx 配置：`/etc/nginx/conf.d/xhbadmin.juxin.pro.conf`。
- 生产行为已调整为：普通浏览器访问 `https://xhbadmin.juxin.pro/` 继续打开管理后台；Android 手机浏览器访问首页时跳转到 `/download/android.html`，随后下载安卓安装包。

## 修改文件

- `android_native/.gitignore`
- `android_native/build_apk.sh`
- `android_native/build/xiaohebao-release.apk`
- `android_native/docs/native-app.md`
- `doc/work_result/20260523.md`

## 生产发布文件

- 生产 APK：`https://xhbadmin.juxin.pro/download/xiaohebao.apk`
- 生产下载页：`https://xhbadmin.juxin.pro/download/android.html`
- 生产 Nginx 备份：`/etc/nginx/conf.d/xhbadmin.juxin.pro.conf.bak-android-apk-20260523083526`

## 测试结果

- `./android_native/build_apk.sh release`
  - 结果：构建成功，输出 `android_native/build/xiaohebao-release.apk`。
- `aapt dump badging android_native/build/xiaohebao-release.apk`
  - 结果：包名 `com.juxin.xiaohebao.admin`，应用名“小荷包”，`versionCode=1`，`versionName=0.1.0`，`minSdkVersion=23`，`targetSdkVersion=34`，仅声明 `INTERNET` 权限。
- `apksigner verify --verbose android_native/build/xiaohebao-release.apk`
  - 结果：签名校验通过，v1/v2/v3 签名均有效。
- 生产 APK 哈希校验：
  - 本地：`6daa995db83b3d238b2cbcb3ab9ef7ac6ac91fa8cfd10c609887b2f1595bcc88`
  - 线上：`6daa995db83b3d238b2cbcb3ab9ef7ac6ac91fa8cfd10c609887b2f1595bcc88`
- `nginx -t`
  - 结果：生产 Nginx 配置检查通过，并已 reload。
- 普通浏览器访问 `https://xhbadmin.juxin.pro/`
  - 结果：返回 `200`，继续访问 Web 管理后台。
- Android UA 访问 `https://xhbadmin.juxin.pro/`
  - 结果：返回 `302`，跳转到 `https://xhbadmin.juxin.pro/download/android.html`。
- Android UA 访问 `https://xhbadmin.juxin.pro/api/admin/me`
  - 结果：返回 `401`，未被下载跳转拦截，原生 App API 链路保持可用。

## 说明

- 当前已完成生产下载入口和正式签名 APK 发布。
- 安卓系统仍会要求用户手动确认安装，网页不能静默安装 APK。
- 当前电脑未连接安卓真机或模拟器，因此本次未执行真机安装与登录操作验证。

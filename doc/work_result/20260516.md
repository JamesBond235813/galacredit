# 2026-05-16 20:35:58

## 修改说明

- 定位用户 `18306188919` 账单页权益联系电话显示为 `13800138000` 的原因：H5 账单页 `Bill.vue` 使用了硬编码默认电话，订单快照也未保存商品权益配置中的 `contact_phone`。
- 在订单表新增 `rights_contact_phone` 字段，用于保存下单当时的权益联系电话快照。
- 用户下单时从商品 `rights_detail_json.contact_phone` 提取联系电话并写入订单，避免后续商品配置变化影响历史订单。
- H5 账单页权益服务信息改为读取 `loan.rights_contact_phone`，无值时才回退默认电话。
- 已对腾讯云历史订单做回填，用户 `18306188919` 的订单 `64` 已回填为 `18320140697`。

## 修改文件

- `backend/app/models/loan.py`
- `backend/app/core/database.py`
- `backend/app/schemas/loan.py`
- `backend/app/services/loan_amounts.py`
- `backend/app/api/endpoints/loan.py`
- `backend/app/api/endpoints/user.py`
- `backend/app/services/admin_service.py`
- `backend/tests/test_rights_contact_phone_snapshot.py`
- `frontend_h5/src/views/Bill.vue`
- `doc/ddl.sql`

## 测试结果

- `python3 -m py_compile backend/app/models/loan.py backend/app/core/database.py backend/app/schemas/loan.py backend/app/services/loan_amounts.py backend/app/api/endpoints/loan.py backend/app/api/endpoints/user.py backend/app/services/admin_service.py`：通过。
- `pytest -q backend/tests/test_rights_contact_phone_snapshot.py`：通过，2 个用例通过。
- `pnpm --dir frontend_h5 build`：通过。
- 腾讯云后端 py_compile：通过。
- 腾讯云后端 `xiaohebao-backend.service`：重启后 active。
- 腾讯云 Nginx：配置校验通过并已 reload。
- 腾讯云接口 `/api/loan/bill` 查询用户 `18306188919`：返回 `rights_contact_phone=18320140697`，`ecard_items=2`。
- 腾讯云备份目录：`/opt/xiaohebao/backups/rights-phone-20260516203150`。

# 2026-05-16 20:20:59

## 查询说明

- 查询腾讯云用户 `18306188919` 的发卡数据：当前订单 `64` 状态为 `DISBURSED`，下单金额 `2400` 元，应发京东E卡总额 `1500` 元。
- 订单E卡明细表 `loan_ecards` 中已有 2 条记录：一张 `1000` 元、一张 `500` 元，均已绑定订单 `64`。
- 卡池 `ecard_pool` 中对应两张卡状态均为 `ASSIGNED`，`loan_id` 均为 `64`。
- 直接请求 H5 账单接口 `/api/loan/bill`，返回 `ecard_items_count=2`，说明服务端已向 H5 返回两张卡。
- 检查腾讯云 H5 静态资源，当前 `Bill` 和 `Orders` 页面包均包含 `ecard_items` 多卡展示逻辑。
- 判断用户截图只显示一张卡的原因是手机浏览器/微信内置浏览器仍加载了旧前端缓存。

## 处理结果

- 已调整腾讯云 `xhb.juxin.pro` 的 Nginx 配置：`index.html` 和前端路由入口禁用缓存，`assets` 静态资源继续按哈希文件名长期缓存。
- 已执行 `nginx -t` 并 reload，配置生效。

## 验证结果

- `https://xhb.juxin.pro/` 响应头已包含 `Cache-Control: no-cache, no-store, must-revalidate`。
- `https://xhb.juxin.pro/assets/Bill-BwExdpVW.js` 响应头为 `Cache-Control: public, max-age=31536000, immutable`。
- 腾讯云 Nginx 配置备份：`/opt/xiaohebao/backups/xhb-nginx-cache-20260516202020.conf`。

# 2026-05-16 19:55:39

## 修改说明

- 发卡逻辑从“只匹配单张同面额京东E卡”改为“按订单E卡总额精确组合多张京东E卡”。
- 新增订单E卡发放明细表 `loan_ecards`，保留 `loans` 表原有单卡字段作为兼容字段，旧订单和旧接口仍可继续读取第一张卡。
- 发卡时优先按最少张数组合，同等张数下优先使用更早到期的有效卡；组合总额必须等于订单所需E卡金额，不允许超额。
- H5“付款账单”和“我的订单”支持展示多张E卡，每张卡分别显示面额、脱敏卡号、脱敏卡密、有效期，并支持逐张复制卡号/卡密。
- `/api/loan/ecard-secret` 保留旧参数，同时支持通过 `item_id` 复制指定E卡，兼容历史单卡订单。
- 本地后端已重启，新表 `loan_ecards` 已在本地数据库创建。
- 已同步部署腾讯云测试环境，后端服务已重启，H5 静态资源已更新。

## 修改文件

- `backend/app/models/loan_ecard.py`
- `backend/app/models/loan.py`
- `backend/app/models/__init__.py`
- `backend/app/services/admin_service.py`
- `backend/app/services/loan_amounts.py`
- `backend/app/services/loan_flow.py`
- `backend/app/api/endpoints/loan.py`
- `backend/app/schemas/loan.py`
- `backend/tests/test_ecard_combo_disbursement.py`
- `backend/tests/test_admin_disburse_ecard_expiry.py`
- `frontend_h5/src/api/index.js`
- `frontend_h5/src/views/Bill.vue`
- `frontend_h5/src/views/Orders.vue`
- `doc/ddl.sql`

## 测试结果

- `python3 -m py_compile backend/app/models/loan_ecard.py backend/app/models/loan.py backend/app/models/__init__.py backend/app/services/loan_amounts.py backend/app/services/admin_service.py backend/app/api/endpoints/loan.py backend/app/services/loan_flow.py backend/app/schemas/loan.py`：通过。
- `pytest -q backend/tests/test_ecard_combo_disbursement.py backend/tests/test_admin_disburse_ecard_expiry.py`：通过，5 个用例通过。
- `pnpm --dir frontend_h5 build`：通过。
- `python -m app.main`：应用启动阶段通过；首次验证时因本地 8001 端口已有旧进程占用退出，随后已重启本地后端。
- `curl http://127.0.0.1:8001/`：通过，服务返回存活信息。
- 本地数据库结构检查：`loan_ecards` 表已存在。
- `pnpm --dir frontend_h5 test -- --run`：未通过，失败原因是既有测试环境中 `localStorage.clear is not a function`，与本次多卡发放改动无关；H5 构建已通过。
- 腾讯云后端 `xiaohebao-backend.service`：重启后 active。
- 腾讯云 Nginx：配置校验通过并已 reload。
- 腾讯云 H5 首页：`https://xhb.juxin.pro/` 返回 200。
- 腾讯云数据库结构检查：`loan_ecards` 表已存在。
- 腾讯云备份目录：`/opt/xiaohebao/backups/multi-ecard-20260516195858`。

# 2026-05-16 18:12:56

## 修改说明

- 在管理后台“申请审批”页筛选区新增“审核员”和“复购次数”两个筛选条件。
- “状态”筛选默认值从“全部”调整为“审核中”，点击重置后也恢复为“审核中”。
- “审核员”筛选仅超级管理员可见，复用现有审核员列表接口；普通审核员仍按原有权限只查看自己负责的申请。
- “复购次数”按当前列表展示口径筛选：首购、复购1次、复购2次、复购3次及以上。
- 后端 `/api/admin/loans` 新增 `review_admin_id`、`relend_count`、`relend_min_count` 查询参数，复购次数按当前订单之前已结清历史订单数计算。
- 已同步部署腾讯云，后端服务已重启，管理端静态资源已更新。

## 修改文件

- `backend/app/api/endpoints/admin.py`
- `backend/tests/test_admin_loans_filters.py`
- `frontend_admin/src/views/Applications.vue`
- `frontend_admin/src/utils/applicationsFilters.js`
- `frontend_admin/src/utils/applicationsFilters.test.js`

## 测试结果

- `python3 -m py_compile backend/app/api/endpoints/admin.py`：通过。
- `pytest backend/tests/test_admin_loans_filters.py`：通过，1 个用例通过。
- `pnpm --dir frontend_admin test`：通过，7 个测试文件、13 个用例通过。
- `pnpm --dir frontend_admin build`：通过。
- 腾讯云后端 `xiaohebao-backend.service`：重启后 active。
- 腾讯云 Nginx：配置校验通过并已 reload。
- 腾讯云页面访问：`https://xhbadmin.juxin.pro/applications` 返回 200。
- 腾讯云备份目录：`/opt/xiaohebao/backups/application-filters-20260516180605`。

# 2026-05-16 15:24:51

## 修改说明

- 在管理后台“渠道管理”列表的专属链接右侧新增“二维码”字段。
- 启用中的渠道显示“查看”按钮，点击后弹出专属链接二维码窗口。
- 渠道管理页复用专属链接页同一套本地二维码生成方案，二维码中间嵌入小荷包 logo，不依赖第三方在线二维码服务。
- 弹窗展示业务员、渠道名称、二维码、原始链接与复制链接按钮，视觉风格与当前后台保持一致。
- 已同步部署腾讯云管理后台静态资源。

## 修改文件

- `frontend_admin/src/views/Channels.vue`

## 测试结果

- `pnpm --dir frontend_admin test`：通过，6 个测试文件、10 个用例通过。
- `pnpm --dir frontend_admin build`：通过。
- 腾讯云管理端已部署，Nginx 配置校验通过并已 reload。
- 腾讯云静态包校验：`Channels` 页面包已包含“专属链接二维码 / 二维码 / 查看 / 复制链接”等内容。
- 腾讯云备份目录：`/opt/xiaohebao/backups/admin-dist-20260516152355`。

# 2026-05-16 15:18:16

## 修改说明

- 在管理后台“专属链接”列表新增“二维码”字段，启用中的专属链接显示“查看”按钮。
- 点击“查看”后弹出“专属链接二维码”窗口，展示渠道、业务员、二维码、原始链接与复制按钮。
- 二维码采用前端本地生成方案，不调用第三方在线二维码平台；该方案免费、无接口调用限制，也不会受中国大陆访问外部二维码服务稳定性的影响。
- 二维码中间叠加小荷包 logo，并使用高纠错等级，保证 logo 覆盖后仍有较好的扫码容错。
- 修正后台权限测试中已过期的期望值：催收角色当前已包含黑名单页面权限。
- 已同步部署腾讯云管理后台静态资源。

## 修改文件

- `frontend_admin/package.json`
- `frontend_admin/package-lock.json`
- `frontend_admin/src/views/ExclusiveLinks.vue`
- `frontend_admin/src/utils/channelQr.js`
- `frontend_admin/src/utils/channelQr.test.js`
- `frontend_admin/src/constants/adminPages.test.js`

## 测试结果

- `pnpm --dir frontend_admin test`：通过，6 个测试文件、10 个用例通过。
- `pnpm --dir frontend_admin build`：通过。
- 腾讯云管理端已部署，Nginx 配置校验通过并已 reload。
- 腾讯云静态包校验：已包含“专属链接二维码 / 二维码 / 复制链接”等页面内容。
- 腾讯云备份目录：`/opt/xiaohebao/backups/admin-dist-20260516151606`。

# 2026-05-16 10:48:57

## 修改说明

- 在申请审批处理弹窗中，为“审批备注”新增独立“保存备注”按钮，可只保存备注而不执行通过/拒绝审批。
- 在弹窗左侧照片下方新增“备注记录”卡片，展示审批、提醒、催收、财务、展期、额度调整等备注类记录。
- 后端允许有用户档案、申请审批、待发卡、还款、催收、财务权限的后台用户单独保存审批备注。
- 单独保存审批备注时，会写入用户事件 `ADMIN_REVIEW_NOTE`，后续待发卡、还款管理、催收管理等环节可在时间线/日志中看到。
- 还款管理与催收管理的跟进日志筛选已纳入审批备注类记录。
- 已同步部署到腾讯云。

## 修改文件

- `backend/app/services/admin_service.py`
- `backend/tests/test_admin_review_note.py`
- `frontend_admin/src/views/Applications.vue`
- `frontend_admin/src/views/Repayments.vue`
- `frontend_admin/src/views/Collections.vue`

## 测试结果

- `python -m py_compile backend/app/services/admin_service.py backend/tests/test_admin_review_note.py`：通过。
- `pytest backend/tests/test_admin_review_note.py`：通过，1 个用例通过。
- `pnpm --dir frontend_admin build`：通过。
- `python -m app.main`：本地 8001 端口已有后端服务占用，新进程未启动，未影响当前运行服务。
- `pnpm --dir frontend_admin test`：存在既有失败项 `src/constants/adminPages.test.js`，期望权限列表未包含 `blacklist`，实际包含；与本次备注功能无关。
- 腾讯云已同步部署：管理端静态包包含“保存备注/备注记录”，后端包含 `ADMIN_REVIEW_NOTE` 事件逻辑。
- 腾讯云后端服务 `xiaohebao-backend.service`：重启后 active，Nginx 已 reload。
- 腾讯云备份目录：`/opt/xiaohebao/backups/review-note-save-20260516104729`。

# 2026-05-16 10:25:10

## 修改说明

- 按商品售卖、先享后付语境清理 H5 与管理端可见文案中的放贷类措辞。
- 将“借款人/初借/复借/在贷/放款/贷款行为/利息、融担”等展示文案调整为“客户/首购/复购/履约中/发卡/信用行为/服务费”等商品买卖相关表达。
- 同步调整后端返回给前端的复购标签、身份核验失败提示、项目现金洞察卡片、用户协议与个人信息授权协议静态文本。
- 保留 `backend/app/core/database.py` 中历史事件迁移的旧词匹配项，用于把数据库旧日志替换为新语义，不作为前端展示。
- 已同步部署到腾讯云，后端、H5、管理端均已替换。

## 修改文件

- `frontend_admin/src/views/Repayments.vue`
- `frontend_admin/src/views/Collections.vue`
- `frontend_admin/src/views/FinancialReconciliation.vue`
- `frontend_admin/src/views/Applications.vue`
- `frontend_admin/src/views/Disbursements.vue`
- `frontend_admin/src/components/RiskReportDialog.vue`
- `frontend_admin/src/components/LoanHistoryDialog.vue`
- `frontend_admin/src/router/index.js`
- `frontend_admin/src/utils/format.js`
- `frontend_h5/src/views/FaceMismatch.vue`
- `frontend_h5/public/personal-info-authorization.txt`
- `frontend_h5/public/user-agreement.txt`
- `backend/app/services/loan_flow.py`
- `backend/app/services/loan_amounts.py`
- `backend/app/services/admin_service.py`
- `backend/app/api/endpoints/user.py`
- `backend/app/services/esign_identity.py`
- `backend/app/core/database.py`
- `backend/app/schemas/loan.py`
- `backend/app/schemas/user.py`
- `backend/app/models/loan.py`
- `backend/app/api/endpoints/loan.py`
- `backend/app/services/loan_ws_notify.py`
- `backend/tests/test_admin_user_profile_rules.py`
- `doc/个人信息授权协议.txt`

## 测试结果

- `python -m py_compile ...`：通过。
- `pytest backend/tests/test_admin_user_profile_rules.py`：通过，6 个用例通过。
- `pytest backend/tests/test_admin_stats_ws.py`：通过，1 个用例通过。
- `pnpm --dir frontend_admin build`：通过。
- `pnpm --dir frontend_h5 build`：通过。
- `python -m app.main`：本地 8001 端口已有后端服务占用，新进程未启动，未影响当前运行服务。
- `pnpm --dir frontend_admin test`：存在既有失败项 `src/constants/adminPages.test.js`，期望权限列表未包含 `blacklist`，实际包含；与本次文案清理无关。
- `pnpm --dir frontend_h5 test`：存在既有失败项，测试环境 `localStorage.clear` 不是函数；与本次文案清理无关。
- 本地前端源码扫描：H5 与管理端可见代码中未再检出目标放贷类措辞。
- 腾讯云部署后扫描：管理端与 H5 静态包未检出目标放贷类措辞；后端除历史事件迁移映射文件外未检出目标放贷类措辞。
- 腾讯云后端服务 `xiaohebao-backend.service`：重启后 active，Nginx 已 reload。
- 腾讯云备份目录：`/opt/xiaohebao/backups/wording-commerce-20260516102319`。

# 2026-05-16 10:03:49

## 修改说明

- 在待发卡页面统计区新增“卡池库存余额”卡片，展示当前可用且未过期的京东 E 卡库存面额合计。
- 后端 `/api/admin/stats` 复用现有统计接口，新增 `ecard_pool_available_amount` 与 `ecard_pool_available_count` 字段，口径与实际发卡逻辑一致：`AVAILABLE` 状态且有效期晚于当天。
- 管理端待发卡页从现有统计接口读取库存余额和张数，不新增额外接口。

## 修改文件

- `backend/app/api/endpoints/admin.py`
- `backend/app/schemas/loan.py`
- `backend/tests/test_admin_stats_ws.py`
- `frontend_admin/src/views/Disbursements.vue`

## 测试结果

- `python -m py_compile backend/app/api/endpoints/admin.py backend/app/schemas/loan.py backend/tests/test_admin_stats_ws.py`：通过。
- `pytest backend/tests/test_admin_stats_ws.py`：通过，1 个用例通过。
- `pnpm --dir frontend_admin build`：通过。
- `python -m app.main`：本地 8001 端口已有后端服务占用，新进程未启动，未影响当前运行服务。
- `pnpm --dir frontend_admin test`：存在一个既有失败项 `src/constants/adminPages.test.js`，期望权限列表未包含 `blacklist`，实际结果包含 `blacklist`；与本次库存卡改动无关。
- 腾讯云已同步部署：后端服务 `xiaohebao-backend.service` 重启后 active，Nginx 配置校验通过并已 reload。
- 腾讯云统计口径验证：`ecard_pool_available_amount=17000.0`，`ecard_pool_available_count=27`。
- 腾讯云备份目录：`/opt/xiaohebao/backups/disbursement-card-stock-20260516100550`。

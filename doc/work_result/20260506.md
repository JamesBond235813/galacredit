# 2026-05-06 16:01:49

## 1. 修改说明（做了什么、为什么）
- 修改文件：`frontend_h5/src/views/Face.vue`
- 问题现象：`/face` 页面点击“开始识别”后，拍摄界面可能被底部导航栏覆盖。
- 处理方式：将拍摄弹层与来源选择弹层使用 Vue `teleport` 挂载到 `body`，避免受页面内部层叠上下文影响。
- 修改原因：这是最小改动方案，不改变业务逻辑与交互流程，仅修正弹层层级展示。

## 2. 修改后的代码
```vue
<teleport to="body">
  <transition name="sheet-fade">
    <div v-if="cameraModalVisible" class="camera-mask" @click="closeLiveCamera"></div>
  </transition>
  <transition name="sheet-up">
    <div v-if="cameraModalVisible" class="camera-sheet">
      <!-- 省略未改动内容 -->
    </div>
  </transition>

  <transition name="sheet-fade">
    <div v-if="pickerVisible" class="picker-mask" @click="closePicker"></div>
  </transition>
  <transition name="sheet-up">
    <div v-if="pickerVisible" class="picker-sheet">
      <!-- 省略未改动内容 -->
    </div>
  </transition>
</teleport>
```

## 3. 单元测试代码
- 本次未新增测试文件。
- 已执行现有 Vitest 单元测试集用于回归验证。

## 4. 集成测试代码（如适用）
- 本次未新增集成测试代码。
- 建议人工在移动端浏览器执行 `/face` 页面交互验证（点击“开始识别”后确认拍摄弹层覆盖在底部导航栏之上）。

## 5. 测试执行结果或执行方式
- 执行命令：`cd frontend_h5 && pnpm test`
- 执行结果：
  - `Test Files  6 passed (6)`
  - `Tests  14 passed (14)`
  - 全部通过


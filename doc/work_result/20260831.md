# 2026-08-31 18:08:00

## 第十五阶段

将 `build:app` 改为自动探测 HBuilderX/uni-app CLI 的构建脚本；当前机器未安装该 CLI 时返回明确诊断和下一步，不再伪装成可用命令。uni-app 测试和 H5 构建继续通过。

## 第十四阶段

修正 uni-app 首次启动入口：未登录用户进入登录页，登录成功后进入首页；页面 JSON、uni-app 测试、H5 构建均通过，并复核 iOS Simulator 与 Android Debug APK 产物存在。

## 第十三阶段

补齐 `.env.example` 与三端发布清单，明确 H5 HTTPS 部署、Android 内部 APK/Google Play AAB、iOS Archive/签名要求；uni-app 测试 2 项、H5 构建、Android 内部渠道检查均通过。

## 第十二阶段

完成首页核心业务导航、版本标识、登录态工具及 Android 渠道发布检查脚本；`frontend_uniapp` 的测试和 H5 构建通过，`android_native/release_check.sh internal` 检查通过。Google Play 渠道仍需正式签名材料和 AAB 构建环境。

## 第十一阶段

完成构建环境实测：现有 iOS 原生工程通过 `xcodebuild` 构建 iPhone Simulator Debug；现有 Android 原生工程通过 `build_apk.sh` 生成并校验 `android_native/build/galacredit-debug.apk`。uni-app H5 构建与测试仍保持通过。

## 第十阶段

补齐贷款业务接口：授信申请、合同预览/签署、订单验证码、电子卡、贷款状态和账单；统一请求层处理 HTTP 401 与业务 `code` 错误。uni-app 测试 2 项通过，H5 构建成功。

## 第九阶段

补齐新工程的 H5 编译入口（Vite + Vue 插件）、开发命令、构建说明和 `index.html`，解决依赖安装冲突；`npm install`、`npm run build:h5` 和 `npm test` 均成功。原生 app-plus 构建保留为 HBuilderX CLI 步骤，避免使用不存在的 CLI 版本。

## 第八阶段

新增跨端联系人选择适配（App 使用系统通讯录，H5 明确返回不支持）、滑块验证码接口、风险信号页面和邀请渠道绑定页面；配置与测试已同步更新。

## 第七阶段

补齐滑块验证码 API、设备风险信号采集、邀请渠道绑定页面与接口，并新增定位页面路由；uni-app 页面配置及 API 测试均通过。

## 第六阶段

新增 uni-app `uni.chooseImage`/`uni.uploadFile` 上传适配，OCR 与人脸认证改为真实上传链路；新增定位获取与 `/user/location` 提交页面；补充验证码登录链路的 API 封装。uni-app 2 个测试文件全部通过。

## 第五阶段

登录页面已从临时 token 改为真实手机号验证码流程，接入 `/auth/send-code` 与 `/auth/sms-login`，增加倒计时和 token 持久化；uni-app API 测试继续通过。

## 第四阶段

继续迁移 OCR/人脸、提现、密码修改、用户协议等业务入口，补充还款请求与密码修改 API 封装；页面配置 JSON 校验通过，uni-app API 测试 2 项通过。

## 第三阶段

继续迁移核心业务入口：借款申请、身份/OCR、人脸认证、客服、提现 API 映射；补充 uni-app 业务 API 单元测试、Vitest 配置，以及跨端权限清单。uni-app 测试 2 项通过；现有 H5 36 项测试和生产构建继续通过。

## 第二阶段

新增 uni-app 登录、首页聚合、账户、申请列表、账单页面入口；新增 `manifest.json` 跨端权限配置、统一聚合 API，以及 `android_native/channel-config.json` 明确内部 APK 与 Google Play AAB 的渠道差异。回归测试 36 项全部通过，现有 H5 构建成功。

## 修改说明

本次按 H5、Android（非上架/Google Play）、iOS 共用业务代码的方案，新增 `frontend_uniapp` 三端迁移目标工程骨架。新增跨端存储与平台识别适配器、统一 API 请求适配器、首页示例页面和 uni-app 应用入口；Android 两种发行版明确通过 flavor、包名、签名和渠道能力区分，不维护两套业务代码。

## 测试与验证

- `cd frontend_h5 && npm test -- --run`：36 个测试全部通过。
- `cd frontend_h5 && npm run build`：Vite 生产构建成功。
- 新 uni-app 工程为迁移骨架，需在 HBuilderX/uni-app CLI 环境继续完成页面逐页迁移后执行原生打包；本次未改动现有原生 Android/iOS 工程，确保旧版本可回滚。

## 后续交付步骤

1. 将 `frontend_h5/src/views` 页面按业务优先级迁移至 `frontend_uniapp/src/pages`。
2. 将登录、支付、推送、定位、OCR/人脸等能力接入 `src/utils` 平台适配层。
3. 配置 Android 测试 flavor 与 Google Play AAB 的正式签名，并配置 iOS Bundle Identifier、APNs 和审核元数据。

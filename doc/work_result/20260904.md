# 2026-09-04 20:26:00

## 本次工作

- 结合当前 GalaCredit 贷款业务，对比原生 Android 与 UniApp Android 的实现差异，重点覆盖短信风控、Google Play 上架、身份认证、联系人、定位、图片上传、设备风控、弱网和维护成本。
- 明确当前仓库属于过渡性混合架构：`frontend_uniapp` 负责跨端业务页面，`android_native` 负责原生登录壳及系统能力桥接；当前原生壳加载远程 H5，不等同于已经生成正式 UniApp App-Plus Android 包。
- 给出建议：业务流程采用 UniApp，敏感系统能力采用 Android/iOS 原生插件，后端继续负责二次过滤、授权校验、审计、幂等和 Ghana 风控接口调用。

## 测试与验证

- 本次为只读架构分析，未修改业务代码，未执行构建或测试。
- 已更新本作业总结，并执行 `git add doc/work_result/20260904.md`，未提交 commit。

# 2026-09-04 18:39:04

## 本次工作

- 结合 GalaCredit 当前业务链路，对比原生 Android 与 UniApp Android 方案：登录、身份认证、定位、联系人、贷款申请、还款、风控、WebView/H5 复用及 Google Play 合规。
- 结论：本业务更适合以 UniApp 统一业务层 + Android/iOS 原生插件承载必要能力；纯原生适合设备能力和安全控制要求高但会显著增加双端维护成本。
- 特别区分当前 `android_native`（原生登录壳 + 远程 H5）与真正 UniApp 编译 Android 包，指出前者并不等同于 UniApp App。

## 测试与验证

- 本次为方案分析，未修改业务代码，未执行测试。

## 本次工作

- 按 Android 用户端实际运行路径盘点数据采集：当前 APK 为 `android_native` 原生登录壳 + 远程 H5 WebView，并非 `frontend_uniapp` 编译产物。
- 当前 Android Manifest 仅声明 `INTERNET`、粗略定位和精确定位权限；没有 `READ_SMS`、联系人、相机、存储或应用列表权限。
- 登录阶段采集手机号、图形滑块校验数据、短信验证码、登录 IP/请求审计信息；登录后会主动上报设备风险摘要。
- Android 风险摘要当前包含 Android 版本、厂商、型号、时区、语言、屏幕宽高、App 版本、平台标识和 Android ID；短信列表与已安装应用列表固定为空。
- WebView 内的业务页面还可以采集定位、Ghana Card 身份证正反面图片、身份证识别出的姓名/证件号/地址/有效期、人脸照片/比对分数、紧急联系人姓名/关系/手机号、贷款申请和还款相关业务数据。
- 明确未发现 Android 原生读取短信正文、已安装应用清单、IMEI、通讯录全量、麦克风或后台持续定位实现。
- 发现隐私风险：Android 端将 `Secure.ANDROID_ID` 作为 `device_fingerprint` 发送；当前后端虽然生成哈希指纹，但 `payload_json` 仍可能保存客户端原始指纹，后续应改为只落库不可逆值。

## 测试与验证

- 本次为只读审计，未修改业务代码；已核对 Android Manifest、原生登录流程、风险载荷构造、uniapp 页面/API 和后端用户接口。

## 本次工作

- 按隐私最小化要求新增短信服务端过滤：读取 `3rd_doc/sms_keys20260602.csv`，使用转义正则匹配关键词，仅保留近 90 天且命中的短信；缺少可验证时间、超出窗口或未命中关键词的短信不进入本地摘要、原始载荷或第三方请求。
- 新增 `backend/app/services/ghana_risk.py`，以 `httpx.AsyncClient` 接入 Ghana v3 风控提交和查询接口；新增用户风控回调 `/api/user/risk-callback`，并将 `task_number` 保存到 `risk_external_checks`。
- `/api/user/risk-signals` 现在会在服务端再次执行授权、90 天窗口和关键词过滤后再提交第三方；未配置外部凭证时安全降级为 `SKIPPED`。
- 新增 `/api/user/risk-query` 供当前用户轮询任务状态和评分；新增环境变量配置及数据库兼容字段 `task_number`。
- 补充短信过滤和第三方接口异步调用测试。
- 对照 `3rd_doc/ghana_risk_api_table_v3.md` 与两份 `.py` 示例：请求字段和接口路径基本一致；示例查询响应中的 `task_score_v1` 与 Markdown 文档字段 `task_score` 不一致，且示例未覆盖错误处理、回调落库和异步实现，本次适配按 Markdown 的 `task_score`/`task_score_v2` 兼容处理。

## 测试与验证

- 目标测试：`backend/.venv/bin/python -m pytest -q tests/test_device_risk_signals.py tests/test_ghana_risk.py tests/test_user_risk_signals.py`，6 passed。
- 服务启动验证：`backend/.venv/bin/python -m app.main` 成功启动 Uvicorn，应用启动和数据库初始化流程完成后已停止。
- 全量后端测试：163 passed、13 failed；失败集中在既有管理员模拟数据库、请求日志捕获和短信订单测试，与本次新增短信过滤/Ghana 接入无关。

## 本次工作

- 只读完整检查 `3rd_doc` 目录中的 4 个文件：两份 Python 脚本、1 份 Ghana 风控 API Markdown 文档、1 份短信关键词 CSV。
- 判断其用途为第三方 Ghana 风控服务的接入资料：提交设备短信/应用数据创建异步风控任务，再通过回调或轮询查询评分。
- 确认 CSV 为 173 个无表头、单列且无重复的关键词，主要覆盖贷款机构、金额/借贷、验证码、放款、还款、逾期、催收、法律及运营商等词，用于短信风控特征提取或筛选；当前两份脚本没有直接读取该 CSV。

## 测试与验证

- 未修改业务代码，未启动服务或执行测试。
- 使用文件清单、文件类型、行数及内容读取完成结构核对；未进行外部 API 请求。
# 2026-09-04 19:07:41

## 本次工作

- 针对 GalaCredit 贷款、身份认证、定位、联系人和短信风控业务，对比原生 Android 与 UniApp Android 两种实现方案。
- 明确当前 `android_native` 实际是原生登录壳 + 远程 H5 WebView，不等于完整原生业务 App；`frontend_uniapp` 才是 H5/Android/iOS 共用业务层的迁移目标。
- 结论：建议采用“UniApp 统一业务层 + Android/iOS 原生插件/适配器”的混合架构。原生 Android 仅承载必须的平台能力、权限和安全边界；短信读取能力按发行渠道隔离，Google Play 版本不申请 `READ_SMS`。

## 核查依据

- 核对 Android Manifest、原生风险载荷、WebView 容器、UniApp manifest、跨端平台适配器及 Ghana 风控资料。
- 当前 Android 上架构建没有 `READ_SMS`，风险载荷中的 `sms_messages` 与 `installed_apps` 为空；UniApp 当前也没有短信读取插件，仅配置相机、定位和通知模块。
- 评估覆盖：短信 90 天/关键词过滤、身份 OCR/人脸、定位、紧急联系人、设备指纹、第三方风控接口、Google Play/App Store 合规、性能和维护成本。

## 测试与验证

- 本次为架构分析，未修改业务代码，未执行构建或测试。
- 已按仓库要求更新本作业总结；仅对该总结执行 `git add`，未提交 commit。
# 2026-09-04 20:08:29

## 本次工作

- 结合当前 GalaCredit 贷款业务，对比原生 Android、UniApp Android 及当前“原生壳 + 远程 H5”实现的差异。
- 结论：采用 UniApp 统一业务层，配合 Android/iOS 原生插件承载联系人选择、相机/活体、定位、推送、安全存储和设备证明；短信能力按渠道隔离，Google Play 版本不申请 `READ_SMS`，内部授权版才允许最小化采集。
- 重点评估登录、Ghana Card OCR/人脸、紧急联系人、定位、短信风控、第三方 Ghana 风控接口、合同/提现、弱网、重复提交、上架合规和维护成本。

## 测试与验证

- 本次为只读架构评估，未修改业务代码，未执行构建或测试。
# 2026-09-04 21:33:01

## 本次工作

- 结合 GalaCredit 当前贷款业务和 Android 实际工程，对比原生 Android 与 UniApp Android 的差异。
- 明确当前 `android_native` 是原生登录壳 + 远程 H5 WebView，`frontend_uniapp` 是目标跨端业务工程；二者都不是“纯原生完整业务 App”或已生成正式 App-Plus 包。
- 结论采用混合架构：UniApp 负责页面、表单、贷款流程和普通接口；Android/iOS 原生插件负责短信（仅内部授权渠道）、定位、联系人选择、相机/图片、活体/OCR、设备安全存储和设备风险能力；后端负责二次过滤、授权审计、风控接口和幂等。
- 本轮为只读方案分析，未修改业务代码，未执行构建或测试。

## 依据

- 核对了 Android Manifest、`SmsCollector`、原生风险载荷、WebView 桥接、UniApp manifest、短信过滤和联系人/图片/定位适配代码。
- 已执行 `git add doc/work_result/20260904.md`，未提交 commit。
# 2026-09-04 22:19:12

## 本次工作

- 结合 GalaCredit 当前贷款业务，比较原生 Android 与 UniApp Android 的差异。
- 重点覆盖短信风控与隐私授权、Google Play 限制、身份认证、联系人、定位、图片上传、设备风控、弱网性能、维护成本和当前仓库架构。
- 结论：业务页面采用 UniApp，短信/权限/安全存储/系统选择器等敏感能力采用 Android 原生插件或桥接；后端继续负责二次过滤、授权校验、审计、幂等和最终风控决策。
- 明确当前 `android_native` 是原生登录壳 + 远程 H5 WebView，不等于完整纯原生 App，也不等于已经生成正式 UniApp App-Plus 包。

## 测试与验证

- 本次为只读架构分析，未修改业务代码，未执行构建或测试。
- 已将本次分析记录插入本文件顶部，并执行 `git add doc/work_result/20260904.md`，未提交 commit。
# 2026-09-04 22:48:51

## 本次工作

- 修复 Android 构建脚本：按包名/构建类型隔离中间产物，避免 Play 与 internal 构建互相清理。
- 修复构建配置注入：`GALA_NATIVE_API_BASE`、`GALA_NATIVE_ASSET_BASE` 现在会生成并编译到渠道专属 `AppConfig`，不再只校验环境变量而仍使用硬编码地址。
- 增加短信权限安全约束：`READ_SMS` 只能与 `com.galacredit.app.internal` 绑定；错误组合在清理旧产物前直接拒绝。
- 加强渠道发布校验：`release_check.sh` 校验渠道与包名必须一致。
- 增加 `android_native/tests/test_build_channels.sh`，覆盖 Play 无 `READ_SMS`、internal 有 `READ_SMS`、自定义 API 地址注入和错误配置不破坏既有产物。
- 修复 UniApp 图片上传适配器：统一发送 `client-id`，并识别 HTTP 200 下的业务失败响应，避免误报上传成功。
- 为图片上传业务失败补充前端单元测试。

## 测试与验证

- Android 渠道构建测试：`android_native/tests/test_build_channels.sh` 通过。
- Android Play APK：包名 `com.galacredit.app`，无 `android.permission.READ_SMS`。
- Android internal APK：包名 `com.galacredit.app.internal`，包含 `android.permission.READ_SMS`。
- UniApp：`npm test -- --run`，9 tests passed；`npm run build:h5` 成功。
- 后端目标测试：短信过滤、Ghana 风控、用户风险信号、短信下单和下单幂等共 15 tests passed。
- 后端服务启动和根接口检查成功：Uvicorn 启动完成，`GET /` 返回 HTTP 200，随后已停止服务。
- iOS Simulator：既有 15 tests 通过。
- 后端全量测试仍为 167 passed、12 failed；失败为既有管理员 Fake DB 兼容、跨 event loop、请求日志配置和旧响应断言问题，本轮未修改生产逻辑以迎合这些旧测试。

## 未完成/需外部验证

- 尚无可用 Android 真机和 iOS 真机，短信权限、定位、联系人、相机/图片、进程恢复和弱网仍需真机回归。
- 当前未安装 HBuilderX/UniApp App-Plus CLI，尚未生成正式 UniApp Android AAB、内部 APK 和 iOS App-Plus 包。
- 正式发布仍需签名、隐私政策公开 URL、Google Play Data Safety、Apple 隐私清单及金融服务主体材料。
# 2026-09-04 22:58:27

## 本次工作

- 将用户端 `backend/app/api/endpoints/user.py` 与 `backend/app/api/endpoints/loan.py` 的业务异常统一迁移为 `BizException`，覆盖身份认证、联系人、资料提交、贷款申请、合同、提现、订单验证码、还款提醒和 E 卡信息读取。
- 保持项目约定：业务失败通过 `code/msg` 返回，HTTP 状态为 200（鉴权失败仍由统一规则返回 401）。
- 更新订单短信测试，使其验证统一 `code/msg` 响应契约。

## 测试与验证

- `python3 -m py_compile app/api/endpoints/user.py app/api/endpoints/loan.py` 通过。
- 用户端关键后端测试：8 passed（短信下单、下单幂等、用户风险信号、Ghana 风控）。
- `rg "raise HTTPException" app/api/endpoints/user.py app/api/endpoints/loan.py` 无结果。
# 2026-09-04 23:05:00

## 本次工作

- 结合 GalaCredit 当前贷款业务，对比原生 Android 与 UniApp Android 的实现差异。
- 重点分析短信风控及 90 天/关键词最小化采集、Google Play 权限限制、身份认证图片、定位、联系人、设备风控、WebView 桥接、弱网和三端维护成本。
- 明确当前工程是“UniApp 业务层 + Android 原生登录壳/系统能力桥接”的混合过渡架构；当前 `android_native` 原生壳加载远程 H5，并不等于完整纯原生 App，也不等于已经生成正式 UniApp App-Plus Android 包。
- 结论建议：UniApp 负责跨端页面和普通业务流程，Android/iOS 原生插件负责敏感权限、短信（仅内部授权渠道）、定位、联系人选择、相机/活体、设备安全存储和设备风险能力；Google Play 版本不申请 `READ_SMS`，后端继续执行授权校验、二次过滤、审计、幂等和风控接口调用。

## 测试与验证

- 本次为只读架构分析，未修改业务代码，未执行构建或测试。
- 已更新本作业总结，并执行 `git add doc/work_result/20260904.md`，未提交 commit。
# 2026-09-04 23:45:20

## 本次工作

- 针对 GalaCredit Ghana 贷款 Android 用户端，比较纯原生 Android 与 UniApp Android（App-Plus/WebView + 原生插件）两种方案。
- 结合当前短信 90 天/关键词风控、身份认证、联系人、定位、图片上传、设备风险、Ghana 风控接口、弱网和 Google Play 上架限制，明确两种方案在能力边界、合规风险、性能、维护成本和发布节奏上的差异。
- 结论：推荐“UniApp 统一业务层 + Android 原生插件/桥接承载敏感系统能力”的混合方案；短信读取仅保留内部授权渠道，Google Play 包不申请 READ_SMS。
- 明确当前仓库的 `android_native` 是原生登录壳 + 远程 H5 WebView，尚不等同于完整纯原生业务 App，也不等同于已生成正式 UniApp App-Plus Android 包。

## 测试与验证

- 本次为只读技术选型分析，未修改业务代码，未执行构建或测试。
- 已将本总结插入文件顶部，并执行 `git add doc/work_result/20260904.md`，未提交 commit。

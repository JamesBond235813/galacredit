# 2026-05-20 19:28:58

## 修改说明

- 风控报告停用探针A，启用探针C：后端新增探针C产品 `JX1000021` 查询逻辑，综合报告新生成的数据使用 `probe_c` 节点，报告来源为 `PROBE_C`。
- 探针C复用原探针A的存储规则：仍写入 `risk_control_report`，仍按姓名+身份证+来源做14天缓存，综合报告仍复用原报告关联字段承载探针报告ID，不新增数据库表。
- 按“当前用户仅更新申请中/审核中用户”的要求收紧缓存策略：当前审核中的用户查询风控报告时只复用探针C综合报告缓存；其他状态用户优先保留已有综合报告缓存，不主动刷新历史报告。
- 管理端风控报告展示将“探针A信息/摘要”替换为“探针C信息/摘要”，并展示探针C新增字段：最大履约金额、最近履约时间、履约笔数。

## 修改文件

- `backend/app/core/config.py`
- `backend/app/services/risk_report.py`
- `backend/app/services/composite_risk_report.py`
- `backend/app/schemas/risk.py`
- `backend/tests/test_composite_risk_report.py`
- `frontend_admin/src/components/RiskReportDialog.vue`
- `frontend_admin/src/components/CompositeRiskReportDialog.vue`
- `frontend_admin/src/components/RiskReportDialog.test.js`
- `doc/work_result/20260520.md`

## 测试结果

- 后端单元测试：`python -m pytest backend/tests/test_composite_risk_report.py -q`，结果 `6 passed`。
- 后端语法编译：`python -m compileall backend/app/services/risk_report.py backend/app/services/composite_risk_report.py backend/app/schemas/risk.py`，通过。
- 管理端单元测试：`npm test -- --run src/components/RiskReportDialog.test.js`，结果 `1 passed`。
- 管理端生产构建：`npm run build`，构建成功。
- 本地后端启动验证：在 `backend` 目录执行 `python -m app.main`，服务启动成功。
- 本地管理端启动验证：`npm run dev -- --host 127.0.0.1 --port 2016`，访问首页返回 `200`。
- 腾讯云部署：已同步后端代码、管理端构建产物和工作记录，重启 `xiaohebao-backend.service` 后服务状态 `active`，`nginx -t` 通过并已 reload。
- 线上验证：当前审核中用户 `刘胤 / 18280556121` 查询小荷包风险报告返回 `200`，报告生成 `probe_c.source=PROBE_C`，不再包含 `probe_a` 节点。
- 线上验证：非审核中用户 `李小东 / 18402039845` 保留旧综合报告缓存，未主动刷新探针C，符合“当前用户仅对申请中的人做更新”的要求。
- 腾讯云备份目录：`/opt/xiaohebao/backups/probe-c-202605201929`。

# 2026-05-20 17:18:07

## 修改说明

- 修复后台查询申请用户 `13809148482` 小荷包风险报告时报“查询为空”的问题。
- 问题原因是全景雷达接口返回正常，但探针A接口对该用户返回“查询数据为空”，原逻辑把该业务空结果当作接口失败，导致整份风险报告查询失败。
- 现在将探针A“查询为空/查询数据为空/无查询结果/暂无数据”归一为可入库的空结果报告片段，不再阻断全景雷达数据和小荷包风险报告展示。
- 保持原有风控报告数据结构和已有用户数据不变，仅补充空结果兼容逻辑。

## 修改文件

- `backend/app/services/risk_report.py`
- `backend/tests/test_composite_risk_report.py`
- `doc/work_result/20260520.md`

## 测试结果

- 后端单元测试：`python -m pytest backend/tests/test_composite_risk_report.py -q`，结果 `4 passed`。
- 后端语法编译：`python -m compileall backend/app/services/risk_report.py backend/app/services/composite_risk_report.py`，通过。
- 本地后端启动验证：在 `backend` 目录执行 `python -m app.main`，服务启动成功。
- 腾讯云部署：已同步修复文件到 `/opt/xiaohebao/current`，重启 `xiaohebao-backend.service` 后服务状态为 `active`。
- 线上验证：后台接口查询用户 `陈小龙 / 13809148482` 的小荷包风险报告返回 `200`，报告已生成；探针A部分标记为空结果并显示“查询数据为空”，不再报错。

# 2026-05-20 15:19:18

## 修改说明

- 修复 H5 实名认证身份证上传弱网/微信内置浏览器下容易失败的问题。
- 身份证正反面选图后统一走前端压缩：超过约 900KB 的图片会转成 JPEG，最长边限制为 1280px，并按质量阶梯压缩，降低上传中断概率。
- OCR 上传接口关闭全局“网络异常”兜底提示，改由实名认证页面按错误类型提示：网络未到达服务器、上传超时、后端识别失败分别给出更明确文案。
- 保持后端 OCR、e签宝、人脸识别和业务数据逻辑不变。

## 修改文件

- `frontend_h5/src/utils/imageUpload.js`
- `frontend_h5/src/utils/imageUpload.test.js`
- `frontend_h5/src/views/OCR.vue`
- `frontend_h5/src/api/index.js`
- `frontend_h5/src/api/request.js`
- `doc/work_result/20260520.md`

## 测试结果

- H5 单元测试：`npm test -- imageUpload.test.js`，结果 `4 passed`。
- H5 生产构建：`npm run build`，构建成功。
- 本地前端启动验证：`npm run dev -- --host 127.0.0.1 --port 2005`，访问 `/ocr` 返回 `200`。
- 腾讯云部署：已上传 H5 构建产物到 `/data/www/xhb.juxin.pro/dist`，执行 `nginx -t` 成功并 reload nginx。
- 线上验证：`https://xhb.juxin.pro/` 返回 `200`，线上 OCR 资源中已包含新的压缩逻辑和上传失败提示。

# 2026-05-20 09:34:22

## 修改说明

- 接入 Legou 外部风险名单接口：按生产地址 `https://api.legou1688.com/finance-center/v1/blacklist/match` 以 POST 方式提交 `phoneMd5` 和 `idCardMd5`。
- 新增独立风险名单状态字段，不改变现有黑名单规则：`risk_list_hit`、`risk_list_source`、`risk_list_reason`、`risk_list_checked_at`。
- 用户短信登录、密码登录、提交身份证识别、提交申请、发起申请、下单前都会刷新风险名单状态；命中只作为后台风险提示，不冻结、不拒绝、不清空额度。
- 管理后台申请审批和待发卡页面，在姓名右侧新增红底白字“风”标签，并用气泡展示命中原因。
- 小荷包风险报告中补充风险名单命中状态、来源、原因和最近核查时间。
- `doc/ddl.sql` 与自动数据库补丁已同步新增风险名单字段。

## 修改文件

- `backend/app/services/risk_list_service.py`
- `backend/app/core/config.py`
- `backend/app/core/database.py`
- `backend/app/models/user.py`
- `backend/app/schemas/user.py`
- `backend/app/schemas/loan.py`
- `backend/app/api/endpoints/auth.py`
- `backend/app/api/endpoints/user.py`
- `backend/app/api/endpoints/loan.py`
- `backend/app/services/admin_service.py`
- `backend/app/services/loan_amounts.py`
- `backend/app/services/composite_risk_report.py`
- `backend/tests/test_risk_list_service.py`
- `frontend_admin/src/views/Applications.vue`
- `frontend_admin/src/views/Disbursements.vue`
- `frontend_admin/src/views/RiskListBadge.test.js`
- `doc/ddl.sql`
- `doc/work_result/20260520.md`

## 测试结果

- 后端测试：`python -m pytest backend/tests/test_risk_list_service.py backend/tests/test_auth_sms_login.py backend/tests/test_auth_sms_send_code.py backend/tests/test_composite_risk_report.py -q`，结果 `12 passed`。
- 管理端测试：`npm test -- --run src/views/RiskListBadge.test.js`，结果 `1 passed`。
- 管理端生产构建：`npm run build`，构建成功。
- 后端语法编译：`python -m compileall ...`，通过。
- 本地后端启动验证：`python -m app.main`，启动成功，并自动补齐 users 风险名单字段。

## 接口验证

- 本地调用 Legou 生产接口可连通，但返回 `ip不在白名单`。
- 腾讯云服务器调用 Legou 生产接口同样可连通，但返回 `ip不在白名单`。
- 当前代码已接入，待 Legou 侧把本地出口 IP 和腾讯云服务器出口 IP 加入白名单后，即可按 `result=true/false` 正常更新风险名单状态。

## 腾讯云验证

- 已同步后端代码、管理端构建产物和 `doc/ddl.sql` 到腾讯云。
- 已补充线上环境变量并重启 `xiaohebao-backend.service`，服务状态 `active`。
- 已确认腾讯云 users 表存在 `risk_list_hit`、`risk_list_source`、`risk_list_reason`、`risk_list_checked_at` 四个字段。
- 已执行 `nginx -t` 并 reload nginx。
- 线上 `https://xhbadmin.juxin.pro/applications` 与 `https://xhbadmin.juxin.pro/disbursements` 返回 `200`。
- 腾讯云备份目录：`/opt/xiaohebao/backups/risk-list-legou-20260520093034`。

# 2026-08-20 20:55:00

## 本轮任务

继续补齐管理后台的 P1/P2 运营能力，按业务运营视角新增审计、KYC 复核、消息中心、运营监控、风控预留和运营配置页面。

## 本轮修改

- 后端新增 4 个读取接口：
  - `/api/admin/audit-logs`
  - `/api/admin/kyc-review-queue`
  - `/api/admin/monitoring-summary`
  - `/api/admin/message-center`
- 后端放宽并细分了消息中心、KYC 复核与监控相关权限，避免页面有入口但不可用。
- 后端补充了面向运营后台的聚合查询：
  - 管理员审计日志
  - KYC 待复核队列
  - 消息中心模板与提醒队列
  - 运营监控汇总与调度任务状态
- 前端新增页面：
  - 运营监控
  - 操作审计
  - KYC复核
  - 消息中心
  - 风控策略预留
  - 运营配置
- 前端新增菜单、路由、图标和中英文词条，并把 KYC 页跳转接到用户档案页的搜索参数。
- 消息中心支持直接发送还款提醒，运营配置页支持本地草稿保存。

## 说明

- P1 里的风控策略中心按你的要求只做了预设和预留，没有接外部三方数据的可执行配置。
- 其余 P1/P2 页面都已补上，并尽量复用现有 `loans`、`users`、`user_events`、`momo_transactions` 和 `compliance_rules` 数据。

## 验证结果

- 后端服务验证实例：`8003` 启动成功。
- 前端开发验证实例：`2011` 启动成功。
- 后端接口冒烟：
  - `monitoring-summary`：200
  - `message-center`：200
  - `audit-logs`：200
  - `kyc-review-queue`：200
- 后端测试：
  - `tests/test_admin_ops_center.py`
  - `tests/test_admin_loans_filters.py`
  - `tests/test_admin_users_deal_time_filter_integration.py`
  - 结果：`10 passed`
- 前端测试：
  - `src/constants/adminPages.test.js`
  - `src/views/opsCenter.test.js`
  - 结果：`8 passed`
- 前端构建：通过

## 遇到的问题与处理

- 监控汇总接口第一次返回 500，原因是 APScheduler 的 `next_run_time` 带时区，而代码用本地 naive `datetime.now()` 比较；已改成按同一时区比较。
- 前端测试最初失败在旧断言和旧函数名，已同步更新为新菜单权限与 `sendAdminReminder`。
- 消息中心提醒动作最初还受旧的回款权限限制，已放宽到 `message-center` 可用。

## Git 操作

- 已执行 `git add` 暂存本轮新增与修改文件，未创建 commit。

# 2026-08-20 17:40:00

## 本轮任务

从业务运营角度审视管理后台现状，判断是否还缺少必要页面与功能，并给出补充建议。

## 已核对内容

- 后端管理接口路由：`backend/app/api/endpoints/admin.py`
- 后台页面路由与菜单：`frontend_admin/src/router/index.js`、`frontend_admin/src/constants/adminPages.js`
- 现有业务审察报告：`管理后台业务功能审察报告_2026-08-19.md`
- 全盘系统审查报告：`doc/full_system_audit_20260819.md`

## 现有后台已覆盖的核心页面

- 洞察看板
- 用户档案
- 申请审批
- 待MoMo放款
- 放款失败客户
- 还款管理
- 催收管理
- 财务平账
- 风控报告单查
- 黑名单
- 逾期配置
- 贷款产品
- 历史卡池兼容
- 渠道管理
- 专属链接
- 后台用户

## 结论

当前管理后台已经覆盖了现金贷主链路的“借、放、收、催、平账”基础操作，适合日常运营使用，但从正式上线运营角度看，仍明显缺少几类关键页面。

## 必须补充（P0）

1. 合同与授权页面

- 需要独立的借款合同、电子签署、MoMo 扣款授权书、版本留痕页面。
- 现有 `purchase_contract` 更像历史兼容残留，不适合继续承担现金贷合同主入口。

2. MoMo 资金与对账页面

- 需要交易流水、回调处理、重试队列、对账批次、失败补偿页面。
- 目前后台偏“手工确认”，不足以支撑真实资金闭环。

3. 合规配置页面

- 需要费率上限、费用披露、产品合规校验、超限告警与导出。
- 这是正式运营的底线能力，不应只靠产品页隐性约束。

## 建议补充（P1）

1. 风控策略中心

- 自动通过/拒绝规则、评分阈值、命中原因、策略版本、灰度控制。

2. 消息与提醒中心

- 短信模板、发送记录、送达回执、到期提醒、逾期分阶段提醒。

3. 报表中心

- 逾期账龄、DPD 分布、资金流、渠道转化、渠道回收率、导出报表。

4. 催收深化页面

- 分案、阶段流转、委外管理、减免策略、外呼记录。

5. 操作审计日志

- 管理员谁在什么时间改了什么，需要可追溯。

## 可选增强（P2）

- 运营配置中心：公告、Banner、活动位、FAQ
- 客服工单
- KYC 人工复核队列
- 系统监控看板

## 运营视角建议

- 先补“合同/授权、MoMo 对账、合规护栏”三件事。
- 再补“风控、消息、报表、审计”四件事。
- 最后再考虑增长类和体验类页面。

## 本轮处理结果

- 完成管理后台缺口分析与优先级整理。
- 未修改业务代码。
- 未执行测试。
# 2026-08-20 21:10:00

## 本轮任务

继续完成支付接入工作，补齐 Hubtel 异步收款供应商实现。

## 修改说明

- 在 `backend/app/services/momo.py` 新增 `HubtelMomoProvider`，使用 `httpx.AsyncClient` 调用 Hubtel Checkout 接口。
- 增加 Basic 认证、收款请求报文、Checkout 响应解析及网络/响应异常处理。
- Hubtel 当前明确不承担放款；放款仍返回失败结果，避免误把收款接口用于出款。
- 根据 `HUBTEL_ENABLED` 自动选择 Hubtel 或原有 Mock，默认配置保持 Mock，兼容现有开发环境。
- 新增 `backend/tests/test_hubtel_momo.py`，覆盖异步收款成功和放款能力边界。

## 测试结果

- `backend/.venv/bin/pytest -q backend/tests/test_hubtel_momo.py`
- 结果：`2 passed`
- 系统全量 pytest 命令不可用，原因是 shell 未激活项目虚拟环境；已使用项目虚拟环境执行目标测试。

## Git 操作

- 已执行 `git add backend/app/services/momo.py backend/tests/test_hubtel_momo.py`，未创建 commit。
# 2026-08-20 21:35:00

## 本轮任务

继续补充运营后台能力，完成 KYC 复核动作的开发、测试与验收。

## 修改说明

- 新增 KYC 单用户审核接口：`POST /api/admin/kyc-review/user/{user_id}`。
- 新增 KYC 批量审核接口：`POST /api/admin/kyc-review/batch`。
- 支持 `APPROVE` / `REJECT` 两种动作和处理备注。
- 审核通过/拒绝会同步更新实名与人脸认证状态，并写入用户事件审计记录。
- 新增请求模型和 schema 单元测试。

## 测试与验收

- `python -m py_compile backend/app/api/endpoints/admin.py backend/app/services/admin_service.py backend/app/schemas/admin.py`：通过。
- `backend/.venv/bin/pytest -q backend/tests/test_admin_ops_center.py backend/tests/test_kyc_review_actions.py`：通过（4 passed）。
- 已确认接口路径将单用户路由置于 `user` 子路径下，避免与批量路由冲突。

## 当前未完成项

- 消息模板版本管理、审计导出、监控下钻、后台登录历史、财务日汇总/差异单、渠道和产品复制及历史记录，尚未完成代码闭环；后续需继续按模块补充模型、接口、页面和集成测试。

## Git 操作

- 已执行 `git add` 暂存本轮新增与修改文件，未创建 commit。
# 2026-08-20 22:20:00

## 本轮任务

继续完成运营后台剩余能力，补齐可直接落地的 KYC、审计与后台账号运营功能。

## 已完成

- KYC 页面增加通过/拒绝与处理备注操作，调用单用户审核接口。
- 操作审计增加事件类型、对象类型筛选。
- 新增审计日志 CSV 导出接口和前端导出按钮。
- 后台管理员新增 `is_active` 状态、启用/禁用操作；禁用账号无法通过鉴权。
- 新增数据库迁移：`004_admin_ops_extensions.sql`。
- 保留前序已完成的 Hubtel 支付、KYC 批量审核等能力。

## 测试结果

- 后端编译检查通过。
- 后端目标测试：`6 passed`。
- 前端 Vitest/build 无法执行：pnpm 依赖检查因 `ERR_PNPM_IGNORED_BUILDS`（esbuild 构建脚本被 pnpm 安全策略阻止）退出；未修改依赖策略。

## 当前验收说明

消息模板版本、监控下钻、后台登录历史、财务差异单/日汇总、渠道与产品版本历史仍需独立数据模型和页面流程，不能在没有数据库字段定义的情况下伪造完成；本轮已完成审计、账号和 KYC 可验收闭环。

## Git 操作

- 已执行 `git add`，未创建 commit。
# 2026-08-20 23:10:00

## 本轮任务

按现有业务逻辑补齐消息、监控、后台登录、财务对账、渠道和产品运营能力。

## 已完成

- 新增 `MessageTemplate`：模板版本、编辑保存、启停、历史版本查询基础能力。
- 新增 `AdminLoginHistory`：登录成功/失败、客户端、时间等记录，并增加登录历史接口。
- 新增 `ConfigChangeHistory`：渠道/产品 CREATE、UPDATE、COPY 快照和版本记录。
- 新增渠道复制接口、产品复制接口、配置历史接口。
- 新增财务对账接口：日汇总、失败差异单、待对账列表。
- 新增运营监控下钻接口：MoMo待处理/失败、KYC待复核指标明细。
- 前端 API 层已补充以上接口调用方法。
- 新增迁移 `004_admin_ops_extensions.sql`，包含表注释和字段注释。

## 测试结果

- 后端 Python 编译检查通过。
- 既有目标测试：`6 passed`。

## 验收说明

本轮完成了后端数据、接口和前端 API 调用层闭环；消息模板、财务对账、登录历史、渠道/产品历史的独立页面交互仍需接入现有页面组件。数据库上线前必须执行 `004_admin_ops_extensions.sql`。

## Git 操作

- 已执行 `git add`，未创建 commit。
# 2026-08-21 05:50:00

## 本轮任务

完成前端页面层对新增运营 API 的接入，消除“只有 API 调用入口、页面未闭环”的遗留项。

## 已完成

- 消息中心：服务端模板列表、模板版本编辑、新建版本、启停状态切换。
- 财务平账：日对账汇总、交易金额/笔数、差异单与待对账数据展示。
- 后台用户：登录历史弹窗，展示登录时间、客户端、成功/失败和失败原因。
- 渠道管理：复制渠道、查看配置变更历史。
- 产品管理：复制产品、查看配置变更历史。
- 运营监控：KYC、MoMo待处理和MoMo失败指标支持点击下钻查看明细。

## 验收结果

- 管理后台 Vite 构建通过。
- H5 Vite 构建通过。
- 后端编译及既有目标测试通过。
- 前端页面依赖的新增 API 已集中登记在 `frontend_admin/src/api/index.js`。

## Git 操作

- 已执行 `git add`，未创建 commit。
# 2026-08-21 06:05:00

## 本轮任务

说明并启用同局域网手机浏览器的 HTTPS H5 登录方式。

## 处理结果

- 确认 HTTPS H5 配置端口为 `2443`，未修改项目约定端口。
- 发现开发证书缺失，已生成局域网开发自签名证书：`frontend_h5/certs/dev-key.pem`、`frontend_h5/certs/dev-cert.pem`。
- 已启动 HTTPS H5，监听 `0.0.0.0:2443`。
- 已验证 `https://127.0.0.1:2443/login` 返回 HTTP 200。

## 手机访问

- 同一 Wi-Fi 下访问：`https://192.168.1.150:2443/login`
- 首次访问需在手机浏览器中接受自签名证书风险提示。
# 2026-08-21 06:25:00

## 本轮任务

处理手机访问 H5 时的 GalaCredit 应用名称、自动安装诉求及 GPS 授权循环问题。

## 修改说明

- H5 页面标题改为 `GalaCredit`。
- 新增 PWA `manifest.webmanifest`，应用名称和短名称均为 GalaCredit，支持浏览器“添加到主屏幕/安装应用”。浏览器出于安全限制不能在打开站点时静默自动下载或安装应用，已采用标准 PWA 安装机制。
- 修复定位授权循环：首次授权失败或超时后写入会话尝试标记，页面重挂载不再自动重复弹窗；用户重新登录时清除该标记，可主动重试。
- 重新生成包含 `192.168.1.150` SAN 的局域网开发证书。

## 测试结果

- H5 Vite 构建通过。
- 证书包含 `IP Address:192.168.1.150`。
# 2026-08-21 06:45:00

## 本轮任务

实现 Android Chrome 将 H5 安装为桌面独立应用的效果，应用名称为 GalaCredit。

## 修改说明

- 新增 `sw.js` Service Worker，满足 PWA 安装条件并支持独立窗口运行。
- 完善 PWA manifest，应用名称/短名称为 GalaCredit。
- 注册 `beforeinstallprompt`，浏览器满足条件时在页面显示 `Install GalaCredit` 安装按钮，由用户确认后添加到桌面。
- 保持安全限制：网页不能静默强制安装，必须由用户点击确认。
- GPS 授权循环修复逻辑继续保留。

## 测试结果

- H5 Vite 构建通过。
- HTTPS 环境下 Service Worker 可注册，安装入口依赖 Android Chrome 的 PWA 条件判断。
# 2026-08-21 07:05:00

## 本轮任务

排查手机端未显示 PWA 安装入口的问题。

## 结论与修改

- 截图地址栏 HTTPS 红叉表示手机未信任局域网自签名证书，Chrome 因此可能不触发 `beforeinstallprompt`、Service Worker 和自动安装条件。
- 安装按钮改为在 HTTPS 非独立窗口页面始终显示；若浏览器没有原生安装事件，点击后显示“浏览器菜单添加到主屏幕/安装应用”的明确指引。
- 已保留 GalaCredit manifest、Service Worker 和独立窗口配置。

## 手机验收步骤

1. 先在手机打开 `https://192.168.1.150:2443`，点击高级并继续访问，完成证书信任/例外确认。
2. 返回或刷新 H5 页面，点击 `Install GalaCredit`；或者 Chrome 菜单选择“添加到主屏幕/安装应用”。
3. 安装后桌面名称应为 GalaCredit，打开后无浏览器地址栏。

## 限制

局域网自签名证书只能用于开发测试；要稳定出现原生安装提示，生产环境必须使用手机信任的正式 HTTPS 域名证书。
# 2026-08-20 23:45:00

## 本次作业总结

- GalaCredit 已切换为独立 `galacredit` 数据库，初始化独立表结构，不再复用 `xiaohebao` 数据库。
- 误在旧库执行的本轮新增结构已核查为空并撤销；旧站点返回 200，旧后端持续运行。
- 新后端独立容器 `galacredit-backend` 运行于 28002；未重启旧后端、VPN、Xray 等服务。
- Caddy 增加独立域名 `galacredit.ebamotor.com`，仅 reload Caddy；HTTPS 证书签发成功。

## 验收结果

- H5、`manifest.webmanifest`、`sw.js` 均返回 200。
- `/api/` 返回 404，表示请求已到达独立后端但该根路由未定义。
- 原 `xhb.juxin.pro` 返回 200；`xiaohebao-backend`、`talking202605-caddy` 正常运行。

## 回退信息

- Caddy 配置备份：`/opt/talking202605/cloud-deploy/caddy/Caddyfile.bak-galacredit-20260820`
- 原数据库配置备份：`/opt/galacredit/backend/.env.bak-before-db-isolation-20260820`
# 2026-08-20 23:55:00

## 管理后台域名部署

- 按用户已配置 DNS 的域名，将管理前端发布到 `https://gala.ebamotor.com`。
- 管理前端静态文件部署到 `/data/www/gala.ebamotor.com`。
- Caddy 新增独立站点并反代 `/api/*` 到 GalaCredit 后端 `127.0.0.1:28002`，仅 reload Caddy。
- HTTPS、后台首页和 H5 首页验收均为 200；旧 `xiaohebao` 服务容器保持运行。
- 配置备份：`/opt/talking202605/cloud-deploy/caddy/Caddyfile.bak-gala-admin-20260820`。
# 2026-08-20 23:58:00

## 管理员账号核查

- 查询 GalaCredit 独立数据库 `galacredit.admins`，确认存在并启用两个 ADMIN 账号：`admin`、`xiaojiang`。
- 默认初始化逻辑显示：`admin` 初始密码为 `admin123`；`xiaojiang` 初始密码为 `Cn11235813!@#`。
- 密码以哈希形式存储，未读取或输出密码哈希。
# 2026-08-20 23:59:00

## 管理员账号与英文界面

- GalaCredit 独立数据库已删除 `admin` 账号，仅保留启用的 `xiaojiang` 超级管理员。
- 后端默认管理员初始化逻辑已同步修改，重启不会重新创建 `admin`。
- 管理前端补充英文登录文案、英文运行时兼容翻译层和常用业务状态/控件文案。
- 管理前端已重新构建并部署到 `https://gala.ebamotor.com`，页面返回 200；H5 返回 200。
- 本地代码和构建产物已提交到本地 Git，提交号 `c6dd9fc`。
- GitHub 推送因当前环境没有 GitHub HTTPS 凭据失败：`could not read Username for 'https://github.com'`，服务器部署不受影响。
# 2026-08-21 00:20:00

## 管理后台英文界面修复

- 修复语言切换后运行时翻译不重新扫描的问题。
- 消息中心页面改为真正的中英双语文案，覆盖统计卡片、提醒队列、模板库、版本列表、发送记录、编辑弹窗和操作提示。
- 管理后台重新构建并发布至 `https://gala.ebamotor.com`，页面验收返回 200。
# 2026-08-21 00:45:00

## 管理后台彻底英文切换修复

- 将语言切换升级为完整页面重载机制，避免中英文 DOM 混杂。
- 全局翻译层覆盖文本节点、动态新增节点、输入框 placeholder、title 和 aria-label。
- 增补催收、还款、订单、统计单位、筛选器、状态词及混合业务文本（如 OverdueLoan、Overdue days、Waived amount）的英文映射。
- English 模式下切换语言会重新加载当前路由，中文模式也能恢复原始中文，不再保留上一语言残留。
- 管理后台重新构建部署到 `https://gala.ebamotor.com`，`/collections` 返回 200。
# 2026-08-21 01:10:00

## 全量双语清查与三端同步

- 扫描管理后台全部 Vue 页面、语言包和接口相关文案，补充全局中英文词典及催收、还款、审批、财务、消息、渠道、产品、后台用户等业务词汇。
- English 模式覆盖文本节点、动态节点、表单属性和混合中英文数据；语言切换会重载当前路由，避免中文残留或语言状态混杂。
- 新增云端部署说明：`doc/deployment_cloud.md`。
- 本地提交、GitHub `main` 分支和服务器后端/管理前端已同步到提交 `8861ddb`。
- 管理后台与 H5 均返回 200；`galacredit-backend`、旧 `xiaohebao-backend`、Caddy 均保持运行。
- GitHub PAT 仅用于本次推送，未写入代码或配置；应立即在 GitHub 撤销并重新生成。

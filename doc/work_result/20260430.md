# 2026-04-30 17:33:15

## 修改说明
1. 后端新增“仅业务顾问”状态映射逻辑：当且仅当后台用户角色仅为 `BUSINESS_CONSULTANT`，且用户列表项存在 `first_disbursed_at` 时，将 `current_loan_status` 改写为 `FIRST_BORROW`（用于前端展示“首借”）。
2. `/api/admin/users` 返回列表时接入上述映射，确保状态值由服务端统一返回。
3. 前端 `frontend_admin` 状态文案与标签映射新增 `FIRST_BORROW`，展示文案为“首借”。

## 修改后的代码
- backend/app/services/admin_service.py
  - 新增 `_is_only_business_consultant(admin)`
  - 新增 `apply_business_consultant_user_summary_status(user_summary, admin)`
- backend/app/api/endpoints/admin.py
  - `/users` 返回项从 `serialize_user_summary(user)` 调整为 `apply_business_consultant_user_summary_status(serialize_user_summary(user), current_admin)`
- frontend_admin/src/utils/format.js
  - `statusTextMap` 新增 `FIRST_BORROW: '首借'`
  - `statusTagMap` 新增 `FIRST_BORROW: 'success'`

## 单元测试代码
- backend/tests/test_admin_user_profile_rules.py
  - `test_apply_business_consultant_user_summary_status_should_set_first_borrow_for_only_consultant`
  - `test_apply_business_consultant_user_summary_status_should_not_change_for_mixed_roles`

## 集成测试代码
- 本次未新增独立集成测试文件；执行了现有路由/页面相关测试集合作为回归验证。

## 测试执行结果
1. `pytest -q backend/tests/test_admin_user_profile_rules.py`
   - 结果：`6 passed`
2. `cd frontend_admin && pnpm -s test src/constants/adminPages.test.js`
   - 结果：`1 passed, 2 tests passed`

## 操作步骤
1. 先新增后端测试并执行，验证红灯（缺少函数导致失败）。
2. 按最小改动补充后端状态映射函数并接入 `/users`。
3. 前端增加 `FIRST_BORROW` 文案映射。
4. 运行后端与前端相关测试回归通过。

# 2026-04-30 15:42:00

## 1. 修改说明（做了什么、为什么）
- 按“事件驱动 + 周期兜底”方案改造后台 stats WebSocket 推送。
- 保留建连即推送；新增“业务写操作后触发即时推送”；保留定时兜底推送。

## 2. 修改后的代码
- `backend/app/services/admin_service.py`
  - 新增 `notify_admin_stats_changed()`：用于标记统计数据已变更。
  - 新增 `wait_admin_stats_changed(last_version, timeout_seconds)`：等待变更，超时则兜底返回。
- `backend/app/api/endpoints/admin.py`
  - `admin_stats_ws` 循环改为：推送后等待“变更事件或超时”，而不是固定 sleep。
  - 在关键写接口成功后调用 `notify_admin_stats_changed()`，实现即时推送触发。

## 3. 单元测试代码
- 复用并通过：`backend/tests/test_admin_stats_ws.py`
- 复用并通过：`backend/tests/test_async_db_routes.py`
- 复用并通过：`backend/tests/test_admin_change_password.py`

## 4. 集成测试代码（如适用）
- 本次未新增集成测试。

## 5. 测试执行结果或执行方式
- 命令：
  - `pytest -q backend/tests/test_admin_stats_ws.py backend/tests/test_async_db_routes.py backend/tests/test_admin_change_password.py`
- 结果：`7 passed`

## 6. 操作步骤记录
1. 改造 WS 推送循环为“事件等待 + 超时兜底”。
2. 在关键写接口加上 stats 变更通知触发。
3. 执行相关后端测试，确保回归通过。

# 2026-04-30 15:21:03

## 1. 修改说明（做了什么、为什么）
- 管理平台右上角用户下拉菜单新增“修改密码”功能。
- 增加后端“当前登录管理员修改自身密码”接口，包含原密码校验、二次确认一致性校验与新旧密码不一致校验。
- 前端新增修改密码弹窗界面，并接入接口调用。

## 2. 修改后的代码
- 后端 schema：`backend/app/schemas/admin.py`
  - 新增 `AdminChangePasswordRequest`。
- 后端 service：`backend/app/services/admin_service.py`
  - 新增 `_change_admin_password`。
- 后端路由：`backend/app/api/endpoints/admin.py`
  - 新增 `POST /api/admin/change-password`。
- 前端 API：`frontend_admin/src/api/index.js`
  - 新增 `changeAdminPassword`。
- 前端界面：`frontend_admin/src/views/Layout.vue`
  - 下拉菜单新增“修改密码”；
  - 新增“修改密码”弹窗和提交流程。

## 3. 单元测试代码
- 新增：`backend/tests/test_admin_change_password.py`
  - 校验原密码错误时拒绝修改；
  - 校验成功时正确更新密码哈希并提交。

## 4. 集成测试代码（如适用）
- 复用：`backend/tests/test_async_db_routes.py`。

## 5. 测试执行结果或执行方式
- 后端：
  - `pytest -q backend/tests/test_admin_change_password.py backend/tests/test_async_db_routes.py`
  - 结果：`6 passed`
- 前端：
  - `cd frontend_admin && pnpm test -- --run src/utils/usersDealColumns.test.js`
  - 结果：`3 files passed, 5 tests passed`
  - `cd frontend_admin && pnpm build`
  - 结果：构建通过。

## 6. 操作步骤记录
1. 检查后端是否已有管理员自助改密接口。
2. 增加后端请求模型、service逻辑、路由。
3. 接入前端下拉菜单、弹窗和调用逻辑。
4. 执行后端测试、前端测试与构建验证。

# 2026-04-30 14:31:38

## 1. 修改说明（做了什么、为什么）
- 按反馈恢复“业务顾问可操作新增用户”。
- 为新增用户弹窗增加专用来源渠道接口：业务顾问仅返回其关联渠道，默认选中第一条；无渠道则不设默认值。
- 修复业务顾问登录后触发 `/api/admin/stats` 报错：在布局页按权限控制统计接口与统计WS连接，仅有 `overview` 权限才请求。

## 2. 修改后的代码
- 后端新增接口与服务：
  - `GET /api/admin/users/source-channels`
  - 文件：`backend/app/api/endpoints/admin.py`、`backend/app/services/admin_service.py`
  - 规则：
    - 具备 `users` 权限可调用；
    - 业务顾问仅返回 `Channel.admin_user_id == current_admin.id` 且 `ACTIVE` 的渠道。
- 前端用户档案页：`frontend_admin/src/views/Users.vue`
  - “新增用户”按钮对业务顾问可见。
  - 来源渠道改为调用 `/admin/users/source-channels`。
  - 打开新增抽屉后：有渠道时默认选中第一个；无渠道则保持空值。
- 前端布局页：`frontend_admin/src/views/Layout.vue`
  - 增加 `canReadStats()`，仅当有 `overview` 权限时才会调用 `getAdminStats` 和连接 `/api/admin/ws/stats`。

## 3. 单元测试代码
- 修改：`backend/tests/test_async_db_routes.py`
  - 增加 `/users/source-channels` 异步路由约束校验。
- 新增：`backend/tests/test_admin_user_profile_rules.py`
  - 增加 `test_get_user_source_channels_should_return_serialized_channels`。

## 4. 集成测试代码（如适用）
- 复用后端异步路由测试与用户档案规则测试。

## 5. 测试执行结果或执行方式
- 后端：
  - `pytest -q backend/tests/test_admin_user_profile_rules.py backend/tests/test_async_db_routes.py`
  - 结果：`8 passed`
- 前端：
  - `cd frontend_admin && pnpm build`
  - 结果：构建通过。

## 6. 操作步骤记录
1. 定位 stats 报错来源为布局页统一统计请求。
2. 增加按权限触发 stats 请求/WS 的前端保护。
3. 新增来源渠道专用接口并接入新增用户流程。
4. 执行后端测试与前端构建验证。

# 2026-04-30 13:59:22

## 1. 修改说明（做了什么、为什么）
- 用户档案列表接口返回数据改为脱敏：手机号和身份证号不再返回明文。
- 用户档案详情页“最新订单概览”补充“成交时间”字段，取值来自 `latest_loan.disbursed_at`。
- 新增业务顾问登录后的用户档案权限与展示规则：
  - 仅可访问用户档案页（权限收敛）；
  - 无权重置密码（前后端同时约束）；
  - 用户列表仅显示当前业务顾问关联渠道下的客户；
  - 用户详情页仅展示“实名资料”与新增“成交情况”（按首次成交口径）。
- 成交情况口径实现为：
  - 优先取该客户第一条非 `REJECTED` 订单；
  - 若无则回退该客户第一条订单；
  - 若仍无则展示空数据。

## 2. 修改后的代码
- 后端：`backend/app/services/admin_permissions.py`
  - `resolve_admin_permissions` 对纯 `BUSINESS_CONSULTANT` 角色强制返回 `['users']`。
- 后端：`backend/app/services/admin_service.py`
  - `serialize_user_summary` 中：
    - `phone` 改为 `*******1234` 样式脱敏；
    - `id_card_num` 改为前6后1脱敏样式。
  - `serialize_user_detail` 新增 `first_deal_loan` 字段生成逻辑（首次成交口径）。
  - `_reset_user_password` 增加业务顾问禁止重置密码（403）。
  - `_get_user_detail` 增加业务顾问按渠道归属校验（非本人渠道客户返回403）。
- 后端：`backend/app/api/endpoints/admin.py`
  - `/admin/users` 列表增加业务顾问数据范围过滤：`Channel.admin_user_id == current_admin.id`。
- 后端：`backend/app/schemas/user.py`
  - `UserDetailResponse` 新增 `first_deal_loan`。
- 前端：`frontend_admin/src/views/Users.vue`
  - 列表中业务顾问不显示“重置密码”；新增用户按钮也不显示；
  - 详情页业务顾问仅显示“实名资料”+“成交情况”；
  - 原“最新订单概览”增加“成交时间”；
  - 增加顾问角色前端兜底提示（点击重置时给出无权提示）。

## 3. 单元测试代码
- 新增：`backend/tests/test_admin_user_profile_rules.py`
  - 验证用户列表脱敏格式。
  - 验证首次成交口径 `first_deal_loan` 取值。
  - 验证业务顾问无权重置密码。
- 修改：`backend/tests/test_admin_business_consultant.py`
  - 保持业务顾问筛选严格性回归覆盖。

## 4. 集成测试代码（如适用）
- 复用：`backend/tests/test_async_db_routes.py` 异步路由约束回归。

## 5. 测试执行结果或执行方式
- 后端：
  - `pytest -q backend/tests/test_admin_user_profile_rules.py backend/tests/test_admin_business_consultant.py backend/tests/test_async_db_routes.py`
  - 结果：`11 passed`
- 前端构建验证：
  - `cd frontend_admin && pnpm build`
  - 结果：构建通过。
- 服务启动验证：
  - `cd backend && python -m app.main`
  - 结果：启动流程正常，但 `8001` 端口被占用（`address already in use`）。

## 6. 操作步骤记录
1. 梳理用户列表、详情、重置密码、角色权限和前端用户档案页实现。
2. 实现列表脱敏与业务顾问数据范围限制。
3. 实现详情首次成交口径并扩展返回字段。
4. 实现业务顾问前后端操作与展示限制。
5. 执行后端测试、前端构建、服务启动验证。

# 2026-04-30 13:16:06

## 1. 修改说明（做了什么、为什么）
- 按要求检查服务端代码中 `datetime.utcnow` 的使用，并统一替换为 `datetime.now`。
- 替换范围仅限模型时间默认值/更新时间表达式，保持最小改动，不修改业务逻辑。

## 2. 修改后的代码
- 以下文件中的 `default=datetime.utcnow` / `onupdate=datetime.utcnow` 已替换为 `datetime.now`：
  - `backend/app/models/admin.py`
  - `backend/app/models/channel.py`
  - `backend/app/models/ecard_pool.py`
  - `backend/app/models/loan.py`
  - `backend/app/models/loan_installment.py`
  - `backend/app/models/loan_transaction.py`
  - `backend/app/models/oauth_client.py`
  - `backend/app/models/oauth_token.py`
  - `backend/app/models/product.py`
  - `backend/app/models/risk_report.py`
  - `backend/app/models/user.py`
  - `backend/app/models/user_event.py`

## 3. 单元测试代码
- 本次未新增测试代码，复用现有测试进行回归验证。

## 4. 集成测试代码（如适用）
- 本次未新增集成测试代码。

## 5. 测试执行结果或执行方式
- 执行命令：
  - `pytest -q backend/tests/test_async_db_routes.py backend/tests/test_admin_business_consultant.py`
- 执行结果：
  - `7 passed`

## 6. 操作步骤记录
1. 全量检索 `backend` 中 `datetime.utcnow` 使用点。
2. 批量替换模型层 `datetime.utcnow -> datetime.now`。
3. 回归执行后端测试并确认通过。

# 2026-04-30 13:08:00

## 1. 修改说明（做了什么、为什么）
- 新增后台角色 `业务顾问`（`BUSINESS_CONSULTANT`），并将其页面权限严格限定为仅可访问 `用户档案(users)`。
- 渠道管理新增渠道与后台用户（`admins`）的 N:1 关联：在渠道表新增 `admin_user_id` 字段，渠道创建/编辑必须绑定一个业务顾问账号。
- 新增“业务顾问候选列表”接口，支持按 `id` 精确搜索（数字关键词）与按用户名关键字模糊搜索（`like`），用于前端可搜索下拉。
- 后台渠道页新增“业务顾问”远程搜索下拉（可输入用户ID或用户名），并在列表展示关联顾问信息。

## 2. 修改后的代码
- 后端角色权限：`backend/app/services/admin_permissions.py`
  - 新增角色：`BUSINESS_CONSULTANT`
  - 新增角色权限映射：`BUSINESS_CONSULTANT -> ['users']`
- 后端渠道模型：`backend/app/models/channel.py`
  - 新增字段：`admin_user_id = Column(Integer, nullable=True, index=True)`
- 后端渠道 schema：`backend/app/schemas/channel.py`
  - `ChannelCreateRequest` 新增必填 `admin_user_id`
  - `ChannelUpdateRequest` 新增可选 `admin_user_id`
  - `ChannelItemResponse` 新增 `admin_user_id/admin_user_name`
  - 新增 `BusinessAdvisorItemResponse`
- 后端渠道/顾问业务：`backend/app/services/admin_service.py`
  - `serialize_channel` 增加顾问信息回传
  - 新增 `_resolve_business_advisor_by_id`：校验被绑定用户必须是业务顾问角色
  - `_create_channel/_update_channel` 增加 `admin_user_id` 处理与校验
  - `_get_channels` 增加顾问账号信息回填
  - 新增 `_get_business_advisors`：返回业务顾问候选列表（支持 id 精确 / 用户名模糊）
- 后端路由：`backend/app/api/endpoints/admin.py`
  - 新增接口：`GET /admin/admin-users/business-advisors`
- 前端 API：`frontend_admin/src/api/index.js`
  - 新增 `getBusinessAdvisors`
- 前端角色常量：`frontend_admin/src/constants/adminPages.js`
  - 新增 `BUSINESS_CONSULTANT` 及权限映射
- 前端渠道页：`frontend_admin/src/views/Channels.vue`
  - 渠道创建/编辑新增业务顾问下拉（远程搜索）
  - 表单保存时提交 `admin_user_id`
  - 列表新增顾问信息展示
- DDL 文档：`doc/ddl.sql`
  - `channels` 表新增 `admin_user_id` 字段与索引 `ix_channels_admin_user_id`

## 3. 单元测试代码
- 新增：`backend/tests/test_admin_business_consultant.py`
  - `test_resolve_business_advisor_by_id_should_reject_non_consultant`
  - `test_get_business_advisors_should_return_only_consultants`
  - `test_business_consultant_role_should_map_to_users_page_only`
- 修改：`backend/tests/test_async_db_routes.py`
  - 增加新路由 `/admin-users/business-advisors` 的异步路由约束校验
- 新增：`frontend_admin/src/constants/adminPages.test.js`
  - 验证 `BUSINESS_CONSULTANT` 的权限映射及与其他角色合并行为

## 4. 集成测试代码（如适用）
- 后端路由异步依赖约束测试：`backend/tests/test_async_db_routes.py`
- 前端 Vitest 运行验证：`frontend_admin/src/constants/adminPages.test.js`

## 5. 测试执行结果或执行方式
- 后端测试：
  - 命令：`pytest -q backend/tests/test_admin_business_consultant.py backend/tests/test_async_db_routes.py`
  - 结果：`7 passed`
- 前端测试：
  - 命令：`cd frontend_admin && pnpm test -- --run src/constants/adminPages.test.js`
  - 结果：`2 files passed, 3 tests passed`
- 服务启动验证（按要求执行）：
  - 命令：`cd backend && python -m app.main`
  - 结果：应用启动流程正常进入 `Application startup complete`，但端口 `8001` 被占用（`address already in use`），因此未能在当前环境持续监听。

## 6. 操作步骤记录
1. 梳理角色、权限、渠道 CRUD、后台用户列表与前端渠道页实现。
2. 新增“业务顾问”角色及权限映射，确保仅可访问用户档案页面。
3. 在渠道模型与 schema 增加 `admin_user_id`，并在创建/编辑流程强制校验为业务顾问账号。
4. 新增业务顾问检索接口，支持 id 精确与用户名模糊搜索。
5. 前端渠道页新增远程搜索下拉并接入创建/编辑提交。
6. 新增并执行后端与前端测试，完成服务启动验证。

# 2026-04-30 10:53:07

## 1. 修改说明（做了什么、为什么）
- `frontend_admin` 的“用户档案 -> 新增用户”中，将“来源渠道”改为非必填。
- 同步修改后端新增用户逻辑，允许请求不带 `source_channel_id`，从而兼容无来源渠道场景。
- `frontend_admin` 登录页将用户名初始默认值由固定账号改为空字符串，避免误填默认账号。

## 2. 修改后的代码
- 后端请求模型：`backend/app/schemas/admin.py`
  - `RegisterUserRequest.source_channel_id` 从 `int` 调整为 `Optional[int] = None`。
- 后端业务逻辑：`backend/app/services/admin_service.py`
  - `_register_user` 中仅当 `source_channel_id` 传入时才校验渠道并绑定。
  - 未传渠道时：`source_channel_id/channel_bound_at/last_channel_visit_at` 均为 `None`。
  - 审计日志在无渠道时写入“来源渠道：未指定”。
- 前端新增用户页：`frontend_admin/src/views/Users.vue`
  - 去掉“来源渠道”表单项的必填标记。
  - 去掉“请选择来源渠道”的前端强校验。
  - 调用新增用户接口时，仅在有值时携带 `source_channel_id` 字段。
- 前端登录页：`frontend_admin/src/views/Login.vue`
  - `form.username` 默认值从 `'xiaojiang'` 改为 `''`。

## 3. 单元测试代码
- 新增：`backend/tests/test_admin_register_user_optional_channel.py`
  - 用例：`test_register_user_should_allow_missing_source_channel`
  - 验证 `_register_user` 在不传 `source_channel_id` 时可成功创建用户，且渠道相关字段为空。

## 4. 集成测试代码（如适用）
- 复用现有后端路由基础测试：`backend/tests/test_async_db_routes.py`。
- 复用现有前端测试：`frontend_admin/src/config/gzip.test.js`（确保前端测试流程可执行）。

## 5. 测试执行结果或执行方式
- 后端：
  - `cd backend && pytest -q tests/test_admin_register_user_optional_channel.py tests/test_async_db_routes.py`
  - 结果：`5 passed`
- 前端：
  - `cd frontend_admin && npm test`
  - 结果：`1 passed`

## 6. 操作步骤记录
1. 定位前后端“新增用户/来源渠道”与登录默认用户名代码位置。
2. 修改后端 schema 与 service，放开来源渠道必填。
3. 修改前端新增用户表单和请求体拼装逻辑。
4. 修改登录页默认用户名为空字符串。
5. 新增后端测试并执行前后端测试验证。

# 2026-04-30 10:45:17

## 1. 修改说明（做了什么、为什么）
- 按要求将 `backend/app/api/endpoints/admin.py` 中的业务逻辑迁移到 Service 层，新增 `backend/app/services/admin_service.py`。
- 迁移策略：保留 `admin.py` 作为 Router 层，业务函数统一从 `admin_service.py` 导入复用，路由层继续只负责参数接收与调用。
- 目的：降低单文件复杂度，分离路由与业务逻辑，便于后续维护与测试。

## 2. 修改后的代码
- 新增文件：`backend/app/services/admin_service.py`
  - 从 `admin.py` 迁移 59 个“非路由函数”（无 `@router...` 装饰器），包含：
  - 通用工具函数（权限校验、序列化、金额计算、作用域过滤等）
  - 业务处理函数（如 `_get_*`、`_create_*`、`_update_*`、`_assign_*`、`_disburse_*` 等）
- 修改文件：`backend/app/api/endpoints/admin.py`
  - 删除本地 59 个非路由函数定义
  - 新增 `from app.services.admin_service import ...` 导入这些业务函数
  - 保留全部路由定义与路由签名不变

## 3. 单元测试代码
- 本次未新增测试文件，复用现有测试验证迁移后行为一致。

## 4. 集成测试代码（如适用）
- 使用现有接口路由测试作为迁移后的集成回归验证：
  - `tests/test_async_db_routes.py`

## 5. 测试执行结果或执行方式
- 语法编译：
  - `cd backend && python -m py_compile app/api/endpoints/admin.py app/services/admin_service.py`
  - 结果：通过
- 回归测试：
  - `cd backend && pytest -q tests/test_async_db_routes.py tests/test_root_profile_endpoint.py tests/test_auth_sms_send_code.py`
  - 结果：`8 passed`

## 6. 操作步骤记录
1. 读取 `admin.py` 最新版本并解析函数结构。
2. 自动提取顶层非路由函数（无装饰器）到 `admin_service.py`。
3. `admin.py` 保留 Router 逻辑，改为导入 service 函数。
4. 执行语法编译与关键回归测试，确认迁移稳定。


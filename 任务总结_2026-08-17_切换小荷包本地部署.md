# 小荷包代码切换与本地部署总结

## 本轮任务

清空当前 GalaCredit 服务目录，将 GitHub 仓库 `JamesBond235813/xiaohebao` 完整拉入当前项目根目录，并完成本地依赖安装、数据库初始化、服务启动和基础验证。

## 目录切换结果

- 当前根目录：`/Users/jackbond/Desktop/Ghana_loan/GalaCredit`
- 当前代码来源：`https://github.com/JamesBond235813/xiaohebao.git`
- 当前分支：`main`
- 当前 Git 提交：`ce7f5a7 Add repayment status filter`
- 当前根目录已变为小荷包仓库结构。

旧 GalaCredit 内容为保证可恢复性已移出当前根目录，备份位置：

`/Users/jackbond/Desktop/Ghana_loan/GalaCredit.pre_xiaohebao_backup_20260817_2235`

## 数据库处理

- 检查到 MySQL 中原 `galacredit` 数据库不存在。
- 检查到 MySQL 中原 `xiaohebao` 数据库不存在。
- 已创建新的 `xiaohebao` 数据库。
- 已执行小荷包数据库初始化，表结构创建成功。
- 已创建本地管理员账号，用户名为 `admin`。

## 本地环境配置

新增本地配置文件：

- `backend/.env`
- `frontend_h5/.env.local`
- `frontend_admin/.env.local`

本地配置使用 MySQL 3306 端口，数据库名为 `xiaohebao`；短信验证码启用 mock，外部风控、实名和地理编码服务关闭，避免本地环境依赖第三方密钥。

## 服务端口

| 服务 | 地址 |
|---|---|
| 后端 API | `http://127.0.0.1:8001` |
| H5 用户端 | `http://127.0.0.1:2001` |
| 管理后台 | `http://127.0.0.1:2002` |

没有使用 `1088`，因为该端口已被其他本地服务占用；也没有停止或修改其他服务。

## 已完成的兼容修正

小荷包仓库声明使用 Python 3.9，但 `backend/app/core/access_audit.py` 中存在 Python 3.10 的 `str | None` 写法，会导致 Python 3.9 在启动导入阶段报错。

已将该处改为 Python 3.9 兼容的 `Optional[str]`，不改变业务逻辑。

## 依赖安装

- 后端 Python 虚拟环境：`backend/.venv`
- 后端依赖：按 `backend/requirements.txt` 安装完成。
- H5 依赖：安装完成。
- 管理端依赖：安装完成。
- 管理端因仓库未提交 lock 文件，使用 `npm install` 生成了本地 `package-lock.json`。

## 验证结果

- 后端根接口正常：返回小荷包存活信息。
- 后端端口 `8001` 正常监听。
- H5 页面 `http://127.0.0.1:2001/login` 正常打开。
- 管理端页面 `http://127.0.0.1:2002/login` 正常打开。
- 管理登录接口验证成功，默认管理员账号可正常取得访问令牌。
- H5 production build 成功。
- 管理端 production build 成功。
- H5 前端测试：`23 passed`。
- 管理端前端测试：`24 passed`。

## 后端测试结果

后端测试结果为：`105 passed, 10 failed`。

失败集中在仓库已有测试与当前实现之间的兼容问题，包括 fake 数据库结果缺少 `.unique()`、测试对象缺少部分用户字段、旧日志断言与当前日志配置不一致，以及测试使用的手机号校验数据未满足当前 schema 约束。服务本身已正常启动，根接口和管理登录接口已通过真实运行服务验证。

## 当前运行会话

本地服务使用独立 screen 会话运行：

- `xiaohebao-backend`
- `xiaohebao-h5`
- `xiaohebao-admin`

## 后续开发基线

后续 Ghana GalaCredit 业务开发将直接基于当前小荷包代码结构进行，重点需要先处理：

1. 将中国手机号校验、中文文案和短信流程改为 Ghana `+233` 号码体系。
2. 将京东 E 卡商品与订单模型逐步替换为现金放款与 MoMo 支付模型。
3. 将短期现金贷的 N1、N2、上扣费用、分期比例和逾期费用纳入产品参数配置。
4. 在保留小荷包管理后台权限、审计、风控和订单处理结构的基础上，重新定义 Ghana 市场的借款、还款和催收流程。
